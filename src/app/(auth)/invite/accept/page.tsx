import Link from "next/link";
import { InviteAcceptanceForm } from "@/components/admin-users/InviteAcceptanceForm";
export const dynamic="force-dynamic";
export default async function InviteAcceptPage({searchParams}:{searchParams:Promise<{membershipId?:string;attempt?:string}>}){const {membershipId,attempt}=await searchParams; const valid=typeof membershipId==="string"&&typeof attempt==="string"; return <main className="mx-auto max-w-lg p-8"><h1 className="text-2xl font-semibold">Bekräfta inbjudan</h1>{valid?<InviteAcceptanceForm membershipId={membershipId} attempt={attempt}/>:<p role="alert" className="mt-3">Inbjudan kan inte bekräftas.</p>}<Link className="mt-5 inline-block text-blue-700" href="/password/update">Välj ett eget lösenord</Link>{!valid&&<Link className="ml-4 mt-5 inline-block text-blue-700" href="/">Fortsätt</Link>}</main>;}
