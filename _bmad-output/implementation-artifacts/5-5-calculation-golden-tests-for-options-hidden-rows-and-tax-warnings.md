# Story 5.5: Calculation Golden Tests For Options, Hidden Rows, And Tax Warnings

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a pilot operator,
I want calculation golden tests for representative edge cases,
so that changes to totals, visibility, and warning behavior are caught before pilot use.

## Acceptance Criteria

1. **Given** calculation fixture cases, **when** golden tests run, **then** they COVER every AC1 calc category at the CALC-ROW layer, driving the REAL `src/features/calculations/totals.ts` (`computeLineTotal`/`computeSectionTotal`/`computeCalcTotal`/`rowCountsTowardTotal`/`resolveTotalDisplay`) + the frozen `@/lib/money` primitives (the recurring calc ORACLE): **labor / material / subcontractor / machinery / other** row types; **fractional quantities**; **margins** (TB% = (sell − cost) / sell, a pure ratio of already-öre values); **options/tillval** (SELECTED counts, UNSELECTED never summed); **hidden rows** (count toward totals); **detailed / summary / text_only** section display modes; **VAT display** (excl / incl / both via `selectVatDisplay` per the resolved posture); **ROT / grön-teknik warnings** (`requiresSignOff`, estimate-not-final); and **attachment-readiness flags** (the `REQUIRED_FILES_DEFERRED` readiness disclosure — Story 8.1 has not landed). A pack-level COVERAGE manifest FAILS if any category is unpinned. [Source: epics.md#Story 5.5 AC1; test-design-epic-5.md#5.5-GOLDEN-01 (the full pack), R-508/R-505; tests/fixtures/golden/money/options-tillval.json (inclusion authority); src/features/calculations/totals.ts; src/features/calculations/readiness.ts (REQUIRED_FILES_DEFERRED); owner-decisions-applied-2026-06-18.md#Epic 5·5.4/5.5 (detailed + text-only first; hidden rows DO count; options accepted with base, individually selectable)]
2. **Given** an intentional old/new delta, **when** the fixture is compared, **then** the delta is DOCUMENTED as an expected simplification, a bug fix, or an unresolved assumption — every NEW pack case carries a three-way `origin` label (`old-lovable` / `new-expected` / `documented-delta`) + a non-empty `note`; a `documented-delta` case ALSO records the divergent old-Lovable value it diverges from, so a golden FAILURE points at the affected assumption/delta rather than an unexplained diff. NOTHING is marked production-approved (`signOff` stays `pending-owner-accounting-legal`; the underlying ROT/grön/VAT/rounding rates stay the Epic-4 UNAPPROVED placeholders). [Source: epics.md#Story 5.5 AC2; test-design-epic-5.md#5.5-UNIT-02 (documented old/new delta labelled; a golden failure points to the affected assumption), R-508; tests/unit/lib/money/golden-pack.test.ts (the LABELLING guard to mirror); tests/fixtures/golden/money/rot-gron-deductions.json (`origin`/`note`/`signOff` convention)]
3. **Given** fixture data is committed, **when** privacy checks run, **then** NO real customer personal data, secrets, or raw customer files are present — the pack REUSES + EXTENDS the existing money-golden PII/secret scan (personnummer `\d{6}-\d{4}`, orgnr `\d{10}` no-dash shape, non-`example.test` email, `secret|password|api_key`, PHONE, ADDRESS heuristics) over the DATA payload of every calc golden fixture, and asserts NO PII path into `@/lib/money` or a calc row/snapshot (the engine takes an eligibility POSTURE, never a personnummer). [Source: epics.md#Story 5.5 AC3; test-design-epic-5.md#5.5-UNIT-01, R-516; tests/unit/lib/money/golden-pack.test.ts (the pack-wide privacy scan to reuse); project-context.md#Money/Tax rules (engine takes a posture, never PII)]

## Tasks / Subtasks

- [x] Task 1: The FULL calc golden PACK — a new calc-row fixture + a live-oracle pack test (AC: 1)
  - [x] 1.1 Create the NEW calc-row golden fixture `tests/fixtures/golden/money/calc-rows.json` (anonymized, öre/rate/qty numbers + policy prose ONLY — NO PII, NO clock). It carries `_doc` provenance, a `policy` block with `signOff: "pending-owner-accounting-legal"`, and a `cases` array covering the AC1 categories NOT already owned by an existing money fixture: the FIVE row types (labor/material/subcontractor/machinery/other), FRACTIONAL quantities at the calc-row level, MARGINS (TB% low/at/above the pilot 0.15 threshold), and the SECTION DISPLAY MODES (detailed/summary/text_only). Each case carries an `origin` (`new-expected` / `documented-delta` / `old-lovable`) + a non-empty `note`; a `documented-delta` case records the divergent old-Lovable value. Money numbers ONLY. Do NOT re-pin numbers that another fixture already owns (see 1.3). [Source: test-design-epic-5.md#5.5-GOLDEN-01 (full pack: row types, fractional qty, margins, section modes, VAT display, ROT/grön, attachment flags), Exit Criteria (each expected value labelled old-Lovable/new-expected/documented-delta); tests/fixtures/golden/money/options-tillval.json + rot-gron-deductions.json (the fixture shape + `origin`/`note`/`policy`/`signOff` convention to mirror); src/features/calculations/read.ts#CalculationRowRow/CalculationSectionRow (the calc-row/section shape); owner-decisions-applied-2026-06-18.md#Epic 5·5.4/5.5 (detailed + text-only first)]
  - [x] 1.2 Create the pack test `tests/unit/features/calculations/calc-golden-pack.test.ts` (`node --test`, `tests/unit/**` — NEVER `tests/golden/**`, the runner-glob vacuous-green trap). It provides the SAME four pack-level guards as `tests/unit/lib/money/golden-pack.test.ts`: (a) a COVERAGE manifest that FAILS if any AC1 calc category is unpinned — mapped either to `calc-rows.json` (new categories) or by REFERENCE to the existing per-category money fixture; (b) LABELLING — every NEW case carries a valid `origin` + a non-empty `note`, `documented-delta` carries its divergent old value; (c) BEHAVIORAL GOLDEN — the NEW numeric cases DRIVE the real `totals.ts` / `@/lib/money` (a LIVE oracle, not static schema); (d) a SCHEMA-SHAPE guard — a malformed/half-authored case (missing `origin`/`note`/an expected öre value) FAILS loud. [Source: test-design-epic-5.md#5.5-GOLDEN-01, Testability Notes 5 (extend the pin, don't re-decide it); tests/unit/lib/money/golden-pack.test.ts (the pack structure + 4 guards to mirror); src/features/calculations/totals.ts (the calc oracle to drive); project-context.md#Testing Rules (runner-glob trap — goldens under tests/unit/**)]
  - [x] 1.3 SINGLE NUMERIC AUTHORITY per category (R-508) — do NOT re-pin numbers another fixture already owns; REFERENCE the existing authority and prove the calc-row surface reproduces it: **options/tillval + hidden rows** → `options-tillval.json` (already extended to the calc-row surface by 5.4's `readiness-inclusion.golden.test.ts` — do NOT duplicate that; the pack ASSERTS the category is covered and may add a section-level composition case that references the same pinned öre); **ROT / grön-teknik + caps + invalid-mix + eligibility** → `rot-gron-deductions.json` (drive `estimateDeduction` with the resolved POSTURE, never PII; assert `requiresSignOff` stays true); **regular VAT + fractional-qty chain** → `vat-rates.json` (`lineVatOre`/`vatBreakdown`/the `fractionalQuantityChainCase`); **rounding** → `rounding-mode.json` (`lineNetOre` half-away-from-zero). A DIVERGENCE from any pinned öre is a STOP (`needs-human`), NOT a re-pin. [Source: test-design-epic-5.md#R-508 (a different inclusion/number treated as production-approved is a STOP), Testability Notes 5; tests/fixtures/golden/money/{options-tillval,rot-gron-deductions,vat-rates,rounding-mode}.json (the per-category authorities); tests/unit/features/calculations/readiness-inclusion.golden.test.ts (the 5.4 calc-row inclusion golden — do NOT duplicate); tests/unit/lib/money/golden-pack.test.ts#CATEGORY_MANIFEST (the reference-not-re-encode pattern)]
- [x] Task 2: VAT-display + attachment-readiness + margin golden coverage at the calc-row layer (AC: 1)
  - [x] 2.1 VAT DISPLAY category — drive the real `resolveTotalDisplay(total, posture)` (which delegates to `@/lib/money#selectVatDisplay`) so a computed calc-row section total renders the correct excl/incl/both view per posture: `private` → always incl (primary = gross, not togglable); `company_excl` → primary = net, not togglable; `company_togglable` → primary = gross, togglable, both available. Reuse the pure `resolveVatDisplayPosture(customerType, tenantDefaultVatDisplay)` (5.4) to pick the posture. Presentation-only — a posture round-trip returns the identical stored öre (the source net + VAT are the immutable truth). Do NOT invent VAT numbers — reuse `vat-rates.json` pinned öre for the underlying breakdown. [Source: test-design-epic-5.md#5.5-GOLDEN-01 (VAT display), #5.2-UNIT-04 (presentation-only, source totals unchanged); src/features/calculations/totals.ts#resolveTotalDisplay; src/lib/money/vat.ts#selectVatDisplay/VatDisplayView/VatDisplayPosture; src/features/calculations/vat-posture.ts#resolveVatDisplayPosture; tests/fixtures/golden/money/vat-rates.json (the VAT authority)]
  - [x] 2.2 ROT / grön-teknik WARNING category — drive the real `estimateDeduction` (via `rot-gron-deductions.json` pinned cases) and assert the tax output is an ESTIMATE: `requiresSignOff === true` on the UNAPPROVED profiles; a ROT×grön mix is a BLOCKING typed failure (`ROT_GRON_MIX_NOT_ALLOWED`), never a silent combined sum; only a `private` posture is eligible. NO calc-row/fixture routes a personnummer into the engine (the engine takes a POSTURE). `persons` stays a FLAT-cap placeholder — NO golden case asserts or implies a per-person-scaled cap (R-512). A deduction is NEVER rendered approved/legally-final. [Source: test-design-epic-5.md#5.5-GOLDEN-01 (ROT/grön warnings), R-512/R-516; tests/fixtures/golden/money/rot-gron-deductions.json (the ROT/grön authority incl. profiles, cap cases, invalid-mix, eligibility, persons); src/lib/money#estimateDeduction/requiresSignOff/EligibilityPosture; deferred-work.md#4-3/#epic-4 (persons flat cap — Sign-Off Q3, do NOT encode a multiplier)]
  - [x] 2.3 ATTACHMENT-READINESS category — the `REQUIRED_FILES_DEFERRED` readiness disclosure (Story 8.1 has not landed) is the "attachment-readiness flags" coverage: drive `classifyReadiness(...)` on a fabricated calc shape and assert the `REQUIRED_FILES_DEFERRED` warning is ALWAYS present (a DOCUMENTED deferral surfaced in the readiness output, never silently omitted). This is the same forward-seam Story 5.4 pinned (R-513) — the pack references it, it does NOT wire a real file check (Epic 8 owns that). [Source: test-design-epic-5.md#5.5-GOLDEN-01 (attachment-readiness flags), R-513; src/features/calculations/readiness.ts#classifyReadiness/REQUIRED_FILES_DEFERRED; deferred-work.md#5-4 implementation (required-file readiness is a placeholder pending Story 8.1); tests/unit/features/calculations/readiness.test.ts (the 5.4 classifier unit template)]
  - [x] 2.4 MARGIN category — TB% cases at the calc-row level: a row/section whose TB% = (sell − cost) / sell is BELOW / AT / ABOVE the pilot `PILOT_LOW_MARGIN_THRESHOLD` (0.15) classifies to a `LOW_MARGIN` warning (below) or no low-margin warning (at/above). TB% is a pure ratio of already-öre values — the fixture supplies `unit_cost_ore`/`unit_sell_ore`; the classifier (`readiness.ts#rowMarginRatio`) computes the ratio (no inline money arithmetic invented). A 0-sell row has NO defined margin (the `ZERO_PRICE_ROW` rule owns it) — do NOT assert a divide-by-zero margin. [Source: test-design-epic-5.md#5.5-GOLDEN-01 (margins); src/features/calculations/readiness.ts#rowMarginRatio/PILOT_LOW_MARGIN_THRESHOLD/LOW_MARGIN; owner-decisions-applied-2026-06-18.md#Epic 5·5.4/5.5 (margin warning = TB% vs a global threshold — pilot constant until the settings column lands); deferred-work.md#5-4 (PILOT_LOW_MARGIN_THRESHOLD is an UNAPPROVED pilot constant — do NOT invent a settings column here)]
- [x] Task 3: Fixture privacy scan + documented-delta labelling (AC: 2, 3)
  - [x] 3.1 Extend the pack-wide privacy scan to the NEW `calc-rows.json` (and any new calc fixture) — REUSE the existing scan classes from `tests/unit/lib/money/golden-pack.test.ts` (personnummer `\b\d{6}-\d{4}\b`, orgnr `\b\d{10}\b` no-dash, non-`example.test` email, `secret|password|api_key`, PHONE, ADDRESS heuristics), scanning the DATA payload WITHOUT the `_doc` prose (which legitimately names the PII rules). Assert every calc fixture's DATA is PII-free. NOTE the standing Iter-2 epic-4 deferral: the bare-`\d{10}` orgnr guard can FALSE-POSITIVE on a legitimate ≥10-digit öre value — keep every calc-fixture öre value UNDER 10 digits (< 1,000,000,000 öre = < 10,000,000 kr), or scope the orgnr scan to string-typed leaves; a spurious trip is a fixture-authoring bug, not a real leak. [Source: test-design-epic-5.md#5.5-UNIT-01, R-516; tests/unit/lib/money/golden-pack.test.ts (the reusable scan, lines 379-418); deferred-work.md#epic-4 Iteration 2 (ORGNR \d{10} pack-wide scan false-positives on a ≥10-digit öre fixture value — keep calc öre values under 10 digits)]
  - [x] 3.2 DOCUMENTED old/new-Lovable DELTA labelling (5.5-UNIT-02 / 5.5-DOCS-01): where a calc case captures an intentional divergence from Lovable behavior, label it `documented-delta` with the divergent old-Lovable value + a note classifying it as expected-simplification / bug-fix / unresolved-assumption. Lovable is a BEHAVIORAL ORACLE ONLY — capture the expected value shape, NEVER copy Lovable code or real data (AGENTS.md). A `documented-delta` golden FAILURE must point at the affected assumption. If NO real Lovable oracle calc example is available at implementation time, the pack still labels every case with a valid `origin` (mostly `new-expected` conservative pilot assumptions) — a `documented-delta` is only authored where a genuine divergence is known; do NOT fabricate one. [Source: epics.md#Story 5.5 AC2 + Migration/Coexistence Impact (shadow comparison inputs); test-design-epic-5.md#5.5-UNIT-02/#5.5-DOCS-01, Entry Criteria (anonymized Lovable calc oracle available; no real data copied); AGENTS.md (Lovable is a behavioral oracle only — do not copy code/data); tests/unit/lib/money/golden-pack.test.ts (the three-way origin discipline)]
- [x] Task 4: Verify, scope-guard sweep, deferred-work reconciliation, finalize (AC: 1-3)
  - [x] 4.1 Run the ACTIVE CI gate sequence locally IN ORDER (establish a clean baseline FIRST — a delegate can inherit a RED tree from an in-progress sibling): `pnpm install --frozen-lockfile` → `pnpm run verify:lockfiles` → `pnpm run verify:service-role-containment` → `pnpm typecheck` → `pnpm lint` → `pnpm run test:unit` → `pnpm build` → `pnpm run verify:bundle-containment`. This story is UNIT/GOLDEN-ONLY (pure `node --test` fixtures + a pack test) — it adds NO DB migration, NO product UI, NO command, NO E2E, so the INT/RLS/E2E DB suites are NOT required by THIS story's diff (they must stay green, but this story touches none of them). If you DO run the DB suites, AFTER `supabase db reset` poll `/auth/v1/health` to 200 before Vitest/Playwright (Kong→GoTrue 502s for ~10s and silently SKIPS DB-backed suites otherwise — a false green). NEVER chain `pnpm run X && pnpm run Y` in a package.json field (Windows PATH — use the bare-Node `scripts/run-tests.mjs` orchestrator; none new needed). [Source: project-context.md#Development Workflow Rules (CI gate), #Language-Specific Rules (no chained pnpm); retro-notes/epic-5.md#5-1 (health-poll gotcha); epic-2 retro (Windows chained-script)]
  - [x] 4.2 Scope guardrail sweep (Stop Conditions): this is a TESTS-ONLY story — it creates fixtures + golden/unit tests and NOTHING ELSE. NO new `src/**` product code, NO migration, NO command, NO UI, NO `nav-items.ts` change (stays EXACTLY seven), NO new dependency (approval-gated + exact-pinned; none expected), NO `.env` edit. NO inline money math anywhere in a fixture-driving test (every öre op routes through `totals.ts` / `@/lib/money`; TB% is a pure ratio of already-öre values done by the classifier, not the test — R-505). The inclusion rule is the FROZEN 2026-06-18 pin (hidden + selected COUNT; unselected does NOT) — a DIFFERENT inclusion rule or a divergent pinned öre treated as production-approved is a STOP (`needs-human`; R-508). `persons` per-person cap stays FLAT — no golden asserts a multiplier (R-512). NO document-level rounding / discount / negative-amount row model (STOP → `needs-human`; R-511). If a real Lovable calc example is REQUIRED to produce an expected value and none is available (or would require real customer data), STOP (`needs-human`). [Source: epics.md#Story 5.5 Stop Conditions (stop if real Lovable customer data is required or owner decisions on hidden rows/options block expected values); test-design-epic-5.md Not-in-Scope, R-505/R-508/R-511/R-512/R-516; project-context.md#Development Workflow Rules; src/components/app-shell/nav-items.ts (stays seven)]
  - [x] 4.3 Security/RLS + Money/Tax statement for the PR: this story adds ONLY anonymized golden fixtures + pure `node --test` tests — NO DB access, NO RLS surface, NO service-role, NO new read/command/audit, NO tenant data. Fixture privacy (no PII/secrets) is the only security concern and is enforced by the extended privacy scan (AC3). NOTHING is marked production-approved: the pack re-approves NO Epic-4 assumption (`signOff` stays pending); every ROT/grön/VAT/rounding number is REFERENCED from an existing UNAPPROVED-labelled authority, never re-decided. Personnummer NEVER enters a calc fixture or the engine (POSTURE only; R-516). [Source: epics.md#Story 5.5 Security/RLS Impact (fixture privacy only; no live tenant data), Money/Tax/Quote Impact (money/tax correctness oracle); project-context.md#Money/Tax rules (UNAPPROVED profiles; engine takes a posture never PII); test-design-epic-5.md#R-516]
  - [x] 4.4 Deferred-work reconciliation + DOCS residual (5.5-DOCS-01/03): (a) Do NOT reopen the inherited low deferrals that do NOT overlap a tests-only story — `is_selected boolean|null` read-type (5.2), row line-total label (RESOLVED by 5.4), pricing-read graceful-degradation (5.3), reorder error-mapping nicety (5.1), the calc-tables migration-reset money-column-guard robustness (5.1). (b) The two standing NFR CONCERNS (no `pnpm audit` CI gate, no coverage reporter) are an EPIC-GATE decision (schedule-or-accept at the Epic 5 trace/gate) — do NOT add them here (R-510). (c) The `persons` flat-cap (R-512) + the required-file 8.1 sequencing (R-513) are AWARENESS residuals the pack surfaces (attachment-readiness + no-multiplier), already ledgered by 5.4 — do NOT re-defer. (d) The epic-4 Iter-2 `ORGNR \d{10}` false-positive-on-large-öre trap is a REAL constraint for THIS story's fixture authoring (keep öre < 10 digits) — honor it, and if you cannot, record a NEW deferral WITH an owner (scope the scan to string leaves). (e) Add a DOCS-03 hygiene note in the pack test docstring: calc goldens live under `tests/unit/**`, never `tests/golden/**` (the vacuous-green runner-glob trap). Record any NEW deferral WITH an owner story. [Source: test-design-epic-5.md#5.5-DOCS-01/#5.5-DOCS-03/#5.4-DOCS-02, R-510/R-512/R-513; deferred-work.md#5-1/#5-2/#5-3/#5-4/#epic-4-Iter-2 (the non-overlapping items — do NOT reopen; the orgnr-scan trap — honor); retro-notes/epic-5.md#epic-5 (standing NFR = epic-gate)]

## Dev Notes

### Critical Constraints (read first)

- **THIS IS A GOLDEN/UNIT-TESTS-ONLY STORY — it authors an ORACLE, it ships NO product code, NO schema, NO UI, NO command, NO migration, NO E2E.** Story 5.1 landed the calc schema + commands; 5.2 the editor + `totals.ts`; 5.3 the pricing-source snapshots; 5.4 the readiness classifier + the VAT-posture helper + the FIRST calc-row inclusion golden. 5.5's ENTIRE job: the FULL calc golden PACK — a new anonymized calc-row fixture + a live-oracle pack test that DRIVES the real `totals.ts` / `readiness.ts` / `@/lib/money` primitives across every AC1 category, references the existing per-category numeric authorities (never re-pins them), documents old/new deltas with a three-way `origin` label, and enforces fixture privacy. State your operating mode (implementation — a Phase A story authorizes the test-authoring work) before editing. [Source: epics.md#Story 5.5 Technical Notes (build on Epic 4 fixture structure; do NOT depend on final PDF rendering); test-design-epic-5.md#5.5-GOLDEN-01; 5-1/5-2/5-3/5-4 stories (the finished substrate to drive as the oracle)]
- **EXTEND THE PIN — DO NOT RE-DECIDE IT. SINGLE NUMERIC AUTHORITY PER CATEGORY (R-508).** Each AC1 money category already has ONE frozen numeric authority: inclusion/options/hidden-rows → `options-tillval.json`; ROT/grön/caps/mix/eligibility → `rot-gron-deductions.json`; regular VAT + fractional-qty → `vat-rates.json`; rounding → `rounding-mode.json`. The pack REFERENCES these (asserts the category is covered + proves the calc-row surface reproduces the pinned öre) rather than re-encoding the numbers. A DIVERGENCE from any pinned öre — a calc-row total that does NOT match the frozen inclusion/VAT/ROT number — is a STOP (`needs-human`), NOT a re-pin and NOT a local tweak. The `golden-pack.test.ts#CATEGORY_MANIFEST` is the exact reference-not-re-encode pattern to mirror. [Source: test-design-epic-5.md#R-508, Testability Notes 5 (the inclusion pin is frozen — MATCH it, a different rule is a STOP); tests/fixtures/golden/money/*.json (the per-category authorities); tests/unit/lib/money/golden-pack.test.ts]
- **DRIVE THE REAL ENGINE — the fixtures are LIVE oracles, never static schema.** Every numeric golden case DRIVES the real `totals.ts` (`computeLineTotal`/`computeSectionTotal`/`computeCalcTotal`/`rowCountsTowardTotal`/`resolveTotalDisplay`) or the real `@/lib/money` (`sumOre`/`lineVatOre`/`lineNetOre`/`vatBreakdown`/`estimateDeduction`/`selectVatDisplay`) or the real `readiness.ts` (`classifyReadiness`/`rowMarginRatio`) and asserts the OUTPUT equals the pinned öre/classification. A fixture that only asserts its own fields (never feeding the engine) is a vacuous pack — the 4.4 `golden-pack-coverage.test.ts` GAP-1..GAP-5 pattern (turning static fixture fields into live negative/positive oracles) is the discipline to follow. [Source: test-design-epic-5.md#Testability Notes 2 (push math to UNIT+GOLDEN through the engine), 5.5-GOLDEN-01; tests/unit/lib/money/golden-pack.test.ts + golden-pack-coverage.test.ts (the live-oracle pattern); src/features/calculations/totals.ts (the calc oracle)]
- **RUNNER-GLOB TRAP: calc goldens live under `tests/unit/**`, NEVER `tests/golden/**`.** The `node --test` fast gate globs `tests/unit/**`; a golden authored under `tests/golden/**` is silently NEVER RUN (a vacuous green). Every 5.5 test file goes under `tests/unit/features/calculations/**` (the calc oracle) — mirroring `readiness-inclusion.golden.test.ts` (5.4) and `tests/unit/lib/money/golden-pack.test.ts` (4.4). The FIXTURES stay under `tests/fixtures/golden/money/**` (data, not run by the globber). [Source: project-context.md#Testing Rules (runner-glob trap; goldens under tests/unit/**); test-design-epic-5.md#5.5-DOCS-03 (calc goldens under tests/unit/**, never tests/golden/** — the vacuous-green trap); tests/unit/features/calculations/readiness-inclusion.golden.test.ts (the correct location)]
- **NOTHING IS PRODUCTION-APPROVED — the pack re-approves NO Epic-4 assumption.** Every ROT/grön rate/cap, every VAT rate, the rounding mode, the inclusion rule, and the low-margin threshold is a CONSERVATIVE UNAPPROVED PILOT ASSUMPTION (`signOff: "pending-owner-accounting-legal"`; profiles named `*_UNAPPROVED`; `PILOT_LOW_MARGIN_THRESHOLD`). The new `calc-rows.json` fixture MUST carry the SAME `signOff` marker, and a sanity test asserts the pack marks nothing production-approved. A ROT/grön warning golden asserts `requiresSignOff === true` and frames the deduction as an ESTIMATE — never approved/legally-final (R-509). `persons` stays a FLAT-cap placeholder — NO golden asserts a per-person-scaled cap (R-512). [Source: test-design-epic-5.md#R-509/#R-512, Not-in-Scope (owner/legal approval of ROT/grön/VAT constants + disclaimer is a standing Epic-4 sign-off); tests/fixtures/golden/money/rot-gron-deductions.json (signOff/approved:false convention); src/features/calculations/readiness.ts#PILOT_LOW_MARGIN_THRESHOLD; deferred-work.md#4-3/#epic-4 (persons flat cap)]
- **GATED / Stop Conditions (this story):** STOP (`needs-human`) if real Lovable CUSTOMER DATA is required to produce a fixture expected value (Lovable is a behavioral oracle only — capture shapes, never copy data/code); if an owner decision on hidden rows / options / a different inclusion rule would BLOCK an expected value (a production-approved DIFFERENT inclusion rule is a STOP); if a calc-row total DIVERGES from a frozen per-category pin (never re-pin silently); or if a document-level-rounding / discount / negative-amount model is needed. Adding ANY dependency is approval-gated + EXACT-pinned (none expected). NO `.env` edit. NO migration. [Source: epics.md#Story 5.5 Stop Conditions; test-design-epic-5.md#R-508/#R-511/#R-516; AGENTS.md (Lovable oracle only); project-context.md#Development Workflow Rules]

### Epic-5 retro-note constraints (surfaced earlier in this epic — apply directly)

From `_bmad-output/auto-bmad/retro-notes/epic-5.md` — reflected in the tasks, not left as a pointer:

- **Pure helpers embedded in a `"use client"` `.tsx` escape the `node:test` fast gate (can't import JSX) — the pack MUST drive the already-EXTRACTED pure modules (`totals.ts`, `readiness.ts`, `vat-posture.ts`, `@/lib/money`), NEVER re-implement their logic in the test.** 5.1/5.2/5.3/5.4 already extracted every calc contract into pure `.ts` (the `source-options.ts`/`totals.ts` split lesson); 5.5 consumes them. If a category's logic is NOT yet in a pure module, that is a gap in the earlier story — do NOT fork the logic into the golden test. [Source: retro-notes/epic-5.md#5-3 (Phase-6 automate: extract pure helpers out of .tsx for the fast gate); project-context.md#Testing Rules (coverage-shape); src/features/calculations/{totals,readiness,vat-posture}.ts]
- **Raw pg pool returns bigint öre columns as strings and timestamptz as Date objects — INT readbacks must coerce.** 5.5 is PURE-UNIT/GOLDEN (in-memory numbers from JSON fixtures + the pure engine) — it touches NO DB, so this does NOT bite here. Do NOT introduce a DB read into a golden test to "verify" a fixture; the golden drives the pure engine on in-memory öre. [Source: retro-notes/epic-5.md#5-3 (Phase-5 dev-story pg representation); this story is pure — no DB]
- **Order-asserting E2E specs on the shared serial calc fixture must not assume a fixed section/row count.** 5.5 adds NO E2E and NO DB seed — it is pure golden/unit — so the count-fragility + `crypto.randomUUID()` seeding gotchas do NOT apply. Do NOT add a DB-backed count-asserting test here. [Source: retro-notes/epic-5.md#5-3 (order-assertion fragility); this story is pure golden/unit]
- **The two standing NFR CONCERNS (no `pnpm audit` CI gate; no coverage reporter) are an EPIC-GATE decision, NOT a 5.5 dev action.** Do NOT add a `pnpm audit` gate or coverage reporter in this story — they are surfaced schedule-or-accept at the Epic 5 trace/gate (carried un-actioned since Epic 2). [Source: retro-notes/epic-5.md#epic-5 + #5-1; test-design-epic-5.md#R-510]
- **Lingering `describe.skip`/`notYetImplemented` is the resumed-run trap.** 5.5 has NO ATDD red phase (the engine + calc surface already EXIST — the pack is authored + greened in ONE pass, exactly like the 4.4 pack). If you scaffold a test file ahead, REMOVE any `.skip`/throwing placeholder in the SAME green run — a skipped golden is a vacuous pass on the exact oracle this story exists to build. [Source: retro-notes/epic-5.md#5-1 (resumed-run trap); tests/unit/lib/money/golden-pack.test.ts (authored + greened in one pass — no red phase for a pack over an existing engine)]

### Inherited Epic-4/5.1/5.2/5.3/5.4 facts & gotchas (apply directly)

- **`src/features/calculations/totals.ts` is the CALC oracle — DRIVE it, never re-derive a total.** `computeLineTotal`/`computeSectionTotal`/`computeCalcTotal`/`rowCountsTowardTotal`/`resolveTotalDisplay` already delegate every öre op to `@/lib/money` AND apply the frozen 2026-06-18 inclusion pin. The pack drives THESE on fixture-supplied `TotalsRowInput` shapes. `TotalsRowInput = { quantity, unit_sell_ore: number|null, vat_rate_bp: number|null, is_hidden, is_optional, is_selected: boolean|null }`. [Source: src/features/calculations/totals.ts (the totals + inclusion authority); test-design-epic-5.md#R-505]
- **`src/features/calculations/readiness.ts#classifyReadiness` is the READINESS oracle** (blockers vs warnings) + `rowMarginRatio` (TB%) + the `REQUIRED_FILES_DEFERRED` / `TAX_SIGN_OFF_REQUIRED` / `HIDDEN_ROWS_INCLUDED` / `LOW_MARGIN` codes. The pack's margin + attachment-readiness + tax-warning + hidden-rows-disclosure cases drive `classifyReadiness` on a fabricated `ReadinessInput` (customer context + sections + `vatPostureResolved` + tax context). NO personnummer in the input (`ReadinessCustomerContext` carries display fields + a TYPE only). [Source: src/features/calculations/readiness.ts#classifyReadiness/ReadinessInput/ReadinessRowInput/rowMarginRatio/ReadinessCode; test-design-epic-5.md#5.4-UNIT-* (the classifier the pack drives)]
- **`src/features/calculations/vat-posture.ts#resolveVatDisplayPosture(customerType, tenantDefaultVatDisplay)` is the POSTURE authority** — `private`→`private` invariant; non-private→tenant default AS-IS (`company_togglable | company_excl`). Feed its output into `resolveTotalDisplay` for the VAT-display category. `DEFAULT_TENANT_VAT_DISPLAY = "company_togglable"` is the no-settings fallback. [Source: src/features/calculations/vat-posture.ts; src/lib/money/vat.ts#selectVatDisplay/VatDisplayView (primary = net for company_excl, gross otherwise; togglable only for company_togglable; private always incl)]
- **The `@/lib/money` barrel resolves under `node --test` via the Story 4.1 alias-hook** — import `computeSectionTotal` etc. from `@/features/calculations/totals` and the engine from `@/lib/money` at the top level (mirroring `readiness-inclusion.golden.test.ts`). The alias hook resolves a bare directory import (`@/lib/money`) to its `index.ts` barrel and handles extensionless `@/*` imports. [Source: project-context.md#Testing Rules (the alias hook); tests/unit/features/calculations/readiness-inclusion.golden.test.ts (the working import pattern to copy)]
- **The 4.4 pack pattern is the exact template.** `tests/unit/lib/money/golden-pack.test.ts` = the 4 pack-level guards (COVERAGE manifest, LABELLING, BEHAVIORAL golden, SCHEMA-SHAPE) + the pack-wide PRIVACY scan; `golden-pack-coverage.test.ts` = the GAP-1..GAP-5 live-oracle expansion. Mirror this structure at the CALC-ROW layer. The `AC1_CATEGORIES` + `CATEGORY_MANIFEST` reference-not-re-encode shape shows how to cover a category by REFERENCE to its existing fixture (a `raw.includes(token)` on the fixture file) vs a NEW live case. [Source: tests/unit/lib/money/golden-pack.test.ts; tests/unit/lib/money/golden-pack-coverage.test.ts]
- **`options-tillval.json` was ALREADY extended to the calc-row surface by 5.4's `readiness-inclusion.golden.test.ts`.** Do NOT duplicate the 5.4 inclusion golden — the pack ASSERTS the options/tillval + hidden-rows category is covered (by reference to the 5.4 golden + the fixture) and adds only NEW composition cases if useful (referencing the SAME pinned öre). [Source: tests/unit/features/calculations/readiness-inclusion.golden.test.ts (the 5.4 calc-row inclusion golden — already lands options/tillval + hidden-rows at the calc-row layer); test-design-epic-5.md#5.4-GOLDEN-01 vs #5.5-GOLDEN-01 (5.4 = inclusion only; 5.5 = the full pack)]
- **Adversarial-review ground-truth check.** The toolchain uses REAL forward-dated pins (Next 16.2.9, React 19.2.4, TS 5.9.3, Tailwind v4, Vitest 4.1.9, Playwright, pnpm 10.24.0, Node ≥20.9.0). If a later review claims a version/API is wrong, cross-check `package.json`/the lockfile before acting. [Source: project-context.md#Technology Stack (the pins are real)]

### Inherited deferred-work items that overlap this story

From `_bmad-output/implementation-artifacts/deferred-work.md` — only entries whose subject overlaps Story 5.5 (a tests/golden-only story):

- **[Low, epic-4 Iter-2] The pack-wide ORGNR `/\b\d{10}\b/` privacy scan false-positives on a future ≥10-digit öre/cap fixture value — DIRECTLY RELEVANT to authoring `calc-rows.json`.** Today no money fixture emits a bare 10-digit number (`capOre: 5_000_000` is 7 digits). A calc fixture with a total/öre value ≥ 1,000,000,000 öre (10 digits = ≥ 10,000,000 kr) would trip the orgnr guard as a spurious "PII leak". CONSTRAINT: keep every calc-fixture öre value UNDER 10 digits, OR (the deferral's suggested fix) scope the orgnr scan to string-typed leaves / exclude the known numeric öre/rate/cap keys. Honor the constraint in fixture authoring; only widen the scan (with a dated justification) if a legitimate large öre value is unavoidable. [Source: deferred-work.md#epic-4 Iteration 2 (ORGNR \d{10} pack-wide scan; owner: a future fixture/golden-pack hardening pass); tests/unit/lib/money/golden-pack.test.ts]
- **[Low, epic-4 Iter-2] The golden-pack `4.4 golden-pack coverage MANIFEST asserts each category by a raw substring token`, so a category can report "covered" on an incidental prose match — a caution for the 5.5 manifest.** When 5.5 mirrors the `CATEGORY_MANIFEST` reference pattern, prefer proving a BEHAVIORAL case is pinned per NEW calc category (drive a value through `totals.ts`/`readiness.ts`), not merely that a token exists in the file — so a category is not "covered" by an incidental note-field match. Reference-only is acceptable for a category whose numbers another fixture already owns (that fixture's own live-oracle test proves the behavior); a NEW calc category gets a live case. [Source: deferred-work.md#epic-4 Iteration 2 (4.4 golden-pack coverage MANIFEST substring-token weakness; owner: a future golden-pack hardening pass); tests/unit/lib/money/golden-pack.test.ts#CATEGORY_MANIFEST]
- **[Low, epic-4 Iter-2] Golden-master surface-present `describe.skip` gates were not removed after the engine landed — do NOT introduce a surface-present `describe.skip` in the 5.5 pack.** The 4.4 goldens left `TAX_SURFACE_PRESENT`/`VAT_SURFACE_PRESENT` `describe.skip` gates that can silently self-disable the suite. The calc surface (`totals.ts`/`readiness.ts`/`@/lib/money`) EXISTS at 5.5 time — assert the imported functions are present as a HARD assertion (fail if absent), NEVER a `describe.skip` precondition that would let the whole pack silently disable itself. [Source: deferred-work.md#epic-4 Iteration 2 (surface-present describe.skip gates can self-disable; owner: a future test-hardening pass); tests/unit/lib/money/{tax,vat}.golden.test.ts]
- **[Awareness, R-512] `persons` per-person ROT cap is a flat placeholder (4.3 + epic-4 review) — do NOT author a golden that asserts a per-person-scaled cap.** `estimateDeduction` clamps to a single flat `capOre`; `persons` is inert. A 5.5 ROT/grön golden may THREAD `persons` into a fixture (as Epic 4 does) but MUST assert the FLAT cap — a per-person multiplier is owner-gated (Sign-Off Q3). [Source: deferred-work.md#4-3 + #epic-4 review (persons flat cap — Sign-Off Q3); test-design-epic-5.md#R-512; tests/fixtures/golden/money/rot-gron-deductions.json (persons:1, flat cap)]
- **[Awareness, R-513] Required-file readiness check is a placeholder pending Story 8.1 — the pack's attachment-readiness coverage is the `REQUIRED_FILES_DEFERRED` disclosure, NOT a real file check.** Story 8.1 (file-metadata foundation) has NOT landed; the "attachment-readiness flags" AC1 category is satisfied by asserting the `REQUIRED_FILES_DEFERRED` readiness warning is always present (a documented deferral). Do NOT build a file-metadata read or a real required-file check (Epic 8 owns it). [Source: deferred-work.md#5-4 implementation (required-file readiness placeholder pending Story 8.1, R-513); src/features/calculations/readiness.ts#REQUIRED_FILES_DEFERRED]
- **[Low, epic-3/5.1/5.2] Non-overlapping deferrals — do NOT reopen in a tests-only story.** The `is_selected boolean|null` read-type inconsistency (5.2), the row line-total label (RESOLVED by 5.4), the pricing-read graceful-degradation UX hint (5.3), the reorder cross-tenant-child error-mapping nicety (5.1), and the calc-tables migration-reset money-column-guard robustness (5.1) are all future-hardening passes on PRODUCT code / DB tests — none is a golden/fixture concern. 5.5 touches none of them. [Source: deferred-work.md#5-1/#5-2/#5-3/#5-4 (the non-overlapping items); this story is fixtures + golden tests only]

### Architecture compliance

- **Testing levels (architecture §10; test-levels-framework):** pure calculation/classification ⇒ UNIT/GOLDEN. The calc money math is ENTIRELY delegated to the frozen `@/lib/money` engine; 5.5 asserts ROUTING + INCLUSION/READINESS classification + labelled deltas, NOT the arithmetic (Epic 4 pinned the numbers). A total computed inline in a test escapes the engine — drive `totals.ts`/`@/lib/money`, never re-derive. [Source: architecture.md#10; test-design-epic-5.md#Testability Notes 2; test-levels-framework.md]
- **Money/Tax (architecture §10):** öre stays integer; VAT stays basis points; the deduction is an UNAPPROVED estimate with `requiresSignOff`; the engine takes an eligibility posture, never PII. The golden pack DISPLAYS/ASSERTS these — it computes no new money rule and re-approves no assumption. [Source: architecture.md#10; src/lib/money; project-context.md#Money/Tax rules]
- **No new product surface (architecture §5/§6/§22):** this story adds NO server read/command, NO DB access, NO RLS surface, NO UI, NO nav change. It adds anonymized fixtures under `tests/fixtures/golden/money/**` + pure tests under `tests/unit/features/calculations/**`. Money in integer öre; fixtures carry öre/rate/qty numbers only. [Source: architecture.md#5/#6/#22; project-context.md#Architecture Rules]
- **Migration/coexistence (architecture §12/§13):** the pack supplies SHADOW-COMPARISON inputs for the later Lovable golden-master harness (Epic 9) — keep fixtures structured, anonymized, and labelled (old-Lovable / new-expected / documented-delta) so a future comparison can consume them. Do NOT depend on final PDF rendering (Epic 6 owns the PDF). [Source: epics.md#Story 5.5 Migration/Coexistence Impact (shadow comparison inputs) + Technical Notes (do not depend on final PDF rendering); architecture.md#12/#13]

### Library / framework requirements

- **No new dependency expected.** `node --test` (the pure fast gate), the `@/lib/money` engine, `totals.ts`/`readiness.ts`/`vat-posture.ts`, and the existing golden fixtures + alias hook all exist. Adding ANY dependency is approval-gated + EXACT-pinned (no `^`/`~`). [Source: project-context.md#Technology Stack; package.json]
- **Pinned stack to respect (do not bump):** Node `>=20.9.0`, pnpm `10.24.0`, Next `16.2.9`, React `19.2.4`, TS `5.9.3` (`strict`, `isolatedModules` → `import type`, `@/*` alias), Tailwind v4, Vitest `4.1.9`, Playwright. These forward-dated pins are REAL. This story runs ONLY on `node --test` (`pnpm run test:unit`) — it needs neither Vitest nor Playwright. [Source: project-context.md#Technology Stack]

### File-structure requirements (where things go)

- `tests/fixtures/golden/money/calc-rows.json` — NEW anonymized calc-row golden fixture: `_doc` provenance + a `policy` block (`signOff: "pending-owner-accounting-legal"`) + a `cases` array covering the NEW AC1 categories (five row types, fractional qty, margins, section display modes). Öre/rate/qty numbers ONLY — NO PII, NO clock, every öre value UNDER 10 digits (the orgnr-scan trap). [test-design-epic-5.md#5.5-GOLDEN-01; tests/fixtures/golden/money/options-tillval.json (the shape to mirror)]
- `tests/unit/features/calculations/calc-golden-pack.test.ts` — NEW pack test (`node --test`, under `tests/unit/**`): the 4 pack-level guards (COVERAGE manifest, LABELLING, BEHAVIORAL live-oracle, SCHEMA-SHAPE) + the extended fixture-privacy scan, driving `totals.ts`/`readiness.ts`/`vat-posture.ts`/`@/lib/money`. Mirrors `tests/unit/lib/money/golden-pack.test.ts`. [test-design-epic-5.md#5.5-GOLDEN-01/#5.5-UNIT-01/02; tests/unit/lib/money/golden-pack.test.ts]
- Optionally split the live-oracle expansion into `tests/unit/features/calculations/calc-golden-pack-coverage.test.ts` (mirroring `golden-pack-coverage.test.ts`) if the single file grows large — same runner, same location. [tests/unit/lib/money/golden-pack-coverage.test.ts]
- REUSE (drive, do NOT edit): `src/features/calculations/totals.ts`, `readiness.ts`, `vat-posture.ts`; `src/lib/money/**`; the existing per-category fixtures `tests/fixtures/golden/money/{options-tillval,rot-gron-deductions,vat-rates,rounding-mode,accepted-price-deltas}.json`; the 5.4 `tests/unit/features/calculations/readiness-inclusion.golden.test.ts` (do NOT duplicate its inclusion cases). [src/features/calculations/**; src/lib/money/**]
- NOT touched: any `src/**` product code (this is tests-only); `nav-items.ts` (stays seven); the frozen migrations; any `quotes`/`quote_versions` table (Epic 6); `tests/golden/**` (the runner-glob trap — goldens go under `tests/unit/**`). [src/components/app-shell/nav-items.ts; src/lib/money]

### Testing requirements (this story)

Pure UNIT + GOLDEN run under `node --test` (the fast gate on every PR). No DB, no E2E. Required (mapped to the epic test design):

- **Full calc golden pack (P0, 5.5-GOLDEN-01, AC1):** COVER every AC1 calc category at the calc-row layer — labor/material/subcontractor/machinery/other, fractional qty, margins, options/tillval, hidden rows, detailed/summary/text_only section modes, VAT display, ROT/grön warnings, attachment-readiness. Drive the REAL `totals.ts`/`readiness.ts`/`@/lib/money`; reference the existing per-category numeric authorities (single authority per category); a divergence is a STOP. [test-design-epic-5.md#5.5-GOLDEN-01, R-508/R-505]
- **Documented old/new delta (P1, 5.5-UNIT-02, AC2):** an intentional Lovable delta is labelled expected-simplification / bug-fix / unresolved-assumption; a `documented-delta` carries its divergent old value; a golden failure points to the affected assumption. [test-design-epic-5.md#5.5-UNIT-02, R-508]
- **Fixture privacy (P0, 5.5-UNIT-01, AC3):** no real names/emails/phones/addresses/personnummer/orgnr/secrets/raw files in any calc fixture DATA payload; no PII into `@/lib/money` or a row snapshot (POSTURE only). Reuse + extend the existing golden PII/secret scan; keep öre values < 10 digits (orgnr-scan trap). [test-design-epic-5.md#5.5-UNIT-01, R-516]
- **Property/fuzz (P3, 5.5-UNIT-03, exploratory):** qty×unit-price + section-sum invariants via the engine (exploratory; not a gate). Optional. [test-design-epic-5.md#5.5-UNIT-03]
- **DOCS residual (P2/P3, 5.5-DOCS-01/03):** documented old/new-Lovable calc deltas where they exist (migration-delta doc, not a gate); the runner-glob reminder (calc goldens under `tests/unit/**`, never `tests/golden/**`). Awareness only. [test-design-epic-5.md#5.5-DOCS-01/#5.5-DOCS-03]
- **NOT in this story's tests:** the calc TABLE/RLS migration reset (Story 5.1 — green); pricing-source freeze/spoof (Story 5.3 — green); the readiness CLASSIFIER unit pins (Story 5.4 — green); the E2E editor flows (Stories 5.2/5.4 — green); the money ARITHMETIC (Epic 4 pinned it); any quote-version create/send/immutability (Epic 6). 5.5 EXTENDS the engine's oracle to the calc-row layer — it does NOT re-test the schema, the commands, the UI, or the arithmetic. [test-design-epic-5.md#What is NEW in Epic 5; epics.md#Epic 5/6 split]

### Project Structure Notes

- Adds an anonymized calc-row golden FIXTURE + a live-oracle pack TEST on TOP of the finished 5.1-5.4 substrate + the frozen Epic-4 money fixtures — NO product code, NO schema, NO migration, NO command, NO UI, NO nav change. The pack drives the already-extracted pure modules (`totals.ts`/`readiness.ts`/`vat-posture.ts`/`@/lib/money`), mirroring the 4.4 `golden-pack.test.ts` structure. [Source: architecture.md#10; tests/unit/lib/money/golden-pack.test.ts]
- The pack is the RECURRING calc ORACLE: a future money/tax/inclusion/readiness change produces a golden FAILURE that points at the affected assumption/delta rather than an unexplained diff. It supplies SHADOW-COMPARISON inputs for the Epic-9 Lovable golden-master harness. Keep every fixture structured, reviewable, anonymized, and labelled (old-Lovable / new-expected / documented-delta). [Source: epics.md#Story 5.5 (Money/Tax + Migration/Coexistence Impact); test-design-epic-5.md#5.5-GOLDEN-01]
- SINGLE NUMERIC AUTHORITY per category is the load-bearing structural discipline: the pack REFERENCES the existing per-category money fixtures rather than re-encoding their numbers, so there is exactly ONE place each ROT/grön/VAT/rounding/inclusion number lives. A later owner sign-off swaps the numbers in ONE fixture with NO code-shape change. [Source: test-design-epic-5.md#R-508; tests/unit/lib/money/golden-pack.test.ts#CATEGORY_MANIFEST]

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Epic 5 (lines 1026-1037: goal/scope/non-scope/risks) + #Story 5.5: Calculation Golden Tests For Options, Hidden Rows, And Tax Warnings (lines 1179-1211; AC1-AC3, Technical Notes (build on Epic 4 fixture structure; do NOT depend on final PDF rendering), Test/Security/Money/Migration Impact, Dependencies Stories 4.4 + 5.1-5.4, Stop Conditions (stop if real Lovable customer data is required or owner decisions on hidden rows/options block expected values))]
- [Source: _bmad-output/planning-artifacts/architecture.md#10 Money And Tax Model (öre, VAT bp, unapproved deduction) / #11 Immutable snapshot model / #12-13 Migration & golden-master comparison / #22 Patterns (naming/format)]
- [Source: _bmad-output/planning-artifacts/owner-decisions-applied-2026-06-18.md#Epic 5·5.4/5.5 (display modes: detailed + text-only first, summary later; options accepted WITH base but individually selectable; margin warning = TB% vs a global threshold in settings; hidden rows DO count in totals + deductions) + #Epic 4·4.3 (only private ROT-eligible; ROT×grön cannot mix)]
- [Source: _bmad-output/test-artifacts/test-design-epic-5.md (R-505 no inline money math; R-508 hidden-row/tillval inclusion pin — a different rule is a STOP; R-509 tax-warning-not-final; R-510 standing NFR epic-gate; R-512 persons flat cap; R-513 required-file check depends on Story 8.1; R-516 calc/fixture PII; 5.5-GOLDEN-01 (the full pack) + 5.5-UNIT-01 (fixture privacy) / 5.5-UNIT-02 (documented delta) / 5.5-UNIT-03 (property) + 5.5-DOCS-01/03; Testability Notes 2 (push math to UNIT+GOLDEN through the engine) + 5 (extend the pin, do NOT re-decide it) + 6 (goldens under tests/unit/**, never tests/golden/**); Exit Criteria (golden pack complete + labelled + fixture privacy green))]
- [Source: _bmad-output/project-context.md#Money, Tax, And Quote Rules (integer öre; consume the engine; UNAPPROVED profiles + requiresSignOff; persons flat cap; engine takes a posture never PII) / #Testing Rules (node --test unit; goldens under tests/unit/**; runner-glob trap; the alias hook resolves @/lib/money to its barrel; standing NFR concerns are epic-gate) / #Development Workflow Rules (CI gate; gated deps/migrations; PR requirements) / #Language-Specific Rules (typed Result; no chained pnpm; import type)]
- [Source: tests/unit/lib/money/golden-pack.test.ts + golden-pack-coverage.test.ts (the 4.4 pack template: 4 pack-level guards + pack-wide privacy scan + the GAP-1..GAP-5 live-oracle expansion); tests/fixtures/golden/money/{options-tillval,rot-gron-deductions,vat-rates,rounding-mode,accepted-price-deltas}.json (the per-category numeric authorities to REFERENCE); tests/unit/features/calculations/readiness-inclusion.golden.test.ts (the 5.4 calc-row inclusion golden — do NOT duplicate)]
- [Source: src/features/calculations/totals.ts (the calc totals + inclusion oracle) / readiness.ts (classifyReadiness + rowMarginRatio + REQUIRED_FILES_DEFERRED/TAX_SIGN_OFF_REQUIRED/LOW_MARGIN codes) / vat-posture.ts (resolveVatDisplayPosture) / read.ts (CalculationRowRow/CalculationSectionRow shapes) ; src/lib/money/vat.ts (selectVatDisplay/VatDisplayPosture/VatDisplayView) ; src/lib/money (sumOre/lineVatOre/lineNetOre/vatBreakdown/estimateDeduction)]
- [Source: AGENTS.md (Lovable is a behavioral oracle only — do not copy code/data by default); _bmad-output/auto-bmad/retro-notes/epic-5.md (extract pure helpers for the fast gate; goldens under tests/unit/**; standing NFR = epic-gate; no ATDD red phase for a pack over an existing engine); _bmad-output/implementation-artifacts/deferred-work.md#epic-4-Iter-2 (orgnr \d{10} scan false-positive on ≥10-digit öre — keep öre < 10 digits; substring-token manifest weakness; surface-present describe.skip self-disable trap) / #5-4 (persons flat cap R-512; required-file 8.1 deferral R-513)]

## Dev Agent Record

### Agent Model Used

Opus 4.8 (1M context) — `claude-opus-4-8[1m]` — via bmad-dev-story (auto-bmad delegate).

### Debug Log References

- `pnpm run test:unit` (`node --test` fast gate) — 845 pass / 0 fail / 0 todo / 0 skip after the pack was authored + greened in ONE pass (the scaffold's 13 `todo(...)` placeholders were all replaced with live-oracle assertions and removed).
- Targeted pack run: `node --experimental-strip-types --import ./tests/support/register.mjs --test "tests/unit/features/calculations/calc-golden-pack.test.ts"` — 14 pass / 0 fail / 0 todo / 0 skip.
- Full active CI gate sequence (in order, each step separate — no chained pnpm): `pnpm install --frozen-lockfile` ✅ → `verify:lockfiles` ✅ → `verify:service-role-containment` ✅ → `typecheck` ✅ → `lint` ✅ (0 errors; 1 PRE-EXISTING warning in the untouched `tests/unit/lib/money/vat.test.ts`) → `test:unit` ✅ → `build` ✅ → `verify:bundle-containment` ✅.

### Completion Notes List

- **Tests-only story — authored the FULL calc golden PACK; ships NO product code, NO schema, NO migration, NO UI, NO command, NO E2E.** Operating mode: implementation (a Phase A story authorizes the test-authoring work). Two deliverables: the NEW anonymized fixture `tests/fixtures/golden/money/calc-rows.json` + the live-oracle pack test `tests/unit/features/calculations/calc-golden-pack.test.ts` (the create-story/ATDD scaffold's `todo(...)` bodies filled + removed in the same green pass — no `test.skip`/`describe.skip`/`todo` remains).
- **Live oracle, not static schema (Guard 3):** every numeric case DRIVES the real engine and asserts the OUTPUT — `computeLineTotal`/`computeSectionTotal` for the five row types (labor/material/subcontractor/machinery/other) + fractional qty + section-mode composition; `classifyReadiness`/`rowMarginRatio` for margins (LOW_MARGIN below 0.15, none at/above, ZERO_PRICE_ROW for a 0-sell row), the MISSING_WORK_ROLE labor warning, TAX_SIGN_OFF_REQUIRED (framed as an ESTIMATE), and the always-present REQUIRED_FILES_DEFERRED disclosure (R-513); `resolveVatDisplayPosture`→`resolveTotalDisplay` for the excl/incl/both VAT-display views + a posture round-trip returning the identical stored öre; `estimateDeduction` for the ROT/grön warning (requiresSignOff stays true, a ROT×grön mix is the blocking ROT_GRON_MIX_NOT_ALLOWED, only `private` eligible, `persons` a FLAT cap — a persons=4 run yields the identical deduction, R-512).
- **Single numeric authority per category (R-508) — extended the pin, re-decided nothing.** `calc-rows.json` OWNS only the row-type / fractional-qty / margin / section-mode calc-row compositions (new-to-this-fixture numbers computed by the SAME frozen engine). Every reused category REFERENCES its existing authority and proves the calc-row surface reproduces the pinned öre: fractional-qty chain + regular VAT → `vat-rates.json` (833/208/1041; net 85000/VAT 21250/gross 106250); ROT/grön/mix → `rot-gron-deductions.json`; options/tillval + hidden rows → `options-tillval.json` + the 5.4 `readiness-inclusion.golden.test.ts` (asserted covered, NOT duplicated; one section-composition case reuses the pinned included net). No divergence from any frozen pin was encountered — no re-pin, no STOP.
- **Nothing production-approved (R-509):** `calc-rows.json` `policy.signOff` = `pending-owner-accounting-legal` (a sanity guard asserts it); the underlying ROT/grön/VAT/rounding rates stay the Epic-4 UNAPPROVED placeholders; the margin threshold is the UNAPPROVED `PILOT_LOW_MARGIN_THRESHOLD` (0.15); a deduction is asserted as an ESTIMATE, never legally-final.
- **Fixture privacy (AC3, R-516):** the pack-wide anonymization scan (personnummer `\d{6}-\d{4}`, orgnr `\d{10}` no-dash, non-`example.test` email, `secret|password|api_key`, PHONE, ADDRESS) is EXTENDED over the `calc-rows.json` DATA payload (scanned WITHOUT the `_doc` prose, which legitimately names the PII rules); a key-name guard asserts no `personnummer`/`pnr`/`orgnr` key (the engine takes a POSTURE only). Every öre value is kept UNDER 10 digits (max 106250) so the bare-`\d{10}` orgnr guard cannot false-positive (the epic-4 Iter-2 trap) — the schema-shape guard enforces `< 1,000,000,000` öre on every expected value.
- **No inline money math (R-505):** verified by grep — no `* 2500`/`0.25`/`/ 10000`/`Math.round`/`* 1.25`/`roundToOre` in the test; every öre op routes through the engine, TB% via the classifier.
- **Scope-guard sweep (4.2) clean:** diff is `calc-rows.json` (new) + `calc-golden-pack.test.ts` (filled) + the story/sprint tracking only — NO `src/**`, NO migration, NO command, NO UI, NO `nav-items.ts` change (stays seven), NO dependency, NO `.env`.
- **Deferred-work reconciliation (4.4):** no NEW deferral opened; the öre-<-10-digits constraint was HONORED (no scan-widening needed). Did NOT reopen the non-overlapping 5.1/5.2/5.3/5.4 product/DB deferrals; did NOT add the standing NFR concerns (no `pnpm audit` gate / no coverage reporter — an epic-gate decision, R-510); the `persons` flat-cap (R-512) + required-file 8.1 sequencing (R-513) are surfaced by the pack, already ledgered by 5.4 — not re-deferred. The runner-glob hygiene note (calc goldens under `tests/unit/**`, never `tests/golden/**`) is in the pack test docstring (DOCS-03).
- **No `documented-delta`/`old-lovable` case authored:** no anonymized Lovable calc-oracle example was available at implementation time, so every case is labelled conservatively `new-expected` (Lovable is a behavioral oracle only — no code/data copied, AGENTS.md). The LABELLING + schema-shape guards STILL enforce the three-way origin discipline (a `documented-delta`/`old-lovable` case would be required to carry its `oldLovableWouldGive`), so the pack is ready to absorb a real delta when Epic 9 captures one — none was fabricated.

### File List

- `tests/fixtures/golden/money/calc-rows.json` (NEW) — the anonymized calc-row golden fixture (row types, fractional qty, margins, section modes, VAT display, attachment-readiness cases; `_doc` + `policy` with `signOff: pending-owner-accounting-legal`).
- `tests/unit/features/calculations/calc-golden-pack.test.ts` (MODIFIED) — the create-story/ATDD scaffold filled: the 4 pack-level guards (COVERAGE manifest, LABELLING, BEHAVIORAL live-oracle 3a–3h, SCHEMA-SHAPE) + the extended fixture-privacy scan + the nothing-approved sanity guard; all `todo(...)` placeholders replaced and removed.
- `tests/unit/features/calculations/calc-golden-pack-coverage.test.ts` (NEW) — the optional live-oracle expansion split (Dev Notes line 97), mirroring the 4.4 `golden-pack-coverage.test.ts` GAP-1..GAP-5 pattern: turns static fixture fields into live negative/positive oracles through `totals.ts`/`readiness.ts`/`vat-posture.ts`/`@/lib/money`. Same runner (`node --test`, under `tests/unit/**`), same location.
- `_bmad-output/implementation-artifacts/5-5-calculation-golden-tests-for-options-hidden-rows-and-tax-warnings.md` (MODIFIED) — tasks/subtasks checked, Dev Agent Record, File List, Status → review.
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (MODIFIED) — story status ready-for-dev → review.

### Change Log

- 2026-07-03 — Implemented Story 5.5 (calc golden pack): authored `calc-rows.json` + filled the `calc-golden-pack.test.ts` live-oracle pack (14 tests) + the `calc-golden-pack-coverage.test.ts` live-oracle expansion split. Full CI gate sequence green. Status → review.
- 2026-07-03 — Review fix [Review][Patch][Low]: added the shipped `calc-golden-pack-coverage.test.ts` deliverable to the File List (it was omitted); story-record accuracy only, no code change.

## Review Findings

Triaged from the Tier-A thin review (Acceptance Auditor lens, reviewer `primary`) + the dedicated security review (auto-bmad-local, no findings). Decision bucket first, then Patch, then Defer.

- [x] [Review][Patch][Low] File List omits the shipped `calc-golden-pack-coverage.test.ts` deliverable — the diff creates a THIRD file (`tests/unit/features/calculations/calc-golden-pack-coverage.test.ts`, permitted by Dev Notes line 97's optional split) but the File List (lines 155-158) records only `calc-rows.json` + `calc-golden-pack.test.ts`. Not a scope violation, but the Dev Agent Record understates the actual diff; a future trace reconciling the story record against the tree finds an untracked deliverable. Fix: add the coverage file to the File List / Change Log. [_bmad-output/implementation-artifacts/5-5-calculation-golden-tests-for-options-hidden-rows-and-tax-warnings.md:155] — RESOLVED 2026-07-03: added `calc-golden-pack-coverage.test.ts` (NEW) to the File List and a Change Log entry. Story-record accuracy fix only; no code change.
- [x] [Review][Defer][Med] `documented-delta`/`old-lovable` guard requires a NUMERIC `oldLovableWouldGive`, cannot express a non-numeric (classification/section-mode) divergence [tests/unit/features/calculations/calc-golden-pack.test.ts:924] — deferred, pre-existing. The LABELLING guard asserts `typeof c.oldLovableWouldGive === "number"` for any `documented-delta`/`old-lovable` case, but several AC1 categories the pack owns are classification oracles (margins → LOW_MARGIN/ZERO_PRICE_ROW; attachment-readiness → REQUIRED_FILES_DEFERRED; VAT display posture). Does NOT violate AC2 for what ships today (all cases are numeric-or-classification `new-expected`); a real future Epic-9 non-numeric Lovable divergence would need the guard widened to accept a non-numeric divergent value. Not actionable now (no such delta exists; ships correct).
- [x] [Review][Defer][Low] Coverage REFERENCE_MANIFEST proves the 6 reference categories by a raw `raw.includes(entry.token)` substring match [tests/unit/features/calculations/calc-golden-pack.test.ts:889] — deferred, pre-existing. The residual substring-manifest weakness inherited from the 4.4 pack pattern (already ledgered as deferred-work.md#epic-4-Iter-2); a token like `gron_teknik`/`halfRoundingMode` could match incidental prose in a large fixture. Spec-permitted (Dev Notes line 75: reference-by-token acceptable for a category another fixture already owns) and the NEW half uses a proper `category`-field match; ROT/grön are additionally proven behaviorally via live `estimateDeduction`. Honor-not-reopen per the standing epic-4 Iter-2 deferral.
