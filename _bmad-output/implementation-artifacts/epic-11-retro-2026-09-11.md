---
epic: 11
date: 09-11-2026 18:00
verdict: rejected
criteria: declared
headless: true
---

# Epic 11 retrospective — RBAC mechanism and admin user management

## Epic summary

Phase B / Wave B1a delivered the code-owned, tenant-scoped RBAC mechanism and the admin-user management surface described in `epic-11-context.md`. The complete aggregate diff is `6ebc163..1ad3df8` (217 files, 17,361 additions, 1,361 deletions); `6ebc163` is the parent of the first Epic 11 commit, so this includes Stories 11.1–11.4. `git_evidence.py` was also run for the post-11.3 slice, but that slice was not used as the epic-wide range.

Evidence available: the epic context, Story 11.1–11.4 specifications, Phase B PRD/architecture, `sprint-status.yaml`, full Git history/diff, NFR and test-quality assessments, trace/gate reports, source and tests. Session logs were not available, so process analysis is limited to commit/artifact evidence. Previous retrospective: `epic-10-retro-2026-07-20.md`; its action-item ledger contains no unresolved entries requiring a transition in this headless run.

`detect-epic --epic 11` found four stories and reports `11-4-roles-surface-effective-permissions-and-the-per-role-test-harness` as pending (`review`). Stories 11.1–11.3 are done. The retro key was optional before this run.

## Findings

### Confirmed delivery and aggregate views

- **Accepted as implemented.** The five closed roles, deny-by-default permission/sensitive-field matrix, and generic capability gate are centralized in `src/server/authz/roles.ts:2-19`, `permission-matrix.ts:11-107`, and `require-capability.ts:5-12`. The additive storage migration preserves legacy `tenant_admin` semantics while adding tenant-scoped roles/RLS (`supabase/migrations/20260904120000_role_storage_and_permission_matrix.sql:4-66`).
- **Accepted as implemented.** Active-manifest authorization derives Phase A navigation and landing from the matrix (`src/server/authz/phase-a-surface.ts:11-40`); matrix coherence is checked in `src/scope/manifest-schema.ts:278-303`; command registration locks its capability at `src/server/commands/envelope.ts:182-191`.
- **Accepted as implemented.** The roles catalogue is active-manifest-only and derives effective unions and active-member counts (`src/server/authz/role-catalogue.ts:42-114`, `src/features/admin-users/read.ts:11-42`). The generated harness validates active table/command/matrix enrollment and RLS probes (`tests/support/authz/role-harness.ts:61-129`).
- **Maintainability concern, deferred.** The SQL policy evolution migration is a 3,279-line, 65-function cross-domain authority surface (`supabase/migrations/20260907171252_role_aware_phase_a_policy_evolution.sql:1,155,690-3168`). It is deliberately a SQL mirror of the TypeScript matrix and covered by policy/matrix tests, but future activation work must keep the mirror and harness synchronized. No current correctness bypass was identified.

### Blocking security defect

- **Fix now.** `admin_accept_membership_invitation` is executable by `authenticated` and is `SECURITY DEFINER`, but accepts caller-supplied `p_email`; it binds `auth.uid()` only to caller-supplied `p_user_id`, then compares the supplied email to the invite before assigning `auth.uid()` to the membership (`supabase/migrations/20260910165124_admin_user_management.sql:160-184`). A non-invitee possessing a valid membership ID and raw token can supply their own ID and the invitee email, thereby activate the invitee membership. The normal Next command derives identity from Auth (`src/server/commands/admin-users/accept-invitation.ts:19-51`), but direct RPC access bypasses it. Existing integration coverage proves the legitimate path only (`tests/integration/commands/admin-user-management.int.test.ts:40-74`). This confirms the NFR audit's Security FAIL finding (`_bmad-output/test-artifacts/nfr-assessment-epic-11.md`, “Security FAIL — direct invitation-acceptance RPC bypass”).

### Admin invitation lifecycle defects

- **Fix now.** Detail reads project an overdue invited row as `expired` (`src/features/admin-users/read.ts:55`), while the detail panel exposes resend/revoke only for literal `invited` (`src/components/admin-users/UserDetailPanel.tsx:24`). An administrator has no UI recovery action for the expired invitation.
- **Fix now.** Successful invitation acceptance returns `void` and neither renders success nor redirects/revalidates (`src/features/admin-users/actions.ts:16-19`; `src/app/(auth)/invite/accept/page.tsx:4`). The user remains on the activation form after the membership becomes active.
- **Fix now.** An uncertain delivery finalizes the persisted operation as `uncertain` (`src/server/auth/admin-user-service.ts:44-48`), while replay of that operation is rejected unless its outcome is `succeeded` (`src/server/commands/admin-users/invite.ts:30-34`). The open dialog retains its operation ID (`src/components/admin-users/UsersPage.tsx:10`), so retry cannot issue the required new attempt.
- **Fix now.** If provider delivery fails and finalizing the durable operation also throws, the catch block awaits the finalizer without containment (`src/server/auth/admin-user-service.ts:44-48`). The action rejects despite a prepared durable invitation, defeating its documented uncertain/reconciliation outcome.

### Test quality and verification

- **Deferred, tracked.** The test-quality review is advisory but records a 75/100 Request Changes result: a live-clock retry fixture (`tests/e2e/global-setup.ts:719`), oversized setup/factory files (`tests/e2e/global-setup.ts:1178`, `tests/factories/tenants.ts:2662`), and two Roles browser specs without readiness signals (`_bmad-output/test-artifacts/test-reviews/test-review-epic-11-wave-b1a-rbac.md`).
- **Accepted evidence.** The trace gate reports PASS 21/21, including the remediated direct missing/foreign detail-read equality proof in `tests/integration/rls/admin-user-detail-isolation.rls.test.ts:27-73`. Recorded validation is 1,734 unit tests passed; required integration/RLS 1,016 executed with zero skips; focused role browser evidence passed; the full browser run passed with 138 started. Cross-model CLI output was empty twice and is excluded as review evidence.

## Behavior verification

This retrospective did not start new runtime infrastructure. It relies on current recorded behavior evidence: required integration/RLS execution with `SUPABASE_TEST_REQUIRED=1` recorded 1,016 executed and zero skipped; traceability reports PASS 21/21; full Playwright passed with 138 tests launched; focused Roles browser evidence passed. The direct-RPC invitation acceptance path was inspected statically and is a reachable authenticated database entry point; its identity binding is not covered by the legitimate-path integration test.

## Previous-retro follow-through

The prior Epic 10 retrospective exists. The current sprint-status action-item ledger has no unresolved Epic 10 item eligible for a status transition. Headless mode makes no prior-item status changes.

## Action items

1. **RBAC/security owner — proposed remediation:** bind invitation acceptance to the authenticated user's verified email in the database procedure (or remove direct RPC exposure behind a safe wrapper), and add a real authenticated mismatched-user/invitee-email negative integration case. Source: `20260910165124_admin_user_management.sql:160-184`. 
2. **Admin-user feature owner — proposed remediation:** make invitation recovery complete: resend/revoke expired invitations, return a confirmed success/failure state or redirect after acceptance, create a fresh retry attempt for uncertain delivery, and preserve reconciliation if finalization fails. Sources: `UserDetailPanel.tsx:24`, `actions.ts:16-19`, `UsersPage.tsx:10`, `admin-user-service.ts:44-48`.
3. **Epic 11 delivery owner — proposed verification:** complete the remaining Story 11.4 review follow-up, attach verifiable review evidence, then move its story state from `review` to `done` through its authorized workflow. Source: `sprint-status.yaml` detection result and Story 11.4 metadata.
4. **QA/test owner — proposed quality debt:** freeze the retry fixture clock, split the two oversized support files, and add Roles-page readiness signals before rerunning the quality audit. Source: `_bmad-output/test-artifacts/test-reviews/test-review-epic-11-wave-b1a-rbac.md`.

## Acceptance verdict

**Rejected** (declared criteria). The machine verdict is rejected because Story 11.4 remains pending in `review`; headless mode has no human override. Independently, the authenticated direct-RPC invitation-identity bypass violates the epic's server-authorized, generic-denial and tenant-authority criteria. The trace and recorded validation evidence demonstrate broad functional coverage but do not negate either condition.

## 2026-09-14 remediation checkpoint

### Decisions

- **Story 11.4 review evidence is satisfied in the draft-PR state.** Focused review of `dcb718c53d8aab7245d9d3e24776d2a33581bf1b` at `49394f3c04b8cac6101d3ed007156fd546c3e51d` found no new consequential defect in the three bounded corrections and confirmed the 13 author review references. This supersedes the stale `review_unverified` interpretation for those corrections. The historical cross-model CLI returned no usable output and remains unavailable evidence, not a contrary review result.
- **Human acceptance remains outstanding.** Story 11.4 stays `review`, Epic 11 stays `in-progress`, and this retrospective stays **Rejected** pending the human checkpoint. Review satisfaction is not approval, merge, or deployment.
- **Security is verified in the candidate, not released.** The historical direct authenticated mismatch is RED on the vulnerable revision. Integrated checkpoint candidate `7af45c46054e0c4a54f15f2d2802055f304365c3` includes the PR #57 repair; its direct-RPC evidence verifies the implementation finding. `main` and demo remain vulnerable and blocked until human approval, merge, and migration application.

### Evidence recorded

- PR #55 CI run `34712355388`, at `dcb718c53d8aab7245d9d3e24776d2a33581bf1b`: 1,748 unit tests; 1,020 required integration/RLS tests with zero skips; 134 browser tests with four skips.
- The focused reviewer ran no new commands. The existing CI evidence is retained as CI evidence; it is not attributed to the reviewer.
- **Security repair, verified candidate state:** CI run [34828698510](https://github.com/rthunborg/ElproSaas/actions/runs/34828698510) passed 1,733 units with zero skips, 96 required database files / 1,011 passed / 0 skipped, and 130 browser tests with four skipped (134 started). Its empty reset log records the corrective migration, and the direct-RPC regression file executed four tests. Focused independent security review found no consequential defect.
- **Lifecycle, verified candidate state:** PR [#58](https://github.com/rthunborg/ElproSaas/pull/58) depends on the security branch. `f804a9573afacdac5df0842864bd610dbbaf425f` is its initial verified source revision: CI run [34829912153](https://github.com/rthunborg/ElproSaas/actions/runs/34829912153) passed 1,736 units / 0 skipped, 97 integration/RLS files / 1,014 passed / 0 skipped, and 132 browser tests with four skipped. Live PR checks are authoritative for the latest published head. The implementation finding is fixed and verified in the candidate; approval, merge, and deployment remain open.
- **Integrated checkpoint evidence:** Proposed order is PR #57 to `main`, PR #58 reviewed atop the security branch, then PR #55 reviewed atop the lifecycle branch. Those branches are review bases only: retarget #58 to `main` after #57 lands, then retarget #55 to `main` after #58 lands, before merging each dependent PR. Candidate `7af45c46054e0c4a54f15f2d2802055f304365c3` remains historical local code evidence: 1,751 units / 0 skipped and 101 required integration/RLS files / 1,023 passed / 0 skipped, including real mismatched-RPC and multi-tenant-admin regressions plus two mocked retry tests. Local SQL-only verification did not perform an empty reset. The combined configured production-browser run completed at 2026-09-14T09:52:59Z with 14 passed / 0 skipped across the four named Admin/Roles and role-aware suites. Typecheck, source and built-bundle service-role containment, and diff checks passed. Independent checkpoint review found no introduced issue in conflict-resolution scope and preserved role/count/paging/enrollment and invitation identity/lifecycle coverage. Live PR checks are authoritative for the latest published heads.
- **Separately tracked, not implemented:** the pre-existing reset retry production-command/action seam remains correctness work. Provider/finalizer failure maps generic denial and reuse of the same operation does not redeliver (`src/server/commands/admin-users/lifecycle.ts` and `service.reset`). It is not advisory test maintenance and still requires implementation and verification.
- Draft PR [#57](https://github.com/rthunborg/ElproSaas/pull/57) carries the security repair at `fc2a85e4e615653c812a002eb48a63c6820e2823` on base `25fafa5dcd926c16ff39f8df27bc7911d9e21fbc`; candidate evidence is verified, but human approval, merge, and deployment remain pending.

### Open items

1. **IN — RBAC/security owner:** carry the verified candidate identity-binding evidence through human approval, merge, and deployment; do not close the main/demo release blocker before migration application.
2. **IN — Admin-user feature owner:** carry the verified lifecycle candidate through human approval, merge, and deployment.
3. **SEAM — Admin-user feature owner:** separately track the unimplemented reset retry production-command/action correction; provider/finalizer failure must preserve a reconciliable outcome rather than generic denial with a non-redeliverable reused operation.
4. **SEAM — Epic 11 delivery owner:** prepare the human checkpoint using the review order and evidence; story status remains `review` until that authorized decision.
5. **DEFERRED — QA/test owner:** retain the advisory live-clock retry fixture, oversized support files, and Roles readiness signals in the existing test-quality ledger. They require their own maintenance work and reassessment; green CI does not close them.

### Post-release reconciliation — 2026-09-14

PRs #57, #58, and #55 merged in order at `9dd6e74`, `7b0b991`, and `5cc08d2`; the merged migrations were applied to the demo, with a repeat dry run reporting no pending migration. The identity-binding and invitation-lifecycle findings are **fixed, verified, and deployed**. Actions 1–3 are done; Story 11.4 and Epic 11 are done in sprint status.

This retrospective remains **Rejected**. The reset-retry production-command/action seam remains an unimplemented non-advisory declared criterion; actions 4 (advisory test maintenance) and 5 (reset retry) remain open. The NFR release result remains **CONCERNS**, not PASS, because the previously recorded operational and measurement gaps remain. See [release verification](../../docs/quality/epic-11-release-verification-2026-09-14.md) for hosted migration, deployment, ACL, and advisor evidence.

## Open questions

- No human answer is needed to record this audit. Whether the security remediation is implemented before the intended draft PR is an orchestration/release decision.
- The complete full-browser total is reported as 138 launched/passed in the evidence brief; the final reporter did not expose an independently parseable total.

## Assumptions

- Headless invocation explicitly selected Epic 11.
- `detect-epic --epic 11` is authoritative for the incomplete Story 11.4 `review` state; the machine verdict is rejected without human override.
- Proposed items 1–5 are evidence-backed remediation/verification work; no human completion decision is recorded.

## Closure follow-up — 2026-09-14

### Decisions

- **IN — identity binding and invitation lifecycle are closed releases.** PRs [#57](https://github.com/rthunborg/ElproSaas/pull/57), [#58](https://github.com/rthunborg/ElproSaas/pull/58), and [#55](https://github.com/rthunborg/ElproSaas/pull/55) were merged in order, their migrations were applied to the demo, and Production reached the merged `main` releases. The historic direct-RPC identity-binding finding is closed by its authenticated negative regression; it is not re-opened by this follow-up.
- **IN — reset retry is closed in `main`.** PR [#60](https://github.com/rthunborg/ElproSaas/pull/60), merged as `0e758310a802a63370e523086eca69f07e68503f`, makes replay reconcile a pending or uncertain reset outcome before any fresh delivery. CI run [34844914530](https://github.com/rthunborg/ElproSaas/actions/runs/34844914530) passed 1,752 unit tests, 1,028 required database/RLS tests, and 136 browser tests; all reported zero skips except the four existing browser skips. The focused local database run executed 12 tests with zero skips, including six authenticated direct-RPC cases and six explicitly mocked recovery seams. This supports closing retrospective action 5 when the action ledger is reconciled through its authorized status command.
- **IN — test maintenance is closed in `main`.** PR [#61](https://github.com/rthunborg/ElproSaas/pull/61), merged as `3dd292a3491d0cec6156a4e340fa44133d87bbf2`, resolves the live clock, oversized support-file, and Roles readiness findings. Its final combined CI run [34846239517](https://github.com/rthunborg/ElproSaas/actions/runs/34846239517) passed 1,753 unit tests and 1,028 required integration/RLS tests with zero skips, plus 136 browser tests with the four existing explicit skips. Local browser verification passed 13 tests with zero skips, and the independent focused reassessment resolved all nine historical findings at 100/100. This supports closing retrospective action 4.
- **IN — the Auth callback repair is implemented and locally verified in a candidate.** Draft PR [#62](https://github.com/rthunborg/ElproSaas/pull/62) at `12bb90b908412d1d7e6abcd080d77eab961cb90f` preserves the encoded callback context that the evidence probe previously stripped. A real Auth-mail browser run at 2026-09-14T13:16:05–13:16:18Z passed two tests with zero skips: invite cookie to membership activation, and recovery to password update then fresh sign-in. Three focused units passed with zero skips; typecheck, lint, production build, author review-order validation (13 references), and focused independent review found no consequential defect. This is candidate evidence only while PR #62 is draft and CI is pending.
- **SEAM — hosted configuration and delivery evidence remain incomplete.** Production has `NEXT_PUBLIC_APP_URL=https://elpro-saas.vercel.app` in Vercel. Hosted Supabase Auth site URL, redirect allow-list, and template configuration still require an authenticated dashboard inspection; no hosted delivery proof exists. The callback repair must preserve the approved Supabase Auth delivery boundary and must not introduce a custom mail service.
- **DEFERRED — performance and recovery targets are not approved.** The local baseline measured a 120-member current tenant plus 24-member second tenant, with 1,377 background rows: Admin read p95 45.72 ms, synthetic permission computation p95 2.58 ms, and three database requests per read across 25 samples. Proposed local regression targets are 250 ms, 25 ms, and three requests at that dataset; they remain proposals until the owner decides. A scratch restore rehearsal could validate migration-dump content but failed on default privileges, and hosted backup metadata did not establish PITR or a restore objective. No RTO/RPO or disaster-recovery PASS claim is supported.

### Action-ledger reconciliation

1. `epic-11-retro-item-5-repair-reset-retry-command-action-seam` is **done**, based on PR #60 and run 34844914530.
2. `epic-11-retro-item-4-freeze-retry-fixture-time-split-oversize` is **done**, based on PR #61, run 34846239517, the 13-case local browser run, and the focused 100/100 reassessment.
3. `epic-11-retro-item-6-repair-and-verify-the-invitation-and-rec` is **in progress**. The candidate repair has local real-email browser proof; published-head CI, merge/deployment, hosted Auth redirect/template inspection, and hosted delivery evidence remain before the release blocker can close.

### Assumptions and questions

- The callback defect is treated as production-relevant because the server cannot consume a browser fragment. The candidate repair proves the local callback flow only; hosted redirect configuration and final browser behavior remain unverified.
- The proposed performance thresholds are local regression limits, not production SLOs.
- The owner decision still needed is whether to approve the proposed performance limits. Hosted Auth settings can be inspected and recorded once dashboard access is available; no secret value needs to be read or committed.
