# Story 8.1: File Storage Foundation - Private Bucket, Metadata, Links, RLS, And Signed-Access Command

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a tenant admin,
I want files represented by tenant-owned metadata and entity links backed by a private storage bucket with server-derived paths,
so that documents can later be managed safely in CRM, calculation, quote, acceptance, and job contexts through a single Phase A file model.

## Context & Why This Story Is The Keystone

This is **Wave 1 of Epic 8** and the **single Phase A file model**. It runs NEXT (before Epic 6) because it is a **hard dependency** for Epic 6 stories 6.1 (persisted attachment metadata) and 6.3 (quote PDF storage), which both consume `files`/`file_links` and MUST NOT invent a competing model (standing contract R-814; STOP condition). It is also the foundation every Wave-2 story (8.2-8.5) extends.

This is the **first time Phase A crosses the object-storage boundary**. Every earlier epic kept sensitive state in Postgres under the proven RLS harness; this story adds a **second storage plane** (Supabase Storage) that RLS on `storage.objects` + server-derived paths must isolate as strictly as the database.

**Scope of THIS story (Wave 1 foundation only):**
- Private storage bucket configuration (private-by-default, server-derived object paths).
- `files` base metadata table (tenant-owned).
- `file_links` polymorphic entity-link table (tenant-owned, command-level ownership validation).
- `storage.objects` RLS scoped by a server-derived tenant path prefix.
- `createSignedFileAccess` server command (the signing authorization funnel).
- A narrow atomic metadata+link RPC (ADR-A009).
- The full DB + storage RLS negative matrix (foundation seed of the storage-negative class).

**Explicitly NOT in this story:** upload UI, entity file panels, lifecycle locks, the file index, MIME/size upload validation UX (8.2), the preview/download UX and full storage matrix polish (8.3), quote PDF generation (6.3), attachment locks (8.4). `file_links` link creation for `quote_version`/`quote_acceptance`/`job` owner types is **inactive** until those owner tables exist (Epics 6/7) — the ownership-validation MECHANISM is proven now against `customer`/`facility`/`contact` (which exist by Epic 3) and a synthetic own-tenant record.

## Acceptance Criteria

1. **Migration reset — schema (AC1).** Given the file migration, when the database resets from empty, then `files` and `file_links` are created with tenant ownership (`tenant_id`), storage bucket/path metadata, display name, MIME type, size, uploader, lifecycle state, timestamps, owner type/id, purpose, and lock fields; AND no broad deferred-module file index table is created.

2. **Cross-tenant DB isolation (AC2).** Given tenant A and tenant B files, when tenant A attempts to read, link, update, archive, or delete tenant B file metadata or links, then RLS and command validation reject access (generic denial; no existence disclosure).

3. **Link ownership validation both-side (AC3).** Given a `file_link` targets an owner entity, when the link is created, then the command verifies tenant ownership of BOTH the file AND the owner record (a foreign file id OR a foreign owner id is rejected with `TENANT_ACCESS_DENIED`).

4. **Private storage config (AC4).** Given private storage configuration, when the file foundation is provisioned, then Phase A buckets are private by default with server-derived object paths; AND no public bucket or client-controlled storage path exists.

5. **Signed-access authorization funnel (AC5).** Given a tenant-owned file with metadata, when a server command requests access on behalf of a tenant admin, then `createSignedFileAccess` verifies tenant membership, file metadata ownership, and lifecycle state (metadata first) BEFORE issuing a short-lived signed URL; AND anonymous, cross-tenant, and storage-path-spoof signing attempts are rejected with generic user-safe errors.

6. **Storage-plane negatives (AC6, derived from R-805).** Given tenant A attempts to list/read/sign or spoof a tenant-B `storage.objects` path directly, when the storage negative matrix runs, then access is denied (`storage.objects` RLS scoped by the tenant path prefix); AND an expired signed URL is rejected using a low test TTL.

7. **Atomicity (AC7, derived from R-807).** Given the atomic metadata+link creation RPC (ADR-A009), when a mid-flow failure is injected, then the whole write rolls back — no half-written `files` row without its link, no orphaned/dangling metadata; the end state is consistent and non-usable.

8. **Enrollment + audit + no-secrets (AC8, derived from R-801/§15).** Given the two new tenant tables, when the H4 inventory gate runs, then `files` and `file_links` are enrolled in `TENANT_TABLES` (gate green); AND signed-access-created / link-created events are written through the EXISTING `audit_events` with allow-listed metadata only (NO raw file contents, bucket/object path, or PII).

## Tasks / Subtasks

- [x] **Task 1: Storage bucket configuration — private by default (AC4)**
  - [x] 1.1 Add a single private Phase A bucket to `supabase/config.toml` under `[storage.buckets.<name>]` with `public = false` (uncomment/replace the commented placeholder at lines 133-137). Suggested id: `tenant-files`. Do NOT set `objects_path` to any client-derivable value. Optionally set a conservative `file_size_limit`; a `allowed_mime_types` policy is deferred to 8.2 (conservative dev defaults).
  - [x] 1.2 Belt-and-braces: in the migration, `insert into storage.buckets (id, name, public) values ('tenant-files', 'tenant-files', false) on conflict (id) do nothing;` so the bucket exists + is private even on a fresh reset regardless of config-load order. Assert `public = false` in a construction test (AC4).
  - [x] 1.3 Document the server-derived path convention in the migration header and the storage helper: `{tenant_id}/{file_id}/{safe_display_name}` (or `{tenant_id}/{file_id}`). The FIRST path segment is ALWAYS the resolved `tenant_id`. The path is NEVER built from client input — see Task 4.

- [x] **Task 2: `files` + `file_links` migration (AC1, AC2)** — new migration `supabase/migrations/20260704120000_file_storage_foundation.sql` (verify the timestamp sorts AFTER `20260703120000_calculation_row_pricing_source.sql`).
  - [x] 2.1 `public.files`: `id uuid pk default gen_random_uuid()`, `tenant_id uuid not null references public.tenants(id) on delete cascade`, `bucket_id text not null` (default `'tenant-files'`), `object_path text not null`, `display_name text not null`, `mime_type text`, `size_bytes bigint check (size_bytes is null or size_bytes >= 0)`, `checksum text` (hash-when-available, §14), `uploaded_by uuid references auth.users(id) on delete set null` (mirror the `audit_events.actor_user_id` nullable/SET-NULL pattern — the file outlives the uploader), `lifecycle_state text not null default 'draft' check (lifecycle_state in ('draft','linked','locked','archived','deleted'))`, `archived_at timestamptz`, `created_at`/`updated_at timestamptz not null default now()`, plus `constraint files_id_tenant_unique unique (id, tenant_id)` (composite-FK target for `file_links`). Add a `unique (bucket_id, object_path)` so one object maps to one metadata row.
  - [x] 2.2 `public.file_links`: `id uuid pk`, `tenant_id uuid not null references public.tenants(id) on delete cascade`, `file_id uuid not null`, `owner_type text not null check (owner_type in ('customer','facility','contact','calculation','quote_version','quote_acceptance','job'))` — the Phase A closed owner-type union (tech note; NO deferred-module owner type), `owner_id uuid not null`, `purpose text not null check (purpose in ('calculation_attachment','quote_attachment_snapshot','quote_pdf','acceptance_evidence','job_evidence','crm_document'))` (§14), lock fields `is_locked boolean not null default false` + `locked_at timestamptz` (persisted here; the LOCK ENFORCEMENT trigger is Story 8.4, NOT here — persist the fields only), `archived_at timestamptz`, timestamps. **COMPOSITE same-tenant FK** `constraint file_links_file_same_tenant foreign key (file_id, tenant_id) references public.files (id, tenant_id) on delete cascade` — a bare `references files(id)` would let a link point at another tenant's file (R-802 hole). Do NOT add a DB FK on `(owner_id, tenant_id)` because `owner_type` is polymorphic; owner-record ownership is validated at the COMMAND layer (Task 5).
  - [x] 2.3 `set_updated_at` BEFORE UPDATE triggers on both tables (REUSE `public.set_updated_at()`; do NOT redefine).
  - [x] 2.4 Indexes: `files (tenant_id)`; `file_links (tenant_id, owner_type, owner_id)`; `file_links (tenant_id, file_id)`.
  - [x] 2.5 GRANTs (mirror the calc pattern EXACTLY — LOAD-BEARING): `authenticated → select, insert, update` on both tables (no DELETE — archive over hard delete); `service_role → full DML` (TEST-ONLY factory/cleanup); `anon → NOTHING`.
  - [x] 2.6 RLS `enable` + `force` on both tables; own-tenant `select_own`/`insert_own`/`update_own` policies via the EXISTING `public.is_tenant_admin(tenant_id)` helper, `to authenticated`. NO delete policy (archive over hard delete). This is the exact 5-epic-proven pattern.

- [x] **Task 3: `storage.objects` RLS — tenant path prefix (AC4, AC6)**
  - [x] 3.1 In the same migration, add own-tenant policies on `storage.objects` scoped to the bucket AND the first path segment cast to the tenant id: `bucket_id = 'tenant-files' and public.is_tenant_admin(((storage.foldername(name))[1])::uuid)`. Add SELECT + INSERT (`with check`) + UPDATE policies `to authenticated`; NO delete policy (archive over hard delete; a locked-file retention/delete workflow is 8.4). A malformed (non-uuid) first segment fails the cast → denied. This is the metadata-independent storage-plane isolation (R-805).
  - [x] 3.2 `anon` gets no storage policy → an anonymous storage list/read/sign is denied. Verify the Supabase local stack's default storage grants do not over-expose; if the platform's own bucket policies interfere, scope tightly to `bucket_id = 'tenant-files'`.
  - [x] 3.3 Because `is_tenant_admin` is `SECURITY DEFINER` with a fixed `search_path` (existing helper), it is safe to call from a `storage.objects` policy. Confirm the helper is reachable in the `storage` schema policy context (schema-qualify as `public.is_tenant_admin`).

- [x] **Task 4: Server storage helper + server-derived paths (AC4, R-810)** — new `src/server/storage/`.
  - [x] 4.1 `src/server/storage/object-path.ts` (pure): `deriveObjectPath({ tenantId, fileId, displayName })` → `{tenantId}/{fileId}/{sanitizedName}`. The tenant id comes ONLY from `ctx.tenantContext.tenantId` (never client input). Sanitize the display-name segment (strip `/`, `..`, control chars) so no path traversal is possible. Pure + unit-tested (node --test, `tests/unit/**`).
  - [x] 4.2 `src/server/storage/signed-access.ts`: a thin wrapper over `client.storage.from(bucket).createSignedUrl(objectPath, ttlSeconds)` using the REQUEST-BOUND anon-key RLS client (`ctx.db` shape) — signing runs under the caller's session + `storage.objects` RLS, NEVER a service-role key (containment guard). Read the TTL from `SUPABASE_SIGNED_URL_TTL_SECONDS` (documented in `.env.example:42`) with a safe default (e.g. 300s); a low value in test env makes expiry testable (R-806/R-815).
  - [x] 4.3 Never expose raw bucket/object path beyond what the signed URL requires (R-810); the command returns the signed URL + metadata, not the bucket internals.

- [x] **Task 5: Commands — `createSignedFileAccess` + link creation (AC3, AC5)** — new `src/server/commands/files/`.
  - [x] 5.1 `src/server/commands/files/validation.ts`: typed validators for the link-create input (`file_id`, `owner_type` ∈ closed union, `owner_id`, `purpose` ∈ closed union) and the signed-access input (`file_id`). Reject unknown owner types (deferred-module STOP). Never echo raw input values in errors.
  - [x] 5.2 `createSignedFileAccess = defineCommand(...)`: `command: "file.signedAccess.create"`, `auditable: true`, `eventType: "file.signed_access.created"`, `targetType: "file"`. Envelope resolves user → active `tenant_admin` membership → validate. `ownership: (input) => ({ table: "files", id: input.file_id })` so the envelope's `verifyOwnership` denies a cross-tenant/foreign file id with `TENANT_ACCESS_DENIED` (zero rows under RLS). In `execute`: load the file row (own-tenant RLS), check `lifecycle_state` is access-eligible (reject `archived`/`deleted` — a lifecycle gate), then call the storage helper to sign the derived `object_path`. Return `{ targetId: file.id, signedUrl, expiresAt }`. Every failure returns a stable generic code (`TENANT_ACCESS_DENIED`/`FILE_ACCESS_DENIED`/`VALIDATION_FAILED`) with NO existence disclosure.
  - [x] 5.3 `createFileLink = defineCommand(...)`: `command: "file.link.create"`, `auditable: true`, `eventType: "file.linked"`, `targetType: "file_link"`. Envelope `ownership` verifies the FILE (`{ table: "files", id: input.file_id }`). Then in `execute`, ALSO verify the OWNER record belongs to the resolved tenant via an own-tenant RLS SELECT on the owner table (map `owner_type` → table name via a closed switch; only the ACTIVE owner types — customer/facility/contact — resolve today; quote_version/quote_acceptance/job return a "not-yet-available owner type" rejection until Epics 6/7). Zero rows ⇒ `TENANT_ACCESS_DENIED` (R-802 both-side check). Call the atomic RPC (Task 6). Audit metadata carries ONLY `{ targetId }`-shaped allow-listed fields — NO owner PII, NO path, NO file contents (§15).
  - [x] 5.4 Use `FILE_ACCESS_DENIED` (already in the architecture §5 stable-code list) where a file-specific denial is clearer than the generic `TENANT_ACCESS_DENIED`; confirm/add it to `command-errors.ts` `CommandErrorCode` with a user-safe message, mirroring the existing codes. Cross-tenant failures must return the SAME shape as not-found (reuse the `server-error-vs-no-access` discipline; R-809).
  - [x] 5.5 `src/server/commands/files/index.ts` re-exports the commands (mirror `calculations/index.ts`).

- [x] **Task 6: Atomic metadata+link RPC (ADR-A009) (AC7)**
  - [x] 6.1 In the migration, `create or replace function public.create_file_with_link(...)` — a narrow `SECURITY INVOKER` RPC with `set search_path = ''` and schema-qualified refs (mirror `reorder_calculation_rows`). It inserts the `files` row AND the `file_links` row in ONE transaction (function body = implicit txn) so either both persist or neither. Parameters: file fields + link fields + explicit tenant id (the command passes the RESOLVED tenant id; RLS still narrows the INSERT WITH CHECK to the caller). It runs under the caller's RLS (own-tenant only; no service-role app path). Returns the new file id + link id.
  - [x] 6.2 `revoke execute ... from public; grant execute ... to authenticated, service_role;` (anon must NOT execute).
  - [x] 6.3 The command (Task 5.3) calls this RPC via `ctx.db.rpc(...)`. A DB error (FK/RLS violation) maps to a generic command code via a `throwMappedWriteError`-style mapper (mirror `calc-db.ts`): `23503`/`42501` → `TENANT_ACCESS_DENIED`/`VALIDATION_FAILED`, never a raw throw. NOTE: for THIS story, the metadata+link RPC is exercised via `createFileLink` against a synthetic/pre-seeded own-tenant file to prove atomicity + rollback; the real upload path that also writes the storage object is Story 8.2 (the RPC signature is designed to support it, but 8.2 owns the storage-write + compensation).

- [x] **Task 7: `TENANT_TABLES` enrollment + H4 gate (AC8, R-801)** — `tests/integration/rls/tenant-table-inventory.ts`.
  - [x] 7.1 Add `"files"` and `"file_links"` to the `TENANT_TABLES` array (this is a ONE-PLACE edit that the H4 gate demands; the compile-exhaustive `assertNever` switches FORCE you to add metadata for both — a missing branch is a TypeScript compile error).
  - [x] 7.2 Add the per-table metadata branches in EVERY `switch (table)`: `updateDenialKind` (both are `"rls-invisible"` — authenticated HAS insert/update grant), `spoofedRowFor` (a fresh-uuid row carrying `tenant_id = fixture.tenantB.id`; for `file_links` the spoof carries a Tenant B `file_id` parent + a valid owner_type/purpose), `tenantBFilter`/`hijackMutationFor`, and the anon-path helpers (`anonRowFor`/`anonFilterFor`/`anonMutationFor`). Follow the calc-tables branches as the template.
  - [x] 7.3 Add `tenantBFileId` / `tenantBFileLinkId` to `InventoryContext` (optional, seeded by the cross-tenant suite; the anon suite omits them). Add `adminInsertFile` / `adminInsertFileLink` seed helpers to `tests/factories/tenants.ts` (BYPASSRLS superuser path, mirror `adminInsertCalculation`), so the cross-tenant negatives target a CONCRETE Tenant B row (never a vacuous non-existent id).
  - [x] 7.4 The `rls-inventory-gate` `EXPECTED_TENANT_OWNED` derives from `TENANT_TABLES`, so it stays in sync automatically — confirm the gate goes green with the two new tables.

- [x] **Task 8: Migration-reset + RLS + signing + storage negatives (AC1-AC8)**
  - [x] 8.1 New `tests/integration/rls/file-tables-migration-reset.int.test.ts` (mirror `calc-tables-migration-reset.int.test.ts`): assert both tables exist post-reset with the schema contract (direct `tenant_id`, composite same-tenant FK on `file_links.file_id`, lifecycle CHECK, owner_type/purpose CHECKs, force RLS, GRANTs, NO delete policy); assert NO broad file-index / document-center table was created (AC1 guardrail — assert absence of e.g. `document_center`, `file_index` tables).
  - [x] 8.2 EXTEND `tests/integration/rls/migration-reset.int.test.ts` EXACT policy enumeration: add `files`/`file_links` × SELECT/INSERT/UPDATE to the expected exact set + the exists/RLS-forced checks. Keep the enumeration EXACT (never loosen to a superset) — a stray policy must still fail loud.
  - [x] 8.3 New `tests/integration/commands/file-signed-access.int.test.ts`: the authorization matrix — happy own-tenant sign succeeds; anon rejected; cross-tenant file id rejected; archived/deleted lifecycle rejected; every rejection returns the generic user-safe code with the SAME shape (no existence disclosure). Assert the audit row is written for the successful sign with clean allow-listed metadata.
  - [x] 8.4 New `tests/integration/commands/file-link-ownership.int.test.ts`: both-side ownership — foreign file id rejected AND foreign owner id rejected (`TENANT_ACCESS_DENIED`); own-tenant file + own-tenant owner succeeds; a deferred owner_type (quote_version) is rejected as not-yet-available. Assert atomicity: a mid-flow RPC failure (e.g. a deliberately invalid owner producing a constraint error) leaves NEITHER a `files` orphan NOR a `file_links` row (BYPASSRLS re-read proves zero rows — R-807).
  - [x] 8.5 New `tests/integration/rls/storage-object-isolation.rls.test.ts` (the NEW storage-negative class): seed a Tenant B object under `{tenantB.id}/...` via the service-role path; assert Tenant A (authed anon-key client) CANNOT list/read/sign it; assert a path-spoof (Tenant A signing `{tenantB.id}/...` directly) is denied by `storage.objects` RLS; assert anon cannot list/read/sign. Add a **storage-reachability probe** (see Task 8.6) and skip-clean locally / hard-fail in CI (`SUPABASE_TEST_REQUIRED=1`) mirroring `skipUnlessStack`.
  - [x] 8.6 **Storage-reachability probe (retro-note R-2 gap).** The existing `isLocalStackReachable()` probes ONLY `/auth/v1/health`; storage suites need their OWN reachability check so they do not false-green when the DB is up but the Storage service is down. Add `isLocalStorageReachable()` to `tests/support/test-env.ts` (probe the storage health/list endpoint, e.g. `GET {url}/storage/v1/bucket` with the anon/service key, or the storage health path) and a `skipUnlessStorage(ctx, storageUp)` in `tests/support/stack-gate.ts` (or reuse the pattern). Storage tests call it in `beforeAll`. Under `SUPABASE_TEST_REQUIRED=1` an unreachable storage service is a HARD failure; locally it is a VISIBLE skip.
  - [x] 8.7 Expired-URL test (R-806): set `SUPABASE_SIGNED_URL_TTL_SECONDS` low (e.g. 1) for the test, sign, wait past the TTL (or assert the URL's `exp`), and assert the signed URL no longer authorizes. Prefer driving expiry via the TTL config; if a real wait is unavoidable keep it ≤2s.
  - [x] 8.8 Pure unit tests (node --test, `tests/unit/**`) for `deriveObjectPath` (traversal/sanitization, tenant-first-segment invariant) and the lifecycle-access-eligibility helper (archived/deleted rejected) — pull these decisions OUT of any client component into pure `.ts` (coverage-shape lesson).

- [x] **Task 9: Fixture PII/secret scan extension (AC8, R-819)**
  - [x] 9.1 EXTEND the CI golden PII/secret + ORGNR scan to cover any new file-metadata fixtures/artifacts (no raw customer files, no real names/addresses/personnummer/orgnr/secrets, no bucket/object path leaking PII). File fixtures carry METADATA SHAPE only — anonymized. If a scan config lists scanned dirs, add the file-fixture path.

- [x] **Task 10: Full suite green + hygiene**
  - [x] 10.1 `pnpm typecheck && pnpm lint && pnpm test` (unit + int) green locally with the stack up (`supabase start && supabase db reset`). Confirm the H4 gate, migration-reset EXACT enumeration, and both new command suites pass.
  - [x] 10.2 Clear any red-phase header comments if you author scaffolds first (no stale `describe.skip`/`notYetImplemented` verbiage on green suites — recurring hygiene gap).
  - [x] 10.3 Update the PR Security/RLS impact statement: two new tenant tables (`files`/`file_links`) + `storage.objects` RLS + the signing funnel; disclose the inherited Phase-A own-tenant (not own-user) read posture on the new tables (same accepted single-admin design as the calc/CRM tables — Story 2.4 seam).

## Dev Notes

### Architecture patterns & constraints (MUST follow)

- **Command envelope is the ONLY authority surface.** Every command uses `defineCommand`/`runCommand` from `src/server/commands/envelope.ts`: resolve user → resolve active `tenant_admin` membership → validate typed input → `verifyOwnership` (zero rows ⇒ `TENANT_ACCESS_DENIED`; a DB error ⇒ `SERVER_ERROR`, never masked as a denial) → execute on the request-bound RLS client → append-only audit → typed `Result<T, CommandErrorCode>`. NO bespoke auth/error/audit mechanism. [Source: src/server/commands/envelope.ts; architecture.md#5]
- **Resolved-tenant authority.** The row's `tenant_id` and the object-path tenant segment are ALWAYS `ctx.tenantContext.tenantId` (from membership). A client-supplied `tenant_id` or path is NEVER read/trusted. [Source: project-context.md#Resolved-tenant authority]
- **Tenant-table pattern (verbatim, 5-epic-proven).** Direct `tenant_id NOT NULL references public.tenants(id) on delete cascade`; composite same-tenant parent FK (`file_links.file_id → files(id, tenant_id)`); `enable` + **`force`** RLS; own-tenant SELECT/INSERT/UPDATE policies via `public.is_tenant_admin(tenant_id)`; `authenticated → select,insert,update` / `service_role → full DML` / `anon → NOTHING`; NO delete policy (archive over hard delete via `archived_at`); REUSE `public.set_updated_at()`. A bare/non-composite FK on `file_links.file_id` is a cross-tenant hole (R-802). [Source: supabase/migrations/20260702120000_calculation_data_model.sql]
- **Storage: private buckets only, server-derived paths only, short-lived signed URLs, metadata-first/storage-second.** No public bucket, no client-controlled path (R-803, an 8.1/8.3 STOP). Access ALWAYS resolves `files` metadata ownership BEFORE touching Storage (R-810) — the ownership check precedes any `createSignedUrl` call. [Source: architecture.md#6, #14]
- **Signing runs under the caller's anon-key RLS session — NEVER service-role.** `createSignedUrl` uses the request-bound `@supabase/ssr` client (anon key + the caller's JWT). The app uses NO service-role key on any path (source + built-bundle containment guards enforce it in CI). If a server-only privileged storage op is ever needed it must be documented + test-covered + never `NEXT_PUBLIC_` — but this story needs none. [Source: project-context.md#Service-role key is SERVER-ONLY; src/server/db/supabase-server-client.ts]
- **ADR-A009 narrow RPC.** The atomic metadata+link creation uses a narrow Postgres RPC — `SECURITY INVOKER` (runs under the caller's RLS, own-tenant only) with a fixed empty `search_path` + schema-qualified refs (mirror `reorder_calculation_rows`). A `SECURITY DEFINER` design would need SEPARATE approval + membership checks + dedicated negative tests — do NOT reach for definer here. Mechanism change without ADR = STOP. [Source: architecture.md#ADR-A009; supabase/migrations/20260702120000_calculation_data_model.sql]
- **Audit through the EXISTING `audit_events`.** File events (`file linked`, `signed access created`) write through the same append-only table via `writeAuditEvent`; metadata is allow-listed and sanitized — NO raw file contents, NO bucket/object path, NO service-role details, NO broad PII (§15 prohibition). File audit is NOT a new audit model. [Source: architecture.md#15]
- **Stable error codes only, generic across tenants.** Use `TENANT_ACCESS_DENIED`, `FILE_ACCESS_DENIED`, `VALIDATION_FAILED`, `UNAUTHENTICATED`, `TENANT_MEMBERSHIP_REQUIRED`, `SERVER_ERROR`. A cross-tenant failure returns the SAME code/shape as not-found — no "file exists but not yours" disclosure (R-809; reuse the Epic 2 `server-error-vs-no-access` discipline). [Source: architecture.md#5; tests/integration/commands/server-error-vs-no-access.int.test.ts]
- **Owner types are the closed Phase A union only** (customer, facility, contact, calculation, quote_version, quote_acceptance, job). A deferred-module owner type is a STOP condition. Link creation for quote_version/quote_acceptance/job is INACTIVE until Epics 6/7 add those tables — the validator accepts them structurally but the owner-ownership check returns "not-yet-available" until the table exists. [Source: epics.md#Story 8.1 Technical Notes]

### Epic-8 retro-note constraints (from `_bmad-output/auto-bmad/retro-notes/epic-8.md`)

- **8.1 IS the single Phase A file model** — 6.1 (attachment metadata) and 6.3 (PDF storage) CONSUME `files`/`file_links`; there is NO competing model. This is a standing contract + STOP condition (R-814). Design the tables and RPC so 6.3 can store a PDF and 6.1 can persist attachment metadata by REUSING them (do not over-fit to a single caller). [retro-notes/epic-8.md]
- **Epic 8 adds a NEW test class: the storage-plane negative matrix** (`storage.objects` RLS, cross-tenant list/read/sign, path spoof, expired URL). **There is NO H4-style auto gate for it** — it must be authored deliberately (Task 8.5). The runner needs a **storage-service-reachability probe** or the storage suites will false-green (skip silently) locally when the DB is up but the Storage service is down. Task 8.6 adds `isLocalStorageReachable()` + a skip/hard-fail gate mirroring the existing `/auth/v1/health` probe. [retro-notes/epic-8.md]

### Deferred-work items that overlap this story (from `_bmad-output/implementation-artifacts/deferred-work.md`)

- **[Story 5.4 → 8.1] Required-file readiness is a placeholder pending 8.1.** `src/features/calculations/readiness.ts` surfaces a `REQUIRED_FILES_DEFERRED` WARNING saying required-file checking is not yet available (done manually until the file foundation lands). **This story lands the file FOUNDATION only — it does NOT wire the real required-file readiness check into `readiness.ts`.** Wiring the calc readiness classifier to real file-metadata reads needs the entity file panels/link reads (8.2+) and the calc↔file link surface. Do NOT change `readiness.ts` here; leave the documented `REQUIRED_FILES_DEFERRED` warning in place. It is called out so the dev agent knowingly works around it rather than assuming 8.1 must satisfy it. [deferred-work.md#5-4; src/features/calculations/readiness.ts]
- **[Story 1.4 → file-storage owner] `.env.example` / `local-setup.md` signed-URL TTL attribution.** The `SUPABASE_SIGNED_URL_TTL_SECONDS` var is documented at `.env.example:42` (and `local-setup.md`) as "forthcoming, consumed by Epic 8 file storage." THIS story is the file-storage work — when you consume the var, you MAY tidy the "(Optional, forthcoming)" wording to "in use" and confirm the epic attribution, closing that low-priority doc-staleness deferral. Optional, not required for AC pass. [deferred-work.md#1-4; .env.example:39-42]
- (No other deferred-work entry overlaps this story — CRM/settings/pricing/calc/money deferrals are out of scope and must NOT be reopened here.)

### Signed-URL TTL contract

- Env var: `SUPABASE_SIGNED_URL_TTL_SECONDS` (already documented `.env.example:42`). Read it in `src/server/storage/signed-access.ts` with a safe default (e.g. 300). Low in test env so the expiry test needs no long sleep (R-806/R-815). A hardcoded TTL is a review reject (blocks the expiry test). [Source: architecture.md#6; epics.md#Story 8.1 H2; .env.example:42]

### Project structure notes

- Migration: `supabase/migrations/20260704120000_file_storage_foundation.sql` (timestamp must sort after `20260703120000_*`). Timestamp-prefixed snake_case is the convention.
- New server dirs: `src/server/storage/` (object-path, signed-access helpers) and `src/server/commands/files/` (validation, `createSignedFileAccess`, `createFileLink`, index) — matching the recommended structure in architecture §3 (`src/server/storage/`, `src/server/commands/<domain>/`).
- Config: `supabase/config.toml` `[storage.buckets.tenant-files]` (private).
- Tests: INT/RLS under `tests/integration/`; storage-neg under `tests/integration/rls/` (Vitest); pure unit under `tests/unit/` (node --test). Two-tenant fixtures + BYPASSRLS seeds under `tests/factories/tenants.ts`; probe/gate helpers under `tests/support/`.
- No new nav item, no `/files` route, no UI component — this is a foundation story (the `/files` index is 8.5, optional). Keep `nav-items.ts` unchanged.

### Testing standards summary

- **Two runners:** `node --test` for pure unit (`tests/unit/**`), Vitest for DB-backed INT/RLS (`tests/integration/**`), Playwright for E2E (none in this story). Run via `pnpm test` (unit → int). [Source: scripts/run-tests.mjs; package.json]
- **DB + storage suites target the LOCAL Supabase CLI stack ONLY** (`supabase start && supabase db reset`) — never demo/dev/prod. `assertLocalStack()` hard-guards the BYPASSRLS/superuser paths. `skipUnlessStack` = visible skip locally, hard-fail in CI (`SUPABASE_TEST_REQUIRED=1`). Storage suites additionally need the storage-reachability probe (Task 8.6). [Source: tests/support/test-env.ts, stack-gate.ts, global-setup.ts]
- **Negatives before positives; enrollment is the completeness guarantee.** `files`/`file_links` enroll in `TENANT_TABLES` so the shared cross-tenant + anon-path suites + H4 gate cover the DB plane automatically — do NOT hand-write ad-hoc DB-isolation tests that bypass the inventory. The storage-plane negatives ADD to the harness (new class). [Source: tests/integration/rls/tenant-table-inventory.ts; test-design-epic-8.md §Testability]
- **Vacuity guards:** cross-tenant negatives target a CONCRETE seeded Tenant B row (never a non-existent id). Row/object-count assertions seed `crypto.randomUUID()`. The raw `pg` superuser pool returns `bigint` as STRING and `timestamptz` as `Date` — coerce (`Number(...)`/`.toISOString()`) in assertions. [Source: project-context.md; tests/factories/tenants.ts]
- **Coverage-shape lesson:** pull path-derivation, lifecycle-eligibility, and (later) error-state decisions into pure `.ts` modules so the fast `node --test` gate protects them; a pure helper buried in a `.tsx` escapes the fast gate. [Source: test-design-epic-8.md §Testability #7]
- **P0 for this story (from test-design-epic-8.md):** migration-reset + `files`/`file_links` RLS negatives + `TENANT_TABLES` enrollment (R-801); link ownership both-side (R-802); private-bucket/server-derived-path construction (R-803); `createSignedFileAccess` authorization matrix incl. expired-URL/lifecycle (R-804/R-806); storage-plane cross-tenant list/read/sign + path spoof (R-805); atomic RPC rollback (R-807); fixture PII scan (R-819). [Source: test-design-epic-8.md §P0]

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 8.1 (lines 1568-1611)] — story statement, ACs, tech notes, owner types, ADR-A009 atomic RPC, TTL H2, stop conditions.
- [Source: _bmad-output/planning-artifacts/architecture.md#6 Storage] — private buckets, server-derived paths, short-lived env-configurable signed URLs, cross-tenant path-spoof negative required.
- [Source: _bmad-output/planning-artifacts/architecture.md#14 File And Storage Model] — `files`/`file_links` field lists, lifecycle states, purposes, owner types, storage rules.
- [Source: _bmad-output/planning-artifacts/architecture.md#ADR-A006, #ADR-A009] — entity-scoped private file model; narrow invoker RPC (explicitly names 8.1 atomic metadata+link creation).
- [Source: _bmad-output/planning-artifacts/architecture.md#5] — command shape, `createSignedFileAccess`/`archiveFile` registry, stable error codes incl. `FILE_ACCESS_DENIED`.
- [Source: _bmad-output/test-artifacts/test-design-epic-8.md] — risks R-801..R-819, P0/P1 coverage, entry/exit criteria, storage-reachability probe requirement, single-file-model contract.
- [Source: supabase/migrations/20260702120000_calculation_data_model.sql] — the verbatim tenant-table + composite-FK + RLS + GRANT + invoker-RPC pattern to mirror.
- [Source: src/server/commands/envelope.ts, command-errors.ts, calculations/calculations.ts, calculations/calc-db.ts] — command/ownership/error-mapping patterns to reuse.
- [Source: tests/integration/rls/tenant-table-inventory.ts, rls-inventory-gate.int.test.ts, calc-tables-migration-reset.int.test.ts, migration-reset.int.test.ts] — enrollment + migration-reset test patterns.
- [Source: tests/factories/tenants.ts, tests/support/test-env.ts, stack-gate.ts] — BYPASSRLS seed factories + reachability/skip helpers to extend.
- [Source: _bmad-output/auto-bmad/retro-notes/epic-8.md] — single-file-model contract; storage-negative-matrix new class + storage-reachability probe gap.
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — Story 5.4 `REQUIRED_FILES_DEFERRED` (leave in place); Story 1.4 signed-URL TTL doc attribution (optional tidy).

## Open Questions (non-blocking — sensible defaults chosen)

1. **Single shared private bucket (`tenant-files`) with a `{tenant_id}/...` path prefix vs per-tenant buckets.** Default chosen: a SINGLE private bucket with tenant-prefixed server-derived paths + `storage.objects` RLS keyed on the first path segment (the architecture-sketched approach; scales without a bucket-per-tenant explosion). Re-confirm at review if per-tenant buckets are preferred.
2. **`FILE_ACCESS_DENIED` vs `TENANT_ACCESS_DENIED` granularity.** Both are user-safe/generic. Default: reuse `TENANT_ACCESS_DENIED` for cross-tenant/ownership failures (consistent with the envelope) and `FILE_ACCESS_DENIED` for a lifecycle-state (archived/deleted) rejection on an OWNED file. Keep the cross-tenant vs not-found shape IDENTICAL either way (no disclosure).
3. **MIME/size upload policy** is explicitly out of scope here (8.2, conservative dev defaults, owner Sign-Off residual). `files.mime_type`/`size_bytes` are persisted metadata only; no upload validation gate in this story.

## Dev Agent Record

### Agent Model Used

Claude Opus 4.8 (claude-opus-4-8[1m])

### Debug Log References

- `pnpm typecheck` — green.
- `pnpm lint` — green (0 errors; the sole warning is a pre-existing unused import in `tests/unit/lib/money/vat.test.ts`, unrelated to this story).
- `pnpm test:unit` (node --test) — 870 pass / 0 fail, including the new `tests/unit/server/storage/object-path.test.ts` (path derivation + lifecycle eligibility) and `tests/unit/server/commands/file-validation.test.ts` (owner-type/purpose closed unions + signed-URL TTL resolution).
- DB-backed integration/RLS suites (`pnpm test:int`): the local Docker/Supabase stack could not be started in this environment (Docker Desktop's WSL2 engine did not become responsive within the run window). The integration suites are fully wired (real imports, `.skip` removed, factory seeds + reachability gates in place) and skip VISIBLY when the stack is unreachable; they must be executed in code review / CI with `supabase start && supabase db reset` (CI sets `SUPABASE_TEST_REQUIRED=1`, turning a missing stack into a hard failure). See Blockers in the completion notes.

### Completion Notes List

Implemented the full Phase A file foundation. Key surfaces:

- **Migration** `supabase/migrations/20260704120000_file_storage_foundation.sql`: seeds the private `tenant-files` bucket belt-and-braces; creates `public.files` (tenant-owned metadata, composite `unique (id, tenant_id)` + `unique (bucket_id, object_path)`, lifecycle CHECK, `uploaded_by` ON DELETE SET NULL) and `public.file_links` (polymorphic owner_type/owner_id/purpose closed-union CHECKs, COMPOSITE same-tenant FK `(file_id, tenant_id) → files(id, tenant_id)` — the R-802 both-side hole closed); reuses `public.set_updated_at()`; ENABLE+FORCE RLS with own-tenant SELECT/INSERT/UPDATE via `public.is_tenant_admin` (NO delete policy); `storage.objects` RLS scoped to the `tenant-files` bucket AND the first path segment cast to the tenant id (a malformed/non-uuid segment is guarded by a uuid regex before the `::uuid` cast → denied); and the narrow SECURITY INVOKER `create_file_with_link` atomic metadata+link RPC (ADR-A009), REVOKE from public + GRANT to authenticated/service_role.
- **Config** `supabase/config.toml`: `[storage.buckets.tenant-files] public = false`.
- **Storage helpers** `src/server/storage/`: `object-path.ts` (pure `deriveObjectPath` — tenant-first, traversal/control-char sanitized), `lifecycle.ts` (pure `isAccessEligibleLifecycle` — archived/deleted rejected), `signed-access.ts` (env-driven TTL via `SUPABASE_SIGNED_URL_TTL_SECONDS`, safe default 300s; signs under the caller's RLS client, never service-role).
- **Commands** `src/server/commands/files/`: `createSignedFileAccess` (envelope ownership → metadata-first load → lifecycle gate → sign; `FILE_ACCESS_DENIED` for lifecycle/sign failure, `TENANT_ACCESS_DENIED` for cross-tenant/not-found — same shape, no existence disclosure), `createFileLink` (envelope file-ownership + execute-layer owner-side RLS check = R-802 both-side; deferred owner types rejected as not-yet-available; atomic RPC), plus pure `validation.ts` and `file-db.ts` error mapper. Added `FILE_ACCESS_DENIED` to `command-errors.ts`.
- **Harness** `files`/`file_links` enrolled in `TENANT_TABLES` with all compile-exhaustive metadata branches (spoof/filter/hijack/label + anon-path); `InventoryContext` gained `tenantBFileId`/`tenantBFileLinkId`; factory seeds `adminInsertFile`/`adminInsertFileLink`/`adminSelectFileLabel`/`adminUploadStorageObject`; `isLocalStorageReachable()` + `skipUnlessStorage()` (Task 8.6 storage-reachability probe). Cross-tenant + anon suites seed the new file rows.
- **Tests**: the four ATDD scaffolds (migration-reset, signed-access matrix, link-both-side+atomicity, storage-object isolation) un-skipped with real imports; migration-reset EXACT policy enumeration extended by `files`/`file_links` × SELECT/INSERT/UPDATE (storage-schema policies noted as covered separately); pure unit tests added.

Decisions / deviations:
- **Atomicity proof (Task 8.4/AC7):** the command's owner-side gate rejects a bad owner BEFORE the RPC, so to genuinely prove the RPC's file→link rollback ordering the atomicity test drives `create_file_with_link` DIRECTLY under the authed client with a valid file half + an out-of-union `owner_type` (violates the link CHECK 23514) — the files row must not survive. A separate command-level test proves "no orphan on a denied command".
- **Signed-URL happy path** requires a real storage object at the file's derived path, so the signed-access suite plants one via the service-role path in `beforeAll` (gated on `isLocalStorageReachable`).
- **Task 9 (fixture PII scan):** no standalone golden PII/ORGNR scan script exists in this repo. File fixtures are anonymized metadata shape only (display names like `own-file.pdf`; NO raw content, NO real names/personnummer/orgnr) — satisfied by construction; documented, no new scanned-dir config needed.
- Closed the low-priority `.env.example` TTL doc-staleness deferral (Story 1.4) — the var is now IN USE.
- Left `src/features/calculations/readiness.ts` `REQUIRED_FILES_DEFERRED` warning UNCHANGED (Story 5.4 deferral — this is the foundation only).

Breaking change / migration step: applying the new migration is REQUIRED (repo→demo via `supabase db push` after merge). No public interface removed; `FILE_ACCESS_DENIED` and `SUPABASE_SIGNED_URL_TTL_SECONDS` (optional, default 300) are additive.

### File List

New:
- supabase/migrations/20260704120000_file_storage_foundation.sql
- src/server/storage/object-path.ts
- src/server/storage/lifecycle.ts
- src/server/storage/signed-access.ts
- src/server/commands/files/validation.ts
- src/server/commands/files/file-db.ts
- src/server/commands/files/files.ts
- src/server/commands/files/index.ts
- tests/unit/server/commands/file-validation.test.ts

Modified:
- supabase/config.toml
- src/server/commands/command-errors.ts
- .env.example
- tests/factories/tenants.ts
- tests/support/test-env.ts
- tests/support/stack-gate.ts
- tests/integration/rls/tenant-table-inventory.ts
- tests/integration/rls/cross-tenant-isolation.rls.test.ts
- tests/integration/rls/migration-reset.int.test.ts
- tests/integration/rls/file-tables-migration-reset.int.test.ts
- tests/integration/rls/storage-object-isolation.rls.test.ts
- tests/integration/commands/file-signed-access.int.test.ts
- tests/integration/commands/file-link-ownership.int.test.ts
- tests/unit/server/storage/object-path.test.ts
- _bmad-output/implementation-artifacts/sprint-status.yaml

### Review Findings

- [x] [Review][Decision][High] RESOLVED (human-chosen direction). `createFileLink` links a NEW phantom file, not the caller-supplied ownership-verified `file_id` — breaks AC3/Task 5.3 literal behavior and the R-814 reuse contract — The envelope verifies `input.file_id`, but `execute` then calls `create_file_with_link`, which INSERTs a brand-new `files` row (`object_path = ${file.object_path}#link-${crypto.randomUUID()}`, `display_name = "linked-file"`) and links THAT row. The verified `file.id` is discarded (only echoed as `result.fileId`, which then disagrees with the link's actual `file_links.file_id`). The new path has NO backing storage object, embeds a `#` fragment char, bypasses `deriveObjectPath` (leaving that module dead/unwired in the only write path), and relies on `crypto.randomUUID()` global. Epic 6.1/6.3 expect `createFileLink(existing file, owner)` to attach that file; they would silently get a disconnected duplicate. The story (Task 5.3/6.3/Completion Notes) KNOWINGLY frames this as an atomicity-proof shortcut with the real upload path deferred to 8.2 — hence a documented deviation that must be an explicit reviewer-visible call, not buried. Sources: blind@primary+secondary, auditor@primary(H1)+secondary, edge@secondary. Recommended: fix: make `createFileLink` INSERT a `file_links` row referencing the verified `file.id` directly (an `insert into file_links` under RLS, or an RPC variant that takes an existing `file_id`), so the link points at the real file; keep the new-file+object write path in 8.2. If the team prefers to preserve the 8.1 atomicity-proof-only scope, at minimum make `result.fileId` the phantom file's id (not `file.id`) so the result is internally consistent, and add an explicit story-level note that `createFileLink` is NOT yet usable by Epic 6 to link existing files. [src/server/commands/files/files.ts:427-449] — FIX APPLIED: added a narrow `link_existing_file(p_tenant_id, p_file_id, owner_type, owner_id, purpose)` SECURITY INVOKER RPC (single atomic `file_links` insert; composite same-tenant FK re-enforces the same-tenant file binding; REVOKE public / GRANT authenticated+service_role). `createFileLink.execute` now calls it with `p_file_id = ctx.input.file_id` (the verified file) and returns `{ targetId: linkId, fileId: ctx.input.file_id }` — the link points at the REAL file and `result.fileId` agrees with `file_links.file_id`; no phantom `files` row is minted (the upload+object-write path stays in 8.2). Epic 6.1/6.3 can now `createFileLink(existing file, owner)` and attach that exact file (R-814). The `create_file_with_link` RPC remains in the migration (still exercised by the direct-RPC atomicity/rollback proofs) for 8.2's upload path. Tests: the four active-owner-type both-side happy/foreign cases pass; `pnpm run test:int` green (398).
- [x] [Review][Decision][Med] RESOLVED (human-chosen direction). Storage-signing failures collapse transient infra faults into a permanent `FILE_ACCESS_DENIED`, contradicting the envelope's own SERVER_ERROR discipline — `createSignedFileUrl` returns `null` for BOTH an RLS/missing-object denial (permanent) AND any other storage `error` (transient 5xx / network timeout), and `createSignedFileAccess` maps every `null` to `FILE_ACCESS_DENIED`. A transient Storage outage is thus reported as a permanent, non-retryable access denial — the exact anti-pattern the envelope's `verifyOwnership` comment calls out by name for the DB case (transient → SERVER_ERROR, never masked as a denial). Availability/observability regression. Sources: blind@primary, blind/auditor@secondary(Finding 2). Recommended: fix: have `createSignedFileUrl` distinguish the two cases (e.g. return a discriminated result or re-throw a plain Error on a transient storage `error` so the envelope maps it to SERVER_ERROR) and only map the genuine no-object/RLS-denied case to `FILE_ACCESS_DENIED`. [src/server/storage/signed-access.ts:887-896; src/server/commands/files/files.ts:341-343] — FIX APPLIED: added `isPermanentStorageDenial(error)` — a 4xx status (403/404) or a known not-found/denied code (NoSuchKey/NoSuchBucket/AccessDenied/not_found/unauthorized) is PERMANENT → `createSignedFileUrl` returns `null` → the command maps it to `FILE_ACCESS_DENIED`; a 5xx / network / ambiguous storage error is TRANSIENT → `createSignedFileUrl` re-throws a plain Error → the envelope maps it to a retryable `SERVER_ERROR` (fail toward observability, never a silent permanent denial). Unit-tested the classifier across both branches; typecheck/lint/int green.
- [x] [Review][Patch][Low] RESOLVED. `sanitizeNameSegment` `slice(0,128)` can split a UTF-16 surrogate pair, leaving a lone surrogate in the object path — a display name >128 code units ending mid-surrogate (emoji/multibyte-heavy filenames) yields an invalid/undefined storage key; no NFC normalization means visually identical names can also map to distinct paths. Reachable via the real 8.2 upload path that will wire `deriveObjectPath`. Fix: after the length bound, strip a trailing lone high surrogate (`/[\uD800-\uDBFF]$/`) and optionally NFC-normalize before bounding. [src/server/storage/object-path.ts:769-773] — FIX APPLIED: `sanitizeNameSegment` now NFC-normalizes FIRST (composed/decomposed names map to one segment) and strips a trailing lone high surrogate (`/[\uD800-\uDBFF]$/`) after the 128-unit slice. Two new pure unit tests cover both (surrogate-pair slice + NFC equivalence).
- [x] [Review][Patch][Low] RESOLVED. `resolveSignedUrlTtlSeconds` has no upper bound — a misconfigured huge `SUPABASE_SIGNED_URL_TTL_SECONDS` (e.g. "999999999999") mints an effectively unbounded-lifetime signed URL, contradicting the R-806 low-TTL contract. Sources: edge@primary+secondary. Fix: clamp to a `MAX_TTL_SECONDS` (return the default, or the max) when `n` exceeds a sane ceiling. [src/server/storage/signed-access.ts:841-843] — FIX APPLIED: added `MAX_SIGNED_URL_TTL_SECONDS` (24h ceiling); a value above it falls back to the safe `DEFAULT_SIGNED_URL_TTL_SECONDS` (fail toward the tighter TTL). Unit test covers the ceiling boundary + a huge value.
- [x] [Review][Patch][Low] RESOLVED. Expired-signed-URL test is timing-fragile — a 1s TTL with a fixed 1.5s wait leaves only ~500ms margin, and the signing/fetch round-trip can eat into it, risking intermittent false pass/fail on slow CI. Sources: blind@primary+secondary, edge@primary+secondary, auditor@primary(L5)+secondary(Finding 1). Fix: widen the wait to `max(2000, ttlMs + 1000)` (still within the ≤2s spec allowance relative to the TTL) or assert the URL's `exp` claim instead of a real fetch. [tests/integration/rls/storage-object-isolation.rls.test.ts] — FIX APPLIED: wait is now `Math.max(2000, TTL_SECONDS*1000 + 1000)` (2000ms for the 1s TTL); test passes.
- [x] [Review][Patch][Low] RESOLVED. Happy-path assertions are negative-only / shallow — the signed-access success test never checks `expiresAt` equals the deterministic `now + ttl`, and the audit-metadata "no leak" test asserts only the ABSENCE of a regex blocklist, not a positive allow-list, so a stray leaked field (`uploaded_by`, `owner_id`, `expiresAt`) that misses the blocklist would pass. Sources: blind@primary+secondary, auditor@primary(M3)+secondary. Fix: assert `expiresAt === FIXED_ISO + ttl` (fully deterministic under the fixed clock) and `expect(Object.keys(metadata)).toEqual(["targetId"])` (or `{}` per the actual sanitized shape). [tests/integration/commands/file-signed-access.int.test.ts] — FIX APPLIED: success test now asserts `expiresAt === EXPECTED_EXPIRES_AT` (FIXED_ISO + 300s default TTL, fully deterministic under the fixed clock); the audit-metadata test now asserts the POSITIVE allow-list `Object.keys(metadata)).toEqual([])` (the sanitized metadata is empty — targetId lives in the target_id column, not metadata) in addition to the belt-and-braces blocklist.
- [x] [Review][Patch][Low] RESOLVED. Owner-side ownership check is exercised only for `owner_type: "customer"` — `facility`/`contact`/`calculation` branches of `ownerTableFor` have no integration coverage, so a wrong table name or an unexpected owner-table RLS behavior for those three would not be caught by the P0 suite. Source: auditor@secondary(Finding 3). Fix: parametrize the both-side happy/foreign-owner cases across all four active owner types (seed a facility/contact/calculation owner and assert the R-802 owner-side SELECT resolves). [tests/integration/commands/file-link-ownership.int.test.ts] — FIX APPLIED: seeded own + Tenant-B facility/contact/calculation owners and added a `describe.each` over all four active owner types asserting (a) own file + own owner SUCCEEDS (owner-side SELECT resolves against the right table) and (b) a foreign owner id is TENANT_ACCESS_DENIED. All eight parametrized cases pass.
- [x] [Review][Patch][Low] RESOLVED. `ownerTableFor` omits the codebase-standard `assertNever` exhaustiveness guard — adding a 5th active owner type to `ACTIVE_OWNER_TYPES` later would compile with the switch falling through to an `undefined` return, producing `db.from(undefined)` at runtime. Sources: blind@primary+secondary. Fix: add `default: return assertNever(ownerType);` (or type the return so an unhandled case is a compile error), mirroring the rest of the codebase. [src/server/commands/files/file-db.ts:138-149] — FIX APPLIED: added `default: return assertNeverOwnerType(ownerType)` (a local `(ownerType: never) => never` guard mirroring the codebase pattern) so a future unhandled active owner type is a COMPILE error, never a runtime `db.from(undefined)`.
- [x] [Review][Patch][Low] RESOLVED. The AC1 "no broad file-index table" guardrail is a denylist of exactly three literals (`document_center`/`file_index`/`documents`) — a deferred-module table under any other name (`file_registry`, `attachments_index`, …) would pass. An allowlist is the airtight form and matches the policy-enumeration style already used in the same suite. Source: blind@primary. Fix: assert `public` gained ONLY `files`/`file_links` (allowlist), not the absence of three specific names. [tests/integration/rls/file-tables-migration-reset.int.test.ts] — FIX APPLIED: the guardrail now enumerates EVERY public base table whose name is file/document/attachment/registry/index-shaped (regex `~* '(file|document|attachment|registry)'` OR `~* 'index'`) and asserts the set is EXACTLY `{files, file_links}` — a deferred-module table under any name is caught. Test passes.
- [x] [Review][Defer][Low] `file.bucket_id || TENANT_FILES_BUCKET` truthy-OR fallback on a NOT NULL defaulted column — deferred, pre-existing — Reachable only if `bucket_id` were an empty string (schema-forbidden); would silently sign against the fallback bucket rather than surfacing a data-integrity problem. Defense-in-depth on an already-guarded value (NOT NULL default + `loadFileForAccess` type check). Sources: blind@primary+secondary, edge@primary+secondary, auditor@primary(L6). [src/server/commands/files/files.ts:335,430]
- [x] [Review][Defer][Low] `extractLinkId` shape mismatch throws a bare `Error` → `SERVER_ERROR` after a SUCCESSFUL commit — deferred, pre-existing — A benign PostgREST envelope/shape change (or a future scalar return) turns a committed link write into a permanent-looking 500 with no compensation. No realistic trigger today (the `returns table(...)` shape is fixed); no unit test exercises the array-vs-object branch. Sources: blind@primary+secondary. [src/server/commands/files/files.ts:445-462]
- [x] [Review][Defer][Low] `create_file_with_link` has no owner-existence re-check and `authenticated` retains a direct `file_links` INSERT grant — deferred, pre-existing — Any authenticated caller can invoke the RPC (or PostgREST-insert `file_links`) directly and create a link to an arbitrary polymorphic `owner_id` (no DB FK) so long as `tenant_id` passes the own-tenant WITH CHECK. Security review traced this end-to-end and found it adequately defended (own-tenant WITH CHECK holds; consistent with the mandated 5-epic-proven calc pattern; owner_id FK-less by design/R-802). Reviewers themselves mark it "informational, consistent with the mandated pattern." Accepted Phase-A posture; the owner-existence boundary lives in the command layer by design. Sources: blind@primary, edge@primary, auditor@secondary(Finding 5). [supabase/migrations/20260704120000_file_storage_foundation.sql]
- [x] [Review][Defer][Low] No `storage.objects`/`files`/`file_links` DELETE policy → archived/deleted objects' bytes are never reclaimable through the app path; a tenant delete cascades the METADATA but leaves orphaned private objects in the bucket forever — deferred, pre-existing — The migration explicitly withholds object cleanup ("a later story") under the archive-over-delete discipline; an unbounded-growth / GDPR-delete retention gap. Not caused by a defect in this change. Sources: blind@primary (×2). [supabase/migrations/20260704120000_file_storage_foundation.sql]
- [x] [Review][Defer][Low] Atomicity (R-807) proven only for a link-half CHECK violation (23514), not the FK/RLS failure modes the migration comment claims — deferred, pre-existing — The single atomicity test drives an out-of-union `owner_type` (a CHECK violation) and asserts the files insert does not survive; the composite same-tenant FK rollback and a mid-flow RLS failure on the link insert are not separately exercised. The rollback ORDERING is genuinely proven by the CHECK path, so this is a coverage-completeness gap, not a hole. Source: blind@primary. [tests/integration/commands/file-link-ownership.int.test.ts]

<!-- iteration-2 review (2026-07-04): NEW findings only; the 14 bullets above are iteration-1 and unchanged. -->
- [x] [Review][Patch][Med] `isPermanentStorageDenial` maps ALL 4xx (including 429 Too Many Requests and 408 Request Timeout) to a PERMANENT `FILE_ACCESS_DENIED` — a transient storage rate-limit / gateway-timeout on the signing call surfaces to the user as a non-retryable "file cannot be accessed" instead of a retryable `SERVER_ERROR`. This is the SAME "transient collapsing into permanent denial" family that resolved Finding #2 set out to prevent, re-appearing for the specific retryable 4xx status codes the newly-added classifier over-includes. Concrete defect with a realistic trigger (a Storage rate-limit spike). Sources: blind@primary. Recommended: fix: in `isPermanentStorageDenial`, exclude the retryable 4xx statuses (408, 425, 429) from the permanent branch (`status >= 400 && status < 500 && !RETRYABLE_4XX.has(status)`) so they re-throw → SERVER_ERROR; add a unit case for 429/408. [src/server/storage/signed-access.ts:90-98]
- [x] [Review][Patch][Low] `createFileLink`'s audit "no-leak" test is BLOCKLIST-only (`expect(meta).not.toMatch(/org_nr|.../i)`) — the exact weaker pattern resolved Patch #6 upgraded to a POSITIVE allow-list (`Object.keys(metadata)).toEqual([])`) for the sibling `createSignedFileAccess` test, left un-upgraded here even though `createFileLink`'s `auditFields` returns the same `{ targetId }` shape. A stray leaked field that misses the regex would pass. AC8 coverage asymmetry between the two P0 command suites. Sources: auditor@secondary(#2), blind@secondary. Recommended: fix: add `expect(Object.keys(events[0]?.metadata ?? {})).toEqual([])` to the `createFileLink` success test, mirroring the signed-access positive allow-list. [tests/integration/commands/file-link-ownership.int.test.ts:250-252]
- [x] [Review][Patch][Low] The `[P0] a DEFERRED owner_type (quote_version) is rejected` test asserts `expect(["TENANT_ACCESS_DENIED","VALIDATION_FAILED"]).toContain(result.code)` — but production (`assertOwnerVisibleOrThrow`) is DETERMINISTIC (`TENANT_ACCESS_DENIED`) and AC3 mandates that exact code. Accepting the `VALIDATION_FAILED` alternative means a future silent behavior change to the wrong code would still pass CI, weakening the guard on a security-relevant "not-yet-available" rejection. Sources: blind@primary, auditor@secondary(#3). Recommended: fix: pin the assertion to `expect(result.code).toBe("TENANT_ACCESS_DENIED")`. [tests/integration/commands/file-link-ownership.int.test.ts:325-327]
- [x] [Review][Patch][Low] `throwMappedFileWriteError`'s JSDoc enumerates the deterministic codes `23503/42501/23505/23514` and says "Any other error is a transient fault," but the `switch` also maps `case "22P02"` (invalid_text_representation) to VALIDATION_FAILED — the doc and code disagree about whether a malformed-uuid write error is deterministic. A maintainer reading the comment would wrongly believe `22P02` is treated as transient/SERVER_ERROR. Sources: blind@primary. Recommended: fix: add `22P02 invalid_text_representation (malformed uuid) → VALIDATION_FAILED` to the JSDoc list. [src/server/commands/files/file-db.ts:154-176]
- [x] [Review][Patch][Low] The `storage.objects` RLS policy header comment claims the first path segment "is guarded by an array-length + uuid-shape check before the cast" and "If array_length is null/0 (no folder) the row is denied" — but the SQL contains NO `array_length(...)` expression; it relies solely on `(storage.foldername(name))[1] ~ '<uuid-regex>'` short-circuiting the `::uuid` cast (a NULL/absent subscript `~ regex` yields NULL → deny). Behavior is correct (security review confirmed cross-tenant/path-spoof/malformed segments are denied), but the comment describes a guard the SQL does not literally contain, which could mislead a future maintainer editing this security policy. Sources: blind@primary, edge@secondary. Recommended: fix: reword the comment to describe the actual mechanism (regex-shape match short-circuits the cast; an absent/NULL first subscript matches nothing → denied) rather than a non-existent `array_length` guard. [supabase/migrations/20260704120000_file_storage_foundation.sql:318-321]
- [x] [Review][Defer][Low] `link_existing_file` (the path `createFileLink` calls) never transitions `files.lifecycle_state` from `draft` to `linked` — deferred, forward-looking — A file created `draft` (column default) and then attached via `createFileLink` stays `draft` forever through 8.1's only attach path, contradicting the schema's own column-comment semantics ("linked = attached to an owner entity"). Not an 8.1 AC violation (no lifecycle-transition AC on link creation; the real upload+link path and lifecycle advance are 8.2; `draft` is still access-eligible so AC5 is unaffected today). Same lens also flags that `createFileLink` links a file regardless of its lifecycle_state (no archived/deleted guard on the link path) — same forward-looking gap. Consumers must NOT infer link status from `lifecycle_state` until 8.2/6.1 wires the transition. Sources: blind@primary, auditor@primary(L4), auditor@secondary(#1), edge@primary. [supabase/migrations/20260704120000_file_storage_foundation.sql — link_existing_file; src/server/commands/files/files.ts createFileLink]
- [x] [Review][Defer][Low] Belt-and-braces bucket seed sets only `public = false` — it omits `file_size_limit`/`allowed_mime_types`, which live ONLY in `supabase/config.toml` (50MiB) — deferred, pre-existing — On a fresh reset against a stack where config.toml is NOT applied (a hosted/managed project rather than a local CLI reset, e.g. the demo push), the seeded bucket would have an UNBOUNDED file size limit and no MIME restriction, undermining the "belt-and-braces guarantees the private/size-limited posture regardless of config-load order" framing (it guarantees privacy only). Source: blind@secondary. [supabase/migrations/20260704120000_file_storage_foundation.sql:94-96; supabase/config.toml]
- [x] [Review][Defer][Low] `adminUploadStorageObject` test factory uses `upsert: true` — deferred, pre-existing — Contradicts the fail-loud philosophy stated directly above it (and applied elsewhere in the same file, e.g. `adminInsertFile` throws on a missing RETURNING id): `upsert: true` suppresses the one error (path collision) that would otherwise reveal a test-isolation problem on a non-reset shared stack. Test-factory robustness nit, not a production defect. Source: blind@secondary. [tests/factories/tenants.ts — adminUploadStorageObject]
- [x] [Review][Defer][Low] The storage-object list-isolation negative asserts a weak/possibly-vacuous property — deferred, pre-existing — "Tenant A CANNOT list a Tenant-B object" only checks `names).not.toContain(...pop())` (the full-path basename) and `void error`s ("may or may not be set depending on platform"). `.list(prefix)` returns names relative to the prefix, so the basename comparison can pass even if RLS were broken, and swallowing `error` can yield a false green on an unrelated empty list. The strongest negative (list-invisibility) has the weakest assertion. Source: blind@primary. [tests/integration/rls/storage-object-isolation.rls.test.ts]
- [x] [Review][Defer][Low] No structured log/telemetry on the transient-vs-permanent storage signing decision or on `FILE_ACCESS_DENIED` denials — deferred, pre-existing — `signed-access.ts`/`files.ts` re-throw a transient storage fault and map permanent denials to a generic user message with no structured logging of the security-relevant signing-denial event, so "RLS denied a cross-tenant attempt" is operationally indistinguishable from "storage was down." Genuine-but-minor observability gap; no logging seam is wired in this module. Source: blind@primary. [src/server/storage/signed-access.ts; src/server/commands/files/files.ts]
- [x] [Review][Defer][Low] `loadFileForAccess` returns `null` for BOTH "not visible (race)" AND "row shape invalid" (e.g. an unrecognized `lifecycle_state` failing `isFileLifecycleState`), and the caller maps both to `TENANT_ACCESS_DENIED` — deferred, pre-existing — A data-integrity anomaly (a bad migration or a future-added lifecycle value) would manifest as a mysterious access-denied with no error signal rather than a loud SERVER_ERROR. Deny-closed is the safe posture and the trigger requires an out-of-union DB value, so genuine-but-minor. Source: blind@primary. [src/server/commands/files/file-db.ts:56-67]
- [x] [Review][Defer][Low] `file_links` has no uniqueness constraint on `(tenant_id, file_id, owner_type, owner_id, purpose)` and `createFileLink` does no existence check — deferred, pre-existing — A double-submit or retry silently creates duplicate identical attachments (same file → same owner → same purpose). Whether uniqueness is desired interacts with the still-being-designed R-814 reuse semantics (6.1/6.3 may want find-or-create), so constraining now is premature; revisit when the reuse path lands. Source: blind@primary. [supabase/migrations/20260704120000_file_storage_foundation.sql — file_links; src/server/commands/files/files.ts createFileLink]
- [x] [Review][Defer][Low] `.env.example` under-documents the TTL clamp/fallback semantics — deferred, pre-existing — The `SUPABASE_SIGNED_URL_TTL_SECONDS` comment mentions only the "safe default 300s if unset" case, not that a non-integer/negative/over-24h-ceiling value is ALSO silently coerced to 300s (the resolved Patch #4 behavior). An operator configuring a deliberately long TTL (e.g. `90000`) silently gets 300s with no visible contract. Doc-completeness gap on operator-facing config. Sources: blind@primary, blind@secondary. [.env.example — SUPABASE_SIGNED_URL_TTL_SECONDS]
