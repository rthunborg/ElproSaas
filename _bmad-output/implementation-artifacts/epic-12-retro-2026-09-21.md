---
epic: 12
date: 09-21-2026
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 12 retrospective — Platform operator, console, and first-admin onboarding

## Epic summary

Epic 12 delivered the internal platform-operator provisioning path, isolated
operator console, and first-Admin onboarding checklist. The original evidence
range was `0ea5f94c..2a021bf12f2474bc01a369eba84e9bce67e31d3d`; follow-up
closure is at `6edbd2d9021310b202ab0e4fc828522d4bcf20b5`.

The authoritative Story 12.1, 12.2, and 12.3 artifacts all declare `status:
done`, no deferred acceptance items, and `followup_review_recommended: false`.
The canonical `detect-epic --epic 12` result reports `story_count: 3`, all
three stories done, and `pending_stories: []`. The same sprint record marks
`epic-12: done` and the retrospective key done.

### Historical rejected run

This document previously recorded a **rejected** machine verdict. That was
correct at the time: canonical sprint status still placed all three stories in
`review`, so the retrospective completeness gate returned a non-empty pending
list and the independent cross-model review artifact was unavailable. This
refresh preserves that historical reason; it does not reinterpret the prior
verdict as an implementation failure.

### Evidence inventory

| Evidence | Status | Source |
| --- | --- | --- |
| Epic intent and declared criteria | Available | `_bmad-output/planning-artifacts/epics-phase-b.md`; Story 12.1–12.3 specifications |
| Canonical completeness gate | Passed | `sprint_status.py detect-epic --epic 12`: three stories, none pending |
| Acceptance trace | Passed | `_bmad-output/test-artifacts/traceability-matrix.md` and gate evidence: 24/24 FULL (P0 21/21; P1 3/3) |
| Independent review | Completed and closed | `_bmad-output/test-artifacts/reviews/epic-12-independent-review-2026-09-21.md` and focused production review addendum |
| Production-finding resolution | Completed | `_bmad-output/test-artifacts/reviews/epic-12-production-findings-resolution.md` |
| CI closure | Passed | CI run `35631411549` at `6edbd2d9021310b202ab0e4fc828522d4bcf20b5` |
| Local pilot performance baseline | Available, advisory | `_bmad-output/test-artifacts/epic-12-performance-baseline.md` |
| Production security/perimeter evidence | Not yet collected | `docs/security/tenant-provisioning-production-readiness.md` and the shared platform/security programme |
| Session logs | Not used | No stable Epic 12 session-log corpus was supplied |

## Findings

### Aggregate and acceptance view

1. **Declared product acceptance is demonstrated.** The accepted trace is
   24/24 FULL. The closure CI at `6edbd2d` passed units (1,838/0/0), database
   tests (1,078/0/1), browser tests (144/0/4), and the separate recovery job
   (1/0/0). The deterministic request-ID race regression observed
   `IDEMPOTENCY_CONFLICT`, and the provisioning-cleanup regression passed.
   **Disposition: accept as implemented.** Sources: closure CI run
   `35631411549`; the production-findings resolution record.

2. **All four independent-review findings are closed.** The broad review found
   two production and two fixture candidates. Focused remediation resolved the
   request-ID/hash race, legal-entity validator false rejection, generated
   browser input, and provisioning protocol cleanup. The final focused
   production review found zero new findings and zero regressions. The former
   validator claim is retained only in its supported form: valid legal-entity
   numbers could be falsely rejected; ordinary personnummer admission was not
   established. **Disposition: accept as resolved.** Sources: independent
   review and production-findings resolution records cited above.

3. **Independent cross-model review evidence is complete.** The final
   independent Luna/xhigh review covered the stable complete Epic 12 change;
   subsequent reviews were limited to finding closure and regressions. The
   story frontmatters and each Suggested Review Order now have the accepted
   follow-up state and valid references (25/17/21 references, zero errors).
   **Disposition: accept as resolved.** Sources: Story 12.1–12.3 frontmatter;
   `_bmad-output/test-artifacts/reviews/epic-12-independent-review-2026-09-21.md`.

4. **Production enablement evidence remains outstanding.** Merge readiness does
   not authorize real production provisioning. Redacted deployed evidence for
   attestation-key rollout and rotation, TLS, encryption at rest, and the
   assigned operator-surface perimeter remains a pre-production requirement.
   **Disposition: defer as tracked release evidence.** Source:
   `docs/security/tenant-provisioning-production-readiness.md`.

5. **Performance evidence is a non-gating local baseline.** Twelve sequential
   samples per path observed P90 values of 16.65 ms provisioning, 16.84 ms
   456-row console list, and 16.60 ms console detail. Each path made one
   observed RPC; database-internal SQL count was unobserved. The evidence is
   insufficient to set a production target until an owner makes that decision.
   **Disposition: continue the existing in-progress action.** Source:
   `_bmad-output/test-artifacts/epic-12-performance-baseline.md`.

### Diff-scope review boundary

No fourth broad review was opened. The final independent review covered the
complete stable change; its later focused passes assessed only the four
findings and their regressions. This retrospective consolidates that accepted
evidence and performs no source re-review.

## Behavior verification

This refresh did not start services or rerun flows. It reuses accepted
evidence: the 24/24 trace, closure CI database/browser/recovery results, the
executed concurrency result, and the executed cleanup regression. Those runs
cover the privileged provisioning protocol, operator console, first-admin
onboarding, recovery, and the two regressions that required database
verification. No hosted deployment, real-production enablement, or production
capacity claim is made.

## Previous-retro follow-through

Epic 11's prior retrospective remains at
`_bmad-output/implementation-artifacts/epic-11-retro-2026-09-11.md`. Its four
open tracked actions remain unchanged. This refresh has no evidence or owner
confirmation to change any Epic 11 action status.

## Action items

The three completed Epic 12 actions are retained in sprint status as completed
history and are not duplicated here. Two Epic 12 items remain open:

1. **Before production provisioning enablement, preserve redacted evidence of
   attestation-key rollout and bounded rotation plus hosted TLS and
   encryption-at-rest configuration.** Owner: Release operator/security owner.
   Status: open. This is a release-evidence prerequisite, not merge or
   production-enablement approval.
2. **Execute `12.X-PERF-001` against a documented pilot dataset and record
   elapsed time and console query count/latency; establish a numeric gate only
   after an owner decision.** Owner: QA + Architect/Product. Status:
   in-progress. The local baseline is complete but remains advisory.

## Acceptance verdict

**Accepted-with-open-items** (declared criteria; machine verdict).

All three stories are complete in canonical sprint status, the acceptance trace
is 24/24 FULL, independent review and all four findings are closed, and the
closure CI passed at `6edbd2d`. The remaining two actions are deliberately
tracked release/performance evidence and do not identify an unmet declared
Epic 12 product criterion. They prevent real production provisioning from being
enabled until their separate conditions are met; they do not revise the
historical rejected verdict recorded above.

## Open questions

- Which owner-approved numeric target, if any, should follow the documented
  pilot performance evidence?
- When the shared platform/security programme has collected the required
  deployed evidence, will the owner authorize real production provisioning?

## Assumptions

- Headless invocation explicitly selected Epic 12.
- The current canonical `detect-epic --epic 12` result is authoritative:
  `story_count: 3` and `pending_stories: []`.
- The accepted closure record supplied the final source commit and CI evidence;
  this refresh did not rerun the CI or runtime suites.
- The two listed actions are the current Epic 12 open items already present in
  sprint status. No action was appended, edited, or transitioned by this
  retrospective refresh.
