# Story 7.4: Accepted State Immutability And Correction Boundary

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Change Log

- 2026-07-07 — dev-story: implemented Story 7.4. Added the additive accepted-immutability migration (`20260711120000_accepted_record_lock.sql` — `enforce_quote_acceptance_lock` on `quote_acceptances`, `enforce_job_source_ref_lock` on `jobs`, custom SQLSTATE `AR704`); added the `ACCEPTED_RECORD_LOCKED` command code + the `AR704 → ACCEPTED_RECORD_LOCKED` mapper branch; added the `job-accepted-lock-notice` correction-boundary UI notice on the job detail. Un-skipped + verified the ATDD 7.4-INT-01/02/03, 7.4-RLS-01, 7.4-E2E-01, and the two unit mapper branches; reconciled the 7.1/7.2-era acceptance-tables trigger guard. All tasks complete; status → review. Full gate green (unit 1126, INT 631, E2E 15, typecheck/lint/build clean).

## Story

As a tenant admin,
I want accepted quote and job source data to be locked at BOTH the command layer and the database — so an already-committed acceptance record (accepted version reference, acceptance evidence, accepted price, accepted timestamp, channel, source sent total) and a job's immutable source references cannot be silently edited through any normal app path OR a direct own-tenant SQL UPDATE, while the sanctioned lifecycle machinery (the 7.2 idempotent accept retry, the 7.3 allowed job edits, soft-delete/archival, append-only events) still works,
so that a mistake requires an explicit audited correction workflow — not a silent edit — and the accepted commitment stays trustworthy end-to-end.

## Acceptance Criteria

**AC1 — Accepted commitment fields are immutable at BOTH layers (command lock code AND DB trigger)**
**Given** a quote version has been accepted (a live `quote_acceptances` row + its one `jobs` row exist via the 7.2 `accept_quote_and_create_job` RPC)
**When** a user attempts to edit any immutable field — on `quote_acceptances`: `quote_id`, `quote_version_id`, `channel`, `accepted_at`, `accepted_price_ore`, `source_sent_total_ore`, `adjustment_reason`, `evidence_file_id`, `evidence_reference`; on `jobs`: the source references `quote_acceptance_id`, `quote_version_id`, `customer_id` (the immutable commitment linkage) — via ANY command OR a DIRECT own-tenant authenticated (anon-key RLS-client) UPDATE
**Then** command validation rejects the mutation with a STABLE lock code (a new `ACCEPTED_RECORD_LOCKED` command code, exact-code-asserted field-by-field) AND a DB trigger/constraint RAISES below the command (a custom SQLSTATE mapped to that same code) — UI-only locking is a STOP; the DB enforcement is the load-bearing proof (architecture §9: immutable lifecycle tables block updates via triggers/constraints, NOT just UI disabling) (R-704; 7.4-INT-01/02).
**And** the UI on the accepted/job surface explains that corrections require an approved audited workflow — no silent edit affordance renders for any immutable field (R-714; 7.4-E2E-01).

**AC2 — No normal edit path mutates immutable accepted data; the event is not hidden or silently overwritten**
**Given** a legitimate correction need on an accepted record
**When** the user (or a crafted request) attempts normal edit paths — an id-only update, an empty-patch update, a bulk update, the 7.3 `updateJob` allowed-edit command, or a re-run of the 7.2 accept RPC
**Then** NO normal edit path changes immutable accepted data: the 7.3 `updateJob` command touches ONLY its four Phase-A-safe columns (`title`/`status`/`planned_start_date`/`planned_end_date`) and its unknown-field reject already blocks smuggling an immutable field; an id-only/empty-patch update is a clean no-op (no false denial, no write); a status-change still appends ONE `job_events` row and never silently overwrites or hides an event (R-704, R-714; 7.4-INT-03).
**And** the 7.2 idempotent accept-retry path is PRESERVED — an already-`accepted` version reaching the RPC's existing-record short-circuit still returns the existing `(acceptance_id, job_id)` with NO write, and the accepted-lock trigger MUST NOT fire on that path (it performs no immutable-field UPDATE) (retro constraint — see Constraints).

**AC3 — Cross-tenant attack on accepted records fails (read + immutable-field update)**
**Given** tenant A and tenant B accepted records (`quote_acceptances`/`jobs`)
**When** tenant A attempts to read OR mutate an immutable field of a tenant B `quote_acceptances`/`jobs` record (via the read path, the command, OR a direct SQL UPDATE)
**Then** RLS rejects the read (foreign id ⇒ zero rows ⇒ generic not-found; anon ⇒ `UNAUTHENTICATED`) AND the immutable-field update fails — a foreign id is invisible under RLS so the trigger never sees the row, and an own-tenant crafted mutation of an immutable field on a visible row is rejected by the trigger; no existence disclosure (R-701, R-704; 7.4-RLS-01). The three tables are already enrolled in the H4 tenant-inventory gate (7.1); 7.4 adds NO table, so the H4/enrollment coverage is unchanged — 7.4's negatives target the LIVE accepted-record immutability path.

## Scope Boundary (read first — this is the LAST story in a multi-story epic)

7.4 lands **the DB-level accepted-immutability trigger + the command lock code + the correction-boundary UI message + the regression suite**. It adds NO new table and NO new column — it is purely ADDITIVE triggers/functions on the EXISTING (frozen) 7.1 tables, plus a new stable command error code, a small write-error-mapper branch, one UI notice, and tests. Precisely what belongs where:

- **7.1 (DONE, do NOT rebuild):** the migration `20260709120000_acceptance_to_job_model.sql` creating `quote_acceptances`/`jobs`/`job_events` (columns, composite same-tenant FKs, `unique (quote_version_id)`/`unique (quote_acceptance_id)` backstops, RLS own-tenant SELECT/INSERT/UPDATE policies, GRANTs, indexes, `TENANT_TABLES` enrollment); the `captureQuoteAcceptance` command + `acceptance-price.ts` delta module; the `quote_acceptance` file-owner activation. **All frozen. 7.4 REUSES the tables verbatim — it adds NO column and NO table. The 7.1 migration header EXPLICITLY reserves the immutability trigger for Story 7.4 ("7.1 CREATES the tables shaped so Story 7.4 can add the accepted-immutability trigger ADDITIVELY").**
- **7.2 (DONE, do NOT rebuild):** the `accept_quote_and_create_job` RPC (migration `20260710120000_accept_quote_and_create_job.sql`) that POPULATES `quote_acceptances`/`jobs`/`job_events` transactionally + idempotently; the `acceptQuoteAndCreateJob` command; the `sent → accepted` lifecycle flip (status ALONE); the epic-6 affordance gating. **7.4 must NOT touch the RPC or the accept transaction. It must ensure the accepted-lock trigger does NOT break the RPC's idempotent short-circuit (which does NO immutable-field write on a retry).**
- **7.3 (DONE, do NOT rebuild):** the `/jobs` list + `/jobs/[jobId]` detail read UX; `src/features/jobs/read.ts`; the `updateJob` allowed-edit command (title/status/planned dates only, audited) + `src/server/commands/jobs/{jobs.ts,jobs-db.ts,validation.ts,index.ts}`; the `job` file-owner-type activation; the accepted-version→job deep link. **7.4 EXTENDS the job command surface with the DB-lock error mapping + the UI correction-boundary message — it does NOT re-do the allowed-edit command or the read layer.** The 7.3 `updateJob` already rejects any unknown field (an immutable field can't be smuggled at the command layer); 7.4 adds the DB backstop below it and the exact-code mapping.
- **7.4 (THIS story):** (a) the ADDITIVE migration `supabase/migrations/20260711120000_accepted_record_lock.sql` — the accepted-immutability BEFORE-UPDATE trigger on `quote_acceptances` and `jobs` (fail-closed-by-construction, custom SQLSTATE), mirroring the 6.4 sent-lock trigger pattern VERBATIM; (b) the new `ACCEPTED_RECORD_LOCKED` stable command code + message in `command-errors.ts`; (c) the write-error-mapper branch (`throwMappedJobWriteError`, and any acceptance mutation path) that maps the new SQLSTATE → `ACCEPTED_RECORD_LOCKED`; (d) the correction-boundary UI notice on the job detail (and, if a mutation is attempted, the message that corrections require an approved audited workflow); (e) the DOCS-01 Lovable-oracle delta note (in the migration header); (f) the INT/RLS regression suite (`7.4-INT-01/02/03`, `7.4-RLS-01`) + the E2E message check (`7.4-E2E-01`).

**HARD STOPS (require human approval):**
- **Do NOT implement a broad correction/edit workflow after acceptance.** 7.4 enforces the immutability BOUNDARY only (mutation rejected at command + DB; UI explains corrections need an approved audited workflow). The correction WORKFLOW itself is owner-gated (R-714). If implementing a correction workflow becomes required, STOP — that needs an explicit owner-approved policy/story.
- **Do NOT modify the 6.4 sent-lock trigger (`enforce_quote_version_sent_lock` / `enforce_quote_version_child_sent_lock`) or the 7.2 accept RPC.** Relying on the sanctioned `sent → accepted` transition is the design — modifying a frozen migration is a drift STOP. The accepted-record lock and the sent-version lock are ONE model at three scopes (Epic 6 sent-freeze R-605, Epic 7 accepted-lock R-704, Epic 8.4 locked-evidence-file) and must AGREE — use the SAME lock-code family and trigger shape; a fork is a STOP.
- **Do NOT add a new table or column** (H4/migration-reset per-table policy enumeration stays unchanged). If you find yourself writing a `create table`/`add column`, that is a design-drift STOP.

## Tasks / Subtasks

- [x] **Task 1 — ADDITIVE migration: the accepted-immutability triggers (`supabase/migrations/20260711120000_accepted_record_lock.sql`) (AC1/AC2/AC3)** — mirror the 6.4 `20260707120000_quote_version_sent_lock.sql` pattern VERBATIM: BEFORE UPDATE triggers on `quote_acceptances` and `jobs`, FAIL-CLOSED-BY-CONSTRUCTION (enumerate the EXEMPT columns that MAY change and RAISE on a change to anything else), a custom SQLSTATE distinguishable by the write-error mapper. Purely additive — a frozen prior migration is NEVER edited; NO new table (H4 untouched); the 7.1 RLS/GRANT posture is inherited verbatim.
  - [x] 1.1 `enforce_quote_acceptance_lock()` — BEFORE UPDATE on `public.quote_acceptances`. `security invoker`, `set search_path = ''`, schema-qualified. **DECISION (recorded in migration header + Dev Agent Record): LOCK everything except `archived_at`/`updated_at`.** The acceptance record has no allowed-edit command, so nothing legitimately UPDATEs it after creation except archival; locking-by-default is safest and AGREES with the 6.4 fail-closed posture. `notes`/planned dates are locked too (no live edit path to break). Uses the exempt-then-full-tuple `row(...) is distinct from row(...)` comparison technique the 6.4 trigger uses. RAISEs `AR704`.
  - [x] 1.2 `enforce_job_source_ref_lock()` — BEFORE UPDATE on `public.jobs`. `security invoker`, `set search_path = ''`, schema-qualified. Locks the IMMUTABLE tuple (`id`, `tenant_id`, `quote_acceptance_id`, `quote_version_id`, `customer_id`, `created_at`) and RAISEs `AR704` if it changed; the four allowed-edit columns (title/status/planned dates) + archived_at + updated_at are NOT in the tuple so `updateJob` is never fought. **DECISION: LOCK `customer_id`; LEAVE `facility_id`/`contact_id` UNLOCKED** (they are ON DELETE SET NULL — locking would fight the cascade; not AC-named commitment fields).
  - [x] 1.3 Custom SQLSTATE `AR704` (Accepted-Record, R-704) — does NOT collide with `23503`/`42501`/`23505`/`23514`/`22P02` NOR with 6.4's `QV409`. Migration comment states the mapping (`AR704` → command `ACCEPTED_RECORD_LOCKED`). `comment on function` docstrings added mirroring the 6.4 trigger comments.
  - [x] 1.4 Created the triggers `quote_acceptances_accepted_lock` + `jobs_source_ref_lock`. NO trigger on `job_events` (not AC-named; the append-only-ness is a separate concern deferred with its own owner — the same later owner as the 6.4 quote_events INSERT-ability item; documented as a DEFERRAL in the migration, not silently expanded).
  - [x] 1.5 DOCS-01 (Lovable-oracle delta) recorded in the migration header comment block: Lovable allowed MUTABLE acceptance evidence + client-side multi-step acceptance; Phase A accepted records + job source refs are IMMUTABLE once accepted (locked at BOTH command + DB layers; corrections require an approved audited workflow). Satisfies `7.4-DOCS-01`.
- [x] **Task 2 — New stable command code `ACCEPTED_RECORD_LOCKED` + write-error mapping (AC1)** — extend the shared error contract; map the new SQLSTATE at every command write path that can hit the trigger.
  - [x] 2.1 Added `"ACCEPTED_RECORD_LOCKED"` to the `CommandErrorCode` union in `src/server/commands/command-errors.ts` + the generic user-safe Swedish message in `COMMAND_MESSAGES` ("Den accepterade posten är låst. Korrigeringar kräver ett godkänt granskat arbetsflöde."). Documented in the code-comment block that this is the Epic-7 accepted-immutability lock, DISTINCT from 6.4's `QUOTE_VERSION_LOCKED` + 6.2's `QUOTE_VERSION_NOT_DRAFT` (the shared lock-code FAMILY not a fork).
  - [x] 2.2 Added the `case "AR704": throw new CommandError("ACCEPTED_RECORD_LOCKED");` branch to `throwMappedJobWriteError` in `src/server/commands/jobs/jobs-db.ts`. Documented that `updateJob` unknown-field-rejects at the command layer (VALIDATION_FAILED) so the command path can't smuggle to the DB — the branch is the below-the-command backstop for a direct SQL attack (7.4-INT-02).
  - [x] 2.3 Confirmed by grep that NO `updateAcceptance`/`updateQuoteAcceptance` (or any `quote_acceptances` UPDATE) command exists — `captureQuoteAcceptance` is INSERT-only, the accept RPC is INSERT/short-circuit. Nothing invented; the note that a future acceptance write path must also map `AR704` is recorded here.
- [x] **Task 3 — Correction-boundary UI message (AC1)** — on the job detail, render a non-editable notice explaining the accepted commitment is locked and corrections require an approved audited workflow. NO silent edit affordance for any immutable field.
  - [x] 3.1 Added the `job-accepted-lock-notice` section on `src/components/jobs/JobDetailView.tsx` (near the source-traceability block) with Swedish copy stating the source commitment is locked ("låst") and corrections require an approved audited workflow ("godkänt granskat arbetsflöde"). Text-not-color (a11y baseline); `role="note"`.
  - [x] 3.2 SCOPE GUARD honored: the notice offers NO correction/edit action, NO "request correction" button, NO field-worker/schedule/invoice/Fortnox surface — explanatory only (R-714 STOP). The 7.3 allowed-edit dialog stays exactly as shipped (verified by the still-green 7.3-E2E-01 "immutable refs display-only" test).
- [x] **Task 4 — Tests (per test-design-epic-7.md §Coverage Plan, story 7.4 rows) (AC1/AC2/AC3)** — the two-layer immutability proof is the headline. The ATDD RED-phase suites were pre-authored (`.skip`/`{ skip: true }`); dev un-skipped them and they now go GREEN against the landed migration + code.
  - [x] 4.1 `7.4-INT-02` (the load-bearing DB proof): DIRECT own-tenant authenticated UPDATE of each immutable field on `quote_acceptances` (accepted_price_ore, accepted_at, evidence_reference, channel, source_sent_total_ore, quote_version_id) + `jobs` (quote_acceptance_id, quote_version_id, customer_id) ⇒ REJECTED with exact SQLSTATE `AR704`; EXEMPT paths (archived_at flip on the acceptance; title/status/planned-date on the job) SUCCEED. GREEN.
  - [x] 4.2 `7.4-INT-01` (command-layer): `updateJob` smuggling `quote_version_id`/`quote_acceptance_id`/`customer_id`/`accepted_price_ore` ⇒ `VALIDATION_FAILED` before the DB, row byte-unchanged. GREEN. Both codes asserted across the INT-01/02 pair.
  - [x] 4.3 `7.4-INT-03` (accidental-update regression): id-only `updateJob` empty-patch ⇒ clean no-op (no false TENANT_ACCESS_DENIED, no write, no audit row, immutable fields byte-unchanged); a status change appends exactly ONE `job_events` row; the 7.2 idempotent accept-retry on the already-accepted version returns the EXISTING (acceptanceId, jobId) with NO write (the accepted-lock trigger did NOT fire on the short-circuit). GREEN — the retro constraint verified.
  - [x] 4.4 `7.4-RLS-01` (cross-tenant): tenant A reading a tenant B `quote_acceptances`/`jobs` row ⇒ zero rows; tenant A's direct UPDATE of an immutable field on a tenant B row ⇒ zero rows affected (RLS-invisible, trigger never sees it), row untouched; anon on the LIVE `updateJob` path ⇒ `UNAUTHENTICATED`. GREEN.
  - [x] 4.5 `7.4-E2E-01` (message + no-silent-edit, STATES + MESSAGING only): the accepted job detail shows the correction-boundary notice ("granskat arbetsflöde"); NO edit control renders for any immutable field; keyboard-reachable, text-not-color. GREEN (3 tests).
  - [x] 4.6 `7.x-UNIT-01` (fixture PII/ORGNR scan): the 7.4 INT/RLS suites reuse the 7.1/7.2/7.3 two-tenant factories + the real accepted-chain seed; öre values are 125_000 (< 10 digits, R-717); NO PII in fixtures. No new fixture value added.
- [x] **Task 5 — Regression: keep the whole gate green (AC1/AC2/AC3)** — the migration adds triggers to two EXISTING tables in the migration chain; ran the full suite, no regression.
  - [x] 5.1 `migration-reset.int.test.ts` (+ per-table policy enumeration) stays green — the trigger adds NO policy; the `quote_acceptances`/`jobs` policy SET is unchanged. RECONCILED the 7.1/7.2-era guard in `acceptance-tables-migration-reset.int.test.ts` that asserted "NO acceptance-immutability trigger (7.4) exists yet" — inverted to assert the triggers now EXIST (the sanctioned reconciliation the 7.2 RPC assertion already did once).
  - [x] 5.2 The 7.2 `accept-quote-and-create-job.int.test.ts` + 7.3 `update-job.int.test.ts` + `job-source-of-truth.int.test.ts` stay green (the `sent → accepted` flip + idempotent short-circuit + the allowed edits are exempt). Full INT suite: 56 files / 631 tests, 0 failures.
  - [x] 5.3 The Epic-6 sent-lock suites (`mark-quote-version-sent.int.test.ts`, `quote-sent-lock.e2e.spec.ts`, `quote-write-error-mapper.test.ts` via the unit gate) stay green — 7.4 does NOT modify the 6.4 trigger or the `QV409`→`QUOTE_VERSION_LOCKED` mapping; the new `AR704`→`ACCEPTED_RECORD_LOCKED` is additive/orthogonal.

## Dev Notes

### Primary sources (read these first)
- **Test design (authoritative test spec):** `_bmad-output/test-artifacts/test-design-epic-7.md` — the 7.4 rows in the Coverage Plan: `7.4-INT-01` (command-layer immutable-field mutation ⇒ stable lock code, exact error-code assertions, field-by-field, line 511), `7.4-INT-02` (DIRECT own-tenant authenticated UPDATE ⇒ rejected by trigger/constraint — "below-the-command proof; UI-only locking is a STOP; AGREES with Epic 6/8.4", line 512), `7.4-INT-03` (accidental update-path regression: id-only/empty-patch/bulk do NOT mutate accepted fields, event never silently overwritten, line 513), `7.4-RLS-01` (cross-tenant attack — read + immutable-field update both fail, line 514), `7.4-E2E-01` (UI explains corrections require an approved audited workflow, line 534), `7.4-DOCS-01` (documented Lovable delta, line 550). The Risk table: R-704 (accepted-state immutability bypass, Score 6, "two independent layers — command guard AND DB trigger/constraint per architecture §9; UI-only locking is a STOP", line 722), R-714 (correction-workflow deferral — the boundary holds, not the workflow), R-717 (fixture PII). The Interworking table (lines 811-817): the immutability model AGREES across Epic 6 (R-605)/Epic 7/Epic 8.4 — a shared lock-code FAMILY not a fork; modifying the sent-lock trigger in an Epic 7 story is a drift STOP. Exit Criteria (lines 451-453): "Accepted immutability proven at BOTH layers — command mutation ⇒ stable lock code; direct own-tenant SQL UPDATE ⇒ rejected by trigger/constraint; every immutable field (version ref, evidence, price, timestamp, channel, source total, job source ref) covered field-by-field."
- **Epic + AC source:** `_bmad-output/planning-artifacts/epics.md#Story 7.4` (lines 1520-1554); the AC ("command validation AND database constraints/triggers reject the mutation; UI explains corrections require approved audited workflow; no normal edit path changes immutable accepted data; the event is not hidden or silently overwritten; cross-tenant + immutable-field updates fail"); the Technical Notes ("This story enforces the boundary. It does NOT implement a broad correction workflow until owner policy is approved"); the Stop Condition ("Stop if implementing correction workflow becomes required; that needs explicit owner-approved policy/story"). FR41-FR48; NFR12 (immutable accepted refs/evidence); NFR20 (no partial acceptance/job state).
- **Architecture:** §9 (line 335 — "Immutable lifecycle tables (`quote_versions` when sent, `quote_acceptances`, locked file snapshots, audit events) should block normal updates through triggers, constraints, or command-only rules, not just UI disabling" — `quote_acceptances` is NAMED as a table that must block updates via triggers; this is 7.4's core mandate); §11 (line 478 — "Accepted versions add immutable acceptance and job source references"); the ADR "Quote versions become immutable when marked sent. Accepted quote version reference, acceptance evidence, accepted price, and source-to-job linkage become immutable after acceptance, except through an explicit audited correction workflow" (line 94); §13 (the accepted-to-job transaction 7.4 locks the OUTPUT of); §"Corrections" (line 828 — "Locked after acceptance; audited correction path later" + the OPEN owner question "What exact correction workflow is approved for accepted quote/job mistakes?" — the R-714 residual); ADR-A005 (immutable accepted model); ADR-A009 (narrow RPC — 7.4 does NOT add an RPC, only triggers).
- **Previous stories:** `_bmad-output/implementation-artifacts/7-3-minimal-job-order-record-and-tenant-admin-ux.md` (the `updateJob` command + `jobs-db.ts` mapper 7.4 extends; the `JobDetailView`/`JobEditDialog` 7.4 adds the notice to; the job read layer); `7-2-idempotent-accept-quote-and-create-job-command.md` (the accept RPC + the idempotent short-circuit 7.4 must preserve; the `sent → accepted` flip); `7-1-acceptance-evidence-capture-for-sent-quote-versions.md` (the frozen acceptance schema 7.4 locks); `6-4-mark-quote-version-sent-and-enforce-immutability.md` (**the closest analog — the exact trigger pattern, INT test structure, error-code family, and DOCS-01 Lovable-delta approach 7.4 mirrors**).

### THE reference implementation to COPY the pattern from (do NOT re-invent)
- **The 6.4 sent-lock migration is the DIRECT template:** `supabase/migrations/20260707120000_quote_version_sent_lock.sql` — `enforce_quote_version_sent_lock()` is the FAIL-CLOSED-BY-CONSTRUCTION BEFORE-UPDATE trigger to mirror: `security invoker` + `set search_path = ''` + schema-qualified refs; enumerate the EXEMPT columns and RAISE with a custom SQLSTATE on a change to anything else; use the `row(new.col, …) is distinct from row(old.col, …)` two-tuple comparison so a FUTURE additive column is locked-by-default. The custom SQLSTATE `QV409` there → `QUOTE_VERSION_LOCKED`; 7.4 uses a NEW distinguishable SQLSTATE (e.g. `AR704`) → the NEW `ACCEPTED_RECORD_LOCKED`. Copy the `comment on function` docstring style, the `create trigger … before update … for each row execute function …` shape, and the revoke/grant discipline (7.4 adds NO function grant — the triggers are not directly callable). **Difference from 6.4:** the 6.4 trigger keys off `old.status <> 'draft'` (only a non-draft version is locked); a `quote_acceptances` row is ALWAYS immutable once it exists (there is no draft acceptance), so 7.4's trigger locks on EVERY update — no status early-return; the `jobs` trigger locks the source-ref tuple on EVERY update but exempts the four allowed-edit columns.
- **The command error contract:** `src/server/commands/command-errors.ts` — add `ACCEPTED_RECORD_LOCKED` to the `CommandErrorCode` union + `COMMAND_MESSAGES` exactly as `QUOTE_VERSION_LOCKED` was added (the code-comment discipline: stable machine contract, generic user-safe message, no leak; co-exists with the other lock codes, never merged).
- **The write-error mapper:** `src/server/commands/jobs/jobs-db.ts` `throwMappedJobWriteError` — add the `case "AR704": throw new CommandError("ACCEPTED_RECORD_LOCKED");` branch (mirror `src/server/commands/quotes/quote-db.ts` `throwMappedQuoteWriteError`, which has the `case "QV409": throw new CommandError("QUOTE_VERSION_LOCKED");` branch and the `QV703 → SERVER_ERROR` transient branch). Keep the "throw the CODE only, never the raw Postgres message" discipline.
- **The DB-below-the-command INT proof:** `tests/integration/commands/mark-quote-version-sent.int.test.ts` (6.4-INT-03, lines ~237-410) — the DIRECT own-tenant authenticated (anon-key `makeAuthedServerClient`, NEVER BYPASSRLS) UPDATE-rejected-by-trigger test + the EXEMPT-column-still-mutable test + the status-reversal-rejected test. This is the exact structure for 7.4-INT-02. Reuse `createTwoTenantFixture`, `makeAuthedServerClient`, `cleanupFixture`, the admin readback helpers, `isLocalStackReachable`, `skipUnlessStack`, `runCommand`, and the injected `CommandClock`.
- **The E2E message check:** `tests/e2e/quotes/quote-sent-lock.e2e.spec.ts` (6.4-E2E-01) — STATES + MESSAGING only; the UI explains the lifecycle rule; do NOT assert DB rejection in E2E. Model 7.4-E2E-01's "corrections require an approved audited workflow" notice on the job detail on it.
- **The write-error-mapper unit test:** `tests/unit/server/commands/quote-write-error-mapper.test.ts` — mirror it for the job mapper's new `AR704 → ACCEPTED_RECORD_LOCKED` branch (a pure unit test, fast gate).
- **The frozen 7.1 schema (do NOT re-create):** `supabase/migrations/20260709120000_acceptance_to_job_model.sql` — the exact `quote_acceptances`/`jobs` columns 7.4 locks (see the Immutable-field inventory below). The header EXPLICITLY reserves the trigger for 7.4.
- **The frozen 7.2 RPC (do NOT modify):** `supabase/migrations/20260710120000_accept_quote_and_create_job.sql` — note the idempotent short-circuit (lines 158-177) returns BEFORE any write on a retry, and the `sent → accepted` flip (lines 206-213) is on `quote_versions.status` ALONE (gated by 6.4's trigger). 7.4's triggers are on `quote_acceptances`/`jobs`; they never fire during the accept transaction's INSERTs (a trigger fires on UPDATE, and the accept path INSERTs new rows) — confirm this by test (5.2).

### Immutable-field inventory (the field-by-field lock — AC1)
From the frozen 7.1 schema. **LOCKED on `quote_acceptances` (RAISE on any change):** `id`, `tenant_id`, `quote_id`, `quote_version_id`, `channel`, `accepted_at`, `accepted_price_ore`, `source_sent_total_ore`, `adjustment_reason`, `evidence_file_id`, `evidence_reference`, `notes`, `planned_start_date`, `planned_end_date`, `created_at`. **EXEMPT on `quote_acceptances`:** `archived_at` (soft-delete), `updated_at` (trigger-owned). **LOCKED on `jobs` (the immutable source-ref + identity tuple):** `id`, `tenant_id`, `quote_acceptance_id`, `quote_version_id`, `customer_id`, `created_at`. **EXEMPT on `jobs` (the 7.3 allowed-edit columns + trigger/soft-delete):** `title`, `status`, `planned_start_date`, `planned_end_date`, `archived_at`, `updated_at`, and (recommended) `facility_id`/`contact_id` (their ON DELETE SET NULL FK cascade must be able to null them — locking them would fight the cascade; they are NOT AC-named commitment fields). Confirm the exact column set against the live schema before writing the trigger (do not hardcode from this note if the schema differs — read the migration).

### Epic-7 retro-note constraints (surfaced by earlier stories in THIS epic — apply directly)
- **[7.2 dev-story — THE headline constraint for 7.4] The command sent-state gate was relaxed to let an already-`accepted` version reach the RPC's idempotent short-circuit; 7.4 immutability MUST preserve this retry path.** A strict `status == sent` gate would break retry idempotency. 7.4's accepted-lock trigger is on `quote_acceptances`/`jobs` — it fires on UPDATE of those tables, NOT on the accept RPC's idempotent SELECT-and-return short-circuit (which performs NO write on a retry). VERIFY IN A TEST (7.4-INT-03): re-run the real `accept_quote_and_create_job` on an already-accepted version ⇒ existing-record return, `was_existing=true`, NO write, trigger did NOT fire. Do NOT add any gate/trigger that would make a retry raise instead of idempotent-returning.
- **[epic test design] Accepted-immutability is ONE model at three scopes across three epics** (Epic 6 sent-freeze R-605, Epic 7 accepted-lock R-704, Epic 8.4 locked-evidence-file). The design pins "shared lock-code FAMILY, not a fork" as an explicit STOP so trace catches divergence. 7.4 uses the SAME trigger shape (fail-closed-by-construction, custom SQLSTATE, security-invoker) and a DISTINCT-but-related lock code (`ACCEPTED_RECORD_LOCKED`, sibling to `QUOTE_VERSION_LOCKED`) — a family = related codes with a shared shape, NOT one reused code and NOT a divergent mechanism. Do NOT fork the mechanism (e.g. do not use a CHECK constraint or a command-only rule where the sibling uses a trigger); do NOT modify the 6.4 sent-lock trigger.
- **[epic test design / 7.2 dev-story] The 6.4 sent-lock trigger enforces a forward-only legal-transition allow-list AND rejects customer-visible column co-mutation in the same non-draft UPDATE — the accept `sent → accepted` flip must transition status ALONE.** This is the 7.2 accept RPC's concern (it flips `quote_versions.status` alone), NOT 7.4's — 7.4 does not touch `quote_versions`. 7.4 must simply NOT break it: do not modify the sent-lock trigger, and confirm the 6.4 suites stay green (Task 5.3).
- **[7.2 dev-story] The RPC reads `customer_id`/`facility_id`/`contact_id` off the parent `quotes` row, NOT `quote_versions`.** So the `jobs.customer_id` 7.4 locks is the AUTHORITATIVE immutable source ref carried from the parent quote (not the version snapshot). Locking `jobs.customer_id` is correct (it is the AC-named "job source reference" linkage); do not confuse it with the version snapshot's display names.

### Deferred-work ledger items that overlap this story (fold in, don't reopen unrelated ones)
- **[7.2 review, Low — assigned to 7.4] The accept RPC's parent-`quotes` read has no `if not found` guard — fail-closed only incidentally via a downstream NOT NULL.** The ledger names "7.4 accepted-immutability work" as a possible owner for adding an explicit `if not found then raise QV409` after the parent-quote read in `accept_quote_and_create_job`. **7.4 must NOT modify the frozen 7.2 migration** (a frozen migration is never edited — that is the drift STOP). This item is a hardening nicety on the 7.2 RPC, not reachable today (the version→quote FK guarantees co-existence). LEAVE IT DEFERRED with its 7.2/RPC-hardening owner — do NOT edit `20260710120000_accept_quote_and_create_job.sql`. If a review insists, it is a NEW additive migration decision, out of 7.4's trigger scope.
- **[7.1 review, Low — Epic-6/7 reconcile] `source_sent_total_ore` derives from `quote_versions.accepted_price_ore` (defaults 0); a 0-frozen sent total is a data-quality dependency on Epic 6.** 7.4 LOCKS `source_sent_total_ore` on `quote_acceptances` as an immutable field regardless of its value; it does NOT add a `> 0` guard or re-derive it. Seed a REAL non-zero commitment in the 7.4 fixtures (drive the real sent→accepted chain — see Fixtures). No 7.4 code impact beyond the lock.
- **[7.2 review, Low] Idempotent re-entry on an already-`accepted` version runs the adjusted-price + evidence gates BEFORE the RPC short-circuit.** This is a `acceptQuoteAndCreateJob` command edge (a divergent-input re-entry surfaces VALIDATION_FAILED instead of the idempotent return); it is NOT 7.4's concern — 7.4 locks the accepted RECORD, it does not touch the accept command's pre-RPC gates. Leave it deferred with its 7.2 owner. 7.4-INT-03's idempotency check uses IDENTICAL-input retries (the sanctioned path).
- **[epic-6 gate, Med — assigned to Epic 7] Correction + PDF affordances render on every non-draft version, including the now-reachable `accepted`.** RESOLVED IN 7.2 (Task 5 gated new-version/PDF-retry OFF `accepted`). 7.4 does NOT re-do this gating and does NOT re-open it. 7.4 only adds the accepted-record DB lock + the correction-boundary notice.
- **[6.4 review, Low] `quote_events` remains INSERT-able by `authenticated` (the append-only trigger only blocks UPDATE/DELETE).** This is a quote-events hardening item with its own later owner. The 7.4 ANALOG — `job_events` INSERT-ability — is NOT in 7.4's AC (7.4 locks the accepted commitment fields, not the event log). Do NOT add a `job_events` append-only trigger unless a review deems it in scope; if you judge it a genuine seam, DEFER it with an owner (do not silently expand scope). The `job_events` append on a status change (7.3) must keep working.
- **[Epic-4/7, R-716] Per-person ROT cap is a flat-cap placeholder (Sign-Off Q3, deferred).** 7.4 LOCKS the accepted price AS GIVEN from the immutable `quote_acceptances` row; it does NOT re-run or imply a per-person-scaled ROT/VAT engine. Documented residual only — no code impact on 7.4.

### DB immutability discipline (money/commitment impact CRITICAL — AC1)
- The accepted price + source sent total are `bigint` integer öre on the frozen `quote_acceptances` row (7.1-landed, `CHECK >= 0`). 7.4 LOCKS them as immutable — it never re-derives, never does float/kronor math, never recomputes a total in SQL (the trigger only COMPARES old↔new and RAISEs). The whole point is that an accepted commitment cannot be silently edited: mutating any immutable field via ANY path is rejected at BOTH layers. The DB trigger is the LOAD-BEARING enforcement (architecture §9) — a UI-disable is explicitly NOT evidence and is a STOP if that is all that ships.
- **Fail-closed-by-construction:** enumerate the EXEMPT columns and RAISE on a change to anything else (mirror 6.4). This makes a FUTURE additive column immutable-by-default (a later story adding a column to `quote_acceptances` gets it locked without touching this trigger — the safe default for a commitment table).

### Fixtures / PII discipline (R-717)
- 7.4 lands NO new golden (the accepted-quote-to-job golden is 7.2's `7.2-GOLDEN-01`). 7.4's fixtures are two-tenant INT/RLS/E2E seeds. Reuse the 7.1/7.2/7.3 factories (`createTwoTenantFixture`, `makeAuthedServerClient`, `adminInsertQuoteVersion`, the 7.2 `adminSelectJobRow`/`adminSelectJobsForAcceptance`/`adminSelectJobEventsForJob`, and the E2E `acceptedJob` fixture 7.3 added to `global-setup.ts`). Seed a REAL sent-then-accepted chain: drive the REAL `mark_quote_version_sent` (Epic 6) then the REAL `accept_quote_and_create_job` (7.2) — NEVER hand-insert a `quote_acceptances`/`jobs` row for a live-path immutability test (the record must come from the real transaction so the locked fields are authentic and the trigger is proven against a genuine accepted row). Seed a NON-ZERO `source_sent_total_ore`/`accepted_price_ore`. Keep every öre value < 10 digits (the orgnr-scan boundary, R-717); NO PII (personnummer/orgnr/name/email/phone/address) in fixtures. Count-asserting tests seed `crypto.randomUUID()`.

### Project Structure Notes
- Migration → NEW `supabase/migrations/20260711120000_accepted_record_lock.sql` (the next dated migration after 7.2's `20260710120000`; additive triggers only — NO table/column). Error contract → EDIT `src/server/commands/command-errors.ts` (add `ACCEPTED_RECORD_LOCKED`). Write-error mapper → EDIT `src/server/commands/jobs/jobs-db.ts` (add the `AR704` branch). UI → EDIT `src/components/jobs/JobDetailView.tsx` (the correction-boundary notice). Tests → NEW `tests/integration/commands/accepted-record-lock.int.test.ts` (7.4-INT-01/02/03 + 7.4-RLS-01), NEW `tests/e2e/jobs/job-accepted-lock.e2e.spec.ts` (7.4-E2E-01), and EXTEND the EXISTING `tests/unit/server/commands/job-write-error-mapping.test.ts` with the `AR704 → ACCEPTED_RECORD_LOCKED` branch (do NOT create a new mapper test file — this one already exists and covers the 23503/42501/23505/23514/22P02 branches).
- NO migration table/column ⇒ the H4 gate + migration-reset per-table policy enumeration are UNCHANGED (the trigger adds NO RLS policy — the `quote_acceptances`/`jobs` policy SET stays exactly as 7.1 shipped, mirroring how 6.4's `quote_events` append-only trigger left the policy enumeration unchanged). Confirm this in `migration-reset.int.test.ts`.
- NO new nav item, NO new route, NO new module. The `/jobs/[jobId]` detail (7.3) is where the correction-boundary notice lands; the accepted-quote surface (`QuoteDetailView`) already links to the job (7.3 deep link) and already explains the version is locked (6.4) — 7.4 does not need a second quote-side notice unless a review wants one (the AC's UI clause is satisfied by the job-detail notice).

### Testing standards summary
- **Two runners:** `node --test` (pure UNIT — the write-error-mapper branch), Vitest (INT/RLS, DB-backed, local Supabase stack — the two-layer immutability proofs), Playwright for E2E (the message check). `SUPABASE_TEST_REQUIRED=1` hard-fails a missing stack in CI (the two-layer immutability proofs are DB-backed — a silent skip would leave the CORE accepted-lock unproven; this is the non-negotiable "post-reset Kong 502 false-green trap" from the epic-5/6/8 retros). Poll `/auth/v1/health` to 200 after `supabase db reset` before trusting a local INT run.
- **`7.4-INT-02` is the highest-value proof (R-704, architecture §9):** the DIRECT own-tenant AUTHENTICATED (anon-key RLS client, NEVER BYPASSRLS) UPDATE of an immutable field ⇒ REJECTED by the trigger. A test that only proves the command validator rejects an unknown field (that is 7.4-INT-01) or that the UI disables a control (that is not evidence at all) does NOT prove the DB-layer lock. The direct-SQL attack via the RLS client is the "below-the-command" proof the AC demands. Prove the EXEMPT paths STILL work (archival on the acceptance; the four allowed edits on the job) so the lock is precise, not a blanket freeze.
- **RLS negatives BEFORE positives; enrollment is the completeness guarantee** — the three tables are already enrolled (7.1). 7.4's cross-tenant/anon negatives (AC3) go against the LIVE accepted-record read + the direct immutable-field UPDATE.
- **Determinism:** assert on the injected `CommandClock`, no sleeps/wall-clock (the accepted_at is an explicit input; the trigger does not read a clock).

### References
- [Source: _bmad-output/test-artifacts/test-design-epic-7.md#Test Coverage Plan (7.4-INT-01 line 511, 7.4-INT-02 line 512, 7.4-INT-03 line 513, 7.4-RLS-01 line 514, 7.4-E2E-01 line 534, 7.4-DOCS-01 line 550, 7.x-UNIT-01 line 515); #Risk Assessment R-704 (line 722), R-714 (correction-workflow deferral), R-717 (fixture PII); #Interworking & Regression (lines 811-817 — the one-model-three-scopes AGREE constraint); #Exit Criteria (Accepted immutability proven at BOTH layers, lines 451-453); #Open Assumptions 5/6 (no correction workflow in Phase A; one model at three scopes must AGREE)]
- [Source: _bmad-output/planning-artifacts/epics.md#Story 7.4 (lines 1520-1554 — AC, Technical Notes "enforces the boundary, does NOT implement a correction workflow", Stop Condition); FR41-FR48; NFR12; NFR20]
- [Source: _bmad-output/planning-artifacts/architecture.md#9 (line 335 — quote_acceptances must block updates via triggers/constraints, not UI); #11 (line 478 — accepted versions add immutable acceptance + job source references); the immutability ADR (line 94); #Corrections (line 828 — locked after acceptance, audited correction path later, the open owner question); ADR-A005 (immutable accepted model); ADR-A009 (narrow RPC)]
- [Source: supabase/migrations/20260707120000_quote_version_sent_lock.sql (THE trigger template — enforce_quote_version_sent_lock fail-closed-by-construction + custom SQLSTATE QV409 + comment/grant discipline); 20260709120000_acceptance_to_job_model.sql (the frozen quote_acceptances/jobs/job_events schema 7.4 locks — the exact immutable columns + the 7.4-reserved header note); 20260710120000_accept_quote_and_create_job.sql (the frozen accept RPC — the idempotent short-circuit 7.4 must preserve, the sent→accepted flip; NEVER modify)]
- [Source: src/server/commands/command-errors.ts (add ACCEPTED_RECORD_LOCKED to the union + COMMAND_MESSAGES, mirroring QUOTE_VERSION_LOCKED); src/server/commands/jobs/jobs-db.ts (throwMappedJobWriteError — add the AR704 branch); src/server/commands/quotes/quote-db.ts (throwMappedQuoteWriteError — the QV409→QUOTE_VERSION_LOCKED template); src/components/jobs/JobDetailView.tsx + JobEditDialog.tsx (the 7.3 job detail — add the correction-boundary notice, do not add an edit affordance)]
- [Source: tests/integration/commands/mark-quote-version-sent.int.test.ts (6.4-INT-03 — the direct-SQL-UPDATE-rejected-by-trigger INT structure to mirror for 7.4-INT-02); tests/e2e/quotes/quote-sent-lock.e2e.spec.ts (6.4-E2E-01 — the message-only E2E to mirror for 7.4-E2E-01); tests/unit/server/commands/quote-write-error-mapper.test.ts (the mapper unit test to mirror); tests/integration/commands/accept-quote-and-create-job.int.test.ts + update-job.int.test.ts (the 7.2/7.3 suites that must stay green); tests/factories/tenants.ts (the two-tenant + accepted-chain factories + the 7.2 job admin-select helpers); tests/integration/rls/migration-reset.int.test.ts (per-table policy enumeration must stay unchanged)]
- [Source: _bmad-output/implementation-artifacts/7-3-minimal-job-order-record-and-tenant-admin-ux.md (the updateJob command + jobs-db mapper + JobDetailView 7.4 extends); 7-2-idempotent-accept-quote-and-create-job-command.md (the accept RPC + idempotent short-circuit + sent→accepted flip); 6-4-mark-quote-version-sent-and-enforce-immutability.md (the closest analog — trigger pattern, INT structure, lock-code family, DOCS-01 Lovable delta)]
- [Source: _bmad-output/implementation-artifacts/deferred-work.md (7-2 review: accept-RPC parent-quotes no-found guard — LEAVE DEFERRED, do not edit the frozen migration; 7-1 review: source_sent_total_ore 0-frozen dependency — seed non-zero; epic-6 gate: affordance-gating resolved in 7.2; 6-4 review: quote_events INSERT-able — the job_events analog is NOT in 7.4's AC; R-716 per-person ROT cap); _bmad-output/auto-bmad/retro-notes/epic-7.md (the relaxed sent-state gate → idempotent short-circuit that 7.4 immutability MUST preserve; accepted-immutability one model at three scopes, shared lock-code family not a fork; sent-lock forward-only transition + status-alone flip is a 7.2 concern; customer/facility/contact off the parent quote)]

## Testing

Per `test-design-epic-7.md` — the story-7.4 rows. All run in the every-PR gate (UNIT seconds; INT/RLS on the local stack; E2E within the 15-min bar).

**P0 (must pass):**
- `7.4-INT-01` — Post-acceptance mutation of any immutable field (accepted version ref/evidence/price/accepted_at/channel/source total/job source ref) via COMMAND ⇒ stable lock code; exact error-code assertions, field-by-field (AC1; R-704) (3-4 tests). At the command layer the smuggled immutable field is rejected `VALIDATION_FAILED` (the `updateJob` unknown-field reject); the DB-layer `ACCEPTED_RECORD_LOCKED` (via `AR704`) is proven by 7.4-INT-02's direct-SQL path — assert BOTH codes.
- `7.4-INT-02` — **Post-acceptance mutation via DIRECT own-tenant authenticated (anon-key RLS client, NEVER BYPASSRLS) UPDATE ⇒ rejected by trigger/constraint (the load-bearing DB proof; UI-only locking is a STOP; AGREES with Epic 6/8.4)** (AC1; R-704) (2-3 tests). Field-by-field on `quote_acceptances` + `jobs`. Prove the EXEMPT paths still succeed (archival on the acceptance; title/status/planned-date on the job).
- `7.4-INT-03` — Accidental update-path regression: id-only update, empty-patch, and bulk update do NOT mutate accepted fields and never silently overwrite the event; the 7.2 idempotent accept-retry still returns the existing records with no write (the relaxed-sent-state-gate retry path is preserved) (AC2; R-704, R-714) (2-3 tests).
- `7.4-RLS-01` — Cross-tenant attack on accepted records (tenant A/B) — read + immutable-field update both fail; anon ⇒ `UNAUTHENTICATED`; no existence disclosure (AC3; R-701, R-704) (2-3 tests).
- `7.x-UNIT-01` — Fixture/artifact privacy: PII/secret + ORGNR scan extended to any 7.4 fixture; öre values under the 10-digit boundary (R-717) (standing control).

**P1 (High):**
- `7.4-E2E-01` — On the accepted job detail, the UI explains that corrections require an approved audited workflow; no silent edit affordance renders for any immutable field. STATES + MESSAGING only — do NOT assert DB rejection in E2E (AC1; R-714) (1-2 tests).

**P2 (Medium):**
- `7.4-DOCS-01` — Documented intentional delta vs Lovable mutable acceptance evidence / client-side multi-step acceptance, recorded in the migration header (oracle delta register; never copied) (R-704) (1).

**Exit criteria this story contributes to (the epic gate):** Accepted immutability proven at BOTH layers — command mutation ⇒ stable lock code; direct own-tenant SQL UPDATE ⇒ rejected by trigger/constraint; every immutable field (version ref, evidence, price, accepted timestamp, channel, source total, job source ref) covered field-by-field; the exempt paths (archival; the four allowed job edits) still work; the 7.2 idempotent accept-retry is preserved; cross-tenant + anon rejected on the accepted-record read + immutable-field UPDATE; the UI explains corrections require an approved audited workflow (no silent edit); NO correction workflow built (owner-gated, R-714 STOP); the immutability model AGREES with Epic 6 (R-605) and Epic 8.4 (a shared lock-code family, not a fork); no new table/column (H4 + migration-reset policy enumeration unchanged); the 6.4 sent-lock + 7.2 accept + 7.3 allowed-edit suites stay green.

## Dev Agent Record

### Agent Model Used

Claude Opus 4.8 (claude-opus-4-8[1m]) — auto-bmad dev-story delegate.

### Debug Log References

- `pnpm run test:unit` — 1126 pass / 0 fail / 0 skip (the two 7.4 `AR704 → ACCEPTED_RECORD_LOCKED` mapper branches un-skipped and green).
- `pnpm run typecheck` — clean. `pnpm run lint` — 0 errors (1 pre-existing unrelated warning in `vat.test.ts`).
- `supabase db reset` applied the full chain incl. `20260711120000_accepted_record_lock.sql` cleanly; polled `/auth/v1/health` → 200 before the DB-backed run (the Kong 502 false-green trap).
- `SUPABASE_TEST_REQUIRED=1 pnpm run test:int` — 56 files / 631 tests, 0 fail (7.4-INT-01/02/03 + 7.4-RLS-01 all green; the reconciled `acceptance-tables-migration-reset` trigger check green; migration-reset per-table policy enumeration unchanged; the 7.2/7.3/6.4 regression suites green).
- `npx playwright test tests/e2e/jobs/ tests/e2e/quotes/quote-sent-lock.e2e.spec.ts` — 15/15 pass (7.4-E2E-01 x3 + the 7.3 job-traceability/list + the 6.4 sent-lock regressions).
- `pnpm run build` — succeeds.

### Completion Notes List

- **Two-layer accepted-immutability landed and proven.** Command layer: the 7.3 `updateJob` validator already unknown-field-rejects any immutable field (`VALIDATION_FAILED`) — no code change needed there; 7.4 added the DB-below-the-command backstop. DB layer: two additive BEFORE-UPDATE triggers (`enforce_quote_acceptance_lock` on `quote_acceptances`, `enforce_job_source_ref_lock` on `jobs`), fail-closed-by-construction, custom SQLSTATE `AR704` → the new `ACCEPTED_RECORD_LOCKED` command code. Mirrors the 6.4 sent-lock trigger shape VERBATIM (security invoker, empty search_path, schema-qualified, exempt-then-tuple comparison) — a shared lock-code FAMILY, not a fork.
- **Lock-vs-exempt decisions:** `quote_acceptances` — LOCK everything except `archived_at`/`updated_at` (no live edit path exists to break; safest fail-closed default). `jobs` — LOCK the source-ref+identity tuple (`id/tenant_id/quote_acceptance_id/quote_version_id/customer_id/created_at`); LEAVE `facility_id`/`contact_id` UNLOCKED (their ON DELETE SET NULL cascade must be able to null them; not AC-named commitment fields). The four 7.3 allowed-edit columns (title/status/planned dates) are exempt so `updateJob` is never fought. Both decisions recorded in the migration header.
- **Trigger firing order verified safe.** Postgres fires same-event row triggers in trigger-NAME alphabetical order. On `quote_acceptances` the lock trigger `quote_acceptances_accepted_lock` sorts BEFORE `quote_acceptances_set_updated_at` (`accepted_lock` < `set_updated_at`), so at lock-check time `new.updated_at == old.updated_at`. On `jobs` the ordering is the REVERSE — `jobs_source_ref_lock` sorts AFTER `jobs_set_updated_at` (`so`urce_ref_lock > `se`t_updated_at), so `updated_at` is already bumped when the job lock runs — but this is harmless BECAUSE `updated_at` is deliberately NOT in the `jobs` locked tuple (`enforce_job_source_ref_lock` compares only id/tenant_id/quote_acceptance_id/quote_version_id/customer_id/created_at), so a bumped `updated_at` cannot trip it. The safety of BOTH triggers rests on `updated_at` being excluded from the compared tuple, NOT on a uniform before-`set_updated_at` ordering. On the acceptance trigger, an edit to a locked column falls through the exempt-check to the full-tuple comparison and RAISEs; the exempt `archived_at` flip enters the exempt branch, finds the commitment tuple unchanged, and returns (then set_updated_at bumps updated_at). Same structure as the 6.4 trigger, which coexists with its own set_updated_at identically. NOTE for a future story: if `updated_at` is ever added to the `jobs` locked tuple, the AFTER-`set_updated_at` firing order would then produce a false RAISE — the ordering, not just the tuple, would need revisiting.
- **7.2 idempotent retry preserved (the headline retro constraint).** The accepted-lock triggers fire on UPDATE of `quote_acceptances`/`jobs`; the accept RPC's idempotent short-circuit SELECTs-and-returns with NO immutable-field UPDATE, so a retry on an already-accepted version idempotent-returns the existing (acceptanceId, jobId) and never trips the lock — proven by 7.4-INT-03.
- **NO frozen migration edited; NO new table/column.** The 6.4 sent-lock trigger and the 7.2 accept RPC are untouched. H4 + the migration-reset per-table policy enumeration are unchanged (the triggers add NO RLS policy).
- **DOCS-01 Lovable-oracle delta** recorded in the migration header (Lovable mutable acceptance evidence + client-side multi-step accept vs Phase-A immutable-at-both-layers, corrections via an approved audited workflow).
- **Test approach was ATDD.** The full 7.4 INT/RLS/E2E suites + the two unit mapper branches were pre-authored in RED-phase (`.skip`/`{ skip: true }`) by create-story. Dev implemented the production code, then un-skipped them; they went GREEN unchanged (no test logic rewritten). One 7.1/7.2-era guard assertion ("NO acceptance-immutability trigger exists yet") was legitimately inverted to assert the triggers now exist.

### File List

**Created:**
- `supabase/migrations/20260711120000_accepted_record_lock.sql` — the additive accepted-immutability BEFORE-UPDATE triggers on `quote_acceptances` + `jobs` (custom SQLSTATE `AR704`; DOCS-01 delta in the header).

**Modified (production):**
- `src/server/commands/command-errors.ts` — added `ACCEPTED_RECORD_LOCKED` to the `CommandErrorCode` union + `COMMAND_MESSAGES` (+ the doc block).
- `src/server/commands/jobs/jobs-db.ts` — added the `case "AR704": throw new CommandError("ACCEPTED_RECORD_LOCKED");` branch to `throwMappedJobWriteError` (+ the doc block).
- `src/components/jobs/JobDetailView.tsx` — added the `job-accepted-lock-notice` correction-boundary notice (explanatory only, text-not-color, no edit affordance).

**Modified (tests — un-skipped ATDD RED-phase suites now GREEN):**
- `tests/integration/commands/accepted-record-lock.int.test.ts` — un-skipped the four 7.4 INT/RLS `describe` blocks.
- `tests/unit/server/commands/job-write-error-mapping.test.ts` — un-skipped the two 7.4 `AR704 → ACCEPTED_RECORD_LOCKED` mapper tests.
- `tests/e2e/jobs/job-accepted-lock.e2e.spec.ts` — un-skipped the three 7.4-E2E-01 tests.
- `tests/integration/rls/acceptance-tables-migration-reset.int.test.ts` — reconciled the 7.1/7.2-era guard: inverted the "NO acceptance-immutability trigger yet" assertion to assert the `quote_acceptances_accepted_lock` + `jobs_source_ref_lock` triggers now EXIST.

**Modified (process):**
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — status → in-progress → review.

### Review Findings

Tier-A thin review (2026-07-07): one lens (Acceptance Auditor) under one reviewer model (`primary`), plus a dedicated security review (auto-bmad-local, reported 0/0/0). No High or Critical acceptance violations; two-layer immutability lock, exempt precision, idempotent-retry preservation, cross-tenant/anon rejection, and the correction-boundary UI message all match the AC and test-design intent. Blind/edge lenses deliberately did not run in this thin pass.

- [x] [Review][Patch][Low] Dev Agent Record trigger-firing-order rationale was inaccurate for the `jobs` lock trigger [_bmad-output/implementation-artifacts/7-4-accepted-state-immutability-and-correction-boundary.md:178 (Completion Notes)] — The recorded claim "Both new lock triggers are named to sort BEFORE the reused `*_set_updated_at` trigger" is false for `jobs`: `jobs_source_ref_lock` sorts AFTER `jobs_set_updated_at` (alphabetical `so` > `se`), so `updated_at` is already bumped at lock-check time. Harmless because `updated_at` is deliberately not in the `jobs` locked tuple, so a bumped `updated_at` cannot trip it. FIXED in this pass — the Completion Note now states the correct per-trigger ordering, that safety rests on `updated_at` being excluded from the compared tuple (not on a uniform ordering), and adds a forward-note that adding `updated_at` to the `jobs` tuple would require revisiting the ordering.
