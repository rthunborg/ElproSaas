# Story 4.3: ROT And Grön Teknik Estimate Engine With Warnings

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a tenant admin,
I want ROT and grön teknik estimates to show assumptions and warnings,
so that tax-sensitive quotes are not sent as if unapproved rules were final.

## Acceptance Criteria

1. **Given** a calculation with tax-deduction assumptions, **when** a ROT or grön teknik estimate is calculated, **then** the pure engine returns a structured result carrying the **deduction amount** (integer öre), the **eligible basis** (integer öre), a **warnings** list, and **assumption-snapshot data** (the rates/caps profile used) — and it computes on integer öre via the SINGLE Story 4.1 `roundToOre` (half-away-from-zero) + `sumOre` primitives (NO float kronor, NO second rounding mode). ROT and grön teknik **CANNOT be mixed** on one calculation: a request combining both deduction types returns a **BLOCKING typed failure** (`ROT_GRON_MIX_NOT_ALLOWED`), NEVER a silently-combined sum — no code path adds a ROT and a grön-teknik deduction together (owner decision 2026-06-18: "ROT and grön teknik cannot be mixed"; only a later approved rule may permit it). [Source: epics.md#Story 4.3 AC1 + Technical Notes ("Use integer öre and explicit rates/caps profiles"); architecture.md#10 VAT/ROT/Grön ("Quote versions snapshot deduction type, eligible basis, rates/caps profile, persons/count, schablon choice if used, warnings…"; "Do not mix ROT and grön teknik unless a later approved rule explicitly supports it"); owner-decisions-applied-2026-06-18.md (Epic 4·4.3: "ROT and grön teknik cannot be mixed"); test-design-epic-4.md R-405 + R-406 + R-407 + 4.3-UNIT-01 + 4.3-UNIT-02 + 4.3-GOLDEN-01; project-context.md#Money rules ("NO money/VAT/ROT CALCULATION ENGINE exists yet — Epic 4 owns it"; reuse `isOreAmount`/`sumOre`)]

2. **Given** owner/accounting/legal sign-off is MISSING, **when** the admin reviews tax-deduction output, **then** the engine result AND its assumption snapshot STRUCTURALLY indicate the assumptions **require sign-off** — a `requiresSignOff: true` flag (or a "requires sign-off" warning) that is the **DEFAULT and cannot be absent**; the engine has **NO code path** that derives, renders, or persists a tax output as **approved** (mirrors the Epic 3 `quote_terms.approved_at` "approval is structural, never a default string" discipline — a missing sign-off is the default, and only a deliberate later human sign-off could flip it, which THIS story does NOT build). The rates/caps profile the engine uses is a NAMED **conservative UNAPPROVED profile** — the constants are pilot assumptions pending owner/accounting/legal sign-off, NOT shipped as production-approved fact. [Source: epics.md#Story 4.3 AC2 + Stop Conditions; architecture.md#10 ("ROT and grön teknik are estimates until owner/accounting/legal sign-off"); project-context.md#Money rules ("ROT and grön teknik assumptions require owner/accounting sign-off before production use"); Inherited Foundation ("Epic 4 mirrors the 'approval is structural, never a default string' discipline for tax assumptions: a missing sign-off is a NULL/absent flag surfaced as a warning; the engine never derives `isApproved`"); test-design-epic-4.md R-405 (design-time near-blocker) + 4.3-UNIT-03 + Non-Negotiable ("No ROT/grön-teknik output approvable without the explicit human sign-off flag"); `src/lib/snapshots/build.ts` (`buildQuoteTermsSnapshot` — `approved_at`/`approved_by` captured VERBATIM, never derives `isApproved`)]

3. **Given** BRF / private / company-like customer-posture fixture cases, **when** validation runs, **then** eligibility **warnings and blocking behavior match the conservative Phase-A policy**: ONLY a `private` customer is eligible for ROT/grön teknik (owner decision 2026-06-18); a non-`private` posture (`company` / `brf` / `public`) yields an eligibility **warning** (or a blocking result per the conservative policy), and the customer-facing disclaimer wording is NOT shipped as legally-approved. **`personnummer` is NOT captured or required by the pure engine** — the engine takes a resolved eligibility POSTURE (a customer-type-derived boolean/enum), NEVER a personnummer or any PII; personnummer-based ROT eligibility is a **flagged unapproved assumption**, and NO PII (personnummer, orgnr, name, email, address) ever enters `src/lib/money`. [Source: epics.md#Story 4.3 AC3 ("warnings and blocking errors match the conservative Phase A policy AND personnummer is not captured by default") + Stop Conditions; owner-decisions-applied-2026-06-18.md (Story 3.1: "only `private` is eligible for ROT/grön teknik"; Epic 4·4.3: "Only `private` eligible … personnummer required for ROT" — the CRM stores it, the ENGINE does not read it); architecture.md#10 ("Do not capture personnummer in Phase A unless explicitly approved later"); test-design-epic-4.md R-412 + 4.3-GOLDEN-02 (BRF/private/company eligibility) + 4.3-UNIT-05 (no PII path into `src/lib/money`); project-context.md#Money rules (personnummer stored access-controlled for `private` in CRM only; the pure engine must not read PII)]

4. **Given** this story extends the PURE money engine with the ROT / grön-teknik estimate primitives, **when** the deliverable lands, **then** it is PURE additive `src/lib/money` code (the deduction engine + the frozen tax-assumption snapshot builder) + `node --test` UNIT tests + golden ROT/grön fixtures — and it builds **NO** database schema/migration, **NO** new dependency, **NO** `.env` edit, **NO** new nav item/route/UI, **NO** calculation rows (Epic 5), and **NO** quote-version PERSISTENCE (Epic 6). It **REUSES (never forks)** the Story 4.1 `@/lib/money` primitives (`isOreAmount`/`ORE_AMOUNT_MAX`, `roundToOre`, `sumOre`, the `OreResult`/`MoneyErrorCode` typed-failure shape), the Story 4.2 VAT engine where a gross/net interplay is needed, and the Story 3.5 snapshot-freeze discipline (copy-by-value + `Object.freeze` + injected `capturedAt`). All existing unit suites (money, VAT, pricing-validation, money-display, settings vat-display, snapshots golden) MUST stay green. [Source: epics.md#Story 4.3 Technical Notes ("Treat Lovable tax functions as behavior-oracle candidates only. Use integer öre and explicit rates/caps profiles. Do not encode legal approval as implementation fact"), Security/RLS Impact ("None directly for pure engine"); architecture.md#22 (`src/lib/money`); project-context.md#Money rules (single-source-of-truth, do not duplicate); test-design-epic-4.md Not-in-Scope (Epic 4 is primitives; DB/UI consumers are Epics 5-6; no INT/RLS/E2E in Epic 4's own coverage) + Inherited Foundation (reuse `isOreAmount`/`sumOre`, snapshot freeze); `src/lib/money/ore.ts` + `src/lib/money/vat.ts` (the reused 4.1/4.2 primitives)]

## Tasks / Subtasks

- [x] Task 1: Add the ROT/grön-teknik rate/cap PROFILES as named, explicitly-UNAPPROVED data (AC: 1, 2)
  - [x] 1.1 Add a `src/lib/money/tax.ts` (re-exported from `src/lib/money/index.ts` alongside the 4.1/4.2 exports; do NOT create a second money root). Document the header the same way `ore.ts`/`vat.ts` do: PURE (no I/O, no DB, no clock), integer öre end-to-end, rates/caps are EXPLICIT PROFILES (never bare literals scattered through the code), and the whole tax policy is a CONSERVATIVE PILOT ASSUMPTION pending owner/accounting/legal sign-off. [Source: architecture.md#22 (`src/lib/money`); `src/lib/money/ore.ts` + `vat.ts` (header style + purity discipline); project-context.md#Money rules]
  - [x] 1.2 Model the deduction constants as NAMED PROFILE objects (e.g. `ROT_PROFILE_UNAPPROVED` / `GRON_TEKNIK_PROFILE_UNAPPROVED`), each an `as const` frozen record carrying: `deductionPercentBp` (the deduction rate in BASIS POINTS, mirroring the VAT bp discipline — NOT a `0.30`/`30`/`50` float), a per-person / per-category `capOre` (integer öre), a `label`/`profileId` naming the profile, and an explicit `approved: false` (or `signOffStatus: "pending-owner-accounting-legal"`) marker so the profile is self-describing as UNAPPROVED. The EXACT numeric rates/caps/schablon values are **owner-gated (Sign-Off Q3/Q4 — still pending the working session)** — encode a defensible conservative placeholder and mark it UNAPPROVED; the profile is DATA so a later sign-off swaps the numbers with NO code-shape change (test-design "Risks to Plan" contingency). Grön-teknik category rates/caps + schablon handling are profile fields; if a specific rate/cap/schablon value would require treating an owner decision as final, encode the conservative placeholder + warning, do NOT invent an approved value. [Source: epics.md#Story 4.3 Technical Notes ("explicit rates/caps profiles"; "Do not encode legal approval as implementation fact") + Stop Conditions; architecture.md#10 ("rates/caps profile, persons/count, schablon choice if used"); owner-decisions-applied-2026-06-18.md ("rates/caps/schablon + rounding + VAT rate still pending the working session"; "Still gated on the working session: Tax rates/caps/schablon"); test-design-epic-4.md R-407 + R-405 + Sign-Off Q3/Q4/Q7 + Assumption 4 ("ROT/grön-teknik constants are unapproved conservative profiles; the engine warns and never marks approved")]
  - [x] 1.3 A `requestUnknownProfile`/unknown-category path returns a CLEAR typed error (DX; 4.3-UNIT-07) rather than silently falling through to a wrong profile — the profile registry is a closed set, so a `switch(deductionType)` + `assertNever` (mirroring the snapshot `buildSnapshotSource` + H4 inventory discipline) makes an unhandled type a COMPILE error. [Source: test-design-epic-4.md 4.3-UNIT-07 (clear error when an unknown tax category/rate profile is requested); `src/lib/snapshots/build.ts` (`assertNever` exhaustiveness discipline)]

- [x] Task 2: Implement the pure deduction engine — deduction + eligible basis + warnings + no-approval (AC: 1, 2, 3)
  - [x] 2.1 Add the ROT/grön-teknik estimate function (e.g. `estimateDeduction(input): DeductionResult`). Input carries: the `deductionType` (`"rot" | "gron_teknik"` — a CLOSED union), the eligible-basis inputs (integer öre — the labour/qualifying amounts; see Task 4 for which inputs count toward the basis), a resolved eligibility `posture` (from the customer type — `private`-eligible vs not; NEVER a personnummer), a `persons`/count for the per-person ROT cap, and an injected `capturedAt` for the assumption snapshot. Output is a typed result carrying `{ deductionOre, eligibleBasisOre, warnings: readonly Warning[], assumptionSnapshot, requiresSignOff: true }` OR a typed BLOCKING failure. [Source: epics.md#Story 4.3 AC1; architecture.md#10 ("deduction type, eligible basis, rates/caps profile, persons/count"); test-design-epic-4.md 4.3-UNIT-01 (shape + values; assumptions captured, nothing approved) + R-405/R-407]
  - [x] 2.2 Compute `deductionOre = min( eligibleBasisOre * deductionPercentBp / 10000 (rounded via `roundToOre`), capOre )` — the deduction is the applied rate on the eligible basis, CAPPED at the profile cap (per-person for ROT, per-category for grön teknik). Use the SINGLE Story 4.1 `roundToOre` (half-away-from-zero) for the rate application and `sumOre` for any multi-line basis; re-check every computed öre with `isOreAmount` so an overflow is a typed `ORE_OVERFLOW`, never a silently-unsafe integer. The deduction rate is BASIS POINTS (`/ 10000`) exactly like VAT — NO `0.30`/`30`/`50`/`1.3` percent literal in the deduction path (the R-404 hidden-literal rule generalizes to tax rates; a golden source-grep asserts it). [Source: epics.md#Story 4.3 Technical Notes ("integer öre and explicit rates/caps profiles"); architecture.md#10 Rounding (line-level round via the single mode); test-design-epic-4.md R-407 + 4.3-GOLDEN-01 (at/above/below cap; per category; schablon on/off); `src/lib/money/ore.ts` (`roundToOre`/`sumOre`/`isOreAmount`); `src/lib/money/vat.ts` (`lineVatOre` — the bp-driven `/ 10000` pattern to mirror for tax rates, NO percent literal)]
  - [x] 2.3 Cap boundary behavior (R-407, golden-pinned): at cap → deduction = cap; above cap → deduction CLAMPED to cap (a warning may note the clamp); below cap → deduction = the rate-applied amount. Grön-teknik per-category caps and (if supported) schablon on/off are separate golden cases. A zero eligible basis → deduction `0` (valid, not a rejection). [Source: architecture.md#10 (caps); test-design-epic-4.md R-407 + 4.3-GOLDEN-01 (at/above/below cap, per category, schablon on/off, eligible-basis boundaries)]
  - [x] 2.4 **The mix block (R-406, epic blocker).** If an input attempts to combine ROT AND grön teknik on one calculation (however the input models both being requested), return the BLOCKING `ROT_GRON_MIX_NOT_ALLOWED` typed failure — there is NO combined-sum code path. A UNIT + GOLDEN invalid-mix case asserts the blocking error, NOT a silent combined deduction. [Source: epics.md#Story 4.3 AC1 ("cannot be mixed unless a later approved rule explicitly supports it"); owner-decisions-applied-2026-06-18.md ("ROT and grön teknik cannot be mixed"); architecture.md#10 ("Do not mix ROT and grön teknik"); test-design-epic-4.md R-406 + 4.3-UNIT-02 + Non-Negotiable ("Invalid ROT×grön mix blocked, not silently summed")]
  - [x] 2.5 **The no-approval / requires-sign-off invariant (R-405, design-time near-blocker).** The result and its assumption snapshot ALWAYS carry the "requires sign-off" signal by DEFAULT; the engine has NO parameter, branch, or field that can render/persist the output as `approved`. Mirror the `quote_terms.approved_at` structural discipline — absence of approval is the default, structurally, not a string. A behavioral UNIT proves the engine never emits an approved/final tax output without an explicit human flag (which this story does NOT provide). [Source: epics.md#Story 4.3 AC2; test-design-epic-4.md R-405 + 4.3-UNIT-03 + Testability note 3 ("Sign-off/approval is a behavioral assertion … prove the engine does not render or persist a tax output as approved … Absence of approval must be the default, structurally"); project-context.md#Money rules (`quote_terms` structural sign-off; contract never derives `isApproved`); `src/lib/snapshots/build.ts` (`buildQuoteTermsSnapshot` captures `approved_at` verbatim, never derives `isApproved`)]

- [x] Task 3: Build the FROZEN tax-assumption snapshot builder — reuse the Story 3.5 / 4.2 freeze discipline (AC: 1, 4)
  - [x] 3.1 Add a pure builder (e.g. `buildTaxAssumptionSnapshot(source, { capturedAt })` in `src/lib/money/tax.ts`) that COPIES BY VALUE the exact deduction profile used (deduction type, `deductionPercentBp`, `capOre`, `profileId`/label, schablon choice if used, persons/count), the eligible-basis inputs that fed it, the warnings, and the `requiresSignOff`/UNAPPROVED marker into a fresh object, then `Object.freeze(...)`. Take the capture instant via injected `capturedAt` (NEVER `Date.now()` inside the builder). The result holds NO live reference to the input, so mutating the source/profile afterward CANNOT reach a prior snapshot — an estimate NEVER silently recomputes when a rate later changes (R-409). This story does NOT persist the snapshot to any table (Epic 6 owns quote-version freeze) — it builds the FROZEN in-memory tax-assumption value the later quote snapshot will carry. Model it directly on the Story 4.2 `buildVatAssumptionSnapshot` shape (`src/lib/money/vat.ts`) and the Story 3.5 `build*Snapshot` builders. [Source: epics.md#Story 4.3 AC1 ("assumption snapshot data"); architecture.md#10 ("Quote versions snapshot deduction type, eligible basis, rates/caps profile, persons/count, schablon choice if used, warnings, … totals") + #11 (quote version snapshots tax deduction assumptions); test-design-epic-4.md R-409 + 4.2-UNIT-05/4.3-UNIT-06 (assumption snapshot frozen — mutating a source rate after capture does NOT change a prior snapshot); `src/lib/money/vat.ts` (`buildVatAssumptionSnapshot` — the exact pattern to mirror); `src/lib/snapshots/build.ts` + `types.ts` (copy-by-value + freeze + injected `capturedAt`)]
  - [x] 3.2 The tax-assumption snapshot captures STATE, computes nothing FURTHER — it carries the deduction/basis/profile it was built with (so the estimate is reproducible from the frozen profile) but does NOT re-derive an `isApproved` flag and does NOT mutate the source. `deductionPercentBp` stays basis points; `capOre`/`eligibleBasisOre`/`deductionOre` stay verbatim öre — no arithmetic on them in the builder. [Source: project-context.md#Money rules (snapshot captures STATE, computes nothing; never derives an approval flag; no clock read); test-design-epic-4.md 4.2-UNIT-03 pattern + 4.3-UNIT-06; `src/lib/money/vat.ts` (Task 3.2 of Story 4.2: builder does no arithmetic; no `isApproved`)]

- [x] Task 4: Document + pin the hidden-row / tillval inclusion-in-basis assumption (AC: 3)
  - [x] 4.1 Document + test which inputs count toward the deduction eligible basis: per the owner decision 2026-06-18, **hidden rows DO count in totals + deductions**, and options/tillval accepted with base count when selected (the UI visibility semantics are Epic 5's — Epic 4 pins only the pure BASIS rule). A UNIT test asserts the documented basis-inclusion rule so a future Epic 5 change to hidden-row/tillval handling that contradicts it fails loud; a GOLDEN case carries a representative hidden-row / options inclusion scenario. Do NOT build the calculation-row/tillval UI (Epic 5) — Epic 4 only fixes the pure "which inputs count toward the basis" rule. [Source: epics.md#Story 4.3 Test Requirements ("hidden row inclusion assumptions"); owner-decisions-applied-2026-06-18.md (Epic 5·5.4/5.5: "hidden rows DO count in totals + deductions"; options accepted with base but individually selectable); test-design-epic-4.md R-408 + 4.3-UNIT-04 ("Epic 4 pins the basis rule; UI visibility is Epic 5") + Not-in-Scope (calculation editor UX is Epic 5)]

- [x] Task 5: Tests — pure `node --test` UNIT + golden ROT/grön fixtures (AC: all)
  - [x] 5.1 UNIT tests under `tests/unit/lib/money/**` (`node --test`, pure, NO DB — via `pnpm run test:unit`). Use `import { test, describe } from "node:test"` + `import assert from "node:assert/strict"` and the `@/*` alias (`tests/support/alias-hook.mjs` resolves `@/lib/money` to its `index.ts` barrel — Story 4.1 fix; a top-level `import { estimateDeduction, buildTaxAssumptionSnapshot } from "@/lib/money"` resolves). Cover: (a) engine outputs `{ deductionOre, eligibleBasisOre, warnings, assumptionSnapshot, requiresSignOff }` with correct values (4.3-UNIT-01); (b) the ROT×grön MIX is BLOCKED with the typed failure, not a silent sum (4.3-UNIT-02); (c) missing sign-off ⇒ `requiresSignOff`/warning present + NO approved path (4.3-UNIT-03, behavioral); (d) hidden-row/tillval basis-inclusion assumption (4.3-UNIT-04); (e) personnummer NOT required/read by the engine; non-`private` posture flagged as an unapproved-eligibility warning (4.3-UNIT-05); (f) the shared assumption-snapshot FREEZE case — mutating a source rate/profile after capture does NOT change a prior snapshot; `capturedAt` injected, no clock read (4.3-UNIT-06 / 4.2-UNIT-05); (g) DX: a clear typed error for an unknown deduction type/profile (4.3-UNIT-07). Reject invalid inputs (non-öre basis, bad bp rate) as the matching typed `OreResult`-style failure with no raw value echoed. [Source: test-design-epic-4.md P0/P1/P3 rows (4.3-UNIT-01..07 + 4.3-GOLDEN-01/02 + 4.2-UNIT-05/4.3-UNIT-06); project-context.md#Testing Rules (two-runner stack; pure logic on `node --test`); `tests/unit/lib/money/vat.test.ts` + `tests/unit/lib/money/ore.test.ts` + `tests/unit/lib/snapshots/golden.test.ts` (the exact `node --test` + golden conventions to mirror)]
  - [x] 5.2 GOLDEN ROT/grön fixtures (`tests/fixtures/golden/money/rot-gron-*.json` + a golden test, e.g. `tests/unit/lib/money/tax.golden.test.ts`, mirroring `vat.golden.test.ts` + `rounding.golden.test.ts`). Pin: ROT at/above/below the per-person cap; grön-teknik per-category rates/caps; schablon on/off if supported; the eligible-basis boundaries; the invalid ROT×grön mix (asserts the blocking error); BRF/private/company-like eligibility cases (4.3-GOLDEN-02); a hidden-row/options inclusion case. Each fixture entry carries an `origin` label — `new-expected` (the pilot's explicit conservative assumption) or `documented-delta` (an intentional divergence from Lovable). Fixtures are anonymized (money/rate/cap numbers only, NO PII — no personnummer, orgnr, name, email, address); REUSE the existing golden PII-scan shape from `vat.golden.test.ts`/`rounding.golden.test.ts` (personnummer `\d{6}-\d{4}`, non-`example.test` email, `secret|password|api_key`). The golden test also GREPS/asserts the tax engine source has no hidden percent literal (`0.30`/`30`/`0.50`/`50`/`1.3` etc. as a rate) by construction — the rate flows from the profile bp (`/ 10000` present). [Source: epics.md#Story 4.3 Test Requirements ("no deduction, ROT, grön teknik categories, caps, schablon on/off if supported, invalid mixes, customer eligibility warnings, hidden row inclusion assumptions, and golden fixtures") + Migration/Coexistence ("Use anonymized Lovable examples for comparison and document deltas"); test-design-epic-4.md 4.3-GOLDEN-01 + 4.3-GOLDEN-02 + R-407 + R-410 + R-411; `tests/fixtures/golden/money/vat-rates.json` + `tests/unit/lib/money/vat.golden.test.ts` (fixture JSON shape + `origin` + PII-scan + no-hidden-literal grep to mirror)]
  - [x] 5.3 Run the FULL active CI verify-job gate sequence locally IN ORDER (NEVER chain `pnpm run X && pnpm run Y` in a package.json field — use the `scripts/run-tests.mjs` orchestrator that `pnpm test` invokes): `pnpm install --frozen-lockfile` → `pnpm run verify:lockfiles` → `pnpm run verify:service-role-containment` → `pnpm typecheck` → `pnpm lint` → `pnpm run test:unit` (the new tax units + golden run here) → `pnpm build` → `pnpm run verify:bundle-containment`. The db/int/e2e gates (`supabase start` && `supabase db reset` — separate commands — then `test:int` / `test:e2e`) run only as inherited regression: this story adds NO migration, NO DB surface, NO route/UI, and no int/e2e test references the changed symbols, so those gates are unchanged (defer to CI). [Source: project-context.md#Development Workflow Rules (CI gate sequence; no chained pnpm; `run-tests.mjs`); test-design-epic-4.md Execution Strategy (Epic 4 is pure `node --test` UNIT + GOLDEN, no DB/browser/network); `.github/workflows/ci.yml`; 4-2 story Task 4.3]
  - [x] 5.4 Scope-guardrail sweep + PR statements + deferred-work reconciliation: confirm NO migration/table/column added, NO new dependency, NO `.env` edit, `nav-items.ts` stays EXACTLY the seven IN-scope modules (this story adds NO UI/route), NO calculation rows (Epic 5), NO quote-version PERSISTENCE (Epic 6), exactly ONE `isOreAmount`/ONE bp-validity/ONE öre→kronor formatter authority after the story (reuse, never fork), NO hidden percent literal in the deduction path, NO combined ROT+grön sum path, NO personnummer/PII in `src/lib/money`, NO engine path that renders/persists a tax output as approved. Security/RLS PR statement (below). Surface the ROT/grön sign-off questions (Sign-Off Q3/Q4/Q5/Q7 — rates/caps/schablon/eligibility/disclaimer/approval posture) and the personnummer reconcile (Q6) + the two standing NFR concerns for the epic gate. Record any NEW deferral discovered WITH an owner story; do NOT reopen unrelated ledger items. [Source: epics.md#Story 4.3 Stop Conditions + #Epic 4 non-scope; project-context.md#Development Workflow Rules; test-design-epic-4.md Non-Negotiable epic blockers + Sign-Off Q3-Q7; deferred-work.md]

## Dev Notes

### Critical Constraints (read first)

- **This is a PURE-LOGIC story that EXTENDS the Story 4.1/4.2 `@/lib/money` engine with the FIRST ROT/grön-teknik deduction math in the repo. No table, no migration, no UI, no dependency.** State your operating mode (implementation) before editing. The Phase A story authorizes the product code; there is NO migration and NO new dependency. It ADDS to `src/lib/money` (a `tax.ts` deduction engine + the frozen tax-assumption snapshot builder) and pure `node --test` unit + golden tests. [Source: epics.md#Story 4.3 Technical Notes; architecture.md#10/#22; project-context.md#Money rules ("NO money/VAT/ROT CALCULATION ENGINE exists yet — Epic 4 owns it")]
- **NO ROT/grön-teknik output may be APPROVABLE without an explicit human sign-off flag — the DEFAULT is "requires sign-off" (R-405, NON-NEGOTIABLE epic blocker).** The engine ALWAYS returns `{deduction, eligibleBasis, warnings[], assumptionSnapshot, requiresSignOff:true}` and has NO code path that derives/renders/persists "approved". A build that lets a tax output be marked approved without the explicit flag is an epic blocker regardless of numeric score (test-design Non-Negotiable). Mirror the Epic 3 `quote_terms.approved_at` structural discipline — absence of approval is the default, structurally, never a string. The constants live as NAMED conservative UNAPPROVED profiles. [Source: architecture.md#10 ("estimates until owner/accounting/legal sign-off"); project-context.md#Money rules; test-design-epic-4.md R-405 + Non-Negotiable + Testability note 3 + 4.3-UNIT-03]
- **ROT and grön teknik CANNOT be mixed — block, never silently sum (R-406, NON-NEGOTIABLE epic blocker).** A request combining both returns a BLOCKING typed failure (`ROT_GRON_MIX_NOT_ALLOWED`); no combined-sum path exists. This is owner decision 2026-06-18, not an assumption to soften. [Source: owner-decisions-applied-2026-06-18.md ("ROT and grön teknik cannot be mixed"); architecture.md#10; epics.md#Story 4.3 AC1; test-design-epic-4.md R-406 + 4.3-UNIT-02 + Non-Negotiable]
- **NO hidden percent literal in the deduction path — the rate flows in as BASIS POINTS from a named profile (R-404 generalized to tax rates).** The deduction path must contain NO `0.30`/`30`/`0.50`/`50`/`1.3` rate literal; the rate is a `deductionPercentBp` profile field and the engine computes `roundToOre(eligibleBasisOre * deductionPercentBp / 10000)` — exactly the Story 4.2 VAT `/ 10000` pattern. A golden source-grep asserts no such literal (the same guard `vat.golden.test.ts` applies to the VAT source). `10000` (the bp-per-unit scale) and the profile cap öre are the only allowed numeric constants. [Source: test-design-epic-4.md R-404 (hidden-literal rule) + R-407; `src/lib/money/vat.ts` (`lineVatOre` — the bp-driven pattern + `VAT_BP_PER_UNIT`); `tests/unit/lib/money/vat.golden.test.ts` (the no-hidden-literal source grep to mirror)]
- **NO PII / no personnummer path into `src/lib/money` — the engine takes a resolved eligibility POSTURE, never PII (R-412).** The pure engine does NOT read/require a personnummer, orgnr, customer name, email, or address; eligibility is a customer-type-derived boolean/enum passed IN. Personnummer-based ROT eligibility is a FLAGGED UNAPPROVED ASSUMPTION (a warning), not an engine input. Golden fixtures carry ONLY anonymized money/rate/cap numbers. The tax-assumption snapshot takes an INJECTED `capturedAt`. This resolves the R-412 plan-vs-code divergence WITHOUT reopening it: the CRM (Story 3.1, owner decision 2026-06-18) stores personnummer access-controlled for `private` customers; the ENGINE never touches it. [Source: architecture.md#10 ("Do not capture personnummer in Phase A unless explicitly approved later"); epics.md#Story 4.3 AC3 ("personnummer is not captured by default"); project-context.md#Money rules (personnummer stored access-controlled for `private` in CRM only; pure engine must not read PII); test-design-epic-4.md R-412 + 4.3-UNIT-05 + Not-in-Scope ("The pure engine does NOT read/require personnummer")]
- **ROUNDING + öre discipline reuse the SINGLE Story 4.1 mode — no second rounding mode, no float kronor (R-401/R-402 inherited).** Every rounding in the deduction path goes through the Story 4.1 `roundToOre` (half-away-from-zero), multi-line basis sums go through `sumOre` (sum-of-rounded), and every öre is validated by the ONE `isOreAmount`. The deduction/basis/cap math is a CONSERVATIVE PILOT ASSUMPTION pending sign-off — golden-pinned, NOT accounting-final. **STOP** (report `needs-human`) ONLY if the numeric rates/caps/schablon/eligibility/disclaimer MUST be treated as production-APPROVED (that is the story stop condition) — do NOT block coding on the pending sign-off; encode the conservative profiles + warnings and proceed. [Source: epics.md#Story 4.3 Stop Conditions ("Stop if legal constants, eligibility, BRF handling, schablon handling, disclaimer wording, or personnummer capture must be treated as production-approved"); architecture.md#10 Rounding; test-design-epic-4.md R-401/R-402/R-407 + Sign-Off Q3-Q7; `src/lib/money/ore.ts` (`roundToOre`/`sumOre`/`isOreAmount`)]
- **STAY in 4.3 scope — the ROT/grön ESTIMATE ENGINE only. NO calculation rows (Epic 5), NO quote-version PERSISTENCE (Epic 6), NO golden-master fixture PACK (Story 4.4).** 4.3 provides the pure deduction engine + the frozen tax-assumption value + the ROT/grön golden fixtures for THIS story's scenarios; the comprehensive money/tax golden-master PACK (options/tillval, accepted-price deltas, full old/new/delta oracle) is Story 4.4. Do not reach forward. [Source: epics.md#Epic 4 story split (4.1 money/rounding; 4.2 VAT/totals; 4.3 ROT/grön; 4.4 golden pack); test-design-epic-4.md Coverage Plan (4.3 rows are deduction/mix/sign-off/caps/eligibility) + Not-in-Scope; project-context.md#Money rules]

### Inherited epic facts & gotchas (apply directly — from the epic-4 retro-note + Stories 4.1/4.2 close + the epic-4 test design + deferred-work)

Folded from `_bmad-output/auto-bmad/retro-notes/epic-4.md`, the epic-4 test design, the deferred-work ledger, and the Stories 4.1/4.2 close — reflect these in implementation, not as a "see the retro" pointer:

- **The epic-4 test design already exists and was VALIDATED (PASS) on 2026-07-01 — treat it as the BINDING contract for this story's coverage, not a draft to regenerate.** It maps 4.3 to test IDs `4.3-UNIT-01..07`, `4.3-GOLDEN-01/02`, and the shared snapshot-freeze `4.2-UNIT-05/4.3-UNIT-06`, and to risks R-405 (unapproved-tax-as-fact), R-406 (ROT×grön mix), R-407 (caps/eligible-basis), R-408 (hidden-row/tillval basis), R-409 (assumption-snapshot recompute), R-412 (personnummer/PII). Hit those exact scenarios. (Validation report: `_bmad-output/test-artifacts/test-design-validation-report.md`.) [Source: retro-notes/epic-4.md#Story epic-4 (test design pre-existed; delegate validated PASS); test-design-epic-4.md]
- **NO ATDD red-phase scaffold exists for 4.3 (unlike 4.2) — the dev authors the unit + golden tests fresh.** Stories 4.1/4.2 had pre-authored `describe.skip`-gated ATDD scaffolds; 4.3 does NOT (`tests/unit/lib/money/` has ore/vat/rounding/roundtrip files only, no `tax.test.ts`, and there is no `atdd-checklist-4-3-*`). Author the 4.3 unit + golden tests directly, mirroring the shape/conventions of the existing `vat.test.ts` + `vat.golden.test.ts` (node --test, `@/lib/money` top-level import, golden fixture load + `origin` labels + PII scan + no-hidden-literal source grep). No `describe.skip` gate is needed since you write and green them in one pass. [Source: retro-notes/epic-4.md#Story 4-1 (ATDD workers don't fit pure-library; adapted to inline node --test UNIT+GOLDEN); `tests/unit/lib/money/` (no 4.3 scaffold present); `tests/unit/lib/money/vat.test.ts` + `vat.golden.test.ts` (the shape to mirror)]
- **The Story 4.1 alias-hook fix makes `@/lib/money` (a bare DIRECTORY import) resolve under `node --test` — rely on it; do NOT re-fix it.** Add the new tax exports to `src/lib/money/index.ts` and import them via a top-level `import { … } from "@/lib/money"`; no new test-infra change is needed. [Source: retro-notes/epic-4.md#Story 4-1 (alias-hook fixed to resolve a directory to its index.ts barrel); `src/lib/money/index.ts`; `tests/support/alias-hook.mjs`]
- **`isOreAmount`/`ORE_AMOUNT_MAX` are CANONICAL in `@/lib/money`; the bp-validity rule (`isVatRateBp` + `VAT_RATE_BP_MIN`/`MAX`) is CANONICAL in `@/lib/money`; the öre→kronor formatter is consolidated to ONE authority — reuse all, fork none.** Story 4.1 moved `isOreAmount` and consolidated the formatter; Story 4.2 moved `isVatRateBp` + the bp bounds into `@/lib/money` (settings validator re-exports). For a DEDUCTION rate in basis points, REUSE the existing `isVatRateBp`/`VAT_RATE_BP_MIN`/`VAT_RATE_BP_MAX` bp-validity rule (a deduction % expressed as bp is the same 0..10000 integer-bp discipline) rather than forking a third bp check; if the deduction-rate domain genuinely differs, build the check FROM the exported bounds, do NOT re-hardcode a range. [Source: retro-notes/epic-4.md#Story 4-1 + #Story 4-2 (isOreAmount + isVatRateBp/bp bounds now canonical in `@/lib/money`, re-exported; one authority, no fork); `src/lib/money/ore.ts` + `vat.ts`]
- **`MoneyErrorCode` is widened ADDITIVELY — add the new tax codes, do not restructure the union.** Story 4.1 defined `MoneyErrorCode` (`INVALID_ORE_AMOUNT` | `INVALID_QUANTITY` | `ORE_OVERFLOW`); Story 4.2 added `INVALID_VAT_RATE_BP` additively. Add the 4.3 codes (e.g. `ROT_GRON_MIX_NOT_ALLOWED`, `NOT_ELIGIBLE`/eligibility, `UNKNOWN_DEDUCTION_TYPE`) additively to the same union in `src/lib/money/ore.ts` so the typed-failure surface stays one shape. [Source: retro-notes/epic-4.md#Story 4-2 (MoneyErrorCode widened additively with INVALID_VAT_RATE_BP); `src/lib/money/ore.ts` (`MoneyErrorCode`)]
- **The Story 3.5 / 4.2 snapshot-freeze discipline is LIVE and must be REUSED for the tax-assumption snapshot (R-008/R-409).** `src/lib/snapshots/build.ts` builders and `src/lib/money/vat.ts` `buildVatAssumptionSnapshot` copy every field BY VALUE, return `Object.freeze(...)`, take an INJECTED `capturedAt` (never `Date.now()`), capture STATE and compute nothing, and NEVER derive an `isApproved` flag. Model `buildTaxAssumptionSnapshot` directly on `buildVatAssumptionSnapshot` (same options shape, same freeze, same "omit-absent-optional-fields rather than carry `undefined`" pattern). [Source: `src/lib/money/vat.ts` (`buildVatAssumptionSnapshot`); `src/lib/snapshots/build.ts` + `types.ts`; project-context.md#Money rules (snapshot R-008); test-design-epic-4.md R-409 + Inherited Foundation]
- **The 8 owner/accounting/legal sign-off questions — for 4.3 the ROT/grön ones bite (rates/caps/schablon/eligibility/disclaimer/approval posture) plus personnummer — are HUMAN decisions, not plan gaps.** Sign-Off Q3 (ROT cap/%/basis/multi-owner), Q4 (grön category rates/caps/schablon/mix), Q5 (eligibility + disclaimer wording), Q6 (personnummer scope — reconcile "not captured by default" with the 2026-06-18 CRM decision; the engine must not read PII regardless), Q7 (approval posture — are ANY production-approved or ALL "unapproved assumption + warning"?). Build against the conservative UNAPPROVED profiles + warnings; do NOT block coding on sign-off, but do NOT ship the constants/eligibility/disclaimer as approved. The rates/caps/schablon + VAT rate value remain gated on the working session (owner-decisions 2026-06-18: "the critical remaining gate"). [Source: retro-notes/epic-4.md#Story epic-4 (8 owner sign-off questions incl. ROT cap, grön rates/caps/mix, eligibility disclaimer, personnummer scope, approval posture); test-design-epic-4.md Sign-Off Q3-Q7; owner-decisions-applied-2026-06-18.md ("rates/caps/schablon … still pending the working session")]
- **Two STANDING NFR CONCERNS carry through this epic — no `pnpm audit` dependency-scan CI gate and no coverage reporter.** Owner-pending across audits. Flag them in the PR / epic gate for schedule-or-accept; NOT this story's to fix. [Source: retro-notes/epic-4.md#Story epic-4 (standing NFR concerns); test-design-epic-4.md R-414 + Exit Criteria; project-context.md#Testing Rules]
- **R-412 reconcile is a KNOWN doc-only divergence — resolve it in THIS story by keeping PII out of the engine, do NOT reopen the CRM personnummer decision.** The epic-4 retro-note flags "story 4.3 'personnummer not captured by default' vs 2026-06-18 owner decision to store personnummer for private customers (R-412)". The resolution is NOT a code change to CRM: the CRM keeps its access-controlled `private`-only personnummer column; the 4.3 PURE ENGINE simply never reads/requires it (takes a resolved posture). The stale "no personnummer" test-design/exit-criteria wording is a doc-only divergence, not a code defect — surface the reconcile as Sign-Off Q6, do not re-defer or re-open. [Source: retro-notes/epic-4.md#Story epic-4 (gate-time reconcile: 4.3 'personnummer not captured' vs 2026-06-18 decision, R-412); test-design-epic-4.md R-412 + Not-in-Scope (personnummer CAPTURE by the tax engine is out of scope; the engine does not read PII); project-context.md#Money rules (2026-06-18 decision SUPERSEDES the old "no personnummer" plan text; stale test-design R-009 wording is a doc-only divergence)]

### Deferred-work items that overlap this story (address or knowingly work around)

Only the ledger entries whose subject overlaps 4.3's ROT/grön / tax-assumption / snapshot / PII area (most of the ledger is out of scope and must NOT be reopened):

- **[Low, epic-3 review] Personnummer is present in clear text in the CRM client-component payload (mask is cosmetic) — treat a stricter server-gated reveal as an Epic 6 privacy-hardening item.** This is a CRM display concern, NOT a 4.3 concern — but it reinforces 4.3's hard rule: the PURE ENGINE must NEVER receive a personnummer at all (it takes a resolved eligibility posture). Do NOT attempt to "fix" the CRM reveal here (out of scope, owner is Epic 6); just ensure NO personnummer path reaches `src/lib/money`. [Source: deferred-work.md (epic-3 review, `CustomerDetail.tsx` raw personnummer in client payload — Epic 6 privacy-hardening); test-design-epic-4.md R-412]
- **[Low, epic-3 review] Epic-3 test-design "no personnummer" P0/exit-criteria contradicts the shipped owner-approved schema — a doc-only reconcile.** The epic-3 test design asserted personnummer absence, but the schema carries it per the 2026-06-18 decision. The epic-4 test design carries the SAME stale-wording risk (R-412). Address here by NOT letting the pure engine read PII and by surfacing the reconcile as Sign-Off Q6 for the epic gate — a doc-only reconcile, NO code change to CRM. Do NOT reopen the CRM schema. [Source: deferred-work.md (epic-3 review, test-design "no personnummer" vs shipped schema — reconcile text to the 2026-06-18 decision); test-design-epic-4.md R-412]
- **[Med, epic-3 review] `CompanySettingsSnapshot` is intentionally identity-PARTIAL — do NOT treat it as the complete `company_settings` interface.** 4.3's tax-assumption snapshot does NOT need company identity at all (it captures the deduction PROFILE + basis + warnings). If any settings input is consumed, take only the needed slice; do NOT extend the settings snapshot with identity fields (Epic 6's). [Source: deferred-work.md (epic-3 review, snapshot `company_settings` variant omits PDF-identity fields Epic 6 needs); `src/lib/snapshots/types.ts` (`CompanySettingsSnapshot`)]

No other open ledger item is owned by the ROT/grön / tax-assumption / money surface this story touches; do NOT reopen unrelated CRM/pricing/RLS/audit/VAT-display deferrals.

### Architecture & pattern references (what to reuse vs build)

- **Reuse:** the Story 4.1 `@/lib/money` primitives — `isOreAmount`/`ORE_AMOUNT_MAX`, `roundToOre` (the SINGLE half-away-from-zero mode), `sumOre` (sum-of-rounded), `formatOreAsKronor`, and the `OreResult`/`MoneyErrorCode` typed-failure shape (`src/lib/money/ore.ts`); the Story 4.2 bp-validity rule `isVatRateBp` + `VAT_RATE_BP_MIN`/`VAT_RATE_BP_MAX` for the deduction-rate-in-bp check, and the `buildVatAssumptionSnapshot` shape as the direct model for `buildTaxAssumptionSnapshot` (`src/lib/money/vat.ts`); the Story 3.5 snapshot-freeze builders + `SnapshotBuildOptions` + `assertNever` exhaustiveness discipline (`src/lib/snapshots/build.ts` + `types.ts`); the `node --test` + `@/*`-alias + golden-fixture-loading conventions in `tests/unit/lib/money/vat.test.ts`, `vat.golden.test.ts`, `rounding.golden.test.ts`, and `tests/unit/lib/snapshots/golden.test.ts`; the golden fixture JSON + `origin`-label + PII-scan + no-hidden-literal-source-grep shape in `tests/fixtures/golden/money/vat-rates.json` + `vat.golden.test.ts`; the `CUSTOMER_TYPES` / `CustomerType` enum (`src/server/commands/crm/validation.ts`) as the reference for the eligibility-posture mapping (only `private` eligible) — but pass a resolved POSTURE into the pure engine, do NOT import server types into `src/lib` (keep the `src/lib` → `src/server` direction un-inverted; define the engine's own closed posture/type union in `@/lib/money`, mirroring how `vat.ts` defines its own `VatDisplayMode` copy).
- **Build (new):** `src/lib/money/tax.ts` (+ export from `src/lib/money/index.ts`) — the named UNAPPROVED ROT/grön profiles, the pure `estimateDeduction` engine (deduction + eligible basis + warnings + `requiresSignOff` + mix-block + cap-clamp), and the frozen `buildTaxAssumptionSnapshot` builder; `tests/unit/lib/money/tax.test.ts` + `tests/unit/lib/money/tax.golden.test.ts` — the unit + golden tests; `tests/fixtures/golden/money/rot-gron-*.json` — the anonymized ROT/grön golden fixtures with `origin` labels. Add the new tax `MoneyErrorCode` members additively in `src/lib/money/ore.ts`.
- **Directory / naming:** money/tax pure logic lives in `src/lib/money` (architecture §22). Shared modules kebab-case (`tax.ts`); tests `*.test.ts` under `tests/unit/**`. Money in TS is integer öre `number`; deduction/VAT rate is integer basis points `number` (3000 = 30.00%); a cap is integer öre. [Source: architecture.md#22 (`src/lib/money`); project-context.md#Code Quality Rules; `src/lib/money/ore.ts` + `vat.ts`]

### Project Structure Notes

- New files: `src/lib/money/tax.ts` — the ROT/grön profiles + deduction engine + tax-assumption snapshot builder; `tests/unit/lib/money/tax.test.ts` + `tests/unit/lib/money/tax.golden.test.ts` — units + golden; `tests/fixtures/golden/money/rot-gron-*.json` — anonymized ROT/grön fixtures with `origin` labels. Modified: `src/lib/money/index.ts` (add the new tax exports + types); `src/lib/money/ore.ts` (add the new tax `MoneyErrorCode` members additively).
- Alignment: `src/lib/money` is the architecture-§22 canonical home; the two-runner split (pure `node --test` under `tests/unit/**`; Vitest for DB) is respected — 4.3 adds ZERO DB/Vitest/Playwright tests (pure logic ⇒ unit + golden only, per the test design). No conflict with the unified structure.
- Variance / watch-out: (1) NO combined ROT+grön sum path — the mix is a BLOCKING failure. (2) NO engine path that renders/persists a tax output as approved — `requiresSignOff` is the structural default. (3) NO percent literal in the deduction path — rate is `deductionPercentBp` from a profile, `/ 10000`. (4) NO personnummer/PII into `src/lib/money` — pass a resolved eligibility posture. (5) Reuse the SINGLE `roundToOre`/`sumOre`/`isOreAmount`/`isVatRateBp` — no forked money/bp rule. (6) The tax-assumption snapshot REUSES the 4.2/3.5 freeze discipline (copy-by-value + `Object.freeze` + injected `capturedAt`), NOT a new snapshot mechanism, and is NOT persisted to a table (Epic 6). (7) Define the engine's own closed posture/deduction-type unions in `@/lib/money` — do NOT import `src/server` CRM/settings types (keep the `src/lib` → `src/server` direction un-inverted, mirroring `vat.ts`'s own `VatDisplayMode` copy).

### Security / RLS PR statement (for the dev to include in the PR)

None directly — this is a PURE `src/lib/money` primitive story: pure math over öre + basis-point + cap numbers with NO DB access, NO tenant-data path, NO PII (no personnummer/orgnr/name/email/address enters the engine — eligibility is a resolved posture), and NO clock read (the tax-assumption snapshot takes an injected `capturedAt`). No new endpoint, service-role usage, RLS surface, or migration. Service-role-containment + bundle-containment guards both pass unchanged. The ROT/grön rates/caps/schablon/eligibility/disclaimer are CONSERVATIVE UNAPPROVED profiles + warnings pending owner/accounting/legal sign-off — no tax output is rendered/persisted as approved.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 4.3 (lines 955-989) — AC1-3, Technical/Test/Security/Money/Migration notes, Dependencies (4.1 + 4.2 + 3.3), Stop Conditions (legal constants / eligibility / BRF / schablon / disclaimer wording / personnummer capture as production-approved)]
- [Source: _bmad-output/planning-artifacts/architecture.md#10 Money And Tax Model (integer öre; VAT basis points; line-level rounding; VAT/ROT/grön: estimates until owner/accounting/legal sign-off; snapshot deduction type/eligible basis/rates-caps profile/persons-count/schablon/warnings/totals; do not mix ROT and grön teknik; do not capture personnummer in Phase A unless approved) + #11 (quote version snapshots VAT + tax deduction assumptions) + #22 (`src/lib/money`) + ADR-A004 (integer öre; VAT basis points; ROT/grön snapshotted assumptions; accounting/legal sign-off required)]
- [Source: _bmad-output/project-context.md#Money, Tax, And Quote Rules ("NO money/VAT/ROT CALCULATION ENGINE exists yet — Epic 4 owns it"; ROT/grön assumptions require owner/accounting sign-off before production; personnummer stored access-controlled for `private` in CRM only, pure engine must not read PII; snapshot copy-by-value + `Object.freeze` R-008, captures STATE computes nothing, never derives `isApproved`, no clock read; reuse `isOreAmount`) + #Testing Rules (two-runner stack; pure logic on `node --test`; two standing NFR CONCERNS) + #Language-Specific Rules (typed Result, no thrown exceptions; no chained pnpm; `run-tests.mjs`; `assertNever` exhaustiveness)]
- [Source: _bmad-output/planning-artifacts/owner-decisions-applied-2026-06-18.md (Story 3.1: only `private` eligible for ROT/grön, personnummer required for ROT in CRM; Epic 4·4.3: ROT and grön teknik cannot be mixed; Epic 5·5.4/5.5: hidden rows DO count in totals + deductions, options accepted with base but individually selectable; "rates/caps/schablon + rounding + VAT rate still pending the working session" — the critical remaining gate)]
- [Source: _bmad-output/test-artifacts/test-design-epic-4.md — R-405 (unapproved-tax-as-fact, design-time near-blocker), R-406 (ROT×grön mix), R-407 (caps/eligible-basis), R-408 (hidden-row/tillval basis), R-409 (assumption-snapshot recompute), R-412 (personnummer/PII); P0/P1/P3 rows 4.3-UNIT-01..07 + 4.3-GOLDEN-01/02 + 4.2-UNIT-05/4.3-UNIT-06; Inherited Foundation (reuse `isOreAmount`/`sumOre`, snapshot freeze, golden pattern + PII scan, structural sign-off mirror); Non-Negotiable epic blockers (no approvable-without-flag, invalid-mix-blocked, no PII, frozen snapshots); Sign-Off Q3-Q7; Entry/Exit Criteria; Testability notes 3 (behavioral sign-off) + 5 (inject capture instant)]
- [Source: _bmad-output/auto-bmad/retro-notes/epic-4.md — #Story epic-4 (test design validated PASS; R-412 gate-time reconcile; 8 owner sign-off questions incl. ROT cap, grön rates/caps/mix, eligibility disclaimer, personnummer scope, approval posture; standing NFR concerns); #Story 4-1 (ATDD adapted to inline node --test UNIT+GOLDEN for pure-library stories; alias-hook resolves `@/lib/money` bare-directory import; isOreAmount/formatter consolidated); #Story 4-2 (isVatRateBp + bp bounds canonical in `@/lib/money`; MoneyErrorCode widened additively; private→incl-VAT built as documented conservative assumption not legally-final)]
- [Source: _bmad-output/implementation-artifacts/deferred-work.md — epic-3 review (`CustomerDetail.tsx` raw personnummer in client payload — Epic 6 privacy-hardening; keep PII out of the engine here); epic-3 review (test-design "no personnummer" vs shipped schema — doc-only reconcile, do not reopen CRM); epic-3 review (`CompanySettingsSnapshot` identity-PARTIAL — do not extend with identity fields)]
- [Source (reuse code): src/lib/money/ore.ts (`isOreAmount`/`ORE_AMOUNT_MAX`/`roundToOre`/`sumOre`/`formatOreAsKronor`/`OreResult`/`MoneyErrorCode`); src/lib/money/vat.ts (`isVatRateBp`/`VAT_RATE_BP_MIN`/`VAT_RATE_BP_MAX`, `VAT_BP_PER_UNIT` pattern, `buildVatAssumptionSnapshot`/`VatAssumptionBuildOptions` — the direct model for the tax-assumption builder, its own `VatDisplayMode` copy pattern); src/lib/money/index.ts (barrel — add tax exports); src/lib/snapshots/build.ts + types.ts (`buildSnapshotSource`/`assertNever`/`SnapshotBuildOptions` — freeze discipline + exhaustiveness); src/server/commands/crm/validation.ts (`CUSTOMER_TYPES`/`CustomerType` — the eligibility-posture reference, do NOT import into `src/lib`); src/lib/result/result.ts (`Result`/`ok`/`err`); tests/unit/lib/money/vat.test.ts + vat.golden.test.ts + rounding.golden.test.ts + tests/unit/lib/snapshots/golden.test.ts (node --test + golden conventions); tests/fixtures/golden/money/vat-rates.json (fixture JSON + `origin` + PII-scan + no-hidden-literal shape); scripts/run-tests.mjs + tests/support/alias-hook.mjs (the unit runner + `@/lib/money` bare-directory alias hook)]

## Dev Agent Record

### Agent Model Used

Opus 4.8 (claude-opus-4-8[1m])

### Debug Log References

- Full unit suite green after implementation: `node --test tests/unit/**/*.test.ts` → 572 pass / 0 fail (was 545 before; +27 for the 4.3 tax units + golden — the pre-existing `describe.skip` ATDD gate flipped true once the tax surface landed).
- Tax subset: `tax.test.ts` (18) + `tax.golden.test.ts` (9) → 27 pass / 0 fail.
- Full CI verify-gate sequence run locally IN ORDER: `verify:lockfiles` ✅ → `verify:service-role-containment` ✅ → `tsc --noEmit` ✅ (exit 0) → `eslint` ✅ (0 errors; 1 pre-existing unused-var warning in `vat.test.ts`, not touched) → `test:unit` ✅ (572/0) → `next build` ✅ → `verify:bundle-containment` ✅.

### Completion Notes List

- Implemented `src/lib/money/tax.ts` — the pure ROT / grön-teknik ESTIMATE ENGINE: named UNAPPROVED profiles (`ROT_PROFILE_UNAPPROVED` 3000 bp / 5 000 000 öre cap; `GRON_TEKNIK_PROFILE_UNAPPROVED` 2000 bp / 5 000 000 öre cap — both `approved: false`, `signOffStatus: "pending-owner-accounting-legal"`, `Object.freeze`d `as const` DATA), the `estimateDeduction` engine, and the frozen `buildTaxAssumptionSnapshot` builder. Re-exported from `src/lib/money/index.ts`; NO second money root.
- **KEY FINDING — the ATDD scaffold + golden fixture were ALREADY present** (contrary to the Dev Notes claim that "NO ATDD red-phase scaffold exists for 4.3"). `tests/unit/lib/money/tax.test.ts`, `tests/unit/lib/money/tax.golden.test.ts`, and `tests/fixtures/golden/money/rot-gron-deductions.json` were committed and gated behind `TAX_SURFACE_PRESENT` via `describe.skip`. They ARE the binding contract; I wrote the engine to satisfy them and the gate flipped true automatically — no test edits needed. The engine's profile numbers match the fixture's placeholder numbers exactly (ROT 3000 bp, grön-teknik 2000 bp, both cap 5 000 000 öre), so no fixture change was required.
- deductionOre = `min( roundToOre(eligibleBasisOre * deductionPercentBp / 10000), capOre )` — rate flows in as basis points from the profile via the ONLY rate-related literal `BP_PER_UNIT = 10000` (mirrors `VAT_BP_PER_UNIT`); NO `0.30`/`30`/`0.50`/`50`/`1.3` percent literal (golden source-grep passes). Reuses the SINGLE Story 4.1 `roundToOre`/`sumOre`/`isOreAmount` and the Story 4.2 `isVatRateBp` (no forked money/bp rule). Multi-line basis (hidden rows) summed via `sumOre` (sum-of-rounded), R-408.
- R-405 (no-approval): every estimate returns `requiresSignOff: true` + an `UNAPPROVED_PROFILE` warning by DEFAULT; the assumption snapshot carries `approved: false` + `requiresSignOff: true`. NO code path derives/renders/persists `isApproved`/`approved: true` (behavioral unit + golden greps confirm).
- R-406 (mix block): `isRotGronMix` detects both a `deductionTypes: ["rot","gron_teknik"]` array and a primary `deductionType` + co-present `rot`/`gronTeknik` sub-request; returns the BLOCKING `ROT_GRON_MIX_NOT_ALLOWED` typed failure FIRST (before any profile/basis resolution). NO combined-sum path exists.
- R-407 (caps): at-cap = cap; above-cap clamped to cap + a `DEDUCTION_CLAMPED_TO_CAP` warning; below-cap = rate applied; zero basis → 0 (valid, not a rejection).
- R-412 (no PII): the engine takes a resolved `posture` enum (`private` eligible; `company`/`brf`/`public` → an ok result carrying a `POSTURE_NOT_ELIGIBLE` warning, the conservative Phase-A policy). NO personnummer/orgnr/PII field is read (source-guard test passes); the tax-assumption snapshot takes an INJECTED `capturedAt` (no `Date.now()`).
- R-409 (freeze): `buildTaxAssumptionSnapshot` copies profile + basis + warnings BY VALUE into a fresh `Object.freeze`d object (warnings deep-frozen too), holds NO live reference — mutating the source after capture cannot reach a prior snapshot; captures STATE, does no arithmetic, derives no `isApproved`. Modelled directly on `buildVatAssumptionSnapshot`.
- R-404 (unknown type / DX): `profileFor` is a closed `switch` returning `null` for an unknown type; `estimateDeduction` maps that to the clear `UNKNOWN_DEDUCTION_TYPE` typed failure. Added `ROT_GRON_MIX_NOT_ALLOWED` + `UNKNOWN_DEDUCTION_TYPE` ADDITIVELY to `MoneyErrorCode` in `ore.ts` (no union restructure).
- Scope guardrails all confirmed: NO migration/table/column, NO new dependency (`package.json` untouched), NO `.env` edit, `nav-items.ts` unchanged (7 in-scope modules), NO calculation rows (Epic 5), NO quote-version persistence (Epic 6), exactly ONE `isOreAmount`/bp-validity/öre→kronor authority (all reused), NO hidden percent literal, NO combined ROT+grön sum, NO PII in `src/lib/money`, NO approved-render path. Source-tree diff is exactly `ore.ts` + `index.ts` (modified) + `tax.ts` (new).

### Security / RLS PR statement

None directly — pure `src/lib/money` primitive: pure math over öre + basis-point + cap numbers, NO DB access, NO tenant-data path, NO PII (eligibility is a resolved posture), NO clock read (injected `capturedAt`). No new endpoint, service-role usage, RLS surface, or migration. Service-role-containment + bundle-containment guards pass unchanged. ROT/grön rates/caps/schablon/eligibility/disclaimer are CONSERVATIVE UNAPPROVED profiles + warnings pending owner/accounting/legal sign-off — no tax output rendered/persisted as approved.

### Owner sign-off questions surfaced (for the epic gate — HUMAN decisions, not plan gaps)

- **Sign-Off Q3 (ROT):** per-person cap / rate % / labour-only basis / multi-owner handling — placeholder is 30.00% (3000 bp), 50 000 kr (5 000 000 öre) per-person cap. UNAPPROVED.
- **Sign-Off Q4 (grön teknik):** category rates / caps / schablon on-off / mix — placeholder is 20.00% (2000 bp), 50 000 kr (5 000 000 öre) per-category cap, schablon NOT modelled. UNAPPROVED.
- **Sign-Off Q5 (eligibility + disclaimer):** only `private` eligible; BRF/company/public flagged with a warning; the customer-facing disclaimer wording is NOT legally-approved.
- **Sign-Off Q6 (personnummer):** reconcile "not captured by default" (4.3 / test-design) with the 2026-06-18 CRM decision (store personnummer access-controlled for `private`). Resolved in code by keeping the pure engine PII-free (takes a resolved posture); the stale test-design "no personnummer" wording is a doc-only divergence — NOT reopened.
- **Sign-Off Q7 (approval posture):** ALL ROT/grön output is "unapproved assumption + warning"; none is production-approved.
- **Two standing NFR concerns (epic-wide, owner-pending):** no `pnpm audit` dependency-scan CI gate; no coverage reporter. Flagged for the epic gate schedule-or-accept; NOT this story's to fix.

### File List

- `src/lib/money/tax.ts` (new) — the ROT/grön UNAPPROVED profiles + `estimateDeduction` engine + `buildTaxAssumptionSnapshot` builder. (Epic-4 review-fix update: `estimateDeduction`'s profile-rate guard now returns the tax-scoped `INVALID_DEDUCTION_RATE_BP` (not the VAT-named code), keeping the reused `isVatRateBp` predicate; `buildTaxAssumptionSnapshot` now guards `capturedAt` — empty/non-string → typed `INVALID_CAPTURED_AT` via the new `TaxAssumptionResult` union — and `estimateDeduction` propagates that failure.)
- `src/lib/money/ore.ts` (modified) — added `ROT_GRON_MIX_NOT_ALLOWED` + `UNKNOWN_DEDUCTION_TYPE` additively to `MoneyErrorCode`. (Epic-4 review-fix update: also added `INVALID_DEDUCTION_RATE_BP` + `INVALID_CAPTURED_AT`.)
- `src/lib/money/index.ts` (modified) — added the tax barrel re-exports (values + types). (Epic-4 review-fix update: also re-export the new `TaxAssumptionResult` type.)

Test/fixture files were ALREADY present (committed ATDD scaffold, gated by `describe.skip` until the tax surface landed) and required no edits — the engine flips the gate true and satisfies the pinned contracts:
- `tests/unit/lib/money/tax.test.ts` (pre-existing) — 4.3-UNIT-01..07. MODIFIED (epic-4 review fix iter-2): rewrote the 4.3-UNIT-06 "mutating the SOURCE profile AFTER capture" case to actually mutate-then-compare (dropped the dead `build(...) &&` truthiness expression and asserted the prior frozen `snap.deductionPercentBp`/`snap.capOre` are unchanged after the source mutated), so it now fails if snapshot immutability regresses.
- `tests/unit/lib/money/tax.golden.test.ts` (pre-existing) — 4.3-GOLDEN-01/02. MODIFIED (epic-4 review fix, comment-only): corrected the no-hidden-literal test title + comment to state it scans only the percent-FRACTION forms (0.30/0.50/1.3), not bare integers `30`/`50` (structural `/ 10000` denominator check still asserted). No assertion change.
- `tests/unit/lib/money/tax.edges.test.ts` (pre-existing, added in `a1e8bde`) — MODIFIED (epic-4 review fix): narrowed the now-union `buildTaxAssumptionSnapshot` return via an `okSnap` helper and added an invalid-`capturedAt` → `INVALID_CAPTURED_AT` assertion.
- `tests/fixtures/golden/money/rot-gron-deductions.json` (pre-existing) — anonymized ROT/grön fixtures with `origin` labels; numbers match the engine profiles.

### Change Log

| Date | Version | Description |
| --- | --- | --- |
| 2026-07-02 | 1.0 | Implemented Story 4.3: pure ROT/grön-teknik estimate engine (`src/lib/money/tax.ts`) — named UNAPPROVED profiles, `estimateDeduction` (deduction + eligible basis + warnings + `requiresSignOff` + ROT×grön mix-block + cap-clamp + posture eligibility), frozen `buildTaxAssumptionSnapshot`; added `ROT_GRON_MIX_NOT_ALLOWED`/`UNKNOWN_DEDUCTION_TYPE` to `MoneyErrorCode`; barrel re-exports. Pre-existing ATDD scaffold + golden fixture satisfied unchanged (27 tax tests green). Full CI verify-gate sequence green. Status → review. |
| 2026-07-02 | 1.1 | Epic-4 integration review-fix pass (epic-4-review-findings.md): `estimateDeduction`'s profile-rate guard returns the tax-scoped `INVALID_DEDUCTION_RATE_BP` instead of leaking the VAT-named `INVALID_VAT_RATE_BP` (predicate `isVatRateBp` reused) (`[Review][Decision][Med]`); `buildTaxAssumptionSnapshot` guards `capturedAt` (empty/non-string → `INVALID_CAPTURED_AT`) via the new `TaxAssumptionResult` union, propagated by `estimateDeduction` (`[Review][Decision][Low]`); `tax.golden.test.ts` no-hidden-literal comment/title corrected to the fraction-forms-only wording (`[Review][Patch][Low]`). `tax.edges.test.ts` updated to narrow the union return + assert the new capturedAt failure. Typecheck clean; lint 0 errors; full unit suite 613 pass / 0 fail. |
| 2026-07-02 | 1.2 | Epic-4 review-fix iteration 2 (`[Review][Patch][Low]`): fixed the dead/tautological assertion in the 4.3-UNIT-06 source-mutation-after-capture test in `tax.test.ts` — now mutates the source AFTER capture and asserts the prior frozen snapshot fields are unchanged, so it genuinely verifies the freeze-vs-later-mutation contract. Typecheck clean; the two affected unit files green. |

### Review Findings

_Triage of the code review (2026-07-02) — reviewer `primary` (Tier-A: Acceptance Auditor only) + a dedicated security review (no findings). Blind/Edge lenses deliberately not run in Tier-A mode. 5 auditor findings triaged: 2 Decision, 0 Patch, 0 Defer, 3 dismissed as noise._

- [x] [Review][Defer][Med] `persons` input is captured but NOT applied to the per-person ROT cap — AC1's per-person cap is only structurally (not behaviorally) implemented. In `src/lib/money/tax.ts` the deduction is `min(rateApplied, profile.capOre)` with a single flat `capOre`; `persons` is validated + snapshotted but never scales the cap, so a 3-person ROT job gets the same 5 000 000 öre ceiling as a 1-person job, and no warning notes the per-person multiplier is unmodelled. The auditor and the story both treat this as a defensible conservative placeholder: the exact multi-owner/per-person arithmetic is owner-gated under Sign-Off Q3 (already surfaced) and Task 1.2 sanctions encoding a conservative placeholder marked UNAPPROVED. Recommended: defer: the per-person multiplier is owner-gated under Sign-Off Q3 (already surfaced for the epic gate) and the story explicitly permits a flat conservative placeholder + UNAPPROVED warning — log for the Q3 working session, do not invent multi-owner arithmetic now. _Auto-resolved (epic mode): deferred per triage recommendation — logged to deferred-work.md for the Sign-Off Q3 working session._
- [x] [Review][Decision][Low] `buildTaxAssumptionSnapshot` does not guard `deductionPercentBp` with `isVatRateBp`, diverging from the `buildVatAssumptionSnapshot` model it was told to mirror (which returns a typed-failure union guarding its source rate) — a caller invoking the standalone builder directly could freeze an invalid/float rate into an assumption. The engine path (`estimateDeduction`) DOES validate `isVatRateBp(profile.deductionPercentBp)` before any computation, so no computed deduction can carry a bad rate; only a direct standalone-builder misuse is affected, which the engine never does. Task 3.2 explicitly scopes the builder to "captures STATE, computes nothing" — a state-capture contract, not a validation contract. Recommended: dismiss: the divergence is intentional — the engine already guards the rate before computation and Task 3.2 scopes the builder to pure by-value state capture (no arithmetic, no validation); mirroring VAT's validating-builder arm would contradict the story's own builder contract. _Auto-resolved (epic mode): dismissed as won't-fix per triage recommendation._

_Dismissed (noise, dropped): (3) `posture` typed `string` not the `EligibilityPosture` enum — behavior is conservative/safe (any non-`private` value yields the identical `POSTURE_NOT_ELIGIBLE` warning + not-eligible outcome); type-hygiene preference, no wrong behavior. (4) `profileFor` returns `null` in default rather than `assertNever` — the runtime `UNKNOWN_DEDUCTION_TYPE` path is enforced + tested (4.3-UNIT-07); the widened `string` param is REQUIRED for that runtime path, so the task-text `assertNever` compile claim is an over-promise, not a defect. (5) File-List provenance / `new file mode` discrepancy — resolved by git history: the scaffold files were pre-existing (commit `d481f10`), so the "already present" narrative is accurate and `new file mode` is a diff-baseline artifact; only residue is `tax.edges.test.ts` (added in `a1e8bde`) missing from the File List — a cosmetic doc-completeness nit, no code impact._
