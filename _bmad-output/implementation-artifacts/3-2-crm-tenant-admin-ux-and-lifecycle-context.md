# Story 3.2: CRM Tenant-Admin UX And Lifecycle Context

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a tenant admin,
I want a focused CRM interface for customers, facilities, and contacts,
so that I can find or create the right context before building a calculation.

## Acceptance Criteria

1. **Given** the CRM customer list at `/customers`, **when** the admin searches or filters, **then** customers can be found by the approved Phase A fields (display name, email, phone, city, and the type-driven identifier — org_nr — where allowed) **and** filter by customer type works, **and** the list clearly renders distinct **empty** (no customers yet), **loading**, **failed-to-load**, **no-results** (search returned nothing), and **duplicate-like warning** states. The list projection MUST NOT include or display `personnummer` (it is access-controlled; private-data posture). [Source: epics.md#Story 3.2 AC1; ux-design-specification.md#3 (Customer list: search by name/email/phone/city/org number; filter by type), #9 (CRM states table); owner-decisions-applied-2026-06-18.md#Story 3.2 (search per approved fields; not shown in list); architecture.md#4 (`/customers` Kunder)]
2. **Given** a customer detail screen at `/customers/[customerId]`, **when** the admin views it, **then** the record shows the customer's facilities (Anläggningar), contacts (Kontakter), and ONLY the Phase A-owned related areas that are backed by completed stories (e.g. files/calculations/quotes/jobs/event-history regions appear only as honest "not yet built" affordances where their owning story does not exist yet) — **and** NO deferred-module section, nav label, route, or placeholder is shown anywhere. The detail screen MAY show `personnummer` for a `private` customer in a restrained/masked form (it is not in the list, but the detail is the access-controlled surface where it is intentionally surfaced). [Source: epics.md#Story 3.2 AC2; ux-design-specification.md#3 (Customer detail hub), #11 (Explicit non-goals — no deferred-module UI/placeholders); owner-decisions-applied-2026-06-18.md#Story 3.2 (personnummer present for private with restrained/masked display); architecture.md#4]
3. **Given** facility/contact create+edit dialogs (or detail views), **when** they open and close, **then** focus moves predictably (focus into the dialog on open; focus returns to the invoking control on close — the established `AppShell` drawer pattern), **and** validation errors are programmatically associated with their fields (`aria-describedby` → the error node, `aria-invalid`, an `aria-live` error summary), with blocking errors separated from warnings, plain-language messages, no color-only signalling, and unsaved form data preserved on a validation failure. [Source: epics.md#Story 3.2 AC3; ux-design-specification.md#9 (validation next to field + summary; separate blocking vs warning; no color-only; preserve unsaved data), #10 (focus moves predictably on open/close; programmatic error association; icon-only controls have accessible names); src/components/app-shell/AppShell.tsx (focus-trap/return-focus pattern to mirror)]
4. **Given** every CRM mutation in the UI (create/update/archive customer/facility/contact), **when** the admin submits, **then** the UI invokes the EXISTING Story 3.1 server commands through the established command envelope (`createCustomer`/`updateCustomer`/`archiveCustomer`, `createFacility`/`updateFacility`/`archiveFacility`, `createContact`/`updateContact`/`archiveContact`) via a server action or route handler — it NEVER writes to Supabase directly from the client, never uses a service-role key, and never bypasses the envelope. The UI is NOT the security boundary; tenant isolation + validation are enforced server-side by RLS + the 3.1 commands. The typed `Result` error codes (`VALIDATION_FAILED`, `TENANT_ACCESS_DENIED`, `UNAUTHENTICATED`, `TENANT_MEMBERSHIP_REQUIRED`, `SERVER_ERROR`) are surfaced as the documented UI states; a transient `SERVER_ERROR` is presented as a retryable failure, NOT as a permanent access denial. [Source: epics.md#Story 3.2 Security/RLS Impact (UI cannot be the security boundary); architecture.md#3 ADR-A003 (server command authority), #5 (command surface); project-context.md#Architecture Rules (prefer the envelope; no service-role on client paths); src/server/commands/crm/*.ts; epic-3 retro-note (server-side RLS already built in 3.1 — UI must call the envelope)]
5. **Given** an unauthenticated or no-membership caller, **when** they request any `/customers` route, **then** route authorization holds via the EXISTING `(app)` server layout boundary (`UNAUTHENTICATED → redirect("/login")`; no active `tenant_admin` membership → `NoTenantAccess`). Every new `/customers` route segment that reads per-request auth/data sets `export const dynamic = "force-dynamic"`. The CRM UI adds NO new auth mechanism and does NOT re-implement the boundary. [Source: epics.md#Story 3.2 Test Requirements (route authorization); src/app/(app)/layout.tsx (the protected boundary); project-context.md#Framework-Specific Rules (force-dynamic on protected routes; auth decided server-side)]
6. **Given** the seven-item shell nav and the deferred-scope guardrails, **when** the CRM UI lands, **then** `src/components/app-shell/nav-items.ts` STILL contains EXACTLY the seven IN-scope modules (no new nav item, no "Pilotstöd/Migrering", no deferred-module label), the Swedish domain labels `Kunder`/`Anläggningar`/`Kontakter` are used for the CRM surfaces, and no deferred module (Fortnox, field-worker, supplier, AI, HR, rentals, assets/QR, DoU, tender/FKU, full RBAC, customer portal, broad CRM analytics) appears as a section, route, or placeholder. [Source: epics.md#Story 3.2 Technical Notes (Swedish labels) + Stop Conditions (no broad CRM analytics); ux-design-specification.md#1/#11; project-context.md#Framework-Specific Rules (nav is exactly seven; never add a deferred module); src/components/app-shell/nav-items.ts]
7. **Given** the test gates, **when** CI runs, **then** the CRM UI is covered by Playwright E2E specs under `tests/e2e/**` (the browser runner + two-tenant fixture + `e2e` CI job now EXIST) asserting: search/filter list states, keyboard navigation + predictable dialog focus, validation states with programmatic association, ABSENCE of deferred navigation labels/routes, and route authorization (anon/no-membership). Any pure presentational helper (e.g. customer-type badge mapping, a list-row view-model builder) MAY also be a `node --test` unit. No new tenant-owned table or migration is added (this is a UI-only story), so the H4 inventory gate is unaffected. [Source: epics.md#Story 3.2 Test Requirements (UI tests for search/filter, keyboard nav, validation states, no deferred labels, route authorization); playwright.config.ts + tests/e2e/** + .github/workflows/ci.yml (`e2e` job); deferred-work.md (e2e gating RESOLVED — runner now exists)]

## Tasks / Subtasks

- [ ] Task 1: Customer list page — `/customers` with search, filter, and all required states (AC: 1, 5, 6)
  - [ ] 1.1 Replace the `PagePlaceholder` in `src/app/(app)/customers/page.tsx` with a real Customer list. Set `export const dynamic = "force-dynamic"` on the page (it reads per-request auth/data). Heading is `Kunder` (the page `<h1>`; the top-bar already shows the same label from `nav-items.ts`). Primary action: `Ny kund` (mount it in the existing `data-slot="primary-action"` region or a page-level header — do NOT add a nav item). [Source: src/app/(app)/customers/page.tsx (current placeholder); ux-design-specification.md#3 (primary action Ny kund); project-context.md#Framework-Specific Rules (force-dynamic); architecture.md#4]
  - [ ] 1.2 Read the customer list SERVER-SIDE on the page using the per-request cookie-bound RLS client (`createSupabaseServerClient()` — anon key only, NEVER service-role), selecting ONLY the list projection: `id, customer_type, display_name, org_nr, email, phone, city, archived_at, created_at`. EXCLUDE `personnummer` from the projection entirely (it must never reach the list payload). Default the list to ACTIVE rows (`archived_at is null`); offer an explicit "show archived" affordance only if trivial (otherwise defer). RLS already scopes rows to the caller's tenant — the page does not pass a tenant id. [Source: owner-decisions-applied-2026-06-18.md#Story 3.2 (personnummer NOT in list); epic-3 retro-note (personnummer must NOT appear in the default list projection); src/server/db/supabase-server-client.ts; supabase/migrations/20260630120000_crm_data_model.sql (columns); architecture.md#6 (RLS scopes reads)]
  - [ ] 1.3 Search + filter: search by `display_name`, `email`, `phone`, `city`, and `org_nr` (the approved fields; personnummer is NOT searchable from the list). Filter by `customer_type` (the four owner-approved values, with a Swedish type badge: Privatperson / Företag / BRF / Offentlig). Decide search execution: a server-side filtered query (preferred — keeps the projection lean) driven by a URL search param so list state survives a detail round-trip (UX §2 "preserve filters/search on return"); a small client-side filter over the already-fetched page is acceptable for the pilot's small data set if simpler. Document the choice. [Source: ux-design-specification.md#3 (search fields), #2 (preserve list state); owner-decisions-applied-2026-06-18.md#Story 3.2 (type badge; search per approved fields); validation.ts (CUSTOMER_TYPES)]
  - [ ] 1.4 Implement ALL five required list states as DISTINCT, clearly-labeled UI (not one generic blank): **empty** (no customers exist — invite to create the first), **loading** (skeleton/spinner while the server fetch resolves — relevant if any client-side fetch/transition is used), **failed** (the read errored — generic Swedish message + retry, never a leaked stack/SQL), **no-results** (search/filter matched nothing — distinct from empty), and a **duplicate-like warning** (surface when a create/typed name closely matches an existing active customer — a non-blocking advisory, NOT a hard block; keep the heuristic simple, e.g. case-insensitive display_name match within the tenant). Do not rely on color alone for any state; pair with icon/text. [Source: epics.md#Story 3.2 AC1 (empty/loading/failed/no-results/duplicate-like states clear); ux-design-specification.md#9 (CRM states: no customers / no search results / duplicate-like warning / invalid formats / missing display name), #10 (no color-only)]
  - [ ] 1.5 Each list row links to `/customers/[customerId]`. Show the type badge and lifecycle/active state. Keep the table keyboard-navigable and horizontally scroll-friendly on narrow widths (UX §2: tables support horizontal scroll or compact cards). Provide a row-level archive affordance only if it is clean to wire to `archiveCustomer` (Task 4); otherwise archive lives on the detail screen. [Source: ux-design-specification.md#2 (responsive tables), #3 (customer list); architecture.md#4 (`/customers/[customerId]` route)]
- [ ] Task 2: Customer detail page — `/customers/[customerId]` hub (AC: 2, 4, 5, 6)
  - [ ] 2.1 Add `src/app/(app)/customers/[customerId]/page.tsx` with `export const dynamic = "force-dynamic"`. Server-fetch the single customer by id via the RLS client (a foreign/other-tenant id is invisible under RLS → zero rows → render a generic "not found / no access" state, NEVER leaking whether the row exists in another tenant). Heading: the customer `display_name` with the type badge (Kund hub). [Source: architecture.md#4 (`/customers/[customerId]` Kund hub), #6 (RLS invisibility); ux-design-specification.md#9 (generic access message, no cross-tenant detail leak); project-context.md#Framework-Specific Rules (force-dynamic)]
  - [ ] 2.2 Detail content: a customer summary (identity/address/contact fields), the `personnummer` for a `private` customer shown in a RESTRAINED/MASKED form (e.g. masked except last digits, or behind an explicit "visa" affordance) — this is the access-controlled surface where personnummer IS intentionally shown (it is NOT in the list). For non-`private` types show `org_nr`. NEVER render personnummer for a non-`private` type (the DB CHECK guarantees it is null there anyway). [Source: owner-decisions-applied-2026-06-18.md#Story 3.2 (personnummer present for private with restrained/masked display); epic-3 retro-note (personnummer access-controlled, not in list); supabase/migrations/20260630120000_crm_data_model.sql (customers_identifier_by_type CHECK)]
  - [ ] 2.3 Facilities (Anläggningar) sub-section: list the customer's active facilities (server-fetch via RLS), with add/edit/archive affordances wired to the facility commands (Task 4) and the dialog pattern (Task 3). A customer may have ZERO facilities (optional, many-per-customer) — render the empty state honestly, do NOT force a facility. [Source: owner-decisions-applied-2026-06-18.md#Story 3.1/3.2 (facilities optional, many; add/manage from customer view); ux-design-specification.md#3 (facility detail/modal), #9; architecture.md#7]
  - [ ] 2.4 Contacts (Kontakter) sub-section: list the customer's active contacts (customer-level AND facility-level — a contact may optionally bind a facility), with add/edit/archive affordances. Show an optional `is_primary` marker if a contact has it, but DO NOT force a primary contact and do not add an "exactly one primary" UI rule. Do NOT enforce the "facility + contact required" rule here — that rule lives at the quote/job command layer (Epics 5-7), not in CRM. [Source: owner-decisions-applied-2026-06-18.md#Story 3.1/3.2 (contacts at customer+facility level, multiple, no enforced primary; the facility+contact-required rule belongs at quote/job creation, NOT CRM); ux-design-specification.md#3 (contact detail/modal)]
  - [ ] 2.5 Related-areas regions (calculations / quotes / jobs / files / event-history): show ONLY honest "not yet built (Epic N)"-style affordances for the areas whose owning story does not exist yet — mirror the `PagePlaceholder` honesty (an area reads as "not built", not as a dormant deferred module). Surface a real area ONLY where a completed story backs it. NO deferred-module region anywhere. [Source: epics.md#Story 3.2 AC2 (visible only where supported by completed stories; no deferred modules); ux-design-specification.md#3 (customer detail shows facilities/contacts/calculations/quotes/jobs/files/events), #11 (no placeholder screens for deferred modules); src/components/app-shell/PagePlaceholder.tsx (honest-empty pattern)]
- [ ] Task 3: Facility/contact create+edit dialogs with predictable focus + programmatic validation (AC: 3, 4)
  - [ ] 3.1 Build the create/edit forms for customer, facility, and contact as accessible dialogs (or, where a full page is cleaner, a route — architecture §4 allows "modal is acceptable for simple edits"). Follow the EXISTING `AppShell` drawer/dialog a11y pattern: `role="dialog"` + `aria-modal="true"` + `aria-label`, move focus INTO the dialog on open, trap Tab within it, close on Escape/overlay/close-button, and RETURN focus to the invoking control on close. Reuse the focus-trap shape from `AppShell.tsx` rather than inventing a new one (or extract a small shared `Dialog` component if that reads cleaner — keep it minimal). [Source: epics.md#Story 3.2 AC3 (focus moves predictably on open/close); ux-design-specification.md#10 (focus moves predictably; icon-only controls have accessible names); src/components/app-shell/AppShell.tsx:159-192 (onDrawerKeyDown focus trap; closeDrawer returns focus)]
  - [ ] 3.2 Field-level validation + programmatic association: each input has a `<label htmlFor>`; on a validation failure set `aria-invalid="true"` and `aria-describedby` pointing at the field's error node; render a top-of-form blocking-error summary in an `aria-live="polite"` region; SEPARATE blocking errors from non-blocking warnings (duplicate-like is a warning, not a blocker); use plain Swedish; PRESERVE the entered values on failure (do not clear the form). Use the React 19 `useActionState` + server-action pattern (the modern Next 16 form pattern) so the server `Result` maps cleanly to field/summary errors. [Source: epics.md#Story 3.2 AC3 (validation errors programmatically associated with fields); ux-design-specification.md#9 (errors next to field + summary; separate blocking/warning; preserve unsaved data; no color-only), #10 (programmatic error association); React 19 useActionState + Next forms guide (https://nextjs.org/docs/app/guides/forms)]
  - [ ] 3.3 Mirror the 3.1 command input contracts EXACTLY in the form fields so the client never sends a shape the validator rejects for a trivial reason: customer create requires `customer_type` (one of the 4) + `display_name`, and the type-driven identifier (`private` ⇒ personnummer present & org_nr absent; non-`private` ⇒ org_nr present & personnummer absent) — drive the identifier field from the selected type. `updateCustomer` does NOT support switching `customer_type` (the validator edits within-type fields only) — do not offer a type switch on edit. Facility create requires `customer_id` + `name`. Contact create requires `customer_id` + `name`, optional `facility_id`/`email`/`phone`/`role_label`/`is_primary`. Email/phone are validated only when present (optional). [Source: src/server/commands/crm/validation.ts (the exact create/update shapes + identifier-by-type + optional email/phone + no type-switch on update); owner-decisions-applied-2026-06-18.md#Story 3.1 (4 types; identifier by type)]
- [ ] Task 4: Wire CRM mutations to the Story 3.1 envelope commands via server actions (AC: 4, 5)
  - [ ] 4.1 Add server actions (or App Router route handlers) under the CRM feature that import the 3.1 commands and invoke them with `runCommand(command, { client, input })`, where `client` is the per-request `createSupabaseServerClient()` (anon key, cookie-bound RLS client). The action passes the form values as the raw `input` — the command's `validateInput` is the authority; the UI form validation is a UX nicety, NOT the security/validation boundary. NEVER instantiate a service-role client; NEVER `.insert()/.update()` a CRM table directly from a page/action (only the commands do). Place server actions in a `"use server"` module (e.g. `src/features/crm/actions.ts` or co-located `actions.ts`), NOT inline in a `"use client"` component. [Source: architecture.md#5 (route handlers/server actions delegate to command functions ONLY), #3 ADR-A003; src/server/commands/envelope.ts (runCommand signature); src/server/commands/crm/*.ts (the 9 commands); project-context.md#Critical Don't-Miss Rules (service-role server-only; no client-path writes)]
  - [ ] 4.2 Map the typed `Result` to UI: `ok` → close dialog, refresh the list/detail (`revalidatePath`/`router.refresh`), show a success affordance; `VALIDATION_FAILED` → field/summary errors (re-display, preserve input); `TENANT_ACCESS_DENIED` → generic "no access" (never reveal another tenant's data); `SERVER_ERROR` → retryable failure message (NOT a permanent denial); `UNAUTHENTICATED`/`TENANT_MEMBERSHIP_REQUIRED` → the boundary already redirects/blocks, but handle defensively. Use the command's user-safe Swedish `message` where surfaced; never echo raw input or internal detail. [Source: src/server/commands/command-errors.ts (COMMAND_MESSAGES + code semantics: SERVER_ERROR is transient/retryable, not a denial); project-context.md#Language-Specific Rules (Result codes; transient ≠ no-access; generic Swedish messages); ux-design-specification.md#9 (lifecycle/security error copy)]
  - [ ] 4.3 Archive is soft-delete via the archive commands (sets `archived_at`); the UI offers "Arkivera", NOT "Radera/Delete" (no hard delete exists or is granted). Confirm a destructive-ish archive where it removes the row from the default active list. Re-fetch / revalidate after archive so the row leaves the active list. [Source: src/server/commands/crm/customers.ts (archiveCustomer soft-delete); architecture.md#6 (prefer archive over delete); supabase/migrations/20260630120000_crm_data_model.sql (no DELETE policy/grant — archive only)]
- [ ] Task 5: Playwright E2E coverage + scope/guardrail sweep + finalize (AC: 6, 7)
  - [ ] 5.1 Add Playwright E2E spec(s) under `tests/e2e/crm/` (matching `**/*.e2e.spec.ts`) that sign in as `adminA` (reuse the existing two-tenant fixture from `tests/e2e/global-setup.ts` + the `waitForHydrated`/`signIn` helpers — extract shared helpers if duplicating). Cover: (a) `/customers` renders the list + the empty/no-results states; search/filter narrows the list; (b) creating a customer via the dialog persists and appears in the list (round-trips the 3.1 `createCustomer`); (c) a validation failure (e.g. missing display_name, or wrong identifier-for-type) shows a field-associated error and preserves input; (d) dialog open moves focus in and Escape/close returns focus to the trigger; (e) the shell shows exactly the seven nav labels and NONE of the deferred labels (assert absence of "Pilotstöd"/"Migrering"/"Fortnox"/etc.); (f) route authorization: an anonymous visit to `/customers` and `/customers/<id>` redirects to `/login`. [Source: epics.md#Story 3.2 Test Requirements; tests/e2e/auth/login-and-tenant-context.e2e.spec.ts (signIn/waitForHydrated patterns to reuse); playwright.config.ts (testMatch); deferred-work.md (e2e runner RESOLVED)]
  - [ ] 5.2 Personnummer non-exposure assertion: add an E2E (or a server-side projection unit) check that the `/customers` LIST never contains a personnummer value (the projection excludes it) — make the private-data posture a tested contract, not just a code comment. The detail screen MAY show it masked for a `private` customer. [Source: epic-3 retro-note (personnummer NOT in default list projection / list UI); owner-decisions-applied-2026-06-18.md#Story 3.2 (restrained/masked on detail only)]
  - [ ] 5.3 Run the FULL active CI gate sequence locally IN ORDER (establish a clean baseline first): `pnpm install --frozen-lockfile` → `pnpm run verify:lockfiles` → `pnpm run verify:service-role-containment` → `pnpm typecheck` → `pnpm lint` → `pnpm run test:unit` → `pnpm build` → `pnpm run verify:bundle-containment` → (`supabase start && supabase db reset`) → `pnpm run test:int` → `pnpm run test:e2e`. This story adds product UI + E2E (no migration), so the `build` + `e2e` gates are the load-bearing new coverage; the bundle-containment grep MUST stay green (no service-role token reaches `.next`). NEVER chain `pnpm run X && pnpm run Y` in a package.json field (Windows PATH). [Source: project-context.md#Development Workflow Rules (CI gate sequence); .github/workflows/ci.yml (verify + db + e2e jobs); project-context.md#Language-Specific Rules (no chained pnpm)]
  - [ ] 5.4 Scope guardrail sweep (Stop Conditions): `nav-items.ts` stays EXACTLY seven; NO deferred-module nav item/route/placeholder/section; NO broad CRM-analytics screen; NO final customer-type/primary-contact POLICY beyond the conservative owner-decided assumptions (4 types, no enforced primary, facility/contact-required deferred to quote/job) — if the UI would need a stricter rule, STOP and surface it; NO new dependency (Playwright/Next/React/Tailwind already present — adding any is approval-gated + exact-pinned); NO `.env` edit; NO new migration or schema change (UI-only over the 3.1 data model). [Source: epics.md#Story 3.2 Stop Conditions + #Epic 3 non-scope; project-context.md#Framework-Specific Rules (exactly seven; never a deferred module) + #Development Workflow Rules (gated deps); ux-design-specification.md#11]
  - [ ] 5.5 PR statements + deferred-work reconciliation: Security/RLS impact — "UI is NOT the security boundary; all CRM mutations go through the 3.1 envelope commands on the RLS client; no service-role; personnummer excluded from the list projection, masked on detail; route auth via the existing `(app)` boundary." Record any NEW deferral discovered (e.g. exact mandatory-PDF-field set still owner-pending; archived-customer browsing UX if deferred) WITH an owner story. Do NOT reopen unrelated ledger items. [Source: project-context.md#Development Workflow Rules (PR requirements; disclose accepted deferrals in the PR); deferred-work.md; owner-decisions-applied-2026-06-18.md#Still open for Epic 3]

## Dev Notes

### Critical Constraints (read first)

- **This is a UI-ONLY story over the data model + commands built in Story 3.1. Do NOT add a migration, a new table, a new server command, or a new dependency.** It builds the `/customers` list + `/customers/[customerId]` detail + facility/contact dialogs and wires them to the EXISTING nine 3.1 commands via server actions. State your operating mode (implementation) before editing — a Phase A story authorizes the product UI. [Source: epics.md#Story 3.2 (UI over 3.1); architecture.md#4; project-context.md#Development Workflow Rules]
- **The server-side RLS boundary already exists (Story 3.1) — THE UI IS NOT THE SECURITY BOUNDARY.** The `customers`/`facilities`/`contacts` tables have enable+FORCE RLS, own-tenant `is_tenant_admin` policies, explicit GRANTs (`authenticated → SELECT/INSERT/UPDATE`, `anon → none`), and the nine commands run the full envelope (resolve user → resolve active `tenant_admin` → validate → verify parent ownership → execute → audit). The UI MUST call those commands (mutations) and the RLS client (reads) — it must NEVER write Supabase directly from the client, NEVER use a service-role key, and NEVER bypass the envelope. Tenant isolation is RLS + command-enforced; the UI just presents it. [Source: epic-3 retro-note (server-side RLS already built in 3.1; UI must call the 3.1 envelope commands, never bypass them); src/server/commands/crm/*.ts; project-context.md#Architecture Rules (prefer the envelope; no service-role on client paths)]
- **Personnummer is access-controlled: NOT in the default list projection / list UI; shown ONLY on the customer DETAIL for a `private` customer, in a restrained/masked form.** The list SERVER query MUST omit `personnummer` from its `select(...)` (it should never be in the list payload). The detail page is the one access-controlled surface where it IS intentionally surfaced (owner decision: "personnummer field present for `private` with restrained/masked display"). It must NEVER appear for a non-`private` type (the DB CHECK keeps it null there). Never route personnummer into logs, audit metadata (the commands already drop it), URL params, or the list. [Source: epic-3 retro-note (personnummer stored access-controlled for private/ROT; must NOT appear in default list projection / list UI); owner-decisions-applied-2026-06-18.md#Story 3.1/3.2 (required for private; not in list; restrained/masked on detail); src/server/commands/crm/customers.ts (personnummer never in audit metadata)]
- **Swedish domain labels are required where helpful: `Kunder` (customers), `Anläggningar` (facilities), `Kontakter` (contacts), plus the four type badges (Privatperson / Företag / BRF / Offentlig).** Use English route paths (`/customers`, `/customers/[customerId]`) with Swedish UI labels — the established split (architecture §4; `nav-items.ts`). [Source: epics.md#Story 3.2 Technical Notes (Kunder, Anläggningar, Kontakter); ux-design-specification.md#1; architecture.md#4 (English paths, Swedish labels)]
- **No deferred-module navigation labels, routes, sections, or placeholders — anywhere.** `nav-items.ts` stays EXACTLY the seven IN-scope modules (Story 1.3 / project-context standing rule). No "Pilotstöd/Migrering". The customer-detail "related areas" must read as honest "not built yet" affordances for un-owned Phase A areas, NEVER as dormant deferred-module UI. No broad CRM-analytics screen (a Stop Condition). [Source: epic-3 retro-note (no deferred-module navigation labels); project-context.md#Framework-Specific Rules (exactly seven; never a deferred module/placeholder); ux-design-specification.md#11; epics.md#Story 3.2 Stop Conditions]
- **GATED actions / Stop Conditions (this story):** STOP if the UI would require a FINAL customer-type or primary-contact POLICY beyond the conservative owner-decided assumptions (4 types; no enforced primary; the facility+contact-required rule belongs at quote/job creation, NOT CRM); STOP if asked for broad CRM analytics; adding ANY dependency is approval-gated + EXACT-pinned; NO `.env` edit; NO migration/schema change. [Source: epics.md#Story 3.2 Stop Conditions; owner-decisions-applied-2026-06-18.md#Story 3.1/3.2; project-context.md#Development Workflow Rules]

### Inherited epic facts & gotchas (apply directly — from epic-3 retro-note + epic-2 conventions + Story 3.1)

Folded from `_bmad-output/auto-bmad/retro-notes/epic-3.md`, the project context, and the Story 3.1 implementation — reflect these in implementation, not as a "see the retro" pointer:

- **The browser E2E runner is now REAL — Playwright is configured and CI-gated.** `playwright.config.ts` exists, `@playwright/test` is a devDependency, `pnpm run test:e2e` runs the specs (`tests/e2e/**`, `**/*.e2e.spec.ts`), and `.github/workflows/ci.yml` has a dedicated `e2e` job (boots the local Supabase stack, seeds the two-tenant fixture, builds+starts the app, runs Playwright). This RESOLVES the prior "e2e gated / no browser runner" deferrals. The CRM UI's "UI tests" (Story 3.2 Test Requirements) belong here as `.e2e.spec.ts` specs. Reuse `tests/e2e/auth/login-and-tenant-context.e2e.spec.ts`'s `signIn()`/`waitForHydrated()` helpers (client islands must be HYDRATED before interaction — poll for `__react*` keys; do NOT click pre-hydration or the native GET-submit fires). [Source: playwright.config.ts; tests/e2e/**; .github/workflows/ci.yml (`e2e` job); deferred-work.md (2-1/2-2 e2e gating now resolved by the runner)]
- **Protected `(app)` routes that read per-request auth/cookies MUST set `export const dynamic = "force-dynamic"`.** The `(app)` server layout (`src/app/(app)/layout.tsx`) is the auth boundary; new `/customers` and `/customers/[customerId]` segments inherit it but each still needs `force-dynamic` (only the `build` gate catches a missing one — typecheck/lint/test do not). The shell's `data-testid="tenant-context"`/`current-user` anchors and the `data-slot="primary-action"` mount point already exist. [Source: project-context.md#Framework-Specific Rules (force-dynamic; auth decided server-side from the re-validated JWT); src/app/(app)/layout.tsx; src/components/app-shell/AppShell.tsx]
- **Auth/authorization is server-side from the re-validated JWT — the UI never decides it.** `resolveTenantContext()` (the `(app)` layout) is the single authority; `getClaims()`/`getUser()`, never `getSession()`. The CRM UI reads/writes under the SAME cookie-bound anon-key RLS client (`createSupabaseServerClient()`); RLS scopes every read to the caller's tenant with no tenant id passed. [Source: project-context.md#Framework-Specific Rules; src/server/db/supabase-server-client.ts; src/server/auth/resolve-tenant-context.ts]
- **Errors cross the boundary as a typed `Result`, never a throw.** The 3.1 commands return `Result<R, CommandErrorCode>` with generic Swedish user-safe messages. CRITICAL: a transient `SERVER_ERROR` is RETRYABLE infra failure, NOT a permanent access denial — present it that way (do not conflate "the DB hiccuped" with "you are not authorized"). Never echo raw input, stack, SQL, or tenant/user-existence in a UI message. [Source: src/server/commands/command-errors.ts; project-context.md#Language-Specific Rules; src/server/commands/envelope.ts]
- **`updateCustomer` does NOT support switching `customer_type`** (the type drives the identifier CHECK; the update validator edits within-type fields only). The edit form must not offer a type switch. A known LOW deferral: `validateUpdateCustomer` does not enforce identifier-by-type mutual exclusion at the command layer (it defers to the DB CHECK → `23514` → `VALIDATION_FAILED`); the UI should keep the identifier field consistent with the existing type so a user never trips that backstop. [Source: src/server/commands/crm/validation.ts (no type-switch on update); deferred-work.md (3-1 review: validateUpdateCustomer identifier-by-type deferred to DB CHECK)]
- **DB column/object names are a production contract** — query the exact snake_case names (`display_name`, `customer_type`, `org_nr`, `personnummer`, `email`, `phone`, `city`, `archived_at`, `created_at`, `customer_id`, `facility_id`, `is_primary`, `role_label`, `name`). Map to camelCase in TS at the boundary (`<entity>_id` ↔ `<entity>Id`). [Source: project-context.md#Code Quality (DB names are a production contract); architecture.md#22; src/server/commands/crm/validation.ts (the field shapes)]
- **Adversarial-review ground-truth check.** The toolchain uses REAL forward-dated pins (Next 16.2.9, React 19.2.4, TS 5.9.3, Tailwind 4, `@playwright/test` ^1.61.1, Vitest 4.1.9). If a later review claims a version/API "doesn't exist," cross-check `package.json`/the lockfile before acting — a past review wrongly flagged these. [Source: project-context.md#Technology Stack; package.json]

### Architecture compliance

- **Routes (architecture §4):** `/customers` (Kunder — list/create) and `/customers/[customerId]` (Kund — hub with facilities/contacts/...) are the IN-scope routes this story builds. `/customers/[customerId]/facilities/[facilityId]` is allowed as a full page where a story requires it, but "modal is acceptable for simple edits" — prefer dialogs for facility/contact edits to keep scope tight. English paths, Swedish labels. [Source: architecture.md#4]
- **Server command authority (architecture §5 / ADR-A003):** route handlers / server actions may ONLY delegate to the command functions; sensitive mutations never run client-side and the client never holds a service-role key. The CRM UI calls `runCommand(createCustomer, {...})` etc. — it does not re-implement validation/ownership/audit. [Source: architecture.md#3 ADR-A003, #5]
- **RLS reads (architecture §6/§9):** the list/detail reads run under the request-bound RLS client; RLS scopes rows to the caller's tenant (read-isolation), so a cross-tenant id simply returns zero rows (render generic not-found, no leakage). No client-trusted tenant id. [Source: architecture.md#6, #9 (read isolation; generic access-denied)]
- **Naming/format (architecture §22):** DB snake_case ↔ TS camelCase; React components `PascalCase`; route segments lowercase; tests under `tests/` (E2E under `tests/e2e/**`), never scattered in UI modules; no money fields in this story (CRM has none). [Source: architecture.md#22]
- **No new audit-history dashboard/route/nav item** — `audit_events` remains substrate; a per-entity record-context audit/event-history read UI is a later story. The customer-detail "event history" region is a NOT-YET-BUILT affordance, not a built audit screen, in this story. [Source: project-context.md#Framework-Specific Rules (no audit-history dashboard yet); deferred-work.md (2-3 audit-history consumer UI deferred per-entity)]

### Library / framework requirements

- **No new dependency expected.** Forms use the React 19 `useActionState` (imported from `react`) + Next 16 server actions; dialogs reuse the `AppShell` focus-trap pattern (inline SVG icons via `NavIcon` if needed — no icon library, AR28); styling is Tailwind v4 utility classes (no `tailwind.config.js`; theme via `@theme` in `globals.css`); E2E uses the already-installed `@playwright/test`. Adding ANY dependency is approval-gated + EXACT-pinned (no `^`/`~` for new app deps). [Source: project-context.md#Technology Stack; package.json; React 19 forms guide; src/app/(auth)/login/page.tsx (the existing client-form pattern); src/components/app-shell/AppShell.tsx (dialog/focus pattern)]
- **Pinned stack to respect (do not bump):** Node `>=20.9.0`, pnpm `10.24.0`, Next `16.2.9`, React/React-DOM `19.2.4`, TS `5.9.3` (`strict`, `isolatedModules` → `import type`; `@/*` alias), Tailwind v4 (`@tailwindcss/postcss` `4.3.1`), `@supabase/ssr@0.12.0` + `@supabase/supabase-js@2.108.2`, `@playwright/test` `^1.61.1`, Vitest `4.1.9`. Supabase CLI in CI pinned `2.106.0`. [Source: project-context.md#Technology Stack; package.json]
- **Server actions go in a `"use server"` module, NOT inline in a `"use client"` component**, and they import the 3.1 commands + `createSupabaseServerClient()`. The browser/client must NOT import the server command modules or any service-role symbol — the containment guards (`verify:service-role-containment` source + `verify:bundle-containment` over `.next`) fail loud on a leak. [Source: architecture.md#5; project-context.md#Security Regression Harness Rules (service-role containment, two layers); src/server/commands/envelope.ts]

### File-structure requirements (where things go)

- `src/app/(app)/customers/page.tsx` — REPLACE the placeholder with the real list (server component reading the projection; `force-dynamic`). [src/app/(app)/customers/page.tsx]
- `src/app/(app)/customers/[customerId]/page.tsx` — NEW detail hub (server component; `force-dynamic`). [architecture.md#4]
- `src/components/crm/**` — NEW feature-scoped client components: list table/rows, the customer/facility/contact dialogs + forms, type badge, state components (empty/loading/failed/no-results/duplicate-warning). PascalCase component files. (Components folder map exists in architecture §3: `src/components/crm/`.) [architecture.md#3]
- `src/features/crm/actions.ts` (or co-located `actions.ts`) — NEW `"use server"` server actions that call `runCommand(<3.1 command>, { client: createSupabaseServerClient(), input })`. [architecture.md#5]
- `tests/e2e/crm/*.e2e.spec.ts` — NEW Playwright specs (search/filter/states, dialog focus, validation association, no deferred labels, route auth, personnummer-not-in-list). Reuse `tests/e2e/global-setup.ts` fixture + the `signIn`/`waitForHydrated` helpers. [playwright.config.ts; tests/e2e/**]
- `tests/unit/**` — OPTIONAL `node --test` units for any pure presentational helper (type-badge map, list-row view-model, masking helper). [project-context.md#Testing Rules]
- NOT touched: `nav-items.ts` (stays seven), existing migrations + the 3.1 command/validation modules (consume, do not edit unless a genuine UI-driven gap surfaces — then justify), `src/app/(app)/layout.tsx` (the boundary is reused as-is). [src/components/app-shell/nav-items.ts; src/server/commands/crm/**]

### Testing requirements (this story)

UI tests live as Playwright E2E (`tests/e2e/**`, run by the `e2e` CI job against the local stack + two-tenant fixture); pure presentational helpers may be `node --test` units. Required:

- **List states (P0, AC1):** `/customers` renders empty, no-results (after a non-matching search), and a populated list; search/filter by the approved fields narrows it; failed/loading states are reachable affordances. [epics.md#Story 3.2 AC1; ux §9]
- **Personnummer non-exposure (P0):** the `/customers` list payload/DOM never contains a personnummer; the detail shows it masked for a `private` customer only. [epic-3 retro-note; owner-decisions-applied-2026-06-18.md#Story 3.2]
- **Create/validation round-trip (P0, AC3/AC4):** creating a customer via the dialog persists (round-trips `createCustomer`); a validation failure (missing `display_name`, or identifier-wrong-for-type) shows a FIELD-ASSOCIATED error (`aria-describedby`/`aria-invalid`) + an `aria-live` summary, and PRESERVES the entered values. [epics.md#Story 3.2 AC3; ux §9/§10]
- **Dialog focus (P0, AC3):** opening a facility/contact dialog moves focus into it; Escape/close returns focus to the invoking control; Tab is trapped. [epics.md#Story 3.2 AC3; AppShell focus pattern]
- **No deferred labels (P0, AC6):** the shell shows EXACTLY the seven nav labels; assert ABSENCE of "Pilotstöd"/"Migrering"/"Fortnox"/etc. and no deferred-module section on the detail. [epics.md#Story 3.2 Test Requirements; ux §11]
- **Route authorization (P0, AC5):** anonymous visit to `/customers` and `/customers/<id>` → redirect to `/login` (reuse the AC3 redirect pattern from the auth e2e). A no-membership user sees `NoTenantAccess` (the boundary already covers this). [epics.md#Story 3.2 Test Requirements; tests/e2e/auth/login-and-tenant-context.e2e.spec.ts]
- **NOT in this story's tests:** new RLS/cross-tenant negatives or H4 enrollment (3.1 already covers the three CRM tables — no new table here); money/VAT/snapshot tests (Epics 4-6, Story 3.5); settings/quote-terms (Story 3.3); pricing (Story 3.4). [epics.md#Epic 3 split; architecture.md#18]

### Inherited-deferral reconciliation (CRM-UI-overlapping items from deferred-work.md)

The project ledger (`_bmad-output/implementation-artifacts/deferred-work.md`) is mostly out of scope; the items whose subject overlaps THIS story (CRM UI, route auth, e2e, validation states) and how this story relates to each:

- **E2E browser runner / `tests/e2e/**` gating (2-1, 2-2 deferrals) — RESOLVED upstream; consume it.** Those entries said no Playwright runner existed and the `(app)`-layout redirect/no-access E2E enforcement rode on a future task. The runner + `e2e` CI job + two-tenant fixture now EXIST. Story 3.2's UI tests land as `.e2e.spec.ts` specs and the `/customers` route-authorization assertion (AC5) is naturally covered here (it reuses the same redirect pattern as the auth e2e). This story does NOT need to re-enable anything — it adds new specs to the live runner. [Source: deferred-work.md (2-1 layout-enforcement E2E; 2-2 Playwright gating — now resolved by the runner); playwright.config.ts; ci.yml]
- **`(app)` layout active-tenant both-null fallback (2-1, Low) — NOT this story.** A pre-existing shell-pass follow-up about the top-bar tenant/user region when both display values are null. The CRM UI does not touch that region; do not reopen it. [Source: deferred-work.md (2-1 iter2 both-null indicator); leave for the follow-up shell pass]
- **`validateUpdateCustomer` identifier-by-type mutual exclusion (3-1, Low) — work AROUND it in the UI.** The update validator defers type-consistency to the DB CHECK (`23514` → `VALIDATION_FAILED`). The edit form must keep the identifier field consistent with the existing `customer_type` (and offer no type switch), so a normal edit never trips the backstop; surfacing `VALIDATION_FAILED` cleanly if it does is the correct fallback. Do NOT change the validator in this story (it is owned by a later CRM command-hardening pass). [Source: deferred-work.md (3-1 review: validateUpdateCustomer deferred to DB CHECK)]
- **No other open ledger item overlaps CRM UI** — the remaining open Lows are auth/audit/test-DX/gate-hardening, not CRM presentation. Confirm and note; do not reopen unrelated items. [Source: deferred-work.md]

### Project Structure Notes

- Adds the FIRST real feature UI (list + detail + dialogs + server actions) over the 3.1 data model — alignment with architecture §3 (`src/components/crm/`, `src/features/crm/`), §4 (routes), §5 (server-action delegation), not divergence. No nav change, no schema change, no new dependency. [Source: architecture.md#3, #4, #5]
- The CRM UI fields are deliberately compatible with anonymized Lovable CRM fixtures (private/company/BRF/public customers, facilities, contacts) — keep forms minimal and fixture-friendly; the Lovable app is a behavioral oracle for layout/terminology ONLY (do not copy code). [Source: epics.md#Story 3.2 Migration/Coexistence Impact; project-context.md#Lovable Oracle Policy; architecture.md#16/#17]
- The UI consumes the 3.1 snapshot-friendly fields (display_name, address, contact) that Story 3.5 will reference into quote snapshots — do not introduce a presentation transform that loses the canonical stored value. [Source: architecture.md#11 (snapshot includes customer/facility/contact display); epics.md#Story 3.5]

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 3.2: CRM Tenant-Admin UX And Lifecycle Context (lines 732-766; AC1-AC3, Technical/Test Requirements, Security/RLS Impact, Stop Conditions); #Epic 3 (scope/non-scope, lines 685-696)]
- [Source: _bmad-output/planning-artifacts/owner-decisions-applied-2026-06-18.md#Story 3.2 — CRM UX (list/detail for 4 types + type badge; facility add/manage optional/multiple; contact add/manage customer+facility, no forced primary; facility+contact-required at quote/job not CRM; personnummer present for private with restrained/masked display); #Story 3.1 (4 types; personnummer access-controlled, not in list); #Still open for Epic 3 (exact mandatory-PDF-field set pending)]
- [Source: _bmad-output/planning-artifacts/ux-design-specification.md#1 (IA: Kunder/Anläggningar/Kontakter), #2 (nav model; preserve list state; responsive tables), #3 (Customer list/detail, Facility/Contact modal screens + core UX), #9 (Validation/error/empty/loading states — CRM row; lifecycle errors), #10 (Accessibility: predictable dialog focus, programmatic error association, icon-only accessible names, no color-only), #11 (Explicit non-goals — no deferred-module UI/placeholders)]
- [Source: _bmad-output/planning-artifacts/architecture.md#3 (repo/component structure: src/components/crm, src/features/crm) / #4 (route table: /customers, /customers/[customerId]; English paths + Swedish labels; deferred modules never nav/placeholder) / #5 Server-Side Command Pattern (route handlers/actions delegate to commands only) / #6 RLS read isolation / #9 RLS test strategy (read isolation, generic access-denied) / #22 Naming]
- [Source: _bmad-output/project-context.md#Architecture Rules (prefer the envelope; no service-role on client paths) / #Framework-Specific Rules (App Router; force-dynamic on protected routes; auth server-side from re-validated JWT; nav exactly seven; never a deferred module/placeholder; no audit dashboard yet) / #Language-Specific Rules (typed Result; transient SERVER_ERROR ≠ no-access; generic Swedish messages) / #Security Regression Harness Rules (service-role containment two layers) / #Testing Rules (Playwright e2e gated runner; node --test units) / #Critical Don't-Miss Rules (service-role server-only)]
- [Source: _bmad-output/auto-bmad/retro-notes/epic-3.md (server-side RLS already built in 3.1 — UI must call the 3.1 envelope commands, never bypass; personnummer access-controlled, NOT in default list projection/list UI; Swedish labels Kunder/Anläggningar/Kontakter; no deferred-module navigation labels; non-negotiables: no supplier scope, integer öre, frozen snapshots, no auto-approved tax/legal text — none apply to this UI story directly but constrain scope)]
- [Source: _bmad-output/implementation-artifacts/3-1-tenant-owned-crm-data-model-and-commands.md (the data model + the nine commands this UI consumes; personnummer access-controlled posture; archive-only soft delete) ; _bmad-output/implementation-artifacts/deferred-work.md (e2e runner now resolved; validateUpdateCustomer identifier-by-type LOW deferral to work around)]
- [Source: src/app/(app)/customers/page.tsx (placeholder to replace) ; src/app/(app)/layout.tsx (the reused `(app)` auth boundary + force-dynamic posture) ; src/components/app-shell/AppShell.tsx (focus-trap/return-focus dialog pattern; data-slot=primary-action; data-testid anchors) ; src/components/app-shell/nav-items.ts (the seven-item single source of truth — DO NOT EDIT) ; src/components/app-shell/PagePlaceholder.tsx (honest-empty pattern) ; src/app/(auth)/login/page.tsx (existing client-form + generic-error pattern)]
- [Source: src/server/commands/crm/customers.ts + facilities.ts + contacts.ts (the nine commands' exact signatures) ; src/server/commands/crm/validation.ts (input shapes: 4 types, identifier-by-type, optional email/phone, no type-switch on update) ; src/server/commands/envelope.ts (runCommand signature) ; src/server/commands/command-errors.ts (CommandErrorCode + COMMAND_MESSAGES; SERVER_ERROR is transient/retryable) ; src/server/db/supabase-server-client.ts (the per-request RLS read client)]
- [Source: playwright.config.ts + tests/e2e/auth/login-and-tenant-context.e2e.spec.ts (signIn/waitForHydrated + redirect-auth patterns to reuse) + tests/e2e/global-setup.ts (two-tenant fixture) + .github/workflows/ci.yml (`e2e` CI job) ; supabase/migrations/20260630120000_crm_data_model.sql (the exact customers/facilities/contacts columns + identifier-by-type CHECK)]
- [Source: Next.js forms with Server Actions + React 19 useActionState — https://nextjs.org/docs/app/guides/forms (the modern form-validation + aria-describedby/aria-live pattern for AC3)]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

## Open Questions

These are surfaced for visibility; they do NOT block implementation under the conservative owner-decided defaults (proceed; confirm at the working session / PR review):

1. **Exact mandatory customer fields for a PDF-ready customer** — owner gave "kundrelevant info" + type-driven identifier, not a precise required-field list (still open for Epic 3). Proceed with sensible defaults on the create form (display_name + identifier-by-type required; address/contact optional); confirm the exact required set in the working session. Not a data-model blocker. [Source: owner-decisions-applied-2026-06-18.md#Still open for Epic 3]
2. **Archived-customer browsing UX** — the list defaults to active rows; is a "show archived" view needed in Phase A, or is archive simply "removed from the active list"? Defaulting to active-only is the conservative choice; defer a dedicated archived view unless trivial. [Source: ux-design-specification.md#9; architecture.md#6 (archive over delete)]
3. **Personnummer masking format on detail** — owner said "restrained/masked display"; the exact mask (e.g. hidden except last 4, or reveal-on-click) is a UX detail to confirm. Proceed with a conservative mask + explicit reveal affordance. [Source: owner-decisions-applied-2026-06-18.md#Story 3.2]
4. **Duplicate-like warning heuristic** — AC1 requires a "duplicate-like warning" state but does not define the match rule. Proceed with a simple case-insensitive `display_name` match within the tenant as a NON-blocking advisory; confirm the desired strictness with the owner if it matters operationally. [Source: epics.md#Story 3.2 AC1; ux-design-specification.md#9]
