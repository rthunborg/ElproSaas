# Epic 9 — Integration Code Review Findings (2026-07-08)

Triage of the epic-boundary INTEGRATION review of Epic 9 (stories 9.1–9.5, all landed).
Three review lenses (Blind Hunter / Edge Case Hunter / Acceptance Auditor) ran independently
under two reviewer models (`primary`, `secondary`), plus a dedicated security review
(auto-bmad-local). Findings below are the deduplicated, classified survivors. The dedicated
security review returned **0 HIGH / 0 MEDIUM / 0 LOW** (explicitly clean: zero real PII/secret
committed, no reachable injection/authz/crypto/deserialization sink). The Acceptance Auditor
(primary) returned **0 High**. Every survivor is docs-accuracy / cross-story-seam / test-harness
maintainability — no production runtime defect and no exploitable finding.

### Review Findings

- [x] [Review][Decision][Med] `A22` carries two contradictory gate statuses in the 9.4 sign-off register — `signed-off` (quote-terms row) and `blocking` (tax-wording row) — with no validator detecting the per-ID conflict — The same owning SoR ID `A22` is `signed-off (demo; real-pilot re-score)` at `pilot-fallback-cutover.md:1164` (quote terms) and `blocking` at `:1165` (tax wording, `A20`/`A21`/`A22`). Both the 9.4 `blockingSignoffIds()`/no-drift guard and the 9.5 `registerBlockingIds()`/reconcile guard assert only ID PRESENCE, never per-ID status consistency, so the conflict is invisible to the executable gates; a downstream consumer keying on "is `A22` blocking?" cannot resolve it. The register also assigns `A20/A21/A22` a `blocking` classification that the automated SoR reconciliation (`blockingSignoffIds()`, which extracts only `öppen/partial (möte)` status rows) cannot source — they live in the SoR's prose "Parked for full-release" section, not a status-table row — so the SoR→register→report chain rests on an un-sourced status assignment (the exact R-917 drift the register exists to catch, papered over by "parked" framing). Source: auditor@primary (M1+L1) + auditor@secondary (#3). Recommended: fix: scope the two rows so `A22` is not one ID with opposite gate statuses — split the tax-wording facet onto a distinct owning ID (e.g. a new `A22-tax` sub-ID, NOT reusing `A22`) OR add an explicit "facet:" qualifier column so quote-terms-`A22` (signed-off) and tax-wording-`A22` (blocking) are unambiguous; and add a per-ID status-coherence assertion to the 9.4 validator so a single ID cannot appear with both `signed-off` and `blocking`.
- [x] [Review][Patch][Med] 9.1 runbook §5 "Approved Asset-Location Seams" table is stale-on-arrival — it declares the fixture/script homes 9.2 actually created as "does NOT exist yet / do NOT create" [docs/migration/migration-runbook.md:225-227] — §5 states `tests/fixtures/golden/lovable/**` and `scripts/migration/**` are "**Forthcoming — does NOT exist yet.** ... do NOT scaffold / Do NOT create," but 9.2 (same epic) committed both (8 fixtures + `lovable-capture.ts` + README). No later story flips the status from "forthcoming" to "landed" (each story's File List forbids touching the 9.1 runbook), so the merged epic ships a living-control-point runbook that misdescribes the repo state at merge time — a reader following the runbook's own "approved asset locations" table would wrongly conclude the fixtures/scripts do not exist (violates R-922 evergreen-doc discipline the runbook itself invokes). Source: auditor@secondary (#1). Fix: flip §5's status cells for the two locations from "Forthcoming — does NOT exist yet" to "landed (Story 9.2)".
- [x] [Review][Patch][Med] 9.5 gate-summary "Golden-master comparisons" evidence row never names the actual Story 9.3 Lovable-comparison harness — it cites only the pre-Epic-9 new-side-only golden packs [docs/migration/phase-a-acceptance-gate.md:103] — the §2 evidence row lists exactly the four pre-existing new-side golden packs (`@/lib/money`, calc, quote-PDF text, accept→job — architecture §19 stage 9) and omits the `tests/unit/fixtures/golden/lovable/lovable-comparison-*.test.ts` suites 9.3 built to drive the anonymized Lovable fixtures through the real oracle and compare old-vs-new — the artifact that actually answers epic AC1's "old/new comparisons ... approve workflows based on evidence instead of memory." A reader auditing "is the old/new comparison evidence real" from the acceptance-gate report alone cannot find it (the only other mention is disconnected prose at line 213). Source: auditor@secondary (#2). Fix: add the `lovable-comparison-*` (guards / calc-quote-pdf / acceptance-job / delta-classification) suite names to the §2 golden-master evidence row so the epic's headline old/new comparison artifact is cited as gate evidence.
- [x] [Review][Patch][Low] 9.5 report §6 "reconciled 1:1 with the sign-off register §4 blocking rows" claim overstates the guard's actual reach — only `blocking`-status rows are cross-checked live; the `signed-off (demo; real-pilot re-score)` rows are hand-authored, unguarded [docs/migration/phase-a-acceptance-gate.md:220-225] — `reconcileReadinessBlocking`/`registerBlockingIds()` only scan register rows matching `/blocking/i`, so the `signed-off` register rows (Acceptance channels, Required files, Adjusted-price policy, Quote terms) are structurally excluded from the live reconciliation and their §6 counterparts are reconciled only by the dev's manual cross-check at authoring time. Content is correct today, but the unqualified "reconciled 1:1" framing oversells the executable guard vs. this epic's own R-917/R-920 "no drift" discipline. Source: auditor@primary (L2) + auditor@secondary (#3). Fix: qualify the §6 claim to "reconciled 1:1 with the register's §4 `blocking` rows (signed-off rows are hand-authored, not live-reconciled)" so the doc's claim matches the guard's actual coverage.
- [x] [Review][Patch][Low] 9.1 runbook §5 still lists `tests/golden/**` as a valid comparison-test home — the vacuous-green runner-glob trap [docs/migration/migration-runbook.md:226] — the runbook offers "`tests/golden/**` / the existing `tests/unit/**` golden pins" as approved comparison-test locations, but only `tests/unit/**` is in the real `test:unit` glob; anything under `tests/golden/**` is never executed (R-904). 9.3 correctly placed every comparison suite under `tests/unit/fixtures/golden/lovable/**` and avoided the trap, but a future migration-script author following the evergreen runbook could land a vacuous-green suite under `tests/golden/**`. Source: auditor@primary (L5). Fix: drop `tests/golden/**` from §5 (or mark it "never executed by `test:unit` — do NOT use for comparison suites").
- [x] [Review][Defer][Low] Two textually-copied PII-scan regex sets (9.2 shared fixture scanner + the 9.1/9.4/9.5 docs validators) are not single-sourced — drift risk on the same "zero real PII" epic-blocker [tests/support/anonymization-scan.ts:2315] — the docs-side validators (`migration-runbook-validators.test.ts`, `sign-off-register-validators.test.ts`, `acceptance-gate-report-validators.test.ts`) re-declare the PII regexes rather than importing the shared `@/tests-support/anonymization-scan` module. Verified the two sets are byte-IDENTICAL today (`SECRET = /secret|password|api_key/i`, personnummer `/\b\d{6}-\d{4}\b/`, orgnr `/\b\d{10}\b/`, etc. — the blind reviewers' claim they had already DIVERGED is a false positive), and the security review confirmed the fixture scanner ports the money-pack authority byte-identical. But a future hardening of one copy (e.g. the 9.2 ORGNR string-leaf scoping) would silently not propagate to the docs-side copies, weakening one half of the same epic-blocker guard, since nothing asserts the sets are equal. Source: auditor@secondary (#4). Deferred, pre-existing intent gap — genuine maintainability drift risk, not a live defect (sets identical today); log for a future scan-consolidation pass that extracts one shared PII-regex module both the fixture scan and the docs validators import (or a test asserting the two sets are identical).

<!-- iteration 2 (2026-07-08) — INTEGRATION review, 2 reviewer models (primary/secondary) × 3 lenses + dedicated security (2 LOW). All iteration-1 bullets above are resolved/deferred; the survivors below are the deduplicated NEW epic-level angles only. No Critical/High/Med. No production runtime defect, no exploitable finding, no [Review][Decision] and no [Review][Patch] — every survivor is a Low docs/test-harness/single-source-of-truth maintainability seam, deferred. The security review returned 0 HIGH / 0 MED; its 2 LOWs (non-dashed/12-digit personnummer format, free-form personal names) are BOTH future-real-capture defense-in-depth already tracked (iter-1 security LOWs + 9.2 resolved free-form-name docs) — not re-persisted. -->
- [x] [Review][Defer][Low] The four migration control-point docs restate large overlapping chunks (demo-vs-real-pilot table, STOP protocol, PII-hygiene R-914 paragraph, per-workflow narrative) despite each header promising it "does not restate" the others — four hand-maintained copies with no single source and validators that spot-check phrases, not cross-doc consistency [docs/migration/legacy-record-classification.md, migration-runbook.md, phase-a-acceptance-gate.md, pilot-fallback-cutover.md] — the demo-data-only policy, the R-914 masking convention, and the six per-workflow bullet sets (CRM / Settings-Pricing / Calculations / Quote Versions / Basic Job / Required Files) are re-summarized near-verbatim across the four control-point docs; a change to the demo-data-only policy or the masking convention must be edited in four places and nothing asserts the copies stay consistent. Content matches today. Source: blind@primary (Doc/evidence honesty) + blind@secondary (#10). Genuine maintainability drift risk, not a live defect. Owner: a future docs-consolidation pass — extract the shared demo-vs-real-pilot table + STOP protocol + PII-hygiene paragraph into ONE canonical section the other docs reference, or add a validator asserting the shared blocks are identical across the four docs.
- [x] [Review][Defer][Low] Runbook §5 restates a volatile enumeration inside an evergreen doc — it hardcodes the 8 fixture filenames inline (drift vs the live `LOVABLE_FIXTURE_MANIFEST` + glob) yet does NOT enumerate the five 9.3 comparison-suite filenames (inconsistent enumeration depth vs its sibling fixture/script rows and the more precise 9.5 report) [docs/migration/migration-runbook.md §5] — the §5 landed-seams table (iter-1 addition) lists `crm.json … files.json` (8 files) by hand, duplicating the enumeration `tests/support/anonymization-scan.ts` owns live; a future fixture added under `tests/fixtures/golden/lovable/**` updates the manifest/glob but not this paragraph. Conversely the "Golden-master comparison tests" row names only the `tests/unit/fixtures/golden/lovable/**` home without the five `lovable-comparison-*.test.ts` suite files the fixtures/scripts rows enumerate for their peers. List/claim accurate today (8 files, 5 suites verified against the diff). Source: auditor@primary (L5) + auditor@secondary (#4). Cosmetic/consistency + evergreen-doc drift (R-922), not a substantive accuracy defect. Owner: a future docs-maintenance pass — reference the live fixture home instead of restating the 8 filenames (or add a validator cross-checking §5's list against the live manifest), and align the comparison-suite row's enumeration depth with its siblings.
- [x] [Review][Defer][Low] Three hand-rolled ID-extraction regexes over the same register content are forked across the 9.1/9.4/9.5 validators — the iter-1 pass widened all three in parallel (they agree today) but they are not a single shared helper, exactly the "do NOT fork a looser copy" duplication the story Dev Notes ban for the PII-scan regexes [tests/unit/docs/sign-off-register-validators.test.ts (`blockingSignoffIds`, `ac2RegisterRows` `idRe`); tests/unit/docs/acceptance-gate-report-validators.test.ts (`registerBlockingIds`)] — `blockingSignoffIds()` (parses `owner-signoff-questions.md` table cells), `ac2RegisterRows()`'s inline `idRe` (parses register prose, iter-1-widened), and `registerBlockingIds()` (a THIRD regex over `pilot-fallback-cutover.md` §4, iter-1-widened for the dotted `A.1`/`B.1-B.4`/`C.1-C.3` families + `A22-tax`) are independently maintained; nothing prevents them re-diverging on a future edit, and each silently DROPS an ID it doesn't recognize (no "unexpected token" assertion) rather than failing loud. No live disagreement today (proven by the passing tests). Source: auditor@secondary (#3) + blind@secondary (#4) + blind@primary (registerBlockingIds brittle). Maintainability/drift risk directly in the epic's own R-917/R-920 "no register drift" theme. Owner: a future test-consolidation pass — extract ONE shared register-ID-extraction helper the three validators import (and add an "unexpected/unmatched ID token" assertion so a new ID shape fails loud rather than silently dropping out of the blocking set).
- [x] [Review][Defer][Low] `READINESS_CODES` single-source-of-truth is only half-enforced — the compile-time exhaustiveness guard checks membership but NOT uniqueness, and the 9.3 test-support `REAL_READINESS_CODES` re-declares a hardcoded copy of the union the epic ships this iteration [src/features/calculations/readiness.ts (`READINESS_CODES` + `_ReadinessCodeExhaustive`); tests/unit/fixtures/golden/lovable/lovable-pack-support.ts:7095 (`REAL_READINESS_CODES`)] — the additive `READINESS_CODES` runtime export (the epic's single `src/**` change) has a `satisfies` + exhaustiveness check that would NOT catch a duplicated string literal (a dup compiles fine and silently inflates `.length`, breaking any consumer assuming length == union arity), and `lovable-pack-support.ts` re-types the whole union as a plain array literal (`REAL_READINESS_CODES`) with only a comment ("the GREEN dev may instead import the real union directly") rather than an enforced import — the exact drift the diff's own `_ReadinessCodeExhaustive` guard exists to prevent. A future added `ReadinessCode` silently staleness the copy; a duplicated entry passes the guard. No live defect (no dup, copy matches today). Source: edge@secondary (READINESS_CODES uniqueness) + blind@secondary (#3, hardcoded copy). Owner: a future calc-readiness cleanup pass — add a uniqueness assertion to the guard (`length === new Set().size`) and replace `REAL_READINESS_CODES` with a direct import of the real `READINESS_CODES` union (or a test asserting the two are identical).
- [x] [Review][Defer][Low] The `@/` alias namespace was widened to non-`src` roots (`@/tests-support/*`, `@/scripts-migration/*`) with no guard that no `src/**` file imports them, weakening the "`@/` means `src/`" / AR25-R-919 capture-assets-off-the-runtime-path boundary the change protects [tsconfig paths + tests/support/alias-hook.mjs] — mapping test/capture roots into `@/` means a product `src/**` file that mistakenly imports `@/tests-support/...` or `@/scripts-migration/...` would now RESOLVE (under tsc and the test hook) rather than fail loudly, and nothing in the diff asserts the absence of such an import. Verified NO `src/**` file imports either alias today (grep clean), so no live violation — the gap is a missing guard on the boundary, not a breach. Source: blind@primary (Scope/config). Owner: a future boundary-hardening pass — add a lint/test asserting no `src/**` file imports `@/tests-support/*` or `@/scripts-migration/*` so the capture/test-asset-off-runtime-path boundary is executable, not conventional.
- [x] [Review][Defer][Low] The `evaluateCutover` checklist model has an unvalidated free-form `workflow: string` and collapses `fallbackRemovalAllowed` to the exact same boolean as `cutoverAllowed`, so the docs' "distinct fallback-removal vs cutover decision points" framing is not reflected in the executable model and a typo'd/nonexistent workflow name silently yields a decision [tests/unit/docs/sign-off-checklist-model.ts (`EvaluateCutoverInput.workflow`, `evaluateCutover`)] — `workflow` has no union/enum tying it to the six documented workflow-name sections (CRM/Settings-Pricing/Calculations/Quote Versions/Basic Job/Required Files), so nothing keeps the six doc sections and the model in sync as the docs evolve; and `fallbackRemovalAllowed: !blocked` mirrors `cutoverAllowed: !blocked` exactly (demo returns `true` for both), making the second field redundant/always-equal despite the docs describing fallback-removal and cutover as conceptually-distinct gates (a workflow could plausibly be cut over while its fallback is retained for a grace period). All current call sites pass correct literal workflow names; nothing is broken today. Source: auditor@secondary (#1) + blind@secondary (#5). Low design/consistency gap, not a functional defect. Owner: a future checklist-model pass — constrain `workflow` to a shared union of the six canonical workflow names, and either model a real fallback-removal-vs-cutover divergence or drop the redundant second field.
- [x] [Review][Defer][Low] The scope-scan whole-word regex `(^|[^a-z])${category}([^a-z]|$)` treats a concatenated deferred-module token as clean — a deferred surface wired in with no separator (`assetregistry`, `hrtool`, `fortnoxx`) sails through the scan [tests/unit/docs/acceptance-gate-report-validators.test.ts (`scanSurfaceForDeferred`)] — the `[^a-z]` boundary correctly trips `fortnox2`/`fortnox_v2` (digit/underscore = boundary) but a trailing/leading `[a-z]` defeats it, so a deferred module concatenated into a larger lowercase token (`assetregistry` for `asset`, `hrtool` for `hr`) is not flagged. The scan is a docs-hygiene guard over `docs/migration/**` authored prose (not runtime), and no such concatenated deferred token exists in the committed docs today, so no live miss. Source: blind@primary (Tests as guards). Low guard-completeness gap on a docs scanner, not a defect. Owner: a future scanner-hardening pass — anchor the deferred-category scan on token/word boundaries that also catch separator-less concatenations (or scan a normalized token stream), if the deferred-surface scan is ever relied on beyond authored prose.
