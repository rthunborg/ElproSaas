# Deferred Work

Tracks real findings that were consciously deferred rather than fixed in the originating story. Each entry names where it came from and where it should land.

## Deferred from: code review of 1-2-establish-ci-and-quality-gate-baseline (2026-06-15)

- **CI `pnpm build` gate is non-hermetic — `next build` fetches a Google font over the network.** The dependency comes from the app scaffold (`next/font/google`, Story 1.1), not the CI workflow this story added; Story 1.2 explicitly documents it as "expected to pass on networked CI." No functional impact on networked GitHub-hosted runners today. Revisit if/when CI runs in a network-restricted environment (self-hosted/air-gapped) or when font loading is reworked. Owner: scaffold/font cleanup (alongside **Story 1.3**) or a future CI-hardening story. [.github/workflows/ci.yml:58]
  - **Status (2026-06-15, Story 1.3 owner decision):** still deferred. Story 1.3 fixes only the `globals.css` Arial override; the `next/font/google` network fetch stays as-is (making it hermetic means switching to self-hosted `next/font/local`). Revisit in a future CI-hardening/font story.

## Deferred from: creation of 1-3-build-phase-a-app-shell-and-deferred-scope-navigation-guardrails (2026-06-15)

- **Automated component/UI tests for the Phase A app shell are deferred to the TEA `testarch-framework` harness (Epic 2).** Story 1.3 verifies its ACs (nav items, active state, keyboard reachability, absence of deferred labels/routes, responsive widths) via documented manual verification, because no test framework exists yet and Stories 1.1/1.2 reserved the framework decision for `testarch-framework` (AR28 dependency approval). When the harness lands, backfill automated tests covering: the seven nav items render and only those (no Pilotstöd/Migrering, no deferred modules), `aria-current` active state with a non-color indicator, icon-only controls expose accessible names + tooltips on hover/focus, and the sidebar→rail→drawer responsive behavior. Owner: first story after the test harness is initialized (Epic 2 / TEA `testarch-framework`). [src/components/app-shell/**, src/app/(app)/**]

## Deferred from: code review of story-1.3 (2026-06-16)

- **Whole app shell is a single client island.** `src/app/(app)/layout.tsx` delegates all chrome to a `"use client"` `AppShell`, shipping the sidebar/top-bar/inline-SVG icons as client JS and forfeiting App-Router server-layout streaming. Defensible for a small Phase A shell; revisit a server-layout + small client-nav-island split if the shell grows or when auth/tenant context lands (Epic 2). [src/app/(app)/layout.tsx:9-16, src/components/app-shell/AppShell.tsx:85]
  - **PARTIALLY RESOLVED (2026-06-22, Story 2.1).** `(app)/layout.tsx` is now a SERVER layout that resolves tenant context server-side (the auth boundary) and passes plain props into the still-client `AppShell` nav island — exactly the "server-layout + small client-nav-island" split this note called for "when auth/tenant context lands." `AppShell` remains a client island (it owns `usePathname` + drawer state); a deeper split of the static nav chrome into pure server components was NOT pursued (out of Story 2.1 scope — the minimum clean split that lets auth resolve server-side was the goal). Remaining client-island reduction stays deferred to a future shell-growth pass.
- **Focus after a drawer-link navigation is not guaranteed.** `handleDrawerNavigate` closes the drawer and relies on client navigation to move focus, but `next/link` does not reliably move focus to the new page, so focus can fall back to `<body>`. This is an app-wide client-navigation focus concern (not unique to the drawer); address holistically (e.g. focus `#main-content` on route change) when route transitions / auth land. Owner: Epic 2 or a route-transition/a11y pass. [src/components/app-shell/AppShell.tsx:102-103]

## Deferred from: code review of 2-1-tenant-admin-login-and-tenant-context-resolution iter 2 (2026-06-22)

- **[Low] `tenants.name` NOT-NULL reconciliation for the AC1 both-null indicator (CONSIDER item).** PARTIALLY ADDRESSED (2026-06-26, Story 2.2): `tenants.name` is now `NOT NULL` in the migration, removing the null-tenant-name source. The AppShell both-null display fallback itself is still a follow-up shell pass (NOT changed here). Owner remains: a follow-up shell pass. [supabase/migrations/20260625122433_tenant_foundation.sql; src/components/app-shell/AppShell.tsx]
- **[Low] AC1 active-tenant indicator collapses when both `tenantName` and `userEmail` are null.** The top-bar tenant/user region in `AppShell` is gated behind `(context.tenantName || context.userEmail)`; on a granted-access shell where both presentational values happen to be null (types permit both `string | null`), the region renders only the sign-out button with no tenant indicator at all, contradicting AC1's "unconditional" active-tenant indicator. Security boundary unaffected (access already granted server-side); hypothetical trigger (a tenant row with a null name AND a user JWT with no email). Owner: a follow-up shell pass aligned with Story 2.2 — add an always-present tenant-identity fallback (a generic label or the resolved `tenantId`) when both display values are null, and reconcile with Story 2.2's `tenants.name` NOT-NULL constraint. [src/components/app-shell/AppShell.tsx:690]

## Deferred from: code review of 1-4-document-local-setup-environment-contract-and-repo-hygiene (2026-06-21)

- **[Low] Evergreen docs hardcode plan-specific epic/story numbers that risk staleness.** `.env.example` and `local-setup.md` attribute the signed-URL TTL var to "Epic 8" (spec sources it only to architecture §6, no epic assignment) and `local-setup.md` attributes test-only factories to "Story 2.2" (spec ties only first migrations to 2.2). The vars/behavior are correctly marked optional/forthcoming; only the number attributions are dev-introduced and unverified, so they go stale if the plan shifts. Owner: a docs-maintenance pass (or whichever story actually lands the file-storage / factory work — update the attribution then). [.env.example:47; docs/process/local-setup.md:264,293]
- **[Low] Hardcoded-light `body` has no dark-mode or `forced-colors`/high-contrast support.** `globals.css` hardcodes `#ffffff`/`#171717` on `body` with the `@media (prefers-color-scheme: dark)` block intentionally removed (Story 1.4 Task 4.1) and no `@media (forced-colors: active)` fallback. Deliberate Phase A hardcoded-light posture (Story 1.3), not a regression from this change, but OS dark-mode and Windows high-contrast users get an unthemed experience. Owner: a future theming / accessibility pass. [src/app/globals.css:11-13]

## Deferred from: implementation of 2-2-tenant-membership-schema-rls-helpers-and-two-tenant-fixtures (2026-06-26)

- **[Low] Vitest INT suites do not auto-run `supabase db reset`.** The runner only PROBES stack reachability (`tests/support/global-setup.ts`); the caller (`pnpm test:db`-equivalent locally, the CI `db` job) is responsible for `supabase db reset`. This keeps the suites from ever wiping a developer's DB unexpectedly, but means a stale schema (un-reset after a migration change) could produce confusing failures locally. Acceptable for now (per-test fixtures use unique ids; CI always resets). Owner: a future test-DX pass could add an opt-in pretest reset. [tests/support/global-setup.ts]

## Deferred from: code review of 2-2-tenant-membership-schema-rls-helpers-and-two-tenant-fixtures (2026-06-26)

- **[Low] `isLocalStackReachable()` probes `/auth/v1/health`, not the schema.** A stack that started but failed or only partially applied `supabase db reset` passes the Auth health probe, so the DB-backed suites run and then fail with confusing schema errors instead of skipping cleanly. CI runs `db reset` before tests and `SUPABASE_TEST_REQUIRED=1` backstops a missing stack, so live impact is low; a schema assertion (e.g. confirm the `tenants` table is present) would be a stronger reachability gate. Owner: a future test-DX pass. [tests/support/test-env.ts:53]
- **[Med] `tenant_memberships_select_own` lets any active admin read every membership row in their own tenant (co-member enumeration).** The policy `using (public.is_tenant_admin(tenant_id))` scopes reads by tenant, not by `user_id = auth.uid()`, so with more than one admin per tenant each can read co-members' `user_id`/`status` — never across a tenant boundary (the security review rejected the vulnerability framing). Deferred per user decision 2026-06-26 as accepted, intended Phase A design (single-admin tenants); disclosed in the PR Security/RLS impact statement rather than buried in a code comment. Owner: Story 2.4 / RBAC seam — tighten the SELECT policy toward `user_id = auth.uid()` (least privilege) when multi-admin / role management lands. [supabase/migrations/20260625122433_tenant_foundation.sql]

## Deferred from: implementation of 2-3-server-command-envelope-and-minimal-audit-events (2026-06-29)

- **Business-table audit wiring (non-`audit_events`) lands in the owning Epic 3-8 stories.** Story 2.3 builds the AUTHORITY SUBSTRATE only: the reusable command envelope (`src/server/commands/envelope.ts`), the `writeAuditEvent` DEFINER-RPC write path, the metadata sanitizer, and the `audit_events` table/trigger/RLS. It deliberately creates NO business table (architecture §7) and wires NO real lifecycle event producer. Each later command (membership/settings/pricing/quote/acceptance/job/file changes) REUSES `defineCommand`/`runCommand` + `writeAuditEvent` and supplies its own `target_type`/`event_type`/allow-listed metadata fields when it lands. Owner: the owning Epic 3-8 command stories. [src/server/commands/envelope.ts; src/server/commands/audit.ts; architecture.md#5, #15]
- **Audit-history consumer UI is substrate-only here (AC7).** AC7 requires that WHEN audit history is surfaced, only minimal relevant lifecycle/audit events are exposed — but Story 2.3 builds the append-only substrate + the own-tenant SELECT policy ONLY, and deliberately creates NO audit-analytics module, dashboard, route, or nav item (Stop Condition; nav-items.ts stays exactly seven). The record-context read UI that surfaces minimal audit history lands in its own story. Owner: the record-context / lifecycle-history UI stories (per entity, Epics 3-8). [supabase/migrations/20260629121136_audit_events.sql (audit_events_select_own); epics.md#Story 2.3 (substrate only)]
- **[Med, accepted Phase A] `audit_events_select_own` co-member audit-read least privilege.** The own-tenant audit SELECT policy `using (public.is_tenant_admin(tenant_id))` mirrors the Story 2.2 `tenant_memberships_select_own` shape: it scopes reads by TENANT, not by `actor_user_id = auth.uid()`, so in a multi-admin tenant each active admin can read co-members' audit rows within their OWN tenant (never across a tenant boundary). For Phase A single-admin tenants this is the accepted, intended design (same decision as the membership co-member-read deferral); disclosed in the PR Security/RLS impact statement, not buried in a comment. Owner: Story 2.4 / RBAC seam — tighten toward `actor_user_id = auth.uid()` (or a role-scoped read) alongside the membership-policy tightening when multi-admin / role management lands. Do NOT tighten here (out of scope). [supabase/migrations/20260629121136_audit_events.sql (audit_events_select_own); deferred-work.md (2-2 co-member enumeration, Story 2.4)]

## Deferred from: code review of 2-3-server-command-envelope-and-minimal-audit-events (2026-06-29)

- **[Low] `record_audit_event` trusts caller-supplied `p_actor_user_id` instead of `auth.uid()` (audit attribution forgery / repudiation within own tenant).** The membership gate keys off `auth.uid()` (`is_active_tenant_member(p_tenant_id)`) but the inserted `actor_user_id` comes from the parameter, so an active member can write a genuine own-tenant audit row attributing the action to any other `auth.users` id (bypassing the app envelope). Security review rated this LOW: the cross-tenant gate still holds (no write outside the caller's tenant), the FK forces a real user id, and Phase A is single-admin-per-tenant (small blast radius). Owner: the RBAC / multi-admin seam (Story 2.4+) — drop `p_actor_user_id` and insert `actor_user_id = (select auth.uid())`, or add `if p_actor_user_id is distinct from (select auth.uid()) then raise`. [supabase/migrations/20260629121136_audit_events.sql:1006]
- **[Low] End-to-end R-010 metadata-hygiene assertion on the happy path is near-vacuous.** The smuggled `apiKey: "sk_live_should_not_persist"` is passed via `input`, but `makeNoopCommand` declares no `auditFields`, so the envelope always derives `metadata = {}` — the secret was never on a path into metadata regardless of the sanitizer, so the assertion passes trivially. AC5 is still met by the strong pure-unit sanitizer suite. Owner: a future test-hardening pass — add a command whose `auditFields` routes caller-controlled metadata through `sanitizeAuditMetadata`, then assert forbidden content is dropped end-to-end. [tests/integration/commands/envelope-audit-write.int.test.ts:1562]
- **[Low] The documented `actor_user_id on delete set null` FK behavior has zero test coverage.** The migration's most-documented design decision (audit row survives a deleted actor by nulling the FK) is never exercised: no test deletes an `auth.users` row and asserts the audit row persists with `actor_user_id IS NULL` and all other columns intact. Owner: a future test pass — add a privileged delete-actor + re-read assertion. [supabase/migrations/20260629121136_audit_events.sql:910]
- **[Low] The disabled-membership failure path is only unit-proven, not DB-backed.** Spec Task 5.2 says "no/disabled membership → `TENANT_MEMBERSHIP_REQUIRED`"; the integration suite covers only the no-membership (`orphanUser`) variant — no fixture creates a user with a `disabled`/`inactive` membership for a live DB-backed resolve (the disabled path is exercised only at the pure-unit level via result injection). Owner: a future test pass — add a disabled-membership fixture + DB-backed failure-mode case. [tests/integration/commands/envelope-failure-modes.int.test.ts:82]
- **[Low] `CONTROL_CHARS` does not strip C1 control characters (U+0080 through U+009F).** The regex covers ASCII control ranges (NUL through U+001F, plus DEL U+007F) only, so C1 controls (e.g. NEL U+0085) in an allow-listed `reason`/hash string pass the check and could persist. No proven security impact (metadata is JSON-serialized; security review did not flag it), but the guard is narrower than its comment implies. Owner: a future hardening pass — extend the class through U+009F. [src/server/commands/audit-metadata.ts:56]
- **[Low] (iter 2) Test factory `tryUpdate` interpolates raw object keys as unquoted SQL identifiers.** `tests/factories/audit-events.ts` builds the UPDATE `SET` clause via `keys.map((k,i) => `${k} = $${i+2}`)` — the patch's column names are concatenated directly into a statement run on the superuser pg pool; only VALUES are parameterized. Test-only, loopback-gated, and keys are author-controlled constants today (no untrusted input reaches it), so no current exploit path — but it is an identifier-injection pattern in privileged test code that will be copy-pasted. Security review examined the file and found service-role usage sound; rated Low. Owner: a future test-hardening pass — allow-list or `pg`-format-quote the identifiers, or constrain `tryUpdate` to a fixed column set. [tests/factories/audit-events.ts:83]

## Deferred from: code review of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (2026-06-29)

- **[Low] Anon-EXECUTE / anon-table negatives pin exactly `error.code === "42501"`, brittle across Postgres/PostgREST versions for a not-visible function.** `assertAnonExecuteDenied` and the data-driven anon table verbs assert exactly `42501` (permission denied). A SECURITY DEFINER function the role cannot even see can surface `42883` (function does not exist) rather than `42501` depending on search_path/visibility, and the suite applies the rigid pin to all three RPCs (`is_active_tenant_member`/`is_tenant_admin`/`record_audit_event`) with no fallback. Green today — the functions are visible and only EXECUTE is revoked — so not a current defect; a future PostgREST/Postgres upgrade that changes function-visibility surfacing is the realistic trigger. Owner: a future test-hardening pass — accept `42501` OR `42883` (deny-or-not-visible) for the EXECUTE negatives, keeping the `data !== true` backstop. [tests/integration/rls/anon-path-isolation.rls.test.ts:693]
  - **WON'T-FIX (2026-06-29, epic-2 hardening) — DX#5. STAYS OPEN as won't-fix, not resolved.** The exact-`42501` SQLSTATE pin is the INTENTIONAL anti-vacuous-pass hardening: a future visibility-surfacing behavior change (e.g. `42883` instead of `42501`) SHOULD fail the test so it is reviewed, rather than being silently absorbed by a loosened `42501 OR 42883` matcher. Deliberately NOT loosened. Re-open only if a real Postgres/PostgREST upgrade actually changes the surfaced code AND that change is reviewed-and-accepted as benign; the fix then is to widen the accepted set WITH a dated justification, not pre-emptively.

## Deferred from: code review of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (iter 2) (2026-06-29)

- **[Low] H4 introspection still has coverage gaps beyond direct-`tenant_id` public base tables.** The broadened `introspectTenantOwnedTables` (iter-1) still: filters `relkind in ('r','p')` so VIEWS / MATERIALIZED VIEWS exposing tenant rows are never introspected; only resolves FKs whose `confrelid` is `public.tenants`, so a table tenant-scoped transitively (FK to `tenant_memberships` or any non-`tenants` tenant-owned parent) escapes all three signals; returns BARE table names, so a non-`public` table sharing a name with an enrolled `public` table collapses and the unenrolled one escapes; treats any `supabase_`/`pgsodium`/`pg_`-prefixed schema as system (masking a hypothetical app schema with a system-looking prefix); and drops a real non-`public` table literally named `tenants` via `nspname !== "public"` (intended to exclude `_realtime.tenants`, but over-broad vs an `isSystemSchema` guard). None bites in Phase A — all three tenant-owned tables are `public` base tables with a direct `tenant_id` — so this is forward-looking gate-DX hardening, the same track as the iter-1 deferred naming-bound concern. Owner: a future gate-hardening pass — revisit when the first view-shaped, transitively-scoped, partitioned, or non-`public` tenant table is proposed. [tests/integration/rls/tenant-table-inventory.ts:1306]
  - **DISPOSITION (2026-06-29, epic-2 hardening) — G-8b: SPECULATIVE, STAYS OPEN (deliberate non-action, NOT resolved).** Per the consolidated design, the introspection is extended only WHEN the first such non-conforming tenant table is actually proposed; no current table is missed (all three Phase-A tenant tables are `public` base tables with a direct `tenant_id`, or the `tenants` root). Building view/transitive-FK/non-`public`-schema introspection now is over-engineering with nothing in the schema to test against, and the gate fails CLOSED on any unknown schema in the meantime. Left as-is on purpose. The current scope + the "extend when a non-conforming table lands" trigger is now made EXPLICIT in the `introspectTenantOwnedTables` module docstring. [tests/integration/rls/tenant-table-inventory.ts]

## Deferred from: code review of 3-1-tenant-owned-crm-data-model-and-commands (2026-06-30)

- **[Low] `validateUpdateCustomer` does not enforce identifier-by-type mutual exclusion (deferred to the DB CHECK).** `validateCreateCustomer` enforces the requiredness + mutual-exclusion rules (`private` ⇒ `personnummer` present and `org_nr` absent; non-`private` ⇒ the inverse), but `validateUpdateCustomer` only bounds `personnummer`/`org_nr` as optional text and explicitly defers type-consistency to the `customers_identifier_by_type` DB CHECK (`23514` → `VALIDATION_FAILED` via `throwMappedWriteError`). The update command cannot know the row's existing `customer_type` without a read, and a violating update is correctly rejected by the DB backstop with the right user-facing code — so no incorrect outcome is reachable, only a less-localized rejection. Introduced by this story's update-command design (not pre-existing), but correctly backstopped, so logged rather than patched. Owner: a future CRM command-hardening pass — add a read-then-validate (or partial-update-aware) mutual-exclusion check in `validateUpdateCustomer` so the command layer rejects an identifier/type mismatch before the DB CHECK. [src/server/commands/crm/validation.ts]

## Deferred from: code review of 3-2-crm-tenant-admin-ux-and-lifecycle-context (2026-06-30)

- **[Low] A contact's `is_primary` flag cannot be turned OFF via the edit dialog.** An unchecked HTML checkbox submits no form entry; `parseUpdateContactForm` reads it via `bool(form, "is_primary")` (returns `undefined` when absent) and only sets `input.is_primary` when `!== undefined`, so `buildContactPatch` skips it. A contact already flagged primary can never be un-flagged via edit — unchecking is a silent no-op. Genuine but minor: owner decided no enforced/exactly-one-primary rule, no data corruption, no security impact. Owner: a future CRM-UI edit-form pass — render a hidden `is_primary=false` companion (or have the update parser treat a present-but-unchecked checkbox as explicit `false`) so the edit form can clear the flag. [src/features/crm/form-parsing.ts:257]
- **[Low] A contact's facility binding cannot be CLEARED ("Ingen anläggning") via the edit dialog.** `ContactDialog.tsx` offers an `value=""` "Ingen anläggning" option, but `parseUpdateContactForm` reads `facility_id: field(form, "facility_id")` and `field()` returns `undefined` for an empty string, so `buildContactPatch` (skips undefined) never writes `facility_id = null`; an already-bound contact keeps its binding. The same omitted-vs-nulled shape applies to other cleared optional fields on update; facility unbind is the user-visible case. Genuine but minor: no data corruption, no security impact; a presented edit action is inert. Owner: a future CRM-UI edit-form pass — have the update parser map a present-but-blank optional field to an explicit `null` (or model "clear" distinctly from "unchanged") for facility/address fields on update. [src/features/crm/form-parsing.ts:251]

## Deferred from: code review of 3-3-company-identity-quote-terms-and-vat-defaults (2026-06-30)

- **[Low] `bpToPercentString` has a dead/identical ternary branch.** `Number.isInteger(percent) ? String(percent) : String(percent)` — both branches are identical, so the conditional is a no-op, and the doc comment ("Trim a trailing '.00'") overstates the logic. Output is functionally correct (`String(percent)` already drops a trailing `.00` for integers), so it is not an AC violation — a trivial cleanup of unfinished intent. Owner: a future settings-helper cleanup pass — collapse to `String(percent)` (or implement the intended trimming) and align the comment. [src/features/settings/vat-display.ts:1124-1128]
- **[Low] No automated audit-row assertion for the `approveQuoteTerms` (`quote_terms.approved`) path.** The company-settings and `updateQuoteTerms` "single audit row, no PII in metadata" assertions exist and are strong, but there is no equivalent "exactly ONE audit row with clean `{ targetId }` metadata" assertion for the approve event. The `auditFields` is `(ctx) => ({ targetId: ctx.input.id })` — clean by construction — so the risk is Low; the gap is test coverage, not a code defect. Owner: a future settings-test pass — add an approve-path audit-row count/shape assertion mirroring the `updateQuoteTerms` no-PII test. [src/server/commands/settings/quote-terms.ts:1370]
- **[Med] Approved-status UI shows a static "ägare/juridik" label, not the real `approved_by` user.** AC2 asks for "who + when"; the "when" + persisted attribution are correct, only the *display* of the "who" is generic. Deferred (auto-resolved from a Tier-A `[Review][Decision]` with recommended `defer`, epic mode): resolving the id to a name needs a user-display read surface out of this story's scope (the read path never touches `auth.users`). Owner: wire the real `approved_by` name when a user-display read lands (e.g. the same owner pass that resolves the exact PDF-identity fields). [src/components/settings/QuoteTermsEditor.tsx:484-488]
- **[Low] `default_vat_display` enum drops the "private → always incl-VAT" half to a code invariant rather than a stored setting.** Diverges from AC5's literal "full rule encoded as the setting"; private-side enforcement is Epic 4's. Deferred (auto-resolved from a Tier-A `[Review][Decision]` with recommended `defer`, epic mode): the precise enum value-set + labels are owner-gated (story Open Question 3) — confirm at the Epic-4 working session; conservative encoding stands until then. [src/server/commands/settings/validation.ts:1483]

## Deferred from: code review of 3-4-work-roles-and-optional-manual-articles (2026-06-30)

- **[Low] No UI reactivate control for an archived work_role/article.** The read layer hides archived rows but the upsert UPDATE path cannot reactivate one from the UI. `readWorkRoles`/`readArticles` filter `.eq("is_active", true)`, so archived rows never appear in the editor lists, and neither editor (`WorkRolesEditor`/`ArticlesEditor`) renders a reactivate control — only an "Arkivera" button. The command/data model CAN reactivate (archive is reversible via UPDATE flipping `is_active`), but no UI surfaces an archived row to reactivate it. AC2's "reactivate" verb is a capability of the data model, not a UI this story ships; the Tasks treat the `is_active` toggle as sufficient and the e2e does not test reactivation — within the documented MINIMAL scope. Genuine but minor: no data corruption, no security impact; archived rows are simply not re-surfaceable from the UI. Owner: a future pricing-UI pass — add an "include archived" view (lift the `is_active=true` read filter behind a toggle) + a reactivate action that UPDATEs `is_active` back to true. [src/features/pricing/read.ts; src/components/pricing/WorkRolesEditor.tsx; src/components/pricing/ArticlesEditor.tsx]
- **[Low] Articles-IN-scope is an owner assumption (Open Question 1), not an independently re-confirmed decision.** The diff builds the full `articles` table/command/UI per `owner-decisions-applied-2026-06-18.md#Story 3.4` ("reusable article/material register IS IN scope — owner: yes"); Story 3.5's snapshot contract + the epic test design's R-007 both depend on articles. Deferred (auto-resolved from a Tier-A `[Review][Decision]` with recommended `defer`, epic mode): re-confirm the articles-IN decision still stands at PR review. The `articles` slice is a clean, additive, cleanly-removable layer if the owner reverses; the deliverable-completing default stands until then. Owner: PR-review confirmation. [supabase/migrations/20260630140000_work_roles_and_articles.sql; src/server/commands/pricing/articles.ts]

## Deferred from: epic review of epic-3 (2026-06-30)

- **[Med] Snapshot `company_settings` variant omits the PDF-identity fields Epic 6 will need.** The `CompanySettingsSnapshot` variant + resolver SELECT capture only `companyName` (plus `vat_rate_bp`/`default_vat_display`), silently dropping `org_nr`, `address_line1`/`address_line2`, `postal_code`, `city`, `email`, `phone`, `logo_url` that a Swedish quote PDF legally/commercially needs. This is a deliberate, documented scope decision (Story 3.5 Open Question 1 — "kept minimal here"; full PDF identity is Epic 6's quote-version concern), so it is not an Epic 3 defect. Risk: a naive Epic 5/6 implementer treating this `SnapshotSource` as the COMPLETE `company_settings` interface would silently drop identity from the frozen quote-version snapshot. Owner: Epic 6 (quote-version PDF identity) — either extend the variant + resolver columns when the PDF snapshot lands, or add a type-level marker on `CompanySettingsSnapshot` documenting it is intentionally identity-partial. [src/lib/snapshots/types.ts:5298-5306; src/server/snapshots/resolve-source.ts:7382-7387]
- **[Med] No compile/runtime marker forces a future Epic 6 "mark sent" command to check terms `approvedAt` before send.** `QuoteTermsSnapshot.approvedAt` faithfully captures `approved_at` verbatim (NULL = not-approved) and `updateQuoteTerms` always resets it to null on edit — the capture is correct. But the mechanism that stops an unapproved-terms quote from being SENT is entirely prose (comments + Dev Notes); the shared contract carries no exported `assertTermsApproved(snapshot)` helper, no `requiresSignOff: true` marker, nothing that makes an Epic 6 "send" path fail-closed if it forgets the `approvedAt !== null` check. This is a legitimate, deliberate scope boundary (Epic 6 owns enforcement), recorded so the epic-boundary trace treats it as an explicit Epic 6 obligation, not a silent gap. Owner: Epic 6 (quote send/immutability) — carry a blocking-state marker on the terms snapshot or an exported approval-gate helper the send command must call. [src/lib/snapshots/types.ts:5316-5322; src/lib/snapshots/build.ts]
- **[Low] Empty-patch UPDATE can produce a false `TENANT_ACCESS_DENIED` on an id-only CRM update.** `updateCustomer`/`updateFacility`/`updateContact` build the patch from only the supplied fields and issue `.update(patch)` with no empty-patch guard; an id-only update (validators accept an id-only payload) sends `.update({})`. Depending on PostgREST behavior this can return zero rows, which the command maps to `TENANT_ACCESS_DENIED` even though the row is owned and visible — a false authorization denial for a legitimate (if pointless) request. Genuine but minor: the UI submits all fields, so it is not reachable from the current forms; no security/data impact. Owner: a future CRM-command hardening pass — short-circuit an empty patch (return the target id unchanged) rather than issuing `.update({})`. [src/server/commands/crm/customers.ts:5699-5711; src/server/commands/crm/facilities.ts:5842; src/server/commands/crm/contacts.ts:5479]
- **[Low] Personnummer is present in clear text in the client-component payload (mask is cosmetic).** `CustomerDetail.tsx` (`"use client"`) receives `customer.personnummer` raw; `maskPersonnummer` + the "Visa" toggle only govern what is painted, so the raw value is serialized into the client props payload on every private-customer detail view regardless of reveal. Admin-only, own-tenant (RLS + `(app)` route auth); the cross-story security pass cleared it as an owner-approved, access-controlled reveal surface (0 findings) — NOT a cross-tenant/unauthorized exposure. Deferred (auto-resolved from an epic Tier-B `[Review][Decision]`, epic mode — deferred rather than dismissed for PII stewardship): treat a stricter server-gated reveal (raw pnr never in the payload until explicitly fetched) as an Epic 6 privacy-hardening item. [src/components/crm/CustomerDetail.tsx]
- **[Low] Epic-3 test-design "no personnummer" P0/exit-criteria contradicts the shipped, owner-approved schema.** The test design (R-009 + Exit Criteria "no personnummer field by default") asserts personnummer absence as a hard gate, but `customers` carries a `personnummer` column per the 2026-06-18 owner decision (list excludes it, detail masks it, audit drops it, identifier-by-type CHECK ties it to `private` only). The divergence lives only in migration prose. Deferred (auto-resolved from an epic Tier-B `[Review][Decision]`, epic mode): reconcile the test-design / exit-criteria text to the 2026-06-18 owner decision — no code change — so a future epic trace does not FAIL on the stale R-009 assertion. [_bmad-output/test-artifacts/test-design-epic-3.md]

## Deferred from: epic review of epic-3 iter 2 (2026-06-30)

- **[Low] Stale pricing lifecycle-error banner can persist across a later successful DIFFERENT action.** `archiveState`/`reactivateState` are independent `useActionState` hooks in each editor; after a failed archive paints the `*-archive-error` banner, a subsequent SUCCESSFUL reactivate (or vice-versa) does not clear the other action's error state, so the stale red banner lingers until that exact action re-runs and succeeds. Minor UX residual, realistic two-step trigger; not a data/security issue. Owner: a future pricing-UI polish pass — clear/supersede the lifecycle banner on any successful lifecycle action (prefer the most-recently-submitted action's state). [src/components/pricing/WorkRolesEditor.tsx:58-63; src/components/pricing/ArticlesEditor.tsx:106-111]
- **[Low] A no-op reactivate of an ALREADY-active pricing row writes a spurious `*.reactivated` audit row.** `reactivateWorkRole`/`reactivateArticle` do `.update({is_active:true}).eq("id",…).select("id")` with NO `.eq("is_active", false)` filter, so reactivating a row that is already active (stale archived list rendered in a concurrent tab) matches by id, returns the row (not zero → no denial), and appends a redundant reactivate audit row. Benign (append-only, correct tenant, allow-listed `{targetId}` metadata) but audit-noisy. Realistic trigger (two tabs). Owner: a future pricing-command hardening pass — add `.eq("is_active", false)` to the reactivate UPDATE so a no-op reactivate is a clean zero-write, or accept the idempotent audit row as designed. [src/server/commands/pricing/work-roles.ts:146-159; src/server/commands/pricing/articles.ts:129-141]
- **[Low] Reactivate E2E derives its unique role name from `Date.now().toString(36)` (weaker than the integration suites' `crypto.randomUUID()`).** Two parallel Playwright workers (or fast reruns) hitting the same millisecond produce identical names, and the test locates rows by `hasText: roleName` + asserts `toHaveCount(1)` — a flake risk. Test-quality only, no product impact. Owner: a test-hardening pass — switch the E2E name key to `crypto.randomUUID()` to match the integration discipline. [tests/e2e/pricing/pricing.e2e.spec.ts:~647]
- **[Low] No component/E2E test exercises a FAILING archive/reactivate to prove the `*-archive-error` banner + "Försök igen." retry hint render.** The banner logic and the command-layer failure→state mapping (TENANT_ACCESS_DENIED / SERVER_ERROR) are correct + integration-tested, but neither the E2E nor a component test drives a lifecycle-action FAILURE to assert the banner actually paints. Coverage gap only (straightforward `status === "error"` render). Owner: a test-hardening pass — add a component/E2E case that forces an archive/reactivate error and asserts the `data-testid="{work-role|article}-archive-error"` `role="alert"` node (and the retryable suffix on `SERVER_ERROR`). [src/components/pricing/{WorkRolesEditor,ArticlesEditor}.tsx; tests/e2e/pricing/pricing.e2e.spec.ts]
- **[Low] An archived-read failure gates the ENTIRE pricing page (introduced by the reactivate fix).** `page.tsx` folds `archivedWorkRolesError ?? archivedArticlesError` into the same top-level `error` that renders the page-level banner and suppresses the primary editors, so a transient failure loading the OPTIONAL archived lists degrades the whole view even when the active lists loaded fine. Archived is a secondary affordance behind a toggle; its read failure should not gate the main UI. Realistic trigger (a transient DB blip on one of the four now-parallel reads). Deferred (auto-resolved from an epic Tier-B iter-2 `[Review][Decision]`, epic mode — recommended defer/fix-small): scope the archived-read errors to a banner INSIDE the archived section (or render zero archived rows) and leave the page-level `error` to the two active reads only. Owner: a future pricing-UI polish pass. [src/app/(app)/settings/pricing/page.tsx:35-39]

## Deferred from: code review of 4-3-rot-and-gron-teknik-estimate-engine-with-warnings (2026-07-02)

- **[Med] `persons` input captured but NOT applied to the per-person ROT cap.** `estimateDeduction` clamps to a single flat `capOre`; `persons` is validated + snapshotted but never scales the cap, so a 3-person ROT job gets the same ceiling as a 1-person job, with no warning that the per-person multiplier is unmodelled. Deferred (auto-resolved from a Tier-A `[Review][Decision]`, epic mode — triage recommended defer): the per-person multiplier is owner-gated under Sign-Off Q3 (already surfaced for the epic gate); the story sanctions the flat conservative placeholder + UNAPPROVED warning. Owner: the Sign-Off Q3 working session — implement the multiplier (or confirm flat) when the owner pins ROT rate/cap/basis. [src/lib/money/tax.ts]
  _(The other 4.3 Decision — standalone `buildTaxAssumptionSnapshot` rate guard — was auto-dismissed as intentional per Task 3.2's state-capture-only builder contract; 3 further findings dismissed as noise.)_

## Deferred from: epic review of epic-4 (2026-07-02)

- **[Med] `persons` captured/snapshotted across the epic but NOT applied to the per-person ROT cap.** `estimateDeduction` clamps to a single flat `capOre`; `persons` is validated and frozen into the assumption snapshot (and passed by the 4.4 golden fixtures) but never multiplies the cap, so a 3-person ROT job gets the identical `5_000_000` öre ceiling as a 1-person job, with no warning that the per-person multiplier is unmodelled. Across the assembled epic surface `persons` threads input → engine → snapshot → golden fixture as a fully-plumbed but behaviorally inert field a reader could mistake for an implemented per-person cap. Deferred (re-surfaced at the epic gate from the Story 4.3 `[Review][Defer][Med]`): the per-person multiplier is owner-gated under Sign-Off Q3; the story sanctions the flat conservative placeholder + UNAPPROVED warning and the 4.4 spec forbids fabricating the multiplier. Owner: the Sign-Off Q3 working session — implement the multiplier (or confirm flat) when the owner pins ROT rate/cap/basis. [src/lib/money/tax.ts]
- **[Med] `company_togglable` default `primaryOre` is GROSS (incl-VAT) for a company customer under a nominally excl-first setting.** The selector resolves `company_togglable` with `primaryOre: grossOre` as the default surfaced amount; AC2 is satisfied (net/VAT/gross all representable), but incl-VAT-as-default for a company customer is a presentation-policy choice that Sign-Off Q2 leaves owner-pending, and the module header documents only the private→always-incl invariant as the Q2 assumption. Deferred (auto-resolved from an epic Tier-B `[Review][Decision][Med]`, epic mode — triage recommended defer): the conservative build stands; confirm the intended company-togglable default at the Epic-4 VAT-display (Sign-Off Q2) working session and add the explicit sign-off line to the module header then. [src/lib/money/vat.ts:1120-1121]

### Iteration 2 (2026-07-02)

- **[Low] ORGNR `/\b\d{10}\b/` pack-wide privacy scan will false-positive on a future ≥10-digit öre/cap fixture value.** The iter-1 ORGNR distinct-shape fix added a bare 10-digit no-dash run to the pack-wide privacy scan; today no money fixture emits a bare 10-digit number (`capOre: 5_000_000` is 7 digits) so the scan is clean, but a later epic-4 fixture with a cap/total ≥ 1,000,000,000 öre (10 digits) would trip it as a spurious "orgnr PII leak" with no signal distinguishing it from a real orgnr. Not a current defect; a latent maintainability trap. Owner: a future fixture/golden-pack hardening pass — scope the orgnr scan to string-typed leaves (or exclude the known numeric öre/rate/cap keys) so a legitimate large öre value cannot trip the PII guard. [tests/unit/lib/money/golden-pack.test.ts]
- **[Med] Three hand-forked closed unions in `src/lib/money` have no parity/drift test against the server-side source enums.** `VatDisplayMode`, the customer-eligibility posture union, and `DeductionType` are deliberately copied into `src/lib/money` to avoid a `src/lib` → `src/server` layer inversion, but nothing ties them to `CustomerType` / the settings `default_vat_display` set — the module comments assert "identical member set" / "mirrors CRM one-for-one" as fact while no test enforces it, so a future fifth customer type or third VAT-display mode added to the server enum would silently drift and the engine would begin mis-eligibility-classifying (a money-correctness risk). Deferred, not-now: the fork itself is the sanctioned layer-inversion-avoidance choice and no drift exists today. Owner: a future money-engine hardening pass — add a compile-time or unit parity assertion (e.g. an exhaustive mapping the server enum must satisfy) tying each `src/lib/money` union to its server source so a drift fails loud. [src/lib/money/vat.ts; src/lib/money/tax.ts]
- **[Low] `buildTaxAssumptionSnapshot`/`buildVatAssumptionSnapshot` use "absence of an `ok` field" as their success-vs-failure discriminant.** Callers narrow via `"ok" in result`, which works only because the frozen snapshot types happen never to declare an `ok` field — unlike every other engine result (`OreResult`/`VatBreakdownResult`/`DeductionResult`) which use a clean `{ok:true,…} | {ok:false,…}` shape. A future field addition (or a caller spreading another result into a snapshot-like object) would silently break the discriminant with no compiler error. Not a live bug (the only failure branch is the now-guarded `capturedAt`). Owner: a future money-engine cleanup pass — normalize the two builders to the same discriminated `{ok:true|false}` shape as the rest of the engine when the snapshot surface is next touched. [src/lib/money/tax.ts; src/lib/money/vat.ts]
- **[Low] Golden-master surface-present `describe.skip` gates (`TAX_SURFACE_PRESENT`/`VAT_SURFACE_PRESENT`) were not removed after the engine landed, so the golden suite can silently self-disable.** If a future refactor accidentally drops a `@/lib/money` export, the surface-present precondition `describe.skip`s the entire golden pin instead of failing — a golden test that can silently disable itself. The gate comments say "delete the gate when green" but the gates persist. Owner: a future test-hardening pass — now that the surface exists, convert the surface-present precondition to a hard assertion (fail if the export is absent) rather than a `describe.skip`. [tests/unit/lib/money/tax.golden.test.ts; tests/unit/lib/money/vat.golden.test.ts]
- **[Low] `alias-hook.mjs` test-resolver directory/`statSync` robustness.** The directory-resolution branch returns the bare directory path when neither `index.ts` nor `index.tsx` exists (dead-ends into an opaque Node `ERR_UNSUPPORTED_DIR_IMPORT` instead of falling through to the sibling `${absPath}.ts` fallback), and `statSync(absPath)` after `existsSync` is an unguarded TOCTOU pair with no try/catch, so a transient FS condition (permission/broken symlink) throws and aborts the whole `node --test` run rather than falling back. Test-support-only, no production path; realistic only in a future directory-import edge. Owner: a future test-infra hardening pass — restore the sibling-`.ts` fallback for the no-index directory branch and wrap `statSync` in try/catch to fall back rather than throw. [tests/support/alias-hook.mjs]
- **[Low] 4.4 golden-pack coverage MANIFEST asserts each AC1 category by a raw substring token, so a category can report "covered" on an incidental prose match.** `raw.includes(token)` on the JSON-stringified fixture means several categories (`caps → "capHit"`, `hidden-rows → "hidden-row"`, `rounding → "halfRoundingMode"`, `fractional → "fractionalQuantityChainCase"`) are satisfied by the token existing anywhere in the file — including inside a `note`/`_doc` prose field — rather than proving a real case is pinned and live-driven; four of the ten categories are only substring-checked here and not behaviorally re-driven by the pack's own live-oracle tests. Weaker than 4.4-GOLDEN-01's "the pack COVERS every category" intent. Owner: a future golden-pack hardening pass — assert a behavioral case is pinned per category (drive a value through the engine), not merely a token present. [tests/unit/lib/money/golden-pack.test.ts]

## Deferred from: code review of 5-1-tenant-owned-calculation-schema-and-server-commands (2026-07-03)

- **[Low] Migration does not defensively VERIFY the customers/facilities `_id_tenant_unique` prerequisites exist before referencing them.** `20260702120000_calculation_data_model.sql` additively adds `contacts_id_tenant_unique` and then creates composite FKs to `customers (id, tenant_id)` / `facilities (id, tenant_id)` relying on the Story 3.1 uniques existing, with no defensive existence guard (e.g. a `do $$ ... raise exception if not exists ...`) as Task 1.2's "VERIFY the customers/facilities constraints exist before referencing them" directs. The reset passes today (the 3.1 constraints exist and those migrations are frozen), so this is an instruction-compliance/robustness gap, not a current break — if the 3.1 constraints were ever renamed/dropped the FK creation would fail opaquely rather than with a clear guard. Owner: a future migration-hardening pass — add an explicit existence guard for the referenced composite uniques before the FK creation. [supabase/migrations/20260702120000_calculation_data_model.sql]
- **[Low] Cross-tenant child id smuggled into an owned parent's reorder surfaces as `VALIDATION_FAILED` rather than `TENANT_ACCESS_DENIED`.** The reorder RPCs raise `check_violation` (23514) when any supplied id is not a same-parent child visible under the caller's RLS, and `throwMappedReorderError` maps 23514 → VALIDATION_FAILED. So a Tenant-A caller who owns the parent section but supplies a Tenant-B row id (invisible under A's RLS) gets a count-mismatch 23514 → VALIDATION_FAILED, whereas the isolation model (5.1-INT-02, R-502) frames cross-tenant references as `TENANT_ACCESS_DENIED`. Mild deviation only: the parent-ownership gate already blocks foreign parents, the whole reorder rolls back (no data leak/partial order), and the security review cleared isolation — it is a "bad request" vs "access denied" code nicety, not an isolation breach. No test asserts the cross-tenant-child-id reorder case (the rollback test uses a random non-existent id, genuinely VALIDATION_FAILED). Owner: a future error-mapping pass — distinguish a foreign-but-existing child id (map to TENANT_ACCESS_DENIED) from a genuinely bad payload (VALIDATION_FAILED), plus a test for the smuggled-foreign-child-id case. [src/server/commands/calculations/calc-db.ts:122]
- **[Low] No integration test proves the empty-patch guard's command-level acceptance behavior.** The empty-patch guard (`if (Object.keys(patch).length === 0) return { targetId: ctx.input.id };`) exists in all three update commands and the validator accepts an id-only patch (unit-tested), but no DB-backed test in `calculation-commands.int.test.ts` runs an id-only `runCommand(updateCalculation, …)` to prove it returns `ok` with the id and does NOT produce a false `TENANT_ACCESS_DENIED` — the exact failure mode the guard (the inherited epic-3 deferral fix, Task 3.5) was added to prevent. The guard is correct by inspection; this is a coverage gap only. Owner: a test-hardening pass — add an id-only update integration case per calc update command asserting `ok` + the returned id and no `.update({})`/false denial. [tests/integration/commands/calculation-commands.int.test.ts]
- **[Low] Migration-reset money-column guard keyed on the `%_ore` name suffix misses a smuggled non-`_ore` float money column.** `calc-tables-migration-reset.int.test.ts` asserts every `%_ore` column is `bigint`, but keying the check on the `_ore` suffix means a future/accidental float money column named without `_ore` (e.g. `unit_price`, `amount`) would pass — narrower than AC7's blanket "NO numeric/double/real money field anywhere" (5.1-INT-01). The current schema is compliant (only `unit_cost_ore`/`unit_sell_ore` are money, both bigint; `quantity numeric` is correctly non-money), so this is a test-robustness gap, not a current schema violation, and the column-name scope-guard already constrains the actual schema. Owner: a future test-hardening pass — assert the absence of any `numeric`/`double precision`/`real` column that is NOT the sanctioned `quantity` (or an explicit money-column allow-list check), not merely that `%_ore` columns are bigint. [tests/integration/rls/calc-tables-migration-reset.int.test.ts]

## Deferred from: code review of 5-2-calculation-editor-ux-for-sections-and-rows (2026-07-03)

- **[Low] `is_selected` read type is `boolean | null` but `FlagField` requires `boolean` (fed `?? false`).** `read.ts` types `is_selected: boolean | null` on `CalculationRowRow` but the row checkbox collapses a stored `null` to unchecked/`false`, flattening the persisted tri-state (`null` = "never a selectable option") to `false` on the next save. Low impact: `rowCountsTowardTotal` treats `null` and `false` identically for non-optional rows (unit-pinned), so no displayed total is wrong — but the read type advertises a null the editor cannot represent, a latent 1:1-mapping inconsistency. Owner: a future editor-polish/type-reconciliation pass — either narrow the read type to `boolean` (coalesce at the read boundary) or teach `FlagField` a null/indeterminate state. [src/components/calculations/RowEditor.tsx:804-806; src/features/calculations/read.ts]

## Deferred from: code review of 5-3-pricing-source-selection-and-row-snapshots (2026-07-03)

- **[Low] Pricing-source read error degrades silently to an empty selection list, with no user-visible signal.** The calc editor page reads the ACTIVE source lists via `readWorkRoles()`/`readArticles()` in parallel and drops their `.error` on the floor (`page.tsx:37-38`); those readers return `{ workRoles: [], error: GENERIC_READ_ERROR }` on a transient failure (`src/features/pricing/read.ts:55-60,115-120`). So a transient pricing-read outage presents as "no sources to select" (manual-only) with no error surfaced to the admin. Violates no AC — this is a deliberate graceful-degradation choice and manual rows stay fully supported (AC1/AC6); flagged only as a UX/observability gap, and no E2E asserts it (5.3-E2E-01/02 only check name + `/Källa/` presence). Owner: a future editor-observability pass — surface a non-blocking "sources temporarily unavailable" hint when a source read errors (mirror the disclose-don't-hard-fail posture), rather than an indistinguishable empty list. [src/app/(app)/calculations/[calculationId]/page.tsx:37-38; src/features/pricing/read.ts:55-60,115-120]

## Deferred from: implementation of 5-4-calculation-readiness-review-and-snapshot-preview (2026-07-03)

- **[Documented deferral] Required-file readiness check is a placeholder pending Story 8.1 (file-metadata foundation) (R-513, 5.4-DOCS-02).** The readiness classifier surfaces a `REQUIRED_FILES_DEFERRED` WARNING that says required-file checking is not yet available and is done manually until the file foundation lands — it builds NO file-metadata read/upload UX (Epic 8). Surfaced in the readiness output (never silently omitted) rather than a real check. Owner: Story 8.1 / wire the real required-file check into `readiness.ts` when 8.1 lands. [src/features/calculations/readiness.ts (REQUIRED_FILES_DEFERRED); test-design-epic-5.md#R-513]
- **[Documented deferral] Low-margin threshold is a named CONSTANT, not a per-tenant setting (Open Question 1).** The 2026-06-18 owner decision says "margin warning = TB% vs a global threshold in settings," but NO `company_settings.margin_threshold` column exists (adding one is a GATED migration + a settings-UI expansion, beyond 5.4's readiness-classifier scope). 5.4 uses a documented UNAPPROVED pilot constant (`PILOT_LOW_MARGIN_THRESHOLD = 0.15` in `readiness.ts`) for the SOFT low-margin warning; the configurable per-tenant setting is deferred. Owner: Story 5.4 follow-up / margin-threshold-setting-when-approved (a gated migration + settings editor). [src/features/calculations/readiness.ts (PILOT_LOW_MARGIN_THRESHOLD); owner-decisions-applied-2026-06-18.md#Epic 5·5.4/5.5]
- **[Awareness, forward-seam] The calc tax context feeds `hasDeductionAssumption: false` today (no persisted ROT/grön assumption on a calc).** No calc schema field carries a ROT/grön deduction assumption (Epic 6 owns quote-version tax assumptions), so the editor feeds the classifier `hasDeductionAssumption: false`. The classifier's tax path (the `TAX_SIGN_OFF_REQUIRED` estimate/non-final framing) is fully UNIT-pinned so Epic 6 can flip it on by feeding a real assumption — no code-shape change. `persons` stays a flat-cap placeholder (R-512); the readiness message NEVER implies a per-person-scaled cap. Owner: Epic 6 (feed the calc/quote-version deduction assumption into the classifier). [src/components/calculations/CalculationEditor.tsx (tax context wiring); src/features/calculations/readiness.ts; test-design-epic-5.md#R-512]

## Deferred from: code review of 5-4-calculation-readiness-review-and-snapshot-preview (2026-07-03)

- **[Low] Low-margin threshold ships as an UNAPPROVED hard-coded constant rather than the owner-decided "global threshold in settings".** AC1 low-margin condition + owner-decisions-applied-2026-06-18 ("margin warning = TB% vs a global threshold in settings"). `readiness.ts` uses `PILOT_LOW_MARGIN_THRESHOLD = 0.15` instead of a `company_settings`-configurable value. Explicitly sanctioned by Open Question 1 (no margin column exists; adding one is a gated migration; conservative default = a documented pilot constant + deferral) and already recorded as a deferral (see the implementation deferral above). Surfaced by the code review only so the acceptance record notes the owner's "in settings" wording is not yet met. Owner: a follow-up settings-column story (`company_settings.margin_threshold_bp` + editor). [src/features/calculations/readiness.ts (PILOT_LOW_MARGIN_THRESHOLD); owner-decisions-applied-2026-06-18.md#Epic 5·5.4/5.5]

## Deferred from: code review of 5-5-calculation-golden-tests-for-options-hidden-rows-and-tax-warnings (2026-07-03)

- **[Med] `documented-delta`/`old-lovable` LABELLING guard requires a NUMERIC `oldLovableWouldGive` — cannot express a non-numeric (classification/section-mode) divergence.** The pack's LABELLING guard asserts `typeof c.oldLovableWouldGive === "number"` for any `documented-delta`/`old-lovable` case, but several AC1 categories the pack owns are classification oracles, not öre oracles (margins → LOW_MARGIN/ZERO_PRICE_ROW; attachment-readiness → REQUIRED_FILES_DEFERRED; VAT display → togglable/primary posture). Does NOT violate AC2 for what ships today — every case is a numeric-or-classification `new-expected`, no `documented-delta`/`old-lovable` exists (no anonymized Lovable oracle was available, and fabricating one is forbidden per AGENTS.md). If Epic 9 later captures a Lovable divergence in a non-numeric category, the guard would force it to a bare number or fail the schema. Owner: Epic 9 (Lovable golden-master harness) — widen the guard to accept a non-numeric divergent value (e.g. a `oldLovableWouldGive` union of number | classification-code) when the first non-numeric delta is captured. [tests/unit/features/calculations/calc-golden-pack.test.ts:924]
- **[Low] Coverage REFERENCE_MANIFEST proves the 6 reference categories by a raw `raw.includes(entry.token)` substring match (residual substring-manifest weakness).** A reference token (`gron_teknik`, `halfRoundingMode`, `inclusionCases`, …) could match incidental prose in a large fixture rather than a real behavioral pin. Spec-permitted (Dev Notes line 75: reference-by-token is acceptable for a category whose numbers another fixture already owns, and that fixture's own live-oracle test proves the behavior); the NEW half of the manifest uses a proper `category`-field match against live cases, and ROT/grön are additionally proven behaviorally via live `estimateDeduction`. This is the SAME weakness already ledgered as deferred-work.md#epic-4-Iteration-2 (4.4 golden-pack coverage MANIFEST substring-token) — honored-not-reopened by this tests-only story per its task 4.4. Owner: the standing future golden-pack hardening pass (epic-4 Iter-2) — scope the reference manifest to a structured per-category key match rather than a raw file substring. [tests/unit/features/calculations/calc-golden-pack.test.ts:889; deferred-work.md#epic-4-Iteration-2]

## Deferred from: epic review of epic-5 iter 2 (2026-07-03)

- **[Low] `archiveCalculation` has no already-archived guard — a re-archive silently overwrites the original `archived_at` soft-delete instant.** `archiveCalculation.execute` (`src/server/commands/calculations/calculations.ts:160-171`) unconditionally does `.update({ archived_at: clock.now(), status: "archived" }).eq("id", …)` with no load of the current status and no `archived_at is null` guard. A second `archiveCalculation` on an already-archived calc (a retry, a double-submit, or a crafted POST) matches by id, returns the row (not zero → no denial), and moves the soft-delete timestamp forward, corrupting the FIRST-archived instant. Distinct root cause from the iter-1 Story 5.1 Decision #2 (which was the OPPOSITE direction — a revive leaving `archived_at` set while status leaves `archived`, closed by the DB-authoritative state machine); nothing covers this re-archive-overwrites case. Own-tenant, audit-trail-cosmetic only (no data loss, no money/total impact, no cross-tenant reach), and NOT reachable from the UI (once archived, `readCalculationDetail` filters `archived_at is null` → the editor 404s, so there is no re-archive affordance). Mirrors the pre-accepted "idempotent no-op reactivate audit row" deferral class from epic-3. Owner: a future calc-command hardening pass — load the current status (or add `.is("archived_at", null)` to the archive UPDATE) so a re-archive is a clean no-op that preserves the original `archived_at`. [src/server/commands/calculations/calculations.ts:160-171]

_(The one other genuinely-new iter-2 finding — the Story 5.3 update-path `row_type ↔ source_kind` cross-check residual left open by iteration 1's Finding #3 fix — is an OPEN `[Review][Decision][Med]` in `epic-5-review-findings.md`, NOT deferred: it is a data-integrity/AC-contract gap with a concrete server-side fix (load the persisted `row_type` in `updateRow.execute` and re-run the cross-check). It is recorded here only as a pointer so this ledger is not read as the complete iter-2 outcome.)_

## Deferred from: epic review of epic-5 (2026-07-03)

- **[Low] Reorder RPCs and `nextSortOrder` do not filter `archived_at is null`, and the RPCs accept a partial-subset / empty reorder payload.** `reorder_calculation_rows`/`reorder_calculation_sections` verify each supplied id belongs to the parent (`v_matched = v_expected`) but never check that the supplied set is the COMPLETE set of the parent's (non-archived) children, and neither the RPCs nor `nextSortOrder` filter out archived rows — so an archived row can consume/collide on an ordinal, and a strict-subset reorder can leave the omitted rows with a stale `sort_order` colliding against the new 1..k ordinals. An empty array makes `v_expected = v_matched = 0` (equal) so the RPC succeeds as a no-op if invoked directly (the command-layer `isUuidArray` non-empty check is the only guard on the app path). Duplicate sort_order is pre-accepted as a "UX nicety, not a correctness invariant" per the story, and the command layer is the only production writer; flagged as a robustness/DB-backstop gap, not a live defect. Owner: a future reorder-hardening pass — add `where archived_at is null` to the RPC membership/count and the sort-order scan, and reject an incomplete/empty payload at the DB layer. [supabase/migrations/20260702120000_calculation_data_model.sql (reorder RPCs); src/server/commands/calculations/sort-order.ts (nextSortOrder)]
- **[Low] Editor reorder move-up/down submits an `ordered_*_ids` array computed at RENDER time with no pending-disable — a fast double-click sends a second reorder against the stale pre-move order (lost-update race).** Each section/row move button's hidden `ordered_*_ids` value is `moveUp(orderedIds, index).join(",")` computed during render from the current server order; there is no disabling of the reorder controls while a reorder is in flight, so a second click before `revalidatePath` re-renders recomputes against the STALE order and silently produces a wrong or no-op reorder. The server-owned atomic RPC prevents corruption, but the last write can lose an ordering the user intended. Owner: an editor-polish pass — disable the move controls (and other lifecycle buttons) while their action is pending. [src/components/calculations/CalculationEditor.tsx; src/components/calculations/SectionEditor.tsx]
- **[Low] Per-row and per-section total failures render nothing (no error UI) when `computeLineTotal`/`computeSectionTotal` returns a failure.** `RowEditor` gates the line total on `lineTotal && lineTotal.ok && lineDisplay` and `SectionEditor` gates the section total on `sectionTotal.ok`, so an öre overflow on a row silently removes that row's/section's total with no error indicator — inconsistent with the whole-calc `TotalsFailure` placeholder (`role="alert"`) added in 5.2. The failure is SAFE (no fabricated zero), just not actionable: for a large calc the admin cannot tell which row overflowed. Owner: an editor-polish pass — render a small `role="alert"` "Radsumman/Sektionssumman kunde inte beräknas" placeholder at the row/section level mirroring `TotalsFailure`. [src/components/calculations/RowEditor.tsx; src/components/calculations/SectionEditor.tsx]
- **[Low] `markupBpToPercentString` / `bpToPercentString` emit a DOT decimal while the editor uses the Swedish COMMA convention on input.** The round-trip display of a stored markup renders "12.5" (and VAT "25.5") with a dot via `String(bp/100)`, while `oreToKronorString` and the parse path use commas ("850,00", "25,5"); the parser accepts a dot after its own comma normalisation, so re-submitting round-trips, but the display/input locale mismatch is a UX inconsistency (and the unit tests pin the dot form). Cosmetic — corrupts no stored value. Owner: a money-input locale pass — format these two with the Swedish comma to match the input convention (and update the pinning tests). [src/features/calculations/money-input.ts (markupBpToPercentString / bpToPercentString)]
- **[Low] `parseQuantity` accepts a Swedish-comma decimal with no scale/magnitude bound.** Unlike the money parsers (which reject sub-öre/sub-bp precision), `parseQuantity` only checks that the value is finite and positive, so an absurd/high-precision quantity (a huge exponent, or many fractional digits) passes the client guard and is handed to the totals engine and the command as a raw float; its single comma-to-dot normalisation is accidentally-correct (a second comma yields a string the numeric parse rejects). The server `isQuantity` validator and the engine overflow check are the real backstops, so no bad value persists — but the client "surface near-misses as field errors" contract is applied unevenly (money/VAT/row_type get field errors, quantity does not). Owner: a form-parsing pass — add an explicit decimal-shape check plus a sane upper bound to `parseQuantity` so an out-of-range quantity gets a scoped field error client-side. [src/features/calculations/form-parsing.ts (parseQuantity)]
- **[Low] The calc editor page fans out five reads via `Promise.all` and checks only the first read's error; a persistent settings/terms read fault silently degrades every calc with no operator diagnostic.** `page.tsx` awaits `readCalculationDetail`/`readWorkRoles`/`readArticles`/`readCompanySettings`/`readQuoteTerms` in parallel but inspects the settings/terms read errors only to degrade gracefully (conservative VAT posture / "Inga offertvillkor angivna ännu"), with no surfaced count/diagnostic — so a transient DB fault renders identically to a genuine absence (telling the admin to add terms that may already exist) and downgrades a tenant's posture during an outage with no alerting. Deliberate disclose-don't-hard-fail posture (matches the 5.2/5.3 read pattern already ledgered); flagged as an observability gap only. Owner: a future editor-observability pass — surface a non-blocking "settings/terms temporarily unavailable" hint distinct from the genuine-absence copy when those reads error. [src/app/(app)/calculations/[calculationId]/page.tsx]
- **[Low] The pricing-source migration adds no `source_id` index and no DB CHECK enforcing the both-or-neither / kind↔columns source invariant.** `20260703120000_calculation_row_pricing_source.sql` adds the `source_*` columns with only per-column non-negative/`_ore` and `source_kind in ('work_role','article')` CHECKs; there is no both-or-neither CHECK on `(source_kind, source_id)` and no CHECK tying the companion columns to the kind (an `article` row could carry `source_cost_ore`, a `work_role` row a `source_sku`). The "never a half-set/half-cleared source" guarantee the story hinges on rests entirely on `validateSourcePair`/`attachSource` in application code; a non-command write (backfill, admin SQL, a future second command) can persist a half-set/mismatched source. There is also no index on the queryable `source_id`. No live defect (the command path is the only production writer and is correct) — a DB-backstop + performance gap. Owner: a future additive migration — add the both-or-neither (and, if cheap, the kind↔columns) CHECK and a `source_id` index. [supabase/migrations/20260703120000_calculation_row_pricing_source.sql]
- **[Low] Golden-pack tests carry structurally-unreachable machinery and grep-shaped coverage guards.** In `calc-golden-pack.test.ts`: (a) the `documented-delta`/`old-lovable` LABELLING branch (requires a numeric `oldLovableWouldGive`) is dead code — every fixture case is `origin: "new-expected"`, so no negative-path test proves the guard fires; (b) the coverage manifest hard-codes the total category count as a literal `18`, a self-referential magic constant derived from the very arrays it counts, that will red-flag on any legitimate category change and mislead the next author; (c) 6 reference categories are proven by a raw substring `includes(token)` match against the owning fixture file (a token can match incidental prose). All three are spec-permitted / already partially ledgered (the numeric-delta guard and the substring manifest are separately ledgered under the 5-5 code review and epic-4 Iter-2); recorded here at the epic level as residual test-rigor weaknesses. Owner: the standing golden-pack hardening pass — derive the manifest count from the arrays, replace substring-token coverage with a structured per-category key match, and add a negative-path guard test (or remove the dead delta branch until a real Lovable delta lands). [tests/unit/features/calculations/calc-golden-pack.test.ts]

## Deferred from: code review of 8-1-file-storage-foundation-private-bucket-metadata-links-rls-and-signed-access-command (2026-07-04)

- **[Low] `file.bucket_id || TENANT_FILES_BUCKET` truthy-OR fallback on a NOT NULL defaulted column.** Reachable only if `bucket_id` were an empty string (schema-forbidden), in which case signing would silently target the fallback bucket rather than surfacing a data-integrity problem. Defense-in-depth on an already-guarded value (NOT NULL default + `loadFileForAccess` type check); appears twice in `files.ts`. Owner: a future storage-hardening pass — either drop the dead fallback or use `?.trim() ? file.bucket_id : reject` so a corrupted row fails loud. [src/server/commands/files/files.ts:335,430]
- **[Low] `extractLinkId` shape mismatch throws a bare `Error` → `SERVER_ERROR` after a SUCCESSFUL commit.** A benign PostgREST envelope/shape change (or a future scalar return) turns a committed `create_file_with_link` write into a permanent-looking 500 with no compensation; no unit test exercises the array-vs-object branch. No realistic trigger today (the `returns table(...)` shape is fixed). Owner: a future test/robustness pass — add a unit test for `extractLinkId`'s branching and consider a stable envelope-shape assertion. [src/server/commands/files/files.ts:445-462]
- **[Low] `create_file_with_link` has no owner-existence re-check and `authenticated` retains a direct `file_links` INSERT grant.** Any authenticated caller can invoke the RPC (or PostgREST-insert `file_links`) directly and create a link to an arbitrary polymorphic `owner_id` (no DB FK) so long as `tenant_id` passes the own-tenant WITH CHECK. Security review traced this and found it adequately defended (own-tenant WITH CHECK holds; consistent with the mandated 5-epic-proven calc pattern; owner_id FK-less by design/R-802); the owner-existence boundary lives in the command layer by design. Accepted Phase-A posture, larger blast radius for `file_links` only because there is no compensating owner FK. Owner: the RBAC / command-bypass-hardening seam (Story 2.4+) if command-bypass hardening is ever pursued. [supabase/migrations/20260704120000_file_storage_foundation.sql]
- **[Low] No `storage.objects`/`files`/`file_links` DELETE policy → object bytes are never reclaimable through the app path; a tenant delete cascades METADATA but leaves orphaned private objects in the bucket forever.** The migration explicitly withholds object cleanup ("a later story") under the archive-over-delete discipline — an unbounded-growth / GDPR-delete retention gap not caused by a defect in this change. Owner: the storage retention/cleanup story (8.4+ or a dedicated lifecycle-reclamation story) — add an object-reclamation path (and reconcile tenant-delete object cleanup). [supabase/migrations/20260704120000_file_storage_foundation.sql]
- **[Low] Atomicity (R-807) proven only for a link-half CHECK violation (23514), not the FK/RLS failure modes the migration comment claims.** The single atomicity test drives an out-of-union `owner_type` (a CHECK violation) and asserts the files insert does not survive; the composite same-tenant FK rollback and a mid-flow RLS failure on the link insert are not separately exercised. The rollback ORDERING is genuinely proven by the CHECK path, so this is a coverage-completeness gap, not a hole. Owner: a future test-hardening pass — add FK-violation and RLS-violation rollback cases to the atomicity suite. [tests/integration/commands/file-link-ownership.int.test.ts]

<!-- iteration-2 review (2026-07-04): new deferrals below -->
- **[Low] `link_existing_file` never transitions `files.lifecycle_state` from `draft` to `linked`.** A file created `draft` (column default) and attached via `createFileLink` stays `draft` forever through 8.1's only attach path, contradicting the schema column-comment semantics ("linked = attached to an owner entity"). Not an 8.1 AC violation (no lifecycle-transition AC on link creation; `draft` is still access-eligible so AC5 is unaffected); the link path also does not gate on lifecycle (an archived/deleted file can be linked). Consumers must not infer link status from `lifecycle_state`. Owner: Story 8.2 (real upload+link path) / Epic 6.1 reuse — wire the `draft→linked` transition (and decide whether the link command should refuse archived/deleted files). [supabase/migrations/20260704120000_file_storage_foundation.sql — link_existing_file; src/server/commands/files/files.ts createFileLink]
- **[Low] Belt-and-braces bucket seed sets only `public = false`, omitting `file_size_limit`/`allowed_mime_types` (which live only in `supabase/config.toml`, 50MiB).** On a fresh reset against a stack where config.toml is NOT applied (a hosted/managed project rather than a local CLI reset — e.g. the demo push), the seeded bucket would have an unbounded file size limit and no MIME restriction, so the "belt-and-braces regardless of config-load order" claim guarantees privacy but not the size/MIME posture. Owner: a storage-hardening pass — set `file_size_limit`/`allowed_mime_types` on the seed insert (or document that the size/MIME cap depends on config.toml being applied). [supabase/migrations/20260704120000_file_storage_foundation.sql:94-96; supabase/config.toml]
- **[Low] `adminUploadStorageObject` test factory uses `upsert: true`, contradicting the fail-loud philosophy stated above it.** `upsert: true` suppresses the path-collision error that would otherwise reveal a test-isolation problem on a non-reset shared stack, inconsistent with the rest of the file (e.g. `adminInsertFile` throws on a missing RETURNING id). Owner: a test-factory hardening pass — drop `upsert` (or make it opt-in) so a colliding seed fails loudly. [tests/factories/tenants.ts — adminUploadStorageObject]
- **[Low] The storage-object list-isolation negative asserts a weak/possibly-vacuous property.** "Tenant A CANNOT list a Tenant-B object" only checks `names).not.toContain(...pop())` (the full-path basename) and `void error`s; `.list(prefix)` returns names relative to the prefix, so the basename comparison can pass even if RLS were broken, and swallowing `error` can yield a false green on an unrelated empty list. Owner: a test-hardening pass — assert an empty result set (or an explicit denial) rather than a basename `not.toContain`. [tests/integration/rls/storage-object-isolation.rls.test.ts]
- **[Low] No structured log/telemetry on the transient-vs-permanent storage signing decision or on `FILE_ACCESS_DENIED` denials.** `signed-access.ts`/`files.ts` re-throw a transient storage fault and map permanent denials to a generic user message with no structured logging of the security-relevant signing-denial event, so "RLS denied a cross-tenant attempt" is operationally indistinguishable from "storage was down." Owner: an observability pass once a logging seam exists — emit a structured, PII-free log line on the permanent/transient branch and on FILE_ACCESS_DENIED (never the signed URL). [src/server/storage/signed-access.ts; src/server/commands/files/files.ts]
- **[Low] `loadFileForAccess` returns `null` for BOTH "not visible (race)" AND "row shape invalid," and the caller maps both to `TENANT_ACCESS_DENIED`.** A data-integrity anomaly (a bad migration or a future-added lifecycle value failing `isFileLifecycleState`) manifests as a mysterious access-denied with no error signal rather than a loud SERVER_ERROR. Deny-closed is the safe posture and the trigger requires an out-of-union DB value. Owner: a robustness pass — distinguish "invalid row shape present" (throw → SERVER_ERROR / log) from "no row visible" (→ TENANT_ACCESS_DENIED). [src/server/commands/files/file-db.ts:56-67]
- **[Low] `file_links` has no uniqueness constraint on `(tenant_id, file_id, owner_type, owner_id, purpose)` and `createFileLink` does no existence check.** A double-submit or retry silently creates duplicate identical attachments (same file → same owner → same purpose). Whether uniqueness is desired interacts with the still-being-designed R-814 reuse semantics (6.1/6.3 may want find-or-create), so constraining now is premature. Owner: the Epic 6.1/6.3 reuse story — decide find-or-create vs a unique constraint when the reuse path lands. [supabase/migrations/20260704120000_file_storage_foundation.sql — file_links; src/server/commands/files/files.ts createFileLink]
- **[Low] `.env.example` under-documents the TTL clamp/fallback semantics.** The `SUPABASE_SIGNED_URL_TTL_SECONDS` comment mentions only the "safe default 300s if unset" case, not that a non-integer/negative/over-24h-ceiling value is also silently coerced to 300s (the Patch #4 behavior). An operator configuring a deliberately long TTL (e.g. `90000`) silently gets 300s with no visible contract. Owner: a docs pass — document the clamp/fallback (invalid/negative/over-ceiling → 300s; 24h max). [.env.example — SUPABASE_SIGNED_URL_TTL_SECONDS]

<!-- iteration-3 review (2026-07-05, user-extended final pass): new deferrals below -->
- **[Low] Explicit non-positive / over-ceiling `ttlSeconds` passed to `createSignedFileUrl` bypasses the `resolveSignedUrlTtlSeconds` clamp.** `const ttl = opts.ttlSeconds ?? resolveSignedUrlTtlSeconds()` uses `??`, so an explicit `0` (`0 ?? x → 0`), a negative, or an over-24h-ceiling value passed by a future in-process caller is handed straight to `createSignedUrl` with no clamp (a `0`/negative mints an instantly-expired URL; a huge value mints a near-permanent one — the R-806 hazard Patch #4 closed for the env path). No trigger today: the sole caller passes no `ttlSeconds`, so the already-clamped env-driven default is always used; the gap opens only when a future caller passes `ttlSeconds` explicitly. Owner: a storage-hardening pass — validate/clamp an explicit `ttlSeconds` too (`opts.ttlSeconds !== undefined && opts.ttlSeconds > 0 && opts.ttlSeconds <= MAX ? opts.ttlSeconds : resolveSignedUrlTtlSeconds()`). [src/server/storage/signed-access.ts:153]
- **[Low] `adminInsertFile` factory's `object_path` fallback is `??`-based, so an explicit empty-string `object_path: ""` is inserted verbatim.** The `??` fallback only triggers on undefined/null, so an explicit `""` bypasses the tenant-first `{tenant}/{uuid}/{name}` default and is inserted verbatim, breaking the tenant-first path convention the storage-RLS regex + isolation tests depend on for that seeded row. Only reachable if a future test passes an explicit `""` (no current caller does); same fail-loud family as the deferred `adminUploadStorageObject upsert:true` item. Owner: a test-factory hardening pass — treat an empty-string `object_path` as absent (`seed.object_path && seed.object_path.length > 0 ? … : default`). [tests/factories/tenants.ts — adminInsertFile]
- **[Low] The `storage.objects` RLS plane has no EXACT-policy-enumeration guardrail — verified by behavior only.** The Task 8.2 EXACT-enumeration guardrail (a stray/extra policy must fail loud) is applied to the `public` schema only (`migration-reset` queries `schemaname = 'public'`), never to `storage.objects`; the storage plane's isolation is proven behaviorally (cross-tenant list/read/sign, path-spoof, malformed-segment, anon, expired) but a stray or silently-loosened `storage.objects` policy that still denies those probes would not fail loud. Completeness gap in the EXACT-enumeration guardrail on the new storage plane, not a functional hole. Owner: a test-hardening pass — add an EXACT `storage.objects` policy-set assertion (the three `tenant_files_objects_*` policies, no more). [tests/integration/rls/migration-reset.int.test.ts; tests/integration/rls/storage-object-isolation.rls.test.ts]
- **[Low] AC1's literal field list is not schema-asserted end-to-end.** `file-tables-migration-reset.int.test.ts` proves the FKs, the `lifecycle_state`/`owner_type`/`purpose`/`size_bytes` CHECKs, and the timestamp trigger, but never queries `information_schema.columns` for `bucket_id`, `object_path`, `display_name`, `mime_type`, `checksum`, `uploaded_by`, `is_locked`, or `locked_at` (all of which the migration does create). A future migration edit that silently dropped e.g. `is_locked`/`checksum`/`uploaded_by` would not be caught by this suite. Owner: a test-hardening pass — add an `information_schema.columns` presence assertion for AC1's full field list. [tests/integration/rls/file-tables-migration-reset.int.test.ts; supabase/migrations/20260704120000_file_storage_foundation.sql]
- **[Low] AC2's "archive" verb has no file/file_link-specific cross-tenant-denied test.** The shared `tenant-table-inventory.ts` harness models "archive" as a generic UPDATE (`hijackMutationFor` mutates `display_name`/`purpose`, not `archived_at`/`lifecycle_state`), so no test attempts to archive a foreign file/link (UPDATE toward `archived`/`deleted` on a Tenant-B row) and assert denial; AC2's "archive" case is asserted only by generalization from the RLS UPDATE-policy mechanism. Inherited from the pre-existing pattern for every other tenant table (not a regression from this story). Owner: a test-hardening pass (or a cross-cutting tenant-inventory harness improvement) — add a dedicated archive-semantics denial for files/file_links. [tests/integration/rls/tenant-table-inventory.ts; tests/integration/rls/cross-tenant-isolation.rls.test.ts]

## Deferred from: implementation of 6-1-quote-snapshot-schema-and-server-side-version-creation (2026-07-05)

### Overlapping items RESOLVED / addressed here

- **[Med, Epic-3] `CompanySettingsSnapshot` variant omits the PDF-identity fields Epic 6 needs — RESOLVED HERE.** Story 6.1 does NOT reuse the identity-PARTIAL `CompanySettingsSnapshot` variant. It captures the FULL company identity (`company_name`, `org_nr`, `address_line1`/`address_line2`, `postal_code`, `city`, `email`, `phone`, `logo_url`) from the LIVE `company_settings` row into the frozen `quote_versions` row via the new pure `buildQuoteVersionSnapshot` (`src/lib/quote-snapshot/build.ts`) — a fresh full read in the command, never the partial variant. Proven by `quote-version.int.test.ts` (6.1-INT-03: every identity field present) + `quote-tables-migration-reset.int.test.ts` (the FULL-identity columns exist). The Epic-3 partial variant is left unchanged (it stays intentionally minimal for its own snapshot-source use). Owner was "Epic 6" — done.
- **[Med, Epic-3] No marker forces a future "mark sent" to check terms `approvedAt` — 6.1 carries the state; 6.4 enforces.** 6.1 captures `terms_approved_at`/`terms_approved_by` VERBATIM into the frozen snapshot (NULL = not-approved; the builder NEVER derives an `isApproved` flag and NEVER mutates the source) AND carries a standing `requires_sign_off` marker (defaults `true`, the demo-data-only UNAPPROVED-estimate posture) on the `quote_versions` row + the snapshot. So Story 6.4's mark-sent path is fail-closed by construction (it can read `terms_approved_at !== null` + `requires_sign_off`). 6.1 does NOT build the send-time enforcement — that remains Story 6.4's obligation. Owner for ENFORCEMENT: Story 6.4.
- **[Low, 8.1] `link_existing_file` never transitions `files.lifecycle_state` draft→linked — DECISION recorded.** 6.1's attachment reuse creates the `file_links` row (owner_type='quote_version') inside the version-creation RPC but DELIBERATELY does NOT flip the referenced file's `lifecycle_state` from draft→linked. Reason: a 6.1 quote version is a DRAFT (mutable until 6.4 sends it), and the file may already be linked to its calc; a lifecycle flip here would be premature and could fight the 8.2 real-upload path that owns the transition. `draft` is still access-eligible, so no signing/access behaviour changes. The draft→linked transition stays owned by Story 8.2 (the real upload+link path) — do NOT wire it in the quote RPC.
- **[Low, 8.1, R-814] `file_links` has no uniqueness on (tenant_id, file_id, owner_type, owner_id, purpose) — 6.1 chose FIND-OR-CREATE.** The version-creation RPC guards the attachment `file_links` insert with an `exists(...)` check for the same (tenant, file, owner_type='quote_version', owner_id=version, purpose='quote_attachment_snapshot') tuple, so a double-submit/retry against the SAME version + file does NOT silently duplicate the link. A DB unique constraint was NOT added (a version id is per-create-unique, so a genuine duplicate can only come from a retry against an already-created version; the RPC guard covers it and keeps the 8.1 schema unchanged). If a later story needs the constraint at the DB, it can add it additively.

### New deferrals (WITH an owner story)

- **[Low, 6.1] The quote-number DISPLAY format is captured as `quote_number_display` (nullable) but 6.1 stores it NULL.** The ALLOCATION is a server-side tenant-scoped integer (`tenant_counters` + the RPC); the DISPLAY format (e.g. `OFF-2026-0001`) is an OPEN owner question (architecture §24). 6.1 persists the raw integer `quote_number` + a nullable `quote_number_display` string so a later story can fill the presentational form WITHOUT a schema change (the display format never became a data-model blocker). Owner: a later 6.x UI/display story (or the owner decision on the display format). [supabase/migrations/20260705120000_quote_version_model.sql — quote_versions.quote_number_display]
- **[Low, 6.1] The atomic-rollback proof drives the RPC directly with a malformed line payload (negative öre → CHECK violation mid-RPC).** The 6.1-INT-05 rollback case exercises a mid-RPC failure AFTER the counter increment (a `quote_version_lines` CHECK (>= 0) violation) and asserts the counter did NOT advance + no partial version. The FK-violation and RLS-violation rollback modes are covered transitively (the composite same-tenant FKs / own-tenant WITH CHECK reject a foreign parent) but not each as a separate rollback case. Owner: a future test-hardening pass — add explicit FK/RLS mid-RPC rollback cases. [tests/integration/commands/quote-version.int.test.ts]
- **[Low, 6.1] `deduction_*` / VAT-display assumption columns on `quote_versions` are populated conservatively (no calc field carries a ROT/grön assumption yet).** 6.1 captures `vat_rate_bp` + the resolved `vat_display` posture + `requires_sign_off=true`, but the `deduction_type`/`deduction_rate_bp`/`deduction_cap_ore`/`deduction_persons` columns are stored NULL/0 because no calc field feeds a real ROT/grön assumption today (the readiness classifier's `hasDeductionAssumption:false`). The columns exist so a later story feeding a real assumption needs no schema change. Owner: a later 6.x/quote-tax story that wires a real deduction assumption into the snapshot.

## Deferred from: code review of 6-1-quote-snapshot-schema-and-server-side-version-creation (2026-07-05)

- **[Low] Direct-RPC bypass lets a tenant_admin forge `terms_approved_at` / `requires_sign_off` / totals on their OWN tenant's row.** `create_quote_version_from_calculation` is directly callable by any authenticated user (`grant execute … to authenticated`); a tenant_admin could call it via PostgREST with a hand-crafted `p_snapshot`, skipping the command's readiness/totals derivation + the `requiresSignOff` default, persisting a `quote_versions` row with a forged `terms_approved_at`/`requires_sign_off=false`/arbitrary totals. Security review: LOW — NO security boundary crossed (every INSERT is narrowed by the own-tenant `is_tenant_admin` WITH CHECK + composite same-tenant FKs; the actor is the tenant's own admin manipulating their own tenant's rows they already hold direct INSERT on; matches the accepted project-wide SECURITY INVOKER posture, e.g. `create_file_with_link`). No change required for 6.1. Owner: Story 6.4 — treat `terms_approved_at`/`requires_sign_off` as re-derived server truth at send time (not trusted from the persisted snapshot), OR move the value-integrity assertions into the RPC body. [supabase/migrations/20260705120000_quote_version_model.sql — grant execute … to authenticated]
- **[Low] Deduction/VAT-tax-assumption snapshot fields shipped inert on the live path (AC2 deduction capture unexercised).** In the command `execute`, `totals.deductionTotalOre: 0` and `assumptions.deductionType/deductionRateBp/deductionCapOre/deductionPersons` are all `null`, and `classifyReadiness` runs with `tax: { hasDeductionAssumption: false }` — consistent with the story Dev Notes + the demo-data-only owner accept (2026-07-03), so the deduction columns/schema ship inert and AC2's deduction-assumption capture is effectively unexercised on the live path. Duplicates the implementation-side deferral above; recorded here as the code-review confirmation. Owner: a later 6.x/quote-tax story that wires a real ROT/grön assumption. [src/server/commands/quotes/quotes.ts]
- **[Low] Golden fixture warning codes are decoupled from the real classifier vocabulary.** `tests/fixtures/golden/snapshots/quote-version-source.json` uses `warnings: ["REQUIRES_SIGN_OFF"]` / `["REQUIRES_SIGN_OFF","DEDUCTION_ESTIMATE_UNAPPROVED"]`, but `readiness.ts` `ReadinessCode` only defines `TAX_SIGN_OFF_REQUIRED`/`REQUIRED_FILES_DEFERRED`/`HIDDEN_ROWS_INCLUDED` (no such codes). The golden pack copies warning codes by value with NO validation, so it asserts a warning shape the system will never emit — a latent representativeness gap that would let an Epic-9 real-Lovable delta reconcile against a fictional code. Owner: a golden-hardening pass — align the fixture codes to the real `ReadinessCode` union (or validate them against it in the pack guard). [tests/fixtures/golden/snapshots/quote-version-source.json:2452,2498]
- **[Low] The DB behavioral-freeze test (6.1-INT-04) never mutates a source ATTACHMENT (file rename) or the calc title.** 6.1-INT-04 mutates `calculation_rows`/`customers`/`company_settings`/`quote_terms`/`facilities` after version creation, but the version it builds uses `attachment_file_ids: []`, so no `quote_version_attachments.display_name` freeze-after-file-rename is exercised at the DB level — the by-value attachment-metadata freeze is proven only in the pure `build-edges` unit test, not behaviorally in the DB path. Owner: a test-hardening pass — add a DB-path attachment-rename (and calc-title) mutate-after-capture case to 6.1-INT-04. [tests/integration/commands/quote-version.int.test.ts]
- ✅ **[Med] Allocated quote number is never threaded into the frozen composite snapshot value** (auto-resolved [Review][Decision] → defer, epic mode, per triage recommendation). The RPC-allocated integer is persisted in `quote_versions.quote_number` (the persisted truth), but the builder runs BEFORE allocation, so the frozen `QuoteVersionSnapshot` JSON carries `quoteNumberDisplay: null` and no numeric quote number — a literal gap vs architecture §11 "the snapshot captures … quote number". [src/server/commands/quotes/quotes.ts — header.quoteNumberDisplay; src/lib/quote-snapshot/build.ts] — resolved in owner decision 2026-07-06 (owner-decisions-applied-2026-07-06.md D-1): plain-integer display ratified, so the immutable `quote_versions.quote_number` row column the UI/PDF already read IS the display truth; no snapshot-JSON threading is needed and 6.1-DOCS-01 is answered.

## Deferred from: code review of 6-2-draft-quote-version-review-and-timeline-ux (2026-07-05)

- **[Low] `deduction_total_ore` and the ROT/grön deduction assumption fields are read but never displayed.** `VERSION_COLUMNS` selects and `toVersionRow` coerces `deduction_total_ore, deduction_type, deduction_rate_bp, deduction_cap_ore, deduction_persons` (`src/features/quotes/read.ts:1413`), but `QuoteDetailView` renders only `base_total_ore`/`option_total_ore`/`vat_total_ore`/`accepted_price_ore` plus the generic `requires_sign_off` note — the deduction amount and assumption fields are fetched then dropped. Task 6.4's wording says 6.2 "displays them with the requiresSignOff framing," but deferred-work 6-1 records these fields ship INERT (deduction nulls, `hasDeductionAssumption:false`), so surfacing them adds no real information yet and must not imply a per-person-scaled cap. Owner: the later 6.x quote-tax story that wires a real ROT/grön assumption — render these fields (with the non-final framing) when a real deduction assumption exists. [src/features/quotes/read.ts:1413; src/components/quotes/QuoteDetailView.tsx]

## Deferred from: implementation of 6-3-quote-pdf-generation-from-snapshot (2026-07-06)

### Overlapping items RESOLVED / addressed here

- **[Low, 8.1, R-814] `file_links` find-or-create for the PDF link — DECISION recorded.** 6.3 keeps EXACTLY ONE live `quote_pdf` file_link per version+purpose: on first generation it inserts the link; on retry it RE-POINTS that one link's `file_id` to the freshly-uploaded file (an UPDATE, not a duplicate insert), mirroring 6.1's `exists(...)` find-or-create intent. A double-submit retry therefore never duplicates the link (proven 6.3-INT-04). No DB unique constraint was added (8.1 schema unchanged). The superseded prior PDF object is left/archived (archive-over-delete — 8.1 has no object-reclamation path). [src/server/commands/quotes/generate-pdf.ts — findExistingPdfLink + re-point]
- **[Low, 8.1] The real object-BYTE upload path is Story 8.2 — 6.3 BREAKS THAT GROUND for the PDF object (recorded for 8.2 reconcile).** 8.1 was metadata-first; 6.3 is the FIRST story to write actual object BYTES. It uploads on the per-request RLS client (`ctx.db.storage.from('tenant-files').upload(...)`, NEVER service-role) BEFORE the metadata write. NOTE: because the object path must contain the SAME file id as the `files` row and `create_file_with_link` self-allocates the id (no `p_file_id`), 6.3 inserts the `files` row via a DIRECT explicit-id RLS-client insert (own-tenant WITH CHECK) rather than that RPC — a NEW capability an 8.2 general-upload story should reconcile into a shared upload helper (id-up-front + object write + verified-compensated metadata). [src/server/commands/quotes/generate-pdf.ts; src/server/commands/files/files.ts:153]
- **[Low, 6.2] Subroute-revalidation fix APPLIED.** `generateQuotePdfAction` revalidates BOTH `/quotes/[quoteId]` AND the `/quotes/[quoteId]/versions/[versionId]` subroute after a generate/retry (the 6.2 review's subroute-revalidation gap — not repeated). [src/features/quotes/actions.ts]
- **[Med, 6.1] Quote number in the frozen JSON — 6.3 reads the PERSISTED row, no schema invention.** The PDF displays `quote_number_display ?? quote_number` OFF THE `quote_versions` row (the persisted truth), so 6.3 did NOT need the number threaded into the frozen composite JSON. The §24 display-format follow-up remains its owner story's. [src/server/commands/quotes/generate-pdf.ts — snapshotFromRows]
- **[Low, 6-1/6-2] Deduction/VAT assumptions rendered factually with the non-final framing.** The PDF DISPLAYS the captured VAT/ROT/grön assumptions AS-CAPTURED with the non-final `requiresSignOff` framing; it NEVER implies a per-person-scaled cap or a finalized deduction (the inert-fields deferral stands with its later 6.x quote-tax owner).

### New deferrals (WITH an owner story)

- **[Low, 6.3] The mid-pipeline consistency is VERIFIED-COMPENSATED, not a single DB transaction.** The object upload + `files`/`file_links` inserts + `quote_events` + the render-column update are sequenced on the RLS client (no single wrapping RPC, because the object bytes must be written to storage first and the file id must bind the object path). A mid-pipeline fault sets `pdf_status='failed'` (retryable) + appends a `pdf_failed` event — never `generated` over a missing file (proven 6.3-INT-04). A superseded prior object on retry is left/archived (no reclamation — an 8.x storage-retention story owns cleanup). Owner: an 8.x storage-retention story (object reclamation) + optionally an 8.2 shared-upload consolidation. [src/server/commands/quotes/generate-pdf.ts]
- **[Low, 6.3] 6.3-GOLDEN-02 (visual snapshot) intentionally NOT authored; final high-fidelity PDF layout deferred.** The text-extraction golden (6.3-GOLDEN-01) is the PRIMARY contract; the visual/pixel snapshot is a P1 stability-only check that must never gate, and the final customer-facing PDF visual design (logo rendering, precise typography/spacing) is a later design pass. PDF perf (R-618) is deferred — no Phase A SLA. Owner: a later PDF-design/perf story.

### DOCS-01 (renderer choice — recorded in the story Dev Agent Record)

- Renderer = `pdf-lib@1.17.1` (pure-JS, zero native binary, base-14 Helvetica, deterministic InfoDict dates from the injected instant). Text extractor = `pdfjs-dist@4.10.38` (test-only devDependency; `pdf-parse@1.1.1` rejected — its bundled pdf.js cannot read modern pdf-lib output). Both exact-pinned + bundle-contained. See the story's Completion Notes 6.3-DOCS-01.

## Deferred from: code review of 6-3-quote-pdf-generation-from-snapshot (2026-07-06)

- **[Low] PDF metadata persisted via separate non-transactional RLS-client inserts instead of the `create_file_with_link` RPC.** Task 4.2 mandated the atomic `create_file_with_link` RPC, but `generateQuotePdf` inserts `files` then `file_links` then `quote_events` then updates `quote_versions` as separate statements. The code documents a genuine constraint (the RPC self-allocates `files.id` via `returning id into v_file_id`, so it cannot bind the id into the server-derived `object_path`). AC3 permits "transactional OR verified-compensated", and `tryCompensateFailed` sets `pdf_status='failed'` on any mid-pipeline fault — so this is a task-level mechanism deviation, not an AC break. The residual (a possible orphaned `files` row + stored object on a `files`-succeeds/`file_links`-fails split) is explicitly tolerated by the 8.1 archive-over-delete / no-object-reclamation deferral. Owner: a shared upload helper reconciled with the Story 8.2 general-upload path. [src/server/commands/quotes/generate-pdf.ts:1295-1348]
- **[Low] No `audit_events` row written on the failed→retryable transition.** Task 4.3 asks for a `quote_events` AND an `audit_events` row on success and "(where meaningful)" the failed→retryable transition. On failure `tryCompensateFailed` writes a `pdf_failed` `quote_events` row and sets `pdf_status='failed'`, but the envelope writes the `audit_events` row ONLY on success — no audit record on the failure path. The task's explicit "(where meaningful)" hedge makes this non-mandatory and the success-path audit trail is present. Owner: a later hardening pass could emit a failed-transition audit row if the failure audit trail becomes required. [src/server/commands/quotes/generate-pdf.ts:1402-1428]

## Deferred from: code review of 6-4-mark-quote-version-sent-and-enforce-immutability (2026-07-06)

- **[Low] `quote_events` remains INSERT-able by `authenticated` — the append-only trigger only blocks UPDATE/DELETE, so a tenant admin can forge lifecycle timeline events below the RPC.** `quote_events_append_only` fires `before update or delete` only, while the retained 6.1 own-tenant INSERT policy + grant let an authenticated own-tenant caller `insert({ ..., event_type: "sent", occurred_at, channel, reference })` directly — forging a `sent`/`accepted` lifecycle event (arbitrary occurred_at/channel/reference) the quote-detail timeline renders as genuine, without going through `mark_quote_version_sent`. Impact is confined to the caller's OWN tenant and the app derives the draft/read-only authorization state from `quote_versions.status` (not from events), so this is an event-log/timeline integrity gap, not an authorization bypass (security review rated it LOW). Fix direction: if the lifecycle event log is meant to be RPC-only, drop the own-tenant INSERT policy/grant on `quote_events` for `authenticated` and let only the SECURITY INVOKER RPCs write it, or add a BEFORE INSERT guard restricting direct client inserts. Owner: a later quote-events hardening pass (or the RBAC seam alongside the 2-2/2-3 co-member reconciliation). [supabase/migrations/20260707120000_quote_version_sent_lock.sql:1066; supabase/migrations/20260705120000_quote_version_model.sql — quote_events INSERT policy + grant]

## Deferred from: code review of 6-5-new-quote-version-after-customer-visible-changes (2026-07-06)

- **[Low] AC1 full change-list under-proven at INT — 6.5-INT-01 mutates only the source calc price.** Test-design 6.5-INT-01 mandates mutating the FULL customer-visible change list (lines/price/VAT/terms/validity/intro/display/attachments/quote-visible notes) between v1's send and v2's creation and asserting v2 re-captures the NEW values while v1 keeps the OLD. The INT proof mutates only `calculation_rows.unit_sell_ore` and asserts `base_total_ore` differs; the broader field breadth is proven only value-level in the 6.5-GOLDEN-01 fixture, not in the DB-backed INT. Behavior is correct (the shared `buildFreshQuoteSnapshot` re-captures all fields) — a genuine-but-minor coverage gap that would miss a future re-capture regression in a non-price field at INT level. Owner: a test-hardening pass (or the epic-boundary test-review) — extend 6.5-INT-01 to mutate terms/validity/intro/display/attachment-selection/quote-notes representatively and assert v2 captures the new values. [tests/integration/commands/create-new-quote-version.int.test.ts:2669]
- **[Low] AC2 PDF-metadata byte-preservation not re-read on the standalone-lifecycle branch.** The v2-creation preservation test asserts v1's PDF columns unchanged (`v1PdfAfter` toEqual `v1PdfBefore`), but the standalone `rejected`-lifecycle preservation test asserts lines/attachments/selected-row-columns unchanged and does NOT re-read `adminSelectQuoteVersionPdfColumns` before/after. PDF-metadata byte-preservation is therefore proven only for the v2-creation path, not for the standalone lifecycle-event path test-design 6.5-INT-02 lists. Behavior is safe (the `mark_quote_version_lifecycle` RPC UPDATEs only the exempt `status` column) — a genuine-but-minor test-completeness gap that would miss a future lifecycle-RPC regression touching PDF columns. Owner: a test-hardening pass (or the epic-boundary test-review) — add a before/after `adminSelectQuoteVersionPdfColumns` equality assertion to the standalone-lifecycle preservation test. [tests/integration/commands/create-new-quote-version.int.test.ts:2833]

## Deferred from: epic review of epic-6 (2026-07-06)

- **[Med] Correction + PDF affordances render on EVERY non-draft version, including terminal states (superseded/rejected/expired + the future Epic-7 accepted).** `CreateNewVersionButton` and `QuotePdfPanel` are mounted for any `selected.status !== "draft"` (`src/components/quotes/QuoteDetailView.tsx:409,442`; the PDF panel sits outside the draft branch), and neither the `createNewQuoteVersion` command (only guards a `draft` parent — `new-version.ts:75`) nor `generateQuotePdf` (no lifecycle gate at all) restricts the action to a live draft/sent commitment. Architecture §12 scopes PDF retry to "draft/sent"; the new-version intent is "correct a live commitment". Verified harmless in Phase A: the new-version RPC always supersedes the highest `sent` version below the new one (the real live commitment — correct even off a stale parent) and flips only the exempt `status`; PDF retry writes only the 6.4-exempt derived columns; `accepted` is unreachable today (no acceptance flow ships). The two live-drafts-under-concurrency sub-case is separately tracked/dismissed in the 6.5 story review findings. Owner: Epic 7 (acceptance boundary) — gate both affordances (command + UI) to draft/sent and never `accepted` when the acceptance flow lands. [src/components/quotes/QuoteDetailView.tsx:409,442; src/server/commands/quotes/new-version.ts:75; src/server/commands/quotes/generate-pdf.ts]
- **[Low, security] Signed-URL preview accepts any own-tenant `file_id`, not just the version's own PDF.** `previewQuotePdfAction` forwards the client-supplied hidden `file_id` into `createSignedFileAccess`, whose envelope binds it to the caller's own tenant and signs under caller RLS — no cross-tenant exposure, and a tenant admin already holds full own-tenant file read authority, so this grants no privilege they lack. Intra-tenant hardening only; mirrors the established Story 8.x signing-funnel posture; not an Epic-6 regression. Owner: a later signing-funnel hardening pass — resolve the file id server-side from the version's `pdf_file_id` under RLS instead of trusting the form field, so the preview is bound to the version in view. [src/features/quotes/actions.ts:2026; src/server/commands/files/files.ts:79]
- **[Low, security] Child-lock parent-status lookup is unfiltered by tenant (relies on RLS + FK, not an explicit predicate).** `enforce_quote_version_child_sent_lock` reads the parent status with `where qv.id = v_parent_id` and no `tenant_id` predicate. Because the trigger is SECURITY INVOKER, a cross-tenant parent is RLS-invisible → NULL → the trigger defers, and the composite same-tenant child FK (23503) rejects the write regardless — no reachable bypass. Defense-in-depth only: the child-lock immutability is carried by FK + RLS rather than the trigger's own predicate. Owner: a later trigger-hardening pass — add `and qv.tenant_id = <child tenant_id>` to the lookup so the trigger enforces the parent-status check within the same tenant explicitly. [supabase/migrations/20260707120000_quote_version_sent_lock.sql — enforce_quote_version_child_sent_lock]

## Deferred from: code review of 7-1-acceptance-evidence-capture-for-sent-quote-versions (2026-07-06)

- **[Low] `source_sent_total_ore` derives from `quote_versions.accepted_price_ore`, which defaults to 0 — a zero-frozen sent total pre-fills the price input to "0 kr" and treats an equal 0 as "no delta, no reason".** Both the command (`loadQuoteVersionAcceptanceSource`) and the UI (`QuoteDetailView.tsx` → `sourceSentTotalOre={selected.accepted_price_ore}`) read `quote_versions.accepted_price_ore` (`bigint not null default 0`). Nothing in 7.1 asserts the frozen source total is > 0, so a 0-frozen sent version silently skips the AC2 adjusted-price gate for a 0 accepted price. This is a data-quality dependency on Epic 6 populating `accepted_price_ore` correctly, NOT a 7.1 code defect (the gate fires correctly for any non-zero frozen total; the INT tests seed `accepted_price_ore = 125_000` and never exercise a 0-frozen sent version). Owner: an Epic-6/Epic-7 reconcile — either backstop the frozen commitment gross as `> 0` at mark-sent time or add a 7.1 guard/note that a 0-frozen version cannot be meaningfully accepted. [src/server/commands/quotes/quote-db.ts:847; src/components/quotes/QuoteDetailView.tsx — sourceSentTotalOre={selected.accepted_price_ore}]

## Deferred from: code review of 7-2-idempotent-accept-quote-and-create-job-command (2026-07-06)

- **[Low] Parent-`quotes` row read/lock in the accept RPC has no `if not found` guard — fail-closed only incidentally via a downstream NOT NULL.** The `accept_quote_and_create_job` RPC's parent-quote read (`select q.customer_id, q.facility_id, q.contact_id ... for update`) has no `if not found`/null guard, unlike the version read directly above it which raises `QV409` on not-found. If the parent quote were not visible (an RLS race), `v_customer_id` would be NULL and the job INSERT would rely on `jobs.customer_id NOT NULL` (7.1 model) to raise a NOT-NULL/23514 and roll back — fail-closed (no partial state; NFR20/AC3 hold), but the safety is incidental to a downstream constraint rather than an explicit precondition check. A symmetric `QV409` on a missing parent would be the honest signal. Not reachable today given the version→quote FK guarantees co-existence. Owner: a later RPC-hardening pass (or 7.4 accepted-immutability work) — add an explicit `if not found then raise ... QV409` after the parent-quote read to mirror the version read. [supabase/migrations/20260710120000_accept_quote_and_create_job.sql:940]
- **[Low] Idempotent re-entry on an already-`accepted` version runs the adjusted-price + evidence gates BEFORE the RPC short-circuit.** The command relaxes the sent-state gate to let `accepted` flow through to the RPC's existing-record short-circuit, but `computeAcceptanceDelta` + `reasonRequired` and the evidence-ownership check run before the RPC on the re-entry path too. A true identical-input retry is harmless (delta 0 ⇒ no reason needed), but a re-attempt on an already-accepted version supplying a *different* price with no reason surfaces `VALIDATION_FAILED` instead of the idempotent existing-record return the RPC would otherwise give under the lock. AC2's "same acceptance request" framing makes divergent-input re-entry an edge, not a strict violation; the INT/E2E idempotent tests only exercise identical-input retries, so this path is unproven. Owner: a later hardening/test pass — either short-circuit to the RPC's existing-record return before the pre-RPC gates when `status='accepted'`, or add a test pinning the intended divergent-input re-entry behavior. [src/server/commands/quotes/accept-and-create-job.ts:414]
