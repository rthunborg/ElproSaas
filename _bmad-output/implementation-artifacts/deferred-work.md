# Deferred Work

Tracks real findings that were consciously deferred rather than fixed in the originating story. Each entry names where it came from and where it should land.

## Deferred from: code review of 1-2-establish-ci-and-quality-gate-baseline (2026-06-15)

- **CI `pnpm build` gate is non-hermetic — `next build` fetches a Google font over the network.** The dependency comes from the app scaffold (`next/font/google`, Story 1.1), not the CI workflow this story added; Story 1.2 explicitly documents it as "expected to pass on networked CI." No functional impact on networked GitHub-hosted runners today. Revisit if/when CI runs in a network-restricted environment (self-hosted/air-gapped) or when font loading is reworked. Owner: scaffold/font cleanup (alongside **Story 1.3**) or a future CI-hardening story. [.github/workflows/ci.yml:58]
  - **Status (2026-06-15, Story 1.3 owner decision):** still deferred. Story 1.3 fixes only the `globals.css` Arial override; the `next/font/google` network fetch stays as-is (making it hermetic means switching to self-hosted `next/font/local`). Revisit in a future CI-hardening/font story.

## Deferred from: creation of 1-3-build-phase-a-app-shell-and-deferred-scope-navigation-guardrails (2026-06-15)

- **Automated component/UI tests for the Phase A app shell are deferred to the TEA `testarch-framework` harness (Epic 2).** Story 1.3 verifies its ACs (nav items, active state, keyboard reachability, absence of deferred labels/routes, responsive widths) via documented manual verification, because no test framework exists yet and Stories 1.1/1.2 reserved the framework decision for `testarch-framework` (AR28 dependency approval). When the harness lands, backfill automated tests covering: the seven nav items render and only those (no Pilotstöd/Migrering, no deferred modules), `aria-current` active state with a non-color indicator, icon-only controls expose accessible names + tooltips on hover/focus, and the sidebar→rail→drawer responsive behavior. Owner: first story after the test harness is initialized (Epic 2 / TEA `testarch-framework`). [src/components/app-shell/**, src/app/(app)/**]

## Deferred from: code review of story-1.3 (2026-06-16)

- **Whole app shell is a single client island.** `src/app/(app)/layout.tsx` delegates all chrome to a `"use client"` `AppShell`, shipping the sidebar/top-bar/inline-SVG icons as client JS and forfeiting App-Router server-layout streaming. Defensible for a small Phase A shell; revisit a server-layout + small client-nav-island split if the shell grows or when auth/tenant context lands (Epic 2). [src/app/(app)/layout.tsx:9-16, src/components/app-shell/AppShell.tsx:85]
  - **PARTIALLY RESOLVED (2026-06-22, Story 2.1).** `(app)/layout.tsx` is now a SERVER layout that resolves tenant context server-side (the auth boundary) and passes plain props into the still-client `AppShell` nav island — exactly the "server-layout + small client-nav-island" split this note called for "when auth/tenant context lands." `AppShell` remains a client island (it owns `usePathname` + drawer state); a deeper split of the static nav chrome into pure server components was NOT pursued (out of Story 2.1 scope — the minimum clean split that lets auth resolve server-side was the goal). Remaining client-island reduction stays deferred to a future shell-growth pass.
- **Focus after a drawer-link navigation is not guaranteed.** `handleDrawerNavigate` closes the drawer and relies on client navigation to move focus, but `next/link` does not reliably move focus to the new page, so focus can fall back to `<body>`. This is an app-wide client-navigation focus concern (not unique to the drawer); address holistically (e.g. focus `#main-content` on route change) when route transitions / auth land. Owner: Epic 2 or a route-transition/a11y pass. [src/components/app-shell/AppShell.tsx:102-103]

## Deferred from: implementation of 2-1-tenant-admin-login-and-tenant-context-resolution (2026-06-22)

- **Gated INT/E2E scaffolds are excluded from `tsconfig` typecheck until their runner + types land.** `tests/integration/**` and `tests/e2e/**` (the DB-backed and browser red-phase scaffolds) reference a `describe`/`it`/`page` API whose type definitions are NOT installed (no test framework / Playwright yet — that is a gated, Story-2.2 / TEA `testarch-framework` decision). To keep `pnpm typecheck` green without smuggling in a framework, those two trees are listed in `tsconfig.json` `exclude`. When Story 2.2 lands the runner + types and removes `.skip`, it MUST re-enroll these trees in typecheck (remove the excludes, or give `tests/` its own tsconfig with the runner's types) so the now-runnable suites are type-checked. Owner: Story 2.2 (local Supabase stack + runner). [tsconfig.json exclude; tests/integration/**; tests/e2e/**]
  - **RESOLVED (2026-06-26, Story 2.2) for `tests/integration/**`.** Vitest (4.1.9) is the runner for the DB-backed INT/RLS suites; `tests/integration/**` is removed from `tsconfig` `exclude` and now type-checks + runs green against the local Supabase stack. `tests/e2e/**` STAYS excluded — see the new e2e/Playwright deferral below (the browser runner is out of Story 2.2 scope). [tsconfig.json; vitest.config.ts; tests/integration/**]

## Deferred from: code review of 2-1-tenant-admin-login-and-tenant-context-resolution iter 2 (2026-06-22)

- **[Low] `tenants.name` NOT-NULL reconciliation for the AC1 both-null indicator (CONSIDER item).** PARTIALLY ADDRESSED (2026-06-26, Story 2.2): `tenants.name` is now `NOT NULL` in the migration, removing the null-tenant-name source. The AppShell both-null display fallback itself is still a follow-up shell pass (NOT changed here). Owner remains: a follow-up shell pass. [supabase/migrations/20260625122433_tenant_foundation.sql; src/components/app-shell/AppShell.tsx]
- **[Low] AC1 active-tenant indicator collapses when both `tenantName` and `userEmail` are null.** The top-bar tenant/user region in `AppShell` is gated behind `(context.tenantName || context.userEmail)`; on a granted-access shell where both presentational values happen to be null (types permit both `string | null`), the region renders only the sign-out button with no tenant indicator at all, contradicting AC1's "unconditional" active-tenant indicator. Security boundary unaffected (access already granted server-side); hypothetical trigger (a tenant row with a null name AND a user JWT with no email). Owner: a follow-up shell pass aligned with Story 2.2 — add an always-present tenant-identity fallback (a generic label or the resolved `tenantId`) when both display values are null, and reconcile with Story 2.2's `tenants.name` NOT-NULL constraint. [src/components/app-shell/AppShell.tsx:690]

## Deferred from: code review of 1-4-document-local-setup-environment-contract-and-repo-hygiene (2026-06-21)

- **[Low] Evergreen docs hardcode plan-specific epic/story numbers that risk staleness.** `.env.example` and `local-setup.md` attribute the signed-URL TTL var to "Epic 8" (spec sources it only to architecture §6, no epic assignment) and `local-setup.md` attributes test-only factories to "Story 2.2" (spec ties only first migrations to 2.2). The vars/behavior are correctly marked optional/forthcoming; only the number attributions are dev-introduced and unverified, so they go stale if the plan shifts. Owner: a docs-maintenance pass (or whichever story actually lands the file-storage / factory work — update the attribution then). [.env.example:47; docs/process/local-setup.md:264,293]
- **[Low] Hardcoded-light `body` has no dark-mode or `forced-colors`/high-contrast support.** `globals.css` hardcodes `#ffffff`/`#171717` on `body` with the `@media (prefers-color-scheme: dark)` block intentionally removed (Story 1.4 Task 4.1) and no `@media (forced-colors: active)` fallback. Deliberate Phase A hardcoded-light posture (Story 1.3), not a regression from this change, but OS dark-mode and Windows high-contrast users get an unthemed experience. Owner: a future theming / accessibility pass. [src/app/globals.css:11-13]

## Deferred from: implementation of 2-2-tenant-membership-schema-rls-helpers-and-two-tenant-fixtures (2026-06-26)

- **[Low] Vitest INT suites do not auto-run `supabase db reset`.** The runner only PROBES stack reachability (`tests/support/global-setup.ts`); the caller (`pnpm test:db`-equivalent locally, the CI `db` job) is responsible for `supabase db reset`. This keeps the suites from ever wiping a developer's DB unexpectedly, but means a stale schema (un-reset after a migration change) could produce confusing failures locally. Acceptable for now (per-test fixtures use unique ids; CI always resets). Owner: a future test-DX pass could add an opt-in pretest reset. [tests/support/global-setup.ts]

## Deferred from: code review of 2-2-tenant-membership-schema-rls-helpers-and-two-tenant-fixtures (2026-06-26)

- **[Low] `isLocalStackReachable()` probes `/auth/v1/health`, not the schema.** A stack that started but failed or only partially applied `supabase db reset` passes the Auth health probe, so the DB-backed suites run and then fail with confusing schema errors instead of skipping cleanly. CI runs `db reset` before tests and `SUPABASE_TEST_REQUIRED=1` backstops a missing stack, so live impact is low; a schema assertion (e.g. confirm the `tenants` table is present) would be a stronger reachability gate. Owner: a future test-DX pass. [tests/support/test-env.ts:53]
- **[Low] `migration-reset.int.test.ts` search_path assertion is over-fit to the empty path.** The regex `/search_path=("")?$/` matches loosely and would also pass a non-empty `search_path=pg_catalog` while FAILING a valid schema-qualified implementation that AC4 explicitly permits. The R-006 behavioral negative (`security-definer-search-path.rls.test.ts`) is the real hijack proof, so this introspection assertion is weaker than it appears, not a code defect. Owner: a future test-hardening pass — parse the specific `search_path` proconfig entry and assert it is exactly empty. [tests/integration/rls/migration-reset.int.test.ts]
  - **RESOLVED (2026-06-29, epic-2 hardening) — G-8a.** Replaced the over-fit regex with a shared `assertSearchPathExactlyEmpty` helper that parses the ACTUAL `search_path` entry from `pg_proc.proconfig` and asserts it is EXACTLY empty (a non-empty path now FAILS). Applied to all three SECURITY DEFINER functions: the two RLS helpers (`migration-reset.int.test.ts`) and `record_audit_event` (new introspection assertion in `record-audit-event-search-path.int.test.ts`). R-006 behavioral hijack negatives unchanged. resolved in tests/support/search-path.ts; tests/integration/rls/migration-reset.int.test.ts; tests/integration/commands/record-audit-event-search-path.int.test.ts.
- **[Med] `tenant_memberships_select_own` lets any active admin read every membership row in their own tenant (co-member enumeration).** The policy `using (public.is_tenant_admin(tenant_id))` scopes reads by tenant, not by `user_id = auth.uid()`, so with more than one admin per tenant each can read co-members' `user_id`/`status` — never across a tenant boundary (the security review rejected the vulnerability framing). Deferred per user decision 2026-06-26 as accepted, intended Phase A design (single-admin tenants); disclosed in the PR Security/RLS impact statement rather than buried in a code comment. Owner: Story 2.4 / RBAC seam — tighten the SELECT policy toward `user_id = auth.uid()` (least privilege) when multi-admin / role management lands. [supabase/migrations/20260625122433_tenant_foundation.sql]

## Deferred from: implementation of 2-3-server-command-envelope-and-minimal-audit-events (2026-06-29)

- **Business-table audit wiring (non-`audit_events`) lands in the owning Epic 3-8 stories.** Story 2.3 builds the AUTHORITY SUBSTRATE only: the reusable command envelope (`src/server/commands/envelope.ts`), the `writeAuditEvent` DEFINER-RPC write path, the metadata sanitizer, and the `audit_events` table/trigger/RLS. It deliberately creates NO business table (architecture §7) and wires NO real lifecycle event producer. Each later command (membership/settings/pricing/quote/acceptance/job/file changes) REUSES `defineCommand`/`runCommand` + `writeAuditEvent` and supplies its own `target_type`/`event_type`/allow-listed metadata fields when it lands. Owner: the owning Epic 3-8 command stories. [src/server/commands/envelope.ts; src/server/commands/audit.ts; architecture.md#5, #15]
- **Audit-history consumer UI is substrate-only here (AC7).** AC7 requires that WHEN audit history is surfaced, only minimal relevant lifecycle/audit events are exposed — but Story 2.3 builds the append-only substrate + the own-tenant SELECT policy ONLY, and deliberately creates NO audit-analytics module, dashboard, route, or nav item (Stop Condition; nav-items.ts stays exactly seven). The record-context read UI that surfaces minimal audit history lands in its own story. Owner: the record-context / lifecycle-history UI stories (per entity, Epics 3-8). [supabase/migrations/20260629121136_audit_events.sql (audit_events_select_own); epics.md#Story 2.3 (substrate only)]
- **[Med, accepted Phase A] `audit_events_select_own` co-member audit-read least privilege.** The own-tenant audit SELECT policy `using (public.is_tenant_admin(tenant_id))` mirrors the Story 2.2 `tenant_memberships_select_own` shape: it scopes reads by TENANT, not by `actor_user_id = auth.uid()`, so in a multi-admin tenant each active admin can read co-members' audit rows within their OWN tenant (never across a tenant boundary). For Phase A single-admin tenants this is the accepted, intended design (same decision as the membership co-member-read deferral); disclosed in the PR Security/RLS impact statement, not buried in a comment. Owner: Story 2.4 / RBAC seam — tighten toward `actor_user_id = auth.uid()` (or a role-scoped read) alongside the membership-policy tightening when multi-admin / role management lands. Do NOT tighten here (out of scope). [supabase/migrations/20260629121136_audit_events.sql (audit_events_select_own); deferred-work.md (2-2 co-member enumeration, Story 2.4)]

## Deferred from: code review of 2-3-server-command-envelope-and-minimal-audit-events (2026-06-29)

- **[Low] `record_audit_event` trusts caller-supplied `p_actor_user_id` instead of `auth.uid()` (audit attribution forgery / repudiation within own tenant).** The membership gate keys off `auth.uid()` (`is_active_tenant_member(p_tenant_id)`) but the inserted `actor_user_id` comes from the parameter, so an active member can write a genuine own-tenant audit row attributing the action to any other `auth.users` id (bypassing the app envelope). Security review rated this LOW: the cross-tenant gate still holds (no write outside the caller's tenant), the FK forces a real user id, and Phase A is single-admin-per-tenant (small blast radius). Owner: the RBAC / multi-admin seam (Story 2.4+) — drop `p_actor_user_id` and insert `actor_user_id = (select auth.uid())`, or add `if p_actor_user_id is distinct from (select auth.uid()) then raise`. [supabase/migrations/20260629121136_audit_events.sql:1006]
- **[Low] End-to-end R-010 metadata-hygiene assertion on the happy path is near-vacuous.** The smuggled `apiKey: "sk_live_should_not_persist"` is passed via `input`, but `makeNoopCommand` declares no `auditFields`, so the envelope always derives `metadata = {}` — the secret was never on a path into metadata regardless of the sanitizer, so the assertion passes trivially. AC5 is still met by the strong pure-unit sanitizer suite. Owner: a future test-hardening pass — add a command whose `auditFields` routes caller-controlled metadata through `sanitizeAuditMetadata`, then assert forbidden content is dropped end-to-end. [tests/integration/commands/envelope-audit-write.int.test.ts:1562]
- **[Low] The documented `actor_user_id on delete set null` FK behavior has zero test coverage.** The migration's most-documented design decision (audit row survives a deleted actor by nulling the FK) is never exercised: no test deletes an `auth.users` row and asserts the audit row persists with `actor_user_id IS NULL` and all other columns intact. Owner: a future test pass — add a privileged delete-actor + re-read assertion. [supabase/migrations/20260629121136_audit_events.sql:910]
- **[Low] The disabled-membership failure path is only unit-proven, not DB-backed.** Spec Task 5.2 says "no/disabled membership → `TENANT_MEMBERSHIP_REQUIRED`"; the integration suite covers only the no-membership (`orphanUser`) variant — no fixture creates a user with a `disabled`/`inactive` membership for a live DB-backed resolve (the disabled path is exercised only at the pure-unit level via result injection). Owner: a future test pass — add a disabled-membership fixture + DB-backed failure-mode case. [tests/integration/commands/envelope-failure-modes.int.test.ts:82]
- **[Low] `CONTROL_CHARS` does not strip C1 control characters (U+0080 through U+009F).** The regex covers ASCII control ranges (NUL through U+001F, plus DEL U+007F) only, so C1 controls (e.g. NEL U+0085) in an allow-listed `reason`/hash string pass the check and could persist. No proven security impact (metadata is JSON-serialized; security review did not flag it), but the guard is narrower than its comment implies. Owner: a future hardening pass — extend the class through U+009F. [src/server/commands/audit-metadata.ts:56]
- **[Low] (iter 2) Test factory `tryUpdate` interpolates raw object keys as unquoted SQL identifiers.** `tests/factories/audit-events.ts` builds the UPDATE `SET` clause via `keys.map((k,i) => `${k} = $${i+2}`)` — the patch's column names are concatenated directly into a statement run on the superuser pg pool; only VALUES are parameterized. Test-only, loopback-gated, and keys are author-controlled constants today (no untrusted input reaches it), so no current exploit path — but it is an identifier-injection pattern in privileged test code that will be copy-pasted. Security review examined the file and found service-role usage sound; rated Low. Owner: a future test-hardening pass — allow-list or `pg`-format-quote the identifiers, or constrain `tryUpdate` to a fixed column set. [tests/factories/audit-events.ts:83]

## Deferred from: implementation of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (2026-06-29)

- **[Low] (NEW) `envelope-audit-write.int.test.ts` uses a HARDCODED `correlationId` and is not isolation-safe across repeated non-reset int runs.** The "writes EXACTLY ONE audit_events row" test (Story 2.3) asserts exactly one `audit_events` row matches the literal `correlationId = "dddddddd-…"` written under `fixture.tenantA.id`, but the test seeds no unique id per run and never cleans the row, so running `test:int` MULTIPLE times against the SAME local DB without a `supabase db reset` between runs accumulates rows under that fixed correlation id and the count assertion fails (`expected 3 to be 1`). NOT a product defect and NOT a CI failure: CI runs `supabase db reset` exactly once before `test:int`, so a clean single run is green (verified). Surfaced only because the Story 2.4 verification re-ran the int suite several times without an intervening reset. Owner: a future test-hardening pass — switch the hardcoded `correlationId` to a per-run `crypto.randomUUID()` (matching the cross-tenant suite's discipline) so the assertion is run-order-independent. Do NOT change in Story 2.4 (out of scope — Story 2.3 test, not a harness task). [tests/integration/commands/envelope-audit-write.int.test.ts:91]
  - **RESOLVED (2026-06-29, epic-2 follow-up).** Replaced the hardcoded `correlationId` literals in all three tests with per-run `crypto.randomUUID()` (matching the cross-tenant/anon suites' discipline), so the exact `toBe(1)` count assertion is run-order-independent and no longer accumulates across repeated non-reset `test:int` runs. [tests/integration/commands/envelope-audit-write.int.test.ts]

## Deferred from: code review of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (2026-06-29)

- **[Low] Live H4 gate + bite-proof INT tests `return`-skip (pass, not skip) when the local stack is down — a Docker-less dev machine gets a vacuous green.** Every `it` in `rls-inventory-gate.int.test.ts` opens with `if (!stackUp) return;`, so when `isLocalStackReachable()` is false the assertions never run and Vitest reports a pass. CI is protected: `SUPABASE_TEST_REQUIRED=1` in the global setup makes a missing stack a HARD failure, and the pure-fn unit (`inventory-gate-core.test.ts`) covers the no-DB comparison logic unconditionally — so the only residual risk is a dev-side false green. This is the established Story 2.2 skip pattern and the enforcement lives in `tests/support/test-env.ts` (outside this diff). Owner: a future test-DX pass — consider converting `return` to a hard assertion when `STACK_REQUIRED` is set inside the gate file itself, so the gate's non-vacuity is self-contained rather than inherited from global setup. [tests/integration/rls/rls-inventory-gate.int.test.ts:918]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#6.** Added a shared `skipUnlessStack(testCtx, stackUp)` helper and replaced every `if (!stackUp) return;` inside the 21 integration suites' `it`/`test` bodies (81 call sites) with a REAL Vitest dynamic skip (`ctx.skip()`), so a stack-down local run now reports those tests as SKIPPED in the reporter instead of a vacuous pass (verified: migration-reset shows `7 skipped` when the URL is unreachable). The `SUPABASE_TEST_REQUIRED=1` hard-fail contract is preserved and now self-contained inside the helper too (it THROWS when required + down), in addition to the global-setup backstop. resolved in tests/support/stack-gate.ts (+ the 21 `tests/integration/**` suites).
- **[Low] Anon-EXECUTE / anon-table negatives pin exactly `error.code === "42501"`, brittle across Postgres/PostgREST versions for a not-visible function.** `assertAnonExecuteDenied` and the data-driven anon table verbs assert exactly `42501` (permission denied). A SECURITY DEFINER function the role cannot even see can surface `42883` (function does not exist) rather than `42501` depending on search_path/visibility, and the suite applies the rigid pin to all three RPCs (`is_active_tenant_member`/`is_tenant_admin`/`record_audit_event`) with no fallback. Green today — the functions are visible and only EXECUTE is revoked — so not a current defect; a future PostgREST/Postgres upgrade that changes function-visibility surfacing is the realistic trigger. Owner: a future test-hardening pass — accept `42501` OR `42883` (deny-or-not-visible) for the EXECUTE negatives, keeping the `data !== true` backstop. [tests/integration/rls/anon-path-isolation.rls.test.ts:693]
  - **WON'T-FIX (2026-06-29, epic-2 hardening) — DX#5. STAYS OPEN as won't-fix, not resolved.** The exact-`42501` SQLSTATE pin is the INTENTIONAL anti-vacuous-pass hardening: a future visibility-surfacing behavior change (e.g. `42883` instead of `42501`) SHOULD fail the test so it is reviewed, rather than being silently absorbed by a loosened `42501 OR 42883` matcher. Deliberately NOT loosened. Re-open only if a real Postgres/PostgREST upgrade actually changes the surfaced code AND that change is reviewed-and-accepted as benign; the fix then is to widen the accepted set WITH a dated justification, not pre-emptively.

## Deferred from: code review of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (iter 2) (2026-06-29)

- **[Low] H4 introspection still has coverage gaps beyond direct-`tenant_id` public base tables.** The broadened `introspectTenantOwnedTables` (iter-1) still: filters `relkind in ('r','p')` so VIEWS / MATERIALIZED VIEWS exposing tenant rows are never introspected; only resolves FKs whose `confrelid` is `public.tenants`, so a table tenant-scoped transitively (FK to `tenant_memberships` or any non-`tenants` tenant-owned parent) escapes all three signals; returns BARE table names, so a non-`public` table sharing a name with an enrolled `public` table collapses and the unenrolled one escapes; treats any `supabase_`/`pgsodium`/`pg_`-prefixed schema as system (masking a hypothetical app schema with a system-looking prefix); and drops a real non-`public` table literally named `tenants` via `nspname !== "public"` (intended to exclude `_realtime.tenants`, but over-broad vs an `isSystemSchema` guard). None bites in Phase A — all three tenant-owned tables are `public` base tables with a direct `tenant_id` — so this is forward-looking gate-DX hardening, the same track as the iter-1 deferred naming-bound concern. Owner: a future gate-hardening pass — revisit when the first view-shaped, transitively-scoped, partitioned, or non-`public` tenant table is proposed. [tests/integration/rls/tenant-table-inventory.ts:1306]
  - **DISPOSITION (2026-06-29, epic-2 hardening) — G-8b: SPECULATIVE, STAYS OPEN (deliberate non-action, NOT resolved).** Per the consolidated design, the introspection is extended only WHEN the first such non-conforming tenant table is actually proposed; no current table is missed (all three Phase-A tenant tables are `public` base tables with a direct `tenant_id`, or the `tenants` root). Building view/transitive-FK/non-`public`-schema introspection now is over-engineering with nothing in the schema to test against, and the gate fails CLOSED on any unknown schema in the meantime. Left as-is on purpose. The current scope + the "extend when a non-conforming table lands" trigger is now made EXPLICIT in the `introspectTenantOwnedTables` module docstring. [tests/integration/rls/tenant-table-inventory.ts]
- **[Low] Bundle scanner `walk()`/`readFileSync` swallow read errors, so an unreadable `.next` subtree is silently excluded.** Both the directory `readdirSync` (`} catch { return; }`) and the per-file `readFileSync` (`} catch { continue; }`) swallow errors, so a permission-denied subtree or file under `.next` is skipped with no signal — a service-role leak there would go undetected on the authoritative R-002 gate. No realistic trigger in CI (the runner owns the `.next` it just built), so it is a vacuous-green hardening concern, not a current defect. Owner: a future hardening pass — distinguish `ENOENT` from `EACCES` and surface (or fail on) unexpected read errors rather than silently skipping. [scripts/verify/check-bundle-containment.mjs:253]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#2.** The `walk()` `readdirSync` and the per-file `readFileSync` no longer swallow errors: an `ENOENT` is treated as legitimately-absent (fine), but ANY other error (EACCES/EPERM/…) on a present-but-unreadable directory or file is now SURFACED (warn to stderr with the path) AND recorded as a VIOLATION, so the authoritative R-002 scan fails-loud / non-zero instead of a silent false-clean. The absent-`.next` fail-loud (throw) is unchanged. New unit tests prove an unreadable (EACCES) entry yields a non-vacuous violation, a vanished (ENOENT) entry stays clean, and an unreadable entry does not mask a real leak in a readable sibling. resolved in scripts/verify/check-bundle-containment.mjs; tests/unit/scripts/verify/bundle-containment.test.ts.
- **[Low] Enrollment guidance omits the anon-path metadata helpers.** `unenrolledTablesMessage` (and the module's enrollment contract) instructs a new enrollee to add `spoofedRowFor/tenantBFilter/mutation` metadata but never mentions the anon helpers (`anonRowFor`/`anonFilterFor`/`anonMutationFor`); a future table enrolled per the message would leave the anon helpers falling through to the membership default. DX-only; related to the open metadata-exhaustiveness Decision. Owner: a future test-DX pass — extend the message + contract to list the anon helpers (best paired with making the helpers compile-exhaustive). [tests/integration/rls/tenant-table-inventory.ts:1401]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#3.** `unenrolledTablesMessage` now names BOTH metadata seams a future enrollee must wire — the cross-tenant helpers (`spoofedRowFor`/`tenantBFilter`/`hijackMutationFor`) AND the anon-path helpers (`anonRowFor`/`anonFilterFor`/`anonMutationFor`) — explaining that omitting the anon helpers would let the new table fall through to a membership-shaped default in `anon-path-isolation.rls.test.ts`. The module STANDING CONTRACT docstring was updated to match. Message/docstring-only change. resolved in tests/integration/rls/tenant-table-inventory.ts.
- **[Low] Cross-tenant `audit_events` test goes vacuous if the Tenant-B audit seed returns no id.** `tenantBFilter("audit_events", ctx)` uses `ctx.tenantBAuditId`; if `adminInsertAuditEvent` ever returned successfully without an id, `tenantBAuditId` is `undefined` and `.eq("id", undefined)` matches nothing, so the SELECT/UPDATE/DELETE cross-tenant assertions pass vacuously. A throwing seed errors the whole `beforeAll` (caught), so the only residual risk is a silent no-id return. Owner: a future test-hardening pass — add a `if (!tenantBAuditId) throw new Error(...)` guard in `beforeAll` so a failed seed is loud, not vacuous. [tests/integration/rls/cross-tenant-isolation.rls.test.ts:62]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#4.** Added an explicit non-null precondition guard: the cross-tenant suite's `beforeAll` now throws if `tenantBAuditId` is null/undefined AFTER the seed, BEFORE any denial assertion runs, and `tenantBFilter('audit_events', …)` in the shared inventory carries the same guard so any future consumer inherits it. A broken/no-id seed now fails loudly instead of green-by-vacuity. The denial mechanism is unchanged (the negatives still assert the 42501 SQLSTATE). resolved in tests/integration/rls/cross-tenant-isolation.rls.test.ts; tests/integration/rls/tenant-table-inventory.ts.

## Deferred from: code review of 3-1-tenant-owned-crm-data-model-and-commands (2026-06-30)

- **[Low] `validateUpdateCustomer` does not enforce identifier-by-type mutual exclusion (deferred to the DB CHECK).** `validateCreateCustomer` enforces the requiredness + mutual-exclusion rules (`private` ⇒ `personnummer` present and `org_nr` absent; non-`private` ⇒ the inverse), but `validateUpdateCustomer` only bounds `personnummer`/`org_nr` as optional text and explicitly defers type-consistency to the `customers_identifier_by_type` DB CHECK (`23514` → `VALIDATION_FAILED` via `throwMappedWriteError`). The update command cannot know the row's existing `customer_type` without a read, and a violating update is correctly rejected by the DB backstop with the right user-facing code — so no incorrect outcome is reachable, only a less-localized rejection. Introduced by this story's update-command design (not pre-existing), but correctly backstopped, so logged rather than patched. Owner: a future CRM command-hardening pass — add a read-then-validate (or partial-update-aware) mutual-exclusion check in `validateUpdateCustomer` so the command layer rejects an identifier/type mismatch before the DB CHECK. [src/server/commands/crm/validation.ts]

## Deferred from: code review of 3-2-crm-tenant-admin-ux-and-lifecycle-context (2026-06-30)

- **[Low] A contact's `is_primary` flag cannot be turned OFF via the edit dialog.** An unchecked HTML checkbox submits no form entry; `parseUpdateContactForm` reads it via `bool(form, "is_primary")` (returns `undefined` when absent) and only sets `input.is_primary` when `!== undefined`, so `buildContactPatch` skips it. A contact already flagged primary can never be un-flagged via edit — unchecking is a silent no-op. Genuine but minor: owner decided no enforced/exactly-one-primary rule, no data corruption, no security impact. Owner: a future CRM-UI edit-form pass — render a hidden `is_primary=false` companion (or have the update parser treat a present-but-unchecked checkbox as explicit `false`) so the edit form can clear the flag. [src/features/crm/form-parsing.ts:257]
- **[Low] A contact's facility binding cannot be CLEARED ("Ingen anläggning") via the edit dialog.** `ContactDialog.tsx` offers an `value=""` "Ingen anläggning" option, but `parseUpdateContactForm` reads `facility_id: field(form, "facility_id")` and `field()` returns `undefined` for an empty string, so `buildContactPatch` (skips undefined) never writes `facility_id = null`; an already-bound contact keeps its binding. The same omitted-vs-nulled shape applies to other cleared optional fields on update; facility unbind is the user-visible case. Genuine but minor: no data corruption, no security impact; a presented edit action is inert. Owner: a future CRM-UI edit-form pass — have the update parser map a present-but-blank optional field to an explicit `null` (or model "clear" distinctly from "unchanged") for facility/address fields on update. [src/features/crm/form-parsing.ts:251]

## Deferred from: code review of 3-3-company-identity-quote-terms-and-vat-defaults (2026-06-30)

- **[Low] `bpToPercentString` has a dead/identical ternary branch.** `Number.isInteger(percent) ? String(percent) : String(percent)` — both branches are identical, so the conditional is a no-op, and the doc comment ("Trim a trailing '.00'") overstates the logic. Output is functionally correct (`String(percent)` already drops a trailing `.00` for integers), so it is not an AC violation — a trivial cleanup of unfinished intent. Owner: a future settings-helper cleanup pass — collapse to `String(percent)` (or implement the intended trimming) and align the comment. [src/features/settings/vat-display.ts:1124-1128]
- **[Low] No automated audit-row assertion for the `approveQuoteTerms` (`quote_terms.approved`) path.** The company-settings and `updateQuoteTerms` "single audit row, no PII in metadata" assertions exist and are strong, but there is no equivalent "exactly ONE audit row with clean `{ targetId }` metadata" assertion for the approve event. The `auditFields` is `(ctx) => ({ targetId: ctx.input.id })` — clean by construction — so the risk is Low; the gap is test coverage, not a code defect. Owner: a future settings-test pass — add an approve-path audit-row count/shape assertion mirroring the `updateQuoteTerms` no-PII test. [src/server/commands/settings/quote-terms.ts:1370]
- **[Med] Approved-status UI shows a static "ägare/juridik" label, not the real `approved_by` user.** AC2 asks for "who + when"; the "when" + persisted attribution are correct, only the *display* of the "who" is generic. Deferred (auto-resolved from a Tier-A `[Review][Decision]` with recommended `defer`, epic mode): resolving the id to a name needs a user-display read surface out of this story's scope (the read path never touches `auth.users`). Owner: wire the real `approved_by` name when a user-display read lands (e.g. the same owner pass that resolves the exact PDF-identity fields). [src/components/settings/QuoteTermsEditor.tsx:484-488]
- **[Low] `default_vat_display` enum drops the "private → always incl-VAT" half to a code invariant rather than a stored setting.** Diverges from AC5's literal "full rule encoded as the setting"; private-side enforcement is Epic 4's. Deferred (auto-resolved from a Tier-A `[Review][Decision]` with recommended `defer`, epic mode): the precise enum value-set + labels are owner-gated (story Open Question 3) — confirm at the Epic-4 working session; conservative encoding stands until then. [src/server/commands/settings/validation.ts:1483]

## Deferred from: code review of 3-4-work-roles-and-optional-manual-articles (2026-06-30)

- **[Low] No UI reactivate control for an archived work_role/article.** The read layer hides archived rows but the upsert UPDATE path cannot reactivate one from the UI. `readWorkRoles`/`readArticles` filter `.eq("is_active", true)`, so archived rows never appear in the editor lists, and neither editor (`WorkRolesEditor`/`ArticlesEditor`) renders a reactivate control — only an "Arkivera" button. The command/data model CAN reactivate (archive is reversible via UPDATE flipping `is_active`), but no UI surfaces an archived row to reactivate it. AC2's "reactivate" verb is a capability of the data model, not a UI this story ships; the Tasks treat the `is_active` toggle as sufficient and the e2e does not test reactivation — within the documented MINIMAL scope. Genuine but minor: no data corruption, no security impact; archived rows are simply not re-surfaceable from the UI. Owner: a future pricing-UI pass — add an "include archived" view (lift the `is_active=true` read filter behind a toggle) + a reactivate action that UPDATEs `is_active` back to true. [src/features/pricing/read.ts; src/components/pricing/WorkRolesEditor.tsx; src/components/pricing/ArticlesEditor.tsx]
- **[Low] Articles-IN-scope is an owner assumption (Open Question 1), not an independently re-confirmed decision.** The diff builds the full `articles` table/command/UI per `owner-decisions-applied-2026-06-18.md#Story 3.4` ("reusable article/material register IS IN scope — owner: yes"); Story 3.5's snapshot contract + the epic test design's R-007 both depend on articles. Deferred (auto-resolved from a Tier-A `[Review][Decision]` with recommended `defer`, epic mode): re-confirm the articles-IN decision still stands at PR review. The `articles` slice is a clean, additive, cleanly-removable layer if the owner reverses; the deliverable-completing default stands until then. Owner: PR-review confirmation. [supabase/migrations/20260630140000_work_roles_and_articles.sql; src/server/commands/pricing/articles.ts]

## Deferred from: epic review of epic-3 (2026-06-30)

- **[Med] Snapshot `company_settings` variant omits the PDF-identity fields Epic 6 will need.** The `CompanySettingsSnapshot` variant + resolver SELECT capture only `companyName` (plus `vat_rate_bp`/`default_vat_display`), silently dropping `org_nr`, `address_line1`/`address_line2`, `postal_code`, `city`, `email`, `phone`, `logo_url` that a Swedish quote PDF legally/commercially needs. This is a deliberate, documented scope decision (Story 3.5 Open Question 1 — "kept minimal here"; full PDF identity is Epic 6's quote-version concern), so it is not an Epic 3 defect. Risk: a naive Epic 5/6 implementer treating this `SnapshotSource` as the COMPLETE `company_settings` interface would silently drop identity from the frozen quote-version snapshot. Owner: Epic 6 (quote-version PDF identity) — either extend the variant + resolver columns when the PDF snapshot lands, or add a type-level marker on `CompanySettingsSnapshot` documenting it is intentionally identity-partial. [src/lib/snapshots/types.ts:5298-5306; src/server/snapshots/resolve-source.ts:7382-7387]
- **[Med] No compile/runtime marker forces a future Epic 6 "mark sent" command to check terms `approvedAt` before send.** `QuoteTermsSnapshot.approvedAt` faithfully captures `approved_at` verbatim (NULL = not-approved) and `updateQuoteTerms` always resets it to null on edit — the capture is correct. But the mechanism that stops an unapproved-terms quote from being SENT is entirely prose (comments + Dev Notes); the shared contract carries no exported `assertTermsApproved(snapshot)` helper, no `requiresSignOff: true` marker, nothing that makes an Epic 6 "send" path fail-closed if it forgets the `approvedAt !== null` check. This is a legitimate, deliberate scope boundary (Epic 6 owns enforcement), recorded so the epic-boundary trace treats it as an explicit Epic 6 obligation, not a silent gap. Owner: Epic 6 (quote send/immutability) — carry a blocking-state marker on the terms snapshot or an exported approval-gate helper the send command must call. [src/lib/snapshots/types.ts:5316-5322; src/lib/snapshots/build.ts]
- **[Low] Empty-patch UPDATE can produce a false `TENANT_ACCESS_DENIED` on an id-only CRM update.** `updateCustomer`/`updateFacility`/`updateContact` build the patch from only the supplied fields and issue `.update(patch)` with no empty-patch guard; an id-only update (validators accept an id-only payload) sends `.update({})`. Depending on PostgREST behavior this can return zero rows, which the command maps to `TENANT_ACCESS_DENIED` even though the row is owned and visible — a false authorization denial for a legitimate (if pointless) request. Genuine but minor: the UI submits all fields, so it is not reachable from the current forms; no security/data impact. Owner: a future CRM-command hardening pass — short-circuit an empty patch (return the target id unchanged) rather than issuing `.update({})`. [src/server/commands/crm/customers.ts:5699-5711; src/server/commands/crm/facilities.ts:5842; src/server/commands/crm/contacts.ts:5479]
- **[Low] Personnummer is present in clear text in the client-component payload (mask is cosmetic).** `CustomerDetail.tsx` (`"use client"`) receives `customer.personnummer` raw; `maskPersonnummer` + the "Visa" toggle only govern what is painted, so the raw value is serialized into the client props payload on every private-customer detail view regardless of reveal. Admin-only, own-tenant (RLS + `(app)` route auth); the cross-story security pass cleared it as an owner-approved, access-controlled reveal surface (0 findings) — NOT a cross-tenant/unauthorized exposure. Deferred (auto-resolved from an epic Tier-B `[Review][Decision]`, epic mode — deferred rather than dismissed for PII stewardship): treat a stricter server-gated reveal (raw pnr never in the payload until explicitly fetched) as an Epic 6 privacy-hardening item. [src/components/crm/CustomerDetail.tsx]
- **[Low] Epic-3 test-design "no personnummer" P0/exit-criteria contradicts the shipped, owner-approved schema.** The test design (R-009 + Exit Criteria "no personnummer field by default") asserts personnummer absence as a hard gate, but `customers` carries a `personnummer` column per the 2026-06-18 owner decision (list excludes it, detail masks it, audit drops it, identifier-by-type CHECK ties it to `private` only). The divergence lives only in migration prose. Deferred (auto-resolved from an epic Tier-B `[Review][Decision]`, epic mode): reconcile the test-design / exit-criteria text to the 2026-06-18 owner decision — no code change — so a future epic trace does not FAIL on the stale R-009 assertion. [_bmad-output/test-artifacts/test-design-epic-3.md]

## Deferred from: epic review of epic-3 iter 2 (2026-06-30)

- **[Low] Stale pricing lifecycle-error banner can persist across a later successful DIFFERENT action.** `archiveState`/`reactivateState` are independent `useActionState` hooks in each editor; after a failed archive paints the `*-archive-error` banner, a subsequent SUCCESSFUL reactivate (or vice-versa) does not clear the other action's error state, so the stale red banner lingers until that exact action re-runs and succeeds. Minor UX residual, realistic two-step trigger; not a data/security issue. Owner: a future pricing-UI polish pass — clear/supersede the lifecycle banner on any successful lifecycle action (prefer the most-recently-submitted action's state). [src/components/pricing/WorkRolesEditor.tsx:58-63; src/components/pricing/ArticlesEditor.tsx:106-111]
- **[Low] A no-op reactivate of an ALREADY-active pricing row writes a spurious `*.reactivated` audit row.** `reactivateWorkRole`/`reactivateArticle` do `.update({is_active:true}).eq("id",…).select("id")` with NO `.eq("is_active", false)` filter, so reactivating a row that is already active (stale archived list rendered in a concurrent tab) matches by id, returns the row (not zero → no denial), and appends a redundant reactivate audit row. Benign (append-only, correct tenant, allow-listed `{targetId}` metadata) but audit-noisy. Realistic trigger (two tabs). Owner: a future pricing-command hardening pass — add `.eq("is_active", false)` to the reactivate UPDATE so a no-op reactivate is a clean zero-write, or accept the idempotent audit row as designed. [src/server/commands/pricing/work-roles.ts:146-159; src/server/commands/pricing/articles.ts:129-141]
- **[Low] Reactivate E2E derives its unique role name from `Date.now().toString(36)` (weaker than the integration suites' `crypto.randomUUID()`).** Two parallel Playwright workers (or fast reruns) hitting the same millisecond produce identical names, and the test locates rows by `hasText: roleName` + asserts `toHaveCount(1)` — a flake risk. Test-quality only, no product impact. Owner: a test-hardening pass — switch the E2E name key to `crypto.randomUUID()` to match the integration discipline. [tests/e2e/pricing/pricing.e2e.spec.ts:~647]
- **[Low] No component/E2E test exercises a FAILING archive/reactivate to prove the `*-archive-error` banner + "Försök igen." retry hint render.** The banner logic and the command-layer failure→state mapping (TENANT_ACCESS_DENIED / SERVER_ERROR) are correct + integration-tested, but neither the E2E nor a component test drives a lifecycle-action FAILURE to assert the banner actually paints. Coverage gap only (straightforward `status === "error"` render). Owner: a test-hardening pass — add a component/E2E case that forces an archive/reactivate error and asserts the `data-testid="{work-role|article}-archive-error"` `role="alert"` node (and the retryable suffix on `SERVER_ERROR`). [src/components/pricing/{WorkRolesEditor,ArticlesEditor}.tsx; tests/e2e/pricing/pricing.e2e.spec.ts]
- **[Low] An archived-read failure gates the ENTIRE pricing page (introduced by the reactivate fix).** `page.tsx` folds `archivedWorkRolesError ?? archivedArticlesError` into the same top-level `error` that renders the page-level banner and suppresses the primary editors, so a transient failure loading the OPTIONAL archived lists degrades the whole view even when the active lists loaded fine. Archived is a secondary affordance behind a toggle; its read failure should not gate the main UI. Realistic trigger (a transient DB blip on one of the four now-parallel reads). Deferred (auto-resolved from an epic Tier-B iter-2 `[Review][Decision]`, epic mode — recommended defer/fix-small): scope the archived-read errors to a banner INSIDE the archived section (or render zero archived rows) and leave the page-level `error` to the two active reads only. Owner: a future pricing-UI polish pass. [src/app/(app)/settings/pricing/page.tsx:35-39]

## Deferred from: code review of 4-3-rot-and-gron-teknik-estimate-engine-with-warnings (2026-07-02)

- **[Med] `persons` input captured but NOT applied to the per-person ROT cap.** `estimateDeduction` clamps to a single flat `capOre`; `persons` is validated + snapshotted but never scales the cap, so a 3-person ROT job gets the same ceiling as a 1-person job, with no warning that the per-person multiplier is unmodelled. Deferred (auto-resolved from a Tier-A `[Review][Decision]`, epic mode — triage recommended defer): the per-person multiplier is owner-gated under Sign-Off Q3 (already surfaced for the epic gate); the story sanctions the flat conservative placeholder + UNAPPROVED warning. Owner: the Sign-Off Q3 working session — implement the multiplier (or confirm flat) when the owner pins ROT rate/cap/basis. [src/lib/money/tax.ts]
  _(The other 4.3 Decision — standalone `buildTaxAssumptionSnapshot` rate guard — was auto-dismissed as intentional per Task 3.2's state-capture-only builder contract; 3 further findings dismissed as noise.)_
