# Deferred Work — Resolved

## Deferred from: code review of story-1.1 (2026-06-14)

- **README is the verbatim create-next-app placeholder** — instructs `npm/yarn/bun dev` (contradicts the pnpm-only AC1 / Stop Condition) and references `app/page.tsx` while the scaffold uses `src/app/page.tsx`. Owner: **Story 1.4** (local-setup / README docs), which rewrites the README wholesale. [README.md:7-19]
  - **RESOLVED (2026-06-21, Story 1.4):** README rewritten wholesale — pnpm-only quickstart, exact `package.json` scripts in CI gate order, Node/.nvmrc + Corepack prerequisites, forthcoming local-Supabase section, env-var pointer, and the Lovable oracle one-liner. The full contract lives in the new `docs/process/local-setup.md`. No `npm/yarn/bun` and no `app/page.tsx` references remain.
- **`globals.css` hardcodes `body { font-family: Arial, Helvetica, sans-serif }`**, overriding the Geist font that `layout.tsx` loads via `next/font/google` and that `globals.css @theme` maps to `--font-sans`. Inherited create-next-app (Tailwind v4) artifact; no functional impact today because `page.tsx` applies the `font-sans` utility. Owner: scaffold cleanup / **Story 1.3** (app shell). [src/app/globals.css:22-26]
  - **RESOLVED (2026-06-15, Story 1.3):** removed the Arial `font-family` override from `body` in `globals.css` and applied the `font-sans` utility on `<body>` in `layout.tsx`, so Geist (loaded via `next/font/google`, mapped to `--font-sans`) now applies app-wide. The old marketing `page.tsx` that masked the bug was also replaced with a `/dashboard` redirect.

## Deferred from: code review of story-1.3 (2026-06-16)

- **create-next-app dark-mode leftover in `globals.css`.** The `@media (prefers-color-scheme: dark)` block plus `--background`/`--foreground` light/dark vars are inconsistent with the hardcoded-light shell (`bg-zinc-50`/`bg-white`/`text-zinc-900`). Same class of scaffold cruft as the Arial override removed in this story, but out of Story 1.3's explicit scope (only the Arial item was assigned). Owner: Story 1.4 repo hygiene or a future theming story. [src/app/globals.css:15-20, 22-25]
  - **RESOLVED (2026-06-21, Story 1.4 repo hygiene):** removed the `@media (prefers-color-scheme: dark)` block and the `--background`/`--foreground` light/dark vars (and the unused `--color-background`/`--color-foreground`/`--font-mono` `@theme` mappings — none referenced anywhere in `src/`). `body` now hardcodes the light values directly (`#ffffff` / `#171717`), internally consistent with the hardcoded-light shell. The `--font-sans → --font-geist-sans` mapping is kept (load-bearing for Geist via the `font-sans` utility on `<body>`).
- **Top-bar tenant/user region deferred to Epic 2 (owner decision 2026-06-16, resolved from code-review DN1).** UX §2 / the story Dev Notes envisioned neutral tenant/user placeholders in the slim top bar; the shell ships only the page-title region + empty primary-action slot (Task 1.1 scope). Tenant context + current user are populated by Epic 2 (tenant-admin login / tenant context resolution), so the placeholders land there rather than as churn-prone stubs now. Owner: Epic 2 (Story 2.1). [src/components/app-shell/AppShell.tsx:204-211]
  - **RESOLVED (2026-06-22, Story 2.1).** `AppShell` now accepts a server-resolved `context` ({ tenantName, userEmail }) passed down from the `(app)` server layout and renders the active tenant/company name + current user email (with stable `data-testid` anchors) plus a sign-out control in the former empty `primary-action` slot. Authority stays server-side (`resolveTenantContext`); the displayed values are presentational only. No placeholder/stub text — real values or nothing. [src/components/app-shell/AppShell.tsx; src/app/(app)/layout.tsx; src/components/app-shell/SignOutButton.tsx]

## Deferred from: implementation of 2-1-tenant-admin-login-and-tenant-context-resolution (2026-06-22)

- **`pnpm test` `node --test` runner has no automated self-test yet, and tsconfig uses no separate test-scope config.** The Story 2.1 unit runner (`node --test` + `tests/support/alias-hook.mjs`) is dependency-free and additive; it does not pre-empt the TEA `testarch-framework` (e.g. Vitest) decision. If/when that heavier framework is chosen, reconcile the two (the alias hook + extensionless-import handling may be superseded). Not a blocker. Owner: TEA `testarch-framework` / Story 2.2. [package.json `test`; tests/support/alias-hook.mjs]
  - **RESOLVED (2026-06-26, Story 2.2).** TEA framework decision: TWO deliberately-separated runners. `node --test` keeps the pure-logic units (`pnpm run test:unit`); Vitest runs the DB-backed INT/RLS suites (`pnpm run test:int`); `pnpm test` runs both via `scripts/run-tests.mjs` (a bare-Node orchestrator that avoids the Windows nested-pnpm-on-cmd PATH issue). The `alias-hook.mjs` is KEPT (it serves the `node --test` runner only); Vitest resolves `@/*` natively via Vite 8 `resolve.tsconfigPaths`. The lockfile-guard self-test (epic-1 highest-priority deferred) also landed now (`tests/unit/scripts/verify/check-lockfiles.test.ts`). [package.json; vitest.config.ts; scripts/run-tests.mjs]
- ✅ **Gated INT/E2E scaffolds are excluded from `tsconfig` typecheck until their runner + types land.** `tests/integration/**` and `tests/e2e/**` (the DB-backed and browser red-phase scaffolds) reference a `describe`/`it`/`page` API whose type definitions are NOT installed (no test framework / Playwright yet — that is a gated, Story-2.2 / TEA `testarch-framework` decision). To keep `pnpm typecheck` green without smuggling in a framework, those two trees are listed in `tsconfig.json` `exclude`. Both trees MUST be re-enrolled in typecheck (remove the excludes) so the now-runnable suites are type-checked. [tsconfig.json exclude; tests/integration/**; tests/e2e/**] — resolved in the `tsconfig.json` exclude list: `tests/integration/**` re-enrolled by Story 2.2 (Vitest runner) and `tests/e2e/**` re-enrolled by the TEA `testarch-framework` Playwright landing (`playwright.config.ts` + `@playwright/test` types); both trees now type-check green (`npx tsc --noEmit` exits 0 with the excludes gone).
  - **Re-enrollment record.** `tests/integration/**` (Vitest 4.1.9, Story 2.2) and `tests/e2e/**` (Playwright, TEA `testarch-framework`) are both removed from `tsconfig` `exclude` and type-check green; the DB-backed INT/RLS suites run against the local Supabase stack and the browser specs use `@playwright/test` + a real `playwright.config.ts`. [tsconfig.json; vitest.config.ts; playwright.config.ts; tests/integration/**; tests/e2e/**]

## Deferred from: code review of 2-1-tenant-admin-login-and-tenant-context-resolution (2026-06-22)

- **[Med] Transient membership-read failure is downgraded to "no access".** When the `tenant_memberships` read errors (`membershipError` truthy), `resolveTenantContext` sets `membership = null`, which the pure core maps to `TENANT_MEMBERSHIP_REQUIRED` — so a DB timeout / RLS misconfig / connection-pool exhaustion presents to a fully-authorized tenant admin as a permanent "contact your administrator" screen, conflating "infrastructure is down" with "you are not authorized" and masking outages. A 2.1 unit test currently enshrines this behavior. There is no distinct transient/`SERVER_ERROR`/`UNAVAILABLE` code. Owner: Story 2.2 — fold the transient-vs-no-access distinction into the DB-backed `Result` error-code taxonomy when the authoritative error-path tests land. [src/server/auth/resolve-tenant-context.ts:980-988]
  - **RESOLVED (2026-06-26, Story 2.2).** Added a distinct `SERVER_ERROR` code to `TenantContextErrorCode` with a generic, retryable, user-safe Swedish message (no internal detail / stack / tenant-existence signal). A real membership-query error now maps to `SERVER_ERROR` (not `TENANT_MEMBERSHIP_REQUIRED`), and the outer `try/catch` maps thrown I/O failures to `SERVER_ERROR` too — fail closed, but no longer mislabel an outage as a permanent no-access denial. Genuine "no active membership" stays on `TENANT_MEMBERSHIP_REQUIRED`. The old 2.1 unit test was updated to expect `SERVER_ERROR`. [src/server/auth/tenant-context.ts; src/server/auth/resolve-tenant-context.ts; tests/unit/server/auth/resolve-tenant-context.test.ts]
- **[Low] Service-role containment guard scope is incomplete.** `check-service-role-containment.mjs` scans only `src`/`scripts`/`tests` + `.env.example` and matches two naive shapes (literal `NEXT_PUBLIC_...SERVICE_ROLE...`; literal `SUPABASE_SERVICE_ROLE_KEY` inside a `"use client"` file). It misses a server module that reads the key and is transitively imported by a client component, dynamic/concatenated `process.env[...]` access, re-exported aliases, a root-level `middleware.ts`/`next.config.*`, and `app/**` outside `src/`. Story 2.1 uses NO service-role key so nothing leaks today, and the full bundle-grep R-002 verification (built `.next` payloads + route payloads) is assigned by the test-design to Story 2.4. Owner: Story 2.4 — broaden scanned roots and add the built-bundle grep as the authoritative R-002 verification. [scripts/verify/check-service-role-containment.mjs:215]
  - **RESOLVED (2026-06-29, Story 2.4).** Added the AUTHORITATIVE built-bundle grep `scripts/verify/check-bundle-containment.mjs` (`scanBuiltBundle`, CLI `verify:bundle-containment`, wired into CI AFTER `pnpm build`): it scans the produced `.next` payload for any `*SERVICE_ROLE*` token, the local-demo service-role JWT VALUE (symbol-independent), a `NEXT_PUBLIC_*SERVICE_ROLE*` name, and a generic `"role":"service_role"` JWT shape — and FAILS LOUD if `.next` is absent (never false-greens). Bite proven by `tests/unit/scripts/verify/bundle-containment.test.ts` (clean→0, planted token/JWT→≥1, absent-build→throws); clean on the REAL `.next` build. The source guard was ALSO broadened: scanned roots now include `app` (an `app/**` tree outside `src/`) and `next.config.*`, and it now catches the `LOCAL_SUPABASE_SERVICE_ROLE_KEY` re-export symbol from a `"use client"` path (see next item). [scripts/verify/check-bundle-containment.mjs; scripts/verify/check-service-role-containment.mjs; tests/unit/scripts/verify/bundle-containment.test.ts]
- **✅ [Low] No executing test asserts the `(app)` layout redirect / no-access enforcement.** All running unit tests target `resolveTenantContextCore` and `resolveTenantContext`; the actual enforcement branch in `layout.tsx` (`UNAUTHENTICATED → redirect("/login")`; membership-required → render `NoTenantAccess`) is covered only by the gated, `.skip`-ped E2E scaffold. A regression dropping the redirect or rendering the shell anyway would pass the entire running suite. Pre-existing by design — the layout-enforcement E2E is explicitly gated on Story 2.2's local Supabase stack + a browser runner (Task 6.1). Owner: Story 2.2 (E2E enrollment of `tests/e2e/auth/login-and-tenant-context.e2e.spec.ts`). [src/app/(app)/layout.tsx:392-400] — resolved in tests/e2e/auth/login-and-tenant-context.e2e.spec.ts (now un-skipped and GREEN: the AC3 anonymous→`/login` redirect and AC2 authenticated-no-membership `NoTenantAccess` assertions run in the `e2e` CI job via playwright.config.ts).

## Deferred from: code review of 2-1-tenant-admin-login-and-tenant-context-resolution iter 2 (2026-06-22)

- **[Low] Membership read selects via fragile lexicographic `.order("status")`.** `resolveTenantContext` picks the preferred membership row with `.order("status", { ascending: true })` relying on `'active' < 'disabled' < 'invited'` alphabetically. Correct for the current 3-value enum, but a future status value sorting before `active` (e.g. `accepted`, `approved`, `archived`) would be selected first and then coerced by `normalizeMembershipStatus` to a denying status, silently locking out a rightful active admin. Does not bite in Phase A (single membership; the status enum is fixed until 2.2 lands the table). Owner: Story 2.2 — when the `tenant_memberships` status enum + CHECK constraint land, replace the lexicographic sort with an explicit `status='active'`-first ordering (or fetch the set and let the pure core choose) and add the two-row (disabled+active) selector test. [src/server/auth/resolve-tenant-context.ts:1155]
  - **RESOLVED (2026-06-26, Story 2.2).** Removed the lexicographic `.order("status")`. The resolver now fetches the small `tenant_admin` candidate set (ordered by `created_at`) and selects the preferred row EXPLICITLY in `selectPreferredMembership`: an `active` row wins over any non-active row, oldest-wins as a stable tiebreak; rows with a null/empty `tenant_id` are dropped; no active row → deny (`TENANT_MEMBERSHIP_REQUIRED`). Two new unit tests cover the disabled+active (across-tenant) selection and the all-non-active denial. The `(tenant_id, user_id)` UNIQUE constraint means the multi-row case is only reachable across tenants. [src/server/auth/resolve-tenant-context.ts; tests/unit/server/auth/resolve-tenant-context.test.ts]

## Deferred from: code review of 1-4-document-local-setup-environment-contract-and-repo-hygiene (2026-06-21)

- **[Low] Env-contract security rules are prose-only with no automated enforcement.** The "never `NEXT_PUBLIC_` the service-role key", "never import into client paths", "document any service-role use with file/purpose/test" rules live only in markdown; there is no lint rule, CI check, or test preventing a future careless client-side service-role exposure. Not actionable in Story 1.4 (nothing reads these vars yet). Owner: Epic 2 auth/tenant-context story — add a lint/CI guard against client-side service-role exposure when env consumption lands. [.env.example; docs/process/local-setup.md:266-275]
  - **RESOLVED (2026-06-22, Story 2.1).** Added `scripts/verify/check-service-role-containment.mjs` (bare-Node, dependency-free, mirroring `check-lockfiles.mjs`), wired into CI via the `verify:service-role-containment` npm script. It FAILS when any `NEXT_PUBLIC_`-prefixed service-role var name appears, or when `SUPABASE_SERVICE_ROLE_KEY` is referenced from a `"use client"` (browser-reachable) module. Its bite is proven by `tests/unit/scripts/verify/service-role-containment.test.ts` (RED on a planted `NEXT_PUBLIC_` var / client-path import, GREEN on a legitimate server-only reference — R-002 verification). [scripts/verify/check-service-role-containment.mjs; tests/unit/scripts/verify/service-role-containment.test.ts]

## Deferred from: implementation of 2-2-tenant-membership-schema-rls-helpers-and-two-tenant-fixtures (2026-06-26)

- **Parameterized inventory-gated RLS suite + the H4 table-inventory gate are Story 2.4's scope.** Story 2.2 built the reusable cross-tenant negative PATTERN and the first concrete negatives for `tenants`/`tenant_memberships` (data-driven over a `TABLES` array so 2.4 can iterate the schema inventory). The STANDING gate that fails CI when a future tenant-owned table is not enrolled in the parameterized suite (architecture §18 / H4) is NOT built here. Owner: Story 2.4 (security regression harness; RLS coverage gate). [tests/integration/rls/cross-tenant-isolation.rls.test.ts; architecture §18]
  - **RESOLVED (2026-06-29, Story 2.4).** Extracted the per-table `TABLES` array + spoof/filter/mutation metadata into ONE shared inventory `tests/integration/rls/tenant-table-inventory.ts` (`TENANT_TABLES`, single source of truth) that the cross-tenant suite, the anon-path suite, AND the new H4 gate all read. Added the H4 gate `tests/integration/rls/rls-inventory-gate.int.test.ts`: it introspects the live schema (`introspectTenantOwnedTables` — unions `tenant_id`-carriers with the literal `tenants` root, schema-qualified to `public`) and fails CI with a named "table not covered" message when a tenant-owned table is unenrolled. Bite PROVEN: the pure comparison `findUnenrolledTenantTables` is unit-tested (`tests/unit/rls/inventory-gate-core.test.ts`, shrunk-inventory case), the live gate exercises a shrunk enrolled set against the REAL schema, and a manual scratch-branch run (omit `audit_events` from `TENANT_TABLES` → CI red, named message) is recorded in the Story 2.4 Dev Agent Record. [tests/integration/rls/tenant-table-inventory.ts; rls-inventory-gate.int.test.ts; tests/unit/rls/inventory-gate-core.test.ts; architecture §18]
- **✅ Browser E2E runner (Playwright) for the login/tenant-context journeys.** The login/tenant-context browser journeys (`tests/e2e/auth/login-and-tenant-context.e2e.spec.ts`) used a bare `describe`/`it`/`page` API with no runner types, and Story 2.2 deliberately did NOT add a browser runner (the DB-backed INT scaffold — which DID need un-gating — runs via Vitest instead). The `(app)` layout redirect / no-access enforcement assertion (Story 2.1 deferred-work Low) also rode on this E2E. — resolved by the TEA `testarch-framework` Playwright runner: `playwright.config.ts` (Chromium, two-tenant fixture seeded via `tests/e2e/global-setup.ts`, webServer against the local Supabase stack), the auth spec is now un-`.skip`-ped and GREEN with `@playwright/test` types, `tests/e2e/**` is re-enrolled in `tsconfig` typecheck (exclude cleared), and the `e2e` CI job runs `pnpm run test:e2e` in `.github/workflows/ci.yml`. [tests/e2e/auth/login-and-tenant-context.e2e.spec.ts; tsconfig.json exclude]

## Deferred from: code review of 2-2-tenant-membership-schema-rls-helpers-and-two-tenant-fixtures (2026-06-26)

- **[Low] (Round 2) `check-service-role-containment.mjs` does not catch the test-env re-export symbol `LOCAL_SUPABASE_SERVICE_ROLE_KEY`.** The guard keys only on the literal env-var names `SUPABASE_SERVICE_ROLE_KEY` / `NEXT_PUBLIC_*SERVICE_ROLE*`; the test service-role key is re-exported from `tests/support/test-env.ts` under a different symbol, so a future `"use client"` module that transitively imported the factories/test-env would slip past the guard because the forbidden literal never appears. The dedicated security review rated this LOW and NOT exploitable: the embedded value is the universal Supabase local-demo key (issuer `supabase-demo`, identical on every machine, grants nothing against any real project), the factories are hard-gated to loopback via `assertLocalStack()`, and they live only under `tests/`. Owner: a future security-hardening pass — extend the guard to also flag any `*SERVICE_ROLE*` JWT-shaped literal or the `LOCAL_SUPABASE_SERVICE_ROLE_KEY` symbol when reachable from a `"use client"` path. [scripts/verify/check-service-role-containment.mjs:25]
  - **RESOLVED (2026-06-29, Story 2.4).** The source guard now flags the `LOCAL_SUPABASE_SERVICE_ROLE_KEY` re-export symbol when referenced from a `"use client"` module (bite proven in `tests/unit/scripts/verify/service-role-containment.test.ts`; the test-env module that DECLARES the symbol — not a client module — stays green). The authoritative built-bundle grep additionally catches the JWT-shaped literal VALUE regardless of symbol in the produced `.next` payload. [scripts/verify/check-service-role-containment.mjs; scripts/verify/check-bundle-containment.mjs; tests/unit/scripts/verify/service-role-containment.test.ts]
- **[Low] `migration-reset.int.test.ts` search_path assertion is over-fit to the empty path.** The regex `/search_path=("")?$/` matches loosely and would also pass a non-empty `search_path=pg_catalog` while FAILING a valid schema-qualified implementation that AC4 explicitly permits. The R-006 behavioral negative (`security-definer-search-path.rls.test.ts`) is the real hijack proof, so this introspection assertion is weaker than it appears, not a code defect. Owner: a future test-hardening pass — parse the specific `search_path` proconfig entry and assert it is exactly empty. [tests/integration/rls/migration-reset.int.test.ts]
  - **RESOLVED (2026-06-29, epic-2 hardening) — G-8a.** Replaced the over-fit regex with a shared `assertSearchPathExactlyEmpty` helper that parses the ACTUAL `search_path` entry from `pg_proc.proconfig` and asserts it is EXACTLY empty (a non-empty path now FAILS). Applied to all three SECURITY DEFINER functions: the two RLS helpers (`migration-reset.int.test.ts`) and `record_audit_event` (new introspection assertion in `record-audit-event-search-path.int.test.ts`). R-006 behavioral hijack negatives unchanged. resolved in tests/support/search-path.ts; tests/integration/rls/migration-reset.int.test.ts; tests/integration/commands/record-audit-event-search-path.int.test.ts.

## Deferred from: code review of 2-3-server-command-envelope-and-minimal-audit-events (2026-06-29)

- **[Low] (iter 2) Anon UPDATE/DELETE on `audit_events` is not enumerated, and `audit_events` is not enrolled in the `anon-path-isolation.rls.test.ts` data-driven `TABLES` seam.** Task 5.4 said "mirror `anon-path-isolation.rls.test.ts`"; the diff adds a standalone `audit-anon-isolation.int.test.ts` covering anon SELECT/INSERT/EXECUTE only and does NOT add `audit_events` to the existing anon `TABLES` array (`["tenants","tenant_memberships"]`) nor prove anon cannot UPDATE/DELETE an audit row. The anon UPDATE/DELETE denial rides the identical absence-of-grant mechanism already proven for SELECT/INSERT (and for the authenticated app path), so the protection exists; only the data-driven enumeration is missing. Owner: a future test-hardening pass — enroll `audit_events` in the `anon-path-isolation.rls.test.ts` `TABLES` seam (all four verbs) so a future accidental anon grant is caught by the data-driven suite. [tests/integration/commands/audit-anon-isolation.int.test.ts]
  - **RESOLVED (2026-06-29, Story 2.4, Task 3.1).** `anon-path-isolation.rls.test.ts` now iterates the SHARED `TENANT_TABLES` inventory, so `audit_events` is enrolled for all four anon verbs (SELECT/INSERT/UPDATE/DELETE, each asserting `42501`), and `record_audit_event` was added to the anon-EXECUTE negatives (also `42501`). The standalone `audit-anon-isolation.int.test.ts` stays as the SELECT/INSERT/EXECUTE focused proof; the data-driven seam now covers the full anon surface. [tests/integration/rls/anon-path-isolation.rls.test.ts; tests/integration/rls/tenant-table-inventory.ts]

## Deferred from: implementation of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (2026-06-29)

- **[Low] (NEW) `envelope-audit-write.int.test.ts` uses a HARDCODED `correlationId` and is not isolation-safe across repeated non-reset int runs.** The "writes EXACTLY ONE audit_events row" test (Story 2.3) asserts exactly one `audit_events` row matches the literal `correlationId = "dddddddd-…"` written under `fixture.tenantA.id`, but the test seeds no unique id per run and never cleans the row, so running `test:int` MULTIPLE times against the SAME local DB without a `supabase db reset` between runs accumulates rows under that fixed correlation id and the count assertion fails (`expected 3 to be 1`). NOT a product defect and NOT a CI failure: CI runs `supabase db reset` exactly once before `test:int`, so a clean single run is green (verified). Surfaced only because the Story 2.4 verification re-ran the int suite several times without an intervening reset. Owner: a future test-hardening pass — switch the hardcoded `correlationId` to a per-run `crypto.randomUUID()` (matching the cross-tenant suite's discipline) so the assertion is run-order-independent. Do NOT change in Story 2.4 (out of scope — Story 2.3 test, not a harness task). [tests/integration/commands/envelope-audit-write.int.test.ts:91]
  - **RESOLVED (2026-06-29, epic-2 follow-up).** Replaced the hardcoded `correlationId` literals in all three tests with per-run `crypto.randomUUID()` (matching the cross-tenant/anon suites' discipline), so the exact `toBe(1)` count assertion is run-order-independent and no longer accumulates across repeated non-reset `test:int` runs. [tests/integration/commands/envelope-audit-write.int.test.ts]

## Deferred from: code review of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (2026-06-29)

- **[Low] Live H4 gate + bite-proof INT tests `return`-skip (pass, not skip) when the local stack is down — a Docker-less dev machine gets a vacuous green.** Every `it` in `rls-inventory-gate.int.test.ts` opens with `if (!stackUp) return;`, so when `isLocalStackReachable()` is false the assertions never run and Vitest reports a pass. CI is protected: `SUPABASE_TEST_REQUIRED=1` in the global setup makes a missing stack a HARD failure, and the pure-fn unit (`inventory-gate-core.test.ts`) covers the no-DB comparison logic unconditionally — so the only residual risk is a dev-side false green. This is the established Story 2.2 skip pattern and the enforcement lives in `tests/support/test-env.ts` (outside this diff). Owner: a future test-DX pass — consider converting `return` to a hard assertion when `STACK_REQUIRED` is set inside the gate file itself, so the gate's non-vacuity is self-contained rather than inherited from global setup. [tests/integration/rls/rls-inventory-gate.int.test.ts:918]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#6.** Added a shared `skipUnlessStack(testCtx, stackUp)` helper and replaced every `if (!stackUp) return;` inside the 21 integration suites' `it`/`test` bodies (81 call sites) with a REAL Vitest dynamic skip (`ctx.skip()`), so a stack-down local run now reports those tests as SKIPPED in the reporter instead of a vacuous pass (verified: migration-reset shows `7 skipped` when the URL is unreachable). The `SUPABASE_TEST_REQUIRED=1` hard-fail contract is preserved and now self-contained inside the helper too (it THROWS when required + down), in addition to the global-setup backstop. resolved in tests/support/stack-gate.ts (+ the 21 `tests/integration/**` suites).

## Deferred from: code review of 2-4-security-regression-harness-for-tenant-and-service-role-boundaries (iter 2) (2026-06-29)

- **[Low] Bundle scanner `walk()`/`readFileSync` swallow read errors, so an unreadable `.next` subtree is silently excluded.** Both the directory `readdirSync` (`} catch { return; }`) and the per-file `readFileSync` (`} catch { continue; }`) swallow errors, so a permission-denied subtree or file under `.next` is skipped with no signal — a service-role leak there would go undetected on the authoritative R-002 gate. No realistic trigger in CI (the runner owns the `.next` it just built), so it is a vacuous-green hardening concern, not a current defect. Owner: a future hardening pass — distinguish `ENOENT` from `EACCES` and surface (or fail on) unexpected read errors rather than silently skipping. [scripts/verify/check-bundle-containment.mjs:253]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#2.** The `walk()` `readdirSync` and the per-file `readFileSync` no longer swallow errors: an `ENOENT` is treated as legitimately-absent (fine), but ANY other error (EACCES/EPERM/…) on a present-but-unreadable directory or file is now SURFACED (warn to stderr with the path) AND recorded as a VIOLATION, so the authoritative R-002 scan fails-loud / non-zero instead of a silent false-clean. The absent-`.next` fail-loud (throw) is unchanged. New unit tests prove an unreadable (EACCES) entry yields a non-vacuous violation, a vanished (ENOENT) entry stays clean, and an unreadable entry does not mask a real leak in a readable sibling. resolved in scripts/verify/check-bundle-containment.mjs; tests/unit/scripts/verify/bundle-containment.test.ts.
- **[Low] Enrollment guidance omits the anon-path metadata helpers.** `unenrolledTablesMessage` (and the module's enrollment contract) instructs a new enrollee to add `spoofedRowFor/tenantBFilter/mutation` metadata but never mentions the anon helpers (`anonRowFor`/`anonFilterFor`/`anonMutationFor`); a future table enrolled per the message would leave the anon helpers falling through to the membership default. DX-only; related to the open metadata-exhaustiveness Decision. Owner: a future test-DX pass — extend the message + contract to list the anon helpers (best paired with making the helpers compile-exhaustive). [tests/integration/rls/tenant-table-inventory.ts:1401]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#3.** `unenrolledTablesMessage` now names BOTH metadata seams a future enrollee must wire — the cross-tenant helpers (`spoofedRowFor`/`tenantBFilter`/`hijackMutationFor`) AND the anon-path helpers (`anonRowFor`/`anonFilterFor`/`anonMutationFor`) — explaining that omitting the anon helpers would let the new table fall through to a membership-shaped default in `anon-path-isolation.rls.test.ts`. The module STANDING CONTRACT docstring was updated to match. Message/docstring-only change. resolved in tests/integration/rls/tenant-table-inventory.ts.
- **[Low] Cross-tenant `audit_events` test goes vacuous if the Tenant-B audit seed returns no id.** `tenantBFilter("audit_events", ctx)` uses `ctx.tenantBAuditId`; if `adminInsertAuditEvent` ever returned successfully without an id, `tenantBAuditId` is `undefined` and `.eq("id", undefined)` matches nothing, so the SELECT/UPDATE/DELETE cross-tenant assertions pass vacuously. A throwing seed errors the whole `beforeAll` (caught), so the only residual risk is a silent no-id return. Owner: a future test-hardening pass — add a `if (!tenantBAuditId) throw new Error(...)` guard in `beforeAll` so a failed seed is loud, not vacuous. [tests/integration/rls/cross-tenant-isolation.rls.test.ts:62]
  - **RESOLVED (2026-06-29, epic-2 hardening) — DX#4.** Added an explicit non-null precondition guard: the cross-tenant suite's `beforeAll` now throws if `tenantBAuditId` is null/undefined AFTER the seed, BEFORE any denial assertion runs, and `tenantBFilter('audit_events', …)` in the shared inventory carries the same guard so any future consumer inherits it. A broken/no-id seed now fails loudly instead of green-by-vacuity. The denial mechanism is unchanged (the negatives still assert the 42501 SQLSTATE). resolved in tests/integration/rls/cross-tenant-isolation.rls.test.ts; tests/integration/rls/tenant-table-inventory.ts.

## Deferred from: code review of 5-2-calculation-editor-ux-for-sections-and-rows (2026-07-03)

- ✅ **[Med] Tenant `default_vat_display` ignored in the calc editor; VAT posture hard-coded to `company_togglable` for all non-private customers (brf/public folded in).** The editor derives posture solely from `customer.customer_type`; it never reads the tenant's configured `default_vat_display`, and the calc-detail read selects only `display_name, customer_type`. Displayed öre are correct — only the default view label can contradict a `company_excl`-configured tenant. Full tenant posture resolution (incl. brf/public mapping) is Story 5.4's VAT-display/readiness surface. [src/components/calculations/CalculationEditor.tsx:206-207; src/features/calculations/read.ts] — resolved in Story 5.4 (`src/features/calculations/vat-posture.ts` `resolveVatDisplayPosture`, wired via `src/app/(app)/calculations/[calculationId]/page.tsx` `readCompanySettings()` → `src/components/calculations/CalculationEditor.tsx`): a non-private customer takes the tenant `default_vat_display` AS-IS, brf/public no longer folded into togglable.
  - **RESOLVED (2026-07-03, Story 5.4).** The page now reads `readCompanySettings()` in the parallel `Promise.all` and hands `default_vat_display` into the editor; the posture is resolved by the PURE `resolveVatDisplayPosture(customerType, tenantDefault)` helper (`src/features/calculations/vat-posture.ts`, unit-pinned): `private` → the `private` invariant; a NON-private customer (`company`/`brf`/`public`) → the tenant default AS-IS (no more hard-coded togglable; brf/public are no longer folded in). A settings read fault degrades gracefully to the conservative togglable default + a "VAT posture unresolved" readiness WARNING. [src/features/calculations/vat-posture.ts; src/components/calculations/CalculationEditor.tsx; src/app/(app)/calculations/[calculationId]/page.tsx]
- ✅ **[Low] Row line-total label unconditionally reads "(inkl. moms)" regardless of the tenant's VAT posture.** Every row renders `... kr (inkl. moms)` even for a `company_excl`-configured tenant where the primary figure elsewhere is net; cosmetic, corrupts no stored value, but the label can contradict an excl-VAT posture. [src/components/calculations/RowEditor.tsx:835-843] — resolved in Story 5.4: `RowEditor` takes the resolved `posture` prop and renders the line-total qualifier via `resolveTotalDisplay` — `company_excl` → "(exkl. moms)" (primary = net), every incl-primary posture keeps "(inkl. moms)" (`src/components/calculations/RowEditor.tsx` posture-aware qualifier).
  - **RESOLVED (2026-07-03, Story 5.4).** `RowEditor` now takes the resolved `posture` and renders the line-total via `resolveTotalDisplay(lineTotal, posture)`: for a `company_excl` posture the primary figure is NET and the qualifier reads "(exkl. moms)"; every incl-primary posture keeps "(inkl. moms)". The label no longer contradicts a resolved `company_excl` tenant. [src/components/calculations/RowEditor.tsx (posture prop + posture-aware qualifier)]

## Deferred from: code review of 6-1-quote-snapshot-schema-and-server-side-version-creation (2026-07-05)

- **✅ [Low] `quote_events` is UPDATE-able despite the "append-friendly events" framing.** The migration gives `quote_events` an own-tenant UPDATE policy (`quote_events_update_own`) + an `authenticated` UPDATE grant — compliant with the letter of Task 1.4 (uniform SELECT/INSERT/UPDATE for all six tables), but a customer-facing lifecycle event log with a live UPDATE path is a latent integrity seam (a 'sent' event could be mutated), in tension with the append-only-triggered `audit_events` posture elsewhere in the repo. Not a 6.1 defect. Owner: Story 6.4 (lifecycle-immutability) — reconcile the event-log UPDATE posture when the sent-immutability enforcement lands. [supabase/migrations/20260705120000_quote_version_model.sql — quote_events_update_own policy + grant update] — resolved in supabase/migrations/20260707120000_quote_version_sent_lock.sql (Story 6.4): the `quote_events_append_only` BEFORE UPDATE OR DELETE trigger (`quote_events_block_mutation`) now RAISES on any UPDATE/DELETE, so a persisted lifecycle event can no longer be mutated — mirrors the `audit_events` append-only posture, closing the event-log UPDATE seam.
- ✅ **[Med] Allocated quote number is never threaded into the frozen composite snapshot value** (auto-resolved [Review][Decision] → defer, epic mode, per triage recommendation). The RPC-allocated integer is persisted in `quote_versions.quote_number` (the persisted truth), but the builder runs BEFORE allocation, so the frozen `QuoteVersionSnapshot` JSON carries `quoteNumberDisplay: null` and no numeric quote number — a literal gap vs architecture §11 "the snapshot captures … quote number". [src/server/commands/quotes/quotes.ts — header.quoteNumberDisplay; src/lib/quote-snapshot/build.ts] — resolved in owner decision 2026-07-06 (owner-decisions-applied-2026-07-06.md D-1): plain-integer display ratified, so the immutable `quote_versions.quote_number` row column the UI/PDF already read IS the display truth; no snapshot-JSON threading is needed and 6.1-DOCS-01 is answered.

## Deferred from: implementation of 6-3-quote-pdf-generation-from-snapshot (2026-07-06)

- **[Low, 8.1] `owner_type='quote_version'` LIVE + `purpose='quote_pdf'` — RESOLVED HERE.** `generateQuotePdf` stores the PDF through the 8.1 foundation with `owner_type='quote_version'`, `owner_id=<quote_version_id>`, `purpose='quote_pdf'` in `file_links`, and a `files` metadata row (private `tenant-files` bucket, server-derived tenant-first `deriveObjectPath`). Proven by 6.3-INT-02. Owner was "Epic 6" — done.
- **✅ [Low, 6.3 → OWNER: Story 6.4] 6.4's sent-immutability trigger MUST EXEMPT the PDF-render columns.** 6.3 intentionally keeps `pdf_status`/`pdf_file_id`/`pdf_generated_at` MUTABLE on a SENT version (retry regenerates a derived PDF, not commitment data — architecture §12; proven by the 6.3-INT-04 "retry on a sent version" case). When Story 6.4 lands the sent-lock DB trigger, it MUST allow UPDATEs to these three PDF-render columns (and the `pdf_status` transitions) while blocking customer-visible-commitment column updates. Owner: Story 6.4. [supabase/migrations/20260706120000_quote_pdf_render_state.sql — pdf_status; src/server/commands/quotes/generate-pdf.ts] — resolved in supabase/migrations/20260707120000_quote_version_sent_lock.sql (Story 6.4): the `enforce_quote_version_sent_lock` trigger enumerates the EXEMPT set as `pdf_status`/`pdf_file_id`/`pdf_generated_at` (plus `status`/`archived_at`/`updated_at`), allowing PDF-render-column UPDATEs on a sent version while raising QV409 on any customer-visible/commitment column change.
- **[Low, 6.3] `quote_events.event_type` was WIDENED additively with `pdf_generated`/`pdf_failed`.** AC3 requires a `quote_events` lifecycle event on generation, but the 6.1 closed set did not include a PDF event → the 6.3 migration drops+re-adds the CHECK with the two derived PDF events added (bounded set, never free text). This is the one schema change beyond `pdf_status` the story's scope note did not anticipate; it is additive and necessary. Owner: none (resolved) — recorded so a future event-vocabulary review sees the widening. [supabase/migrations/20260706120000_quote_pdf_render_state.sql — quote_events_event_type_check]
- ✅ **[Low, 8.1, R-814] `file_links` find-or-create for the PDF link — DECISION recorded.** 6.3 keeps EXACTLY ONE live `quote_pdf` file_link per version+purpose: on first generation it inserts the link; on retry it RE-POINTS that one link's `file_id` to the freshly-uploaded file (an UPDATE, not a duplicate insert), mirroring 6.1's `exists(...)` find-or-create intent. A double-submit retry therefore never duplicates the link (proven 6.3-INT-04). No DB unique constraint was added (8.1 schema unchanged). The superseded prior PDF object is left/archived (archive-over-delete — 8.1 has no object-reclamation path). [src/server/commands/quotes/generate-pdf.ts — findExistingPdfLink + re-point] — resolved in src/server/commands/quotes/generate-pdf.ts and 6.3-INT-04
- ✅ **[Low, 6.2] Subroute-revalidation fix APPLIED.** `generateQuotePdfAction` revalidates BOTH `/quotes/[quoteId]` AND the `/quotes/[quoteId]/versions/[versionId]` subroute after a generate/retry (the 6.2 review's subroute-revalidation gap — not repeated). [src/features/quotes/actions.ts] — resolved in src/features/quotes/actions.ts

## Deferred from: epic review of epic-6 (2026-07-06)

- ✅ **[Med] Correction + PDF affordances render on EVERY non-draft version, including terminal states (superseded/rejected/expired + the future Epic-7 accepted).** `CreateNewVersionButton` and `QuotePdfPanel` are mounted for any `selected.status !== "draft"` (`src/components/quotes/QuoteDetailView.tsx:409,442`; the PDF panel sits outside the draft branch), and neither the `createNewQuoteVersion` command (only guards a `draft` parent — `new-version.ts:75`) nor `generateQuotePdf` (no lifecycle gate at all) restricts the action to a live draft/sent commitment. Architecture §12 scopes PDF retry to "draft/sent"; the new-version intent is "correct a live commitment". Verified harmless in Phase A: the new-version RPC always supersedes the highest `sent` version below the new one (the real live commitment — correct even off a stale parent) and flips only the exempt `status`; PDF retry writes only the 6.4-exempt derived columns; `accepted` is unreachable today (no acceptance flow ships). The two live-drafts-under-concurrency sub-case is separately tracked/dismissed in the 6.5 story review findings. Both affordances are now gated at the command AND UI level: `createNewQuoteVersion` rejects an `accepted` parent (`new-version.ts:80`), `generateQuotePdf` rejects any status other than draft/sent (`generate-pdf.ts:198`), and `QuoteDetailView` mounts `QuotePdfPanel` and `CreateNewVersionButton` only when `!isAccepted` (`QuoteDetailView.tsx:411,498`). — resolved in Story 7.2 (Task 5). [src/components/quotes/QuoteDetailView.tsx:411,498; src/server/commands/quotes/new-version.ts:80; src/server/commands/quotes/generate-pdf.ts:198]

## Deferred from: code review of 8-4-quote-pdf-attachment-and-acceptance-evidence-locks (2026-07-07)

- ✅ **[Low] E2E panel spec ships fully `test.skip` — AC1/AC3 UI messaging is not exercised end-to-end.** All three tests in `file-lock-panel.e2e.spec.ts` were `test.skip` (RED-phase scaffolding) because the `EntityFilePanel` had no replace/delete affordance and the locked-file panel state was unbuilt (panel wiring is 8.5). The story frames the UI lock as "UX only, NEVER the guarantee," and the actual enforcement is fully proven at the command + DB layers, so this is not an enforcement gap. Story 8.5 wired the panel lock-notice + archive-only affordance (`src/components/files/FilePreviewRow.tsx` / `CommitmentFilesPanel.tsx`), un-skipped the spec, and added the `data-testid`s it asserts (`file-lock-notice`/`evidence-lock-notice`/`archive-file` visible; `replace-file`/`delete-file` count 0) — all three tests now execute green. [tests/e2e/files/file-lock-panel.e2e.spec.ts] — resolved in Story 8.5 (limited-file-index-and-file-audit; Tasks 2.5/4.3, src/components/files/FilePreviewRow.tsx)

## Deferred from: code review of 10-3-quote-follow-up-workflow (2026-07-19)

- ✅ [Low] Follow-up chip + overdue badge use bespoke inline tones instead of a shared `status.ts` primitive — fold the follow-up tri-state tones (overdue/due-today/upcoming) into `status.ts` during Story 10.4's "render consistently" pass (SETTLED DESIGN DECISION 6 / 10.4 AC2). Auto-deferred by epic-mode triage recommendation. — resolved in `src/components/quotes/status.ts` (Story 10.4)
  - **RESOLVED (2026-07-19, Story 10.4).** Folded into `status.ts` (`FOLLOW_UP_TONE_LABELS/COLORS` + `followUpToneLabel/Color`); `FollowUpChip.tsx` and the `QuoteList.tsx` overdue row badge now consume the shared authority (inline rose/amber/blue literals deleted, colors byte-preserved).

## Deferred from: implementation of 6-1-quote-snapshot-schema-and-server-side-version-creation (2026-07-05)

- ✅ **[Med, Epic-3] `CompanySettingsSnapshot` variant omits the PDF-identity fields Epic 6 needs — RESOLVED HERE.** Story 6.1 does NOT reuse the identity-minimal `CompanySettingsSnapshot` variant. It captures the FULL company identity (`company_name`, `org_nr`, `address_line1`/`address_line2`, `postal_code`, `city`, `email`, `phone`, `logo_url`) from the LIVE `company_settings` row into the frozen `quote_versions` row via the new pure `buildQuoteVersionSnapshot` (`src/lib/quote-snapshot/build.ts`) — a fresh full read in the command, never the minimal variant. Proven by `quote-version.int.test.ts` (6.1-INT-03: every identity field present) + `quote-tables-migration-reset.int.test.ts` (the FULL-identity columns exist). The Epic-3 minimal variant is left unchanged (it stays intentionally minimal for its own snapshot-source use). Owner was "Epic 6" — done. — resolved in src/lib/quote-snapshot/build.ts and Story 6.1 verification
- ✅ **[Med, Epic-3] No marker forces a future "mark sent" to check terms `approvedAt` — 6.1 carries the state; 6.4 enforces.** 6.1 captures `terms_approved_at`/`terms_approved_by` VERBATIM into the frozen snapshot (NULL = not-approved; the builder NEVER derives an `isApproved` flag and NEVER mutates the source) AND carries a standing `requires_sign_off` marker (defaults `true`, the demo-data-only UNAPPROVED-estimate posture) on the `quote_versions` row + the snapshot. So Story 6.4's mark-sent path is fail-closed by construction (it can read `terms_approved_at !== null` + `requires_sign_off`). 6.1 does NOT build the send-time enforcement — that remains Story 6.4's obligation. Owner for ENFORCEMENT: Story 6.4. — resolved in src/server/commands/quotes/mark-sent.ts and Story 6.4

## Deferred from: epic review of epic-10 (2026-07-20)

- ✅ [x] [Review][Defer][Med] Pipeline read-model + list projection fetch ALL events/open-follow-ups/lost-reasons with no DB-side date bound or LIMIT [src/server/read-models/quote-pipeline.ts:113-158; src/features/quotes/read.ts:124-142] — deferred, MVP-safe at pilot scale but the PostgREST implicit max-rows cap could silently truncate → undercount, and this is the precedent read-model E19 inherits; push window/pagination to the DB in a scale pass. — resolved in src/server/read-models/pagination.ts, src/server/read-models/quote-pipeline.ts, and src/features/quotes/read.ts (Story 10.5)
- ✅ [x] [Review][Defer][Low] DB `quote_follow_ups_completed_shape` CHECK enforces `completed_at` but not `outcome` on completed rows (AC3 "outcome required to complete" is app-only) [supabase/migrations/20260719130000_quote_follow_ups.sql:78-79] — deferred, needs a follow-up migration; the command layer always sets outcome today. — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql
- ✅ [x] [Review][Defer][Low] Own-tenant `follow_up_id` tampering on the mark-lost form could complete a DIFFERENT own open follow-up (no quote/version-match assertion before `completeQuoteFollowUp`) [src/features/quotes/actions.ts:358-369] — deferred, own-tenant-admin-only data integrity (no cross-tenant per the security review); add a `quote_version_id`/`quote_id` match assertion. — resolved in src/features/quotes/actions.ts expected_quote_id validation
- ✅ [x] [Review][Defer][Low] `QuotePdfPanel` generate/retry gate checks only `!isAccepted`, so the PDF affordance renders on a `lost` version where the scoped command always rejects it [src/components/quotes/QuoteDetailView.tsx] — deferred, minor dead-end UX; also gate on `!isLost`. — resolved in src/components/quotes/QuoteDetailView.tsx canShowPdfPanel

## Deferred from: epic review of epic-10 iter 2 (2026-07-20)

- ✅ [x] [Review][Defer][Low] Accept path strands an open follow-up permanently (broader than the documented lost-flip residual) — the decided-quote exclusion stops ESCALATION, but the completion surface renders only on a `sent` version and `acceptQuoteAndCreateJob` never calls `completeQuoteFollowUp` (only the lost path does), so any quote accepted via the normal "Registrera accept" while it holds an open follow-up leaves an OPEN row with no UI or auto-complete path to ever reach `completed` [src/features/quotes/actions.ts]. The 10.4 completion notes document only the narrow lost-flip-completion-failure race, not this larger accept-path orphan population — deferred (exclusion-only is sanctioned by Decision #2's "and/or"); auto-complete the open follow-up in `acceptQuoteAndCreateJob` (mirroring the lost path) or add a drain, and document the residual. — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql (atomic accepted transition closure)

## Deferred from: Codex review of epic-10 (2026-07-25)

- ✅ [Med] `quote_lost_reasons` insert policy checks only tenant membership — a direct insert can create a reason for a still-`sent` version or a mismatched `quote_id`, consuming the unique `(quote_version_id)` slot so a later legitimate `mark_quote_version_lost` fails 23505 and rolls back. → Story 10.5 AC1. — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql
- ✅ [Med] `quote_follow_ups` insert policy checks only `tenant_id`; the two FKs prove same-tenant parents but not that `quote_version_id` belongs to `quote_id`, nor that the anchor is `sent`. A forged open row can attach a draft, or quote A with quote B's version, and occupy the one-open slot. → Story 10.5 AC2. — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql
- ✅ [Med] `quote_follow_ups` composite parent relationship not enforced (`quote_id` + `quote_version_id` independently constrained). → Story 10.5 AC2 (same constraint closes both). — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql
- ✅ [Med] `quote_follow_ups` UPDATE policy permits every column while in-tenant — a row can be moved to another quote/version, a completed row reopened, or completion fields pre-populated outside the audited commands. → Story 10.5 AC3. — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql
- ✅ [Med] `planQuoteFollowUp` reads the anchor status then inserts (TOCTOU): a concurrent accept/lost between read and insert still allows an open follow-up on a terminal quote, which the terminal-state read paths then hide with no completion UI. → Story 10.5 AC4. — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql
- ✅ [Med] Pipeline event read is unbounded — `supabase/config.toml` `max_rows = 1000` truncates SILENTLY (no error, so the empty-result fallback never fires), so counts/hit-rate/accepted-value degrade quietly past 1000 events. → Story 10.5 AC5. — resolved in src/server/read-models/quote-pipeline.ts readAllPages (Story 10.5)
- ✅ [Med] Latest-status lookup is unpaginated — same silent truncation; an omitted latest terminal version leaves a decided quote's stale follow-up counted as open/overdue. → Story 10.5 AC5. — resolved in src/server/read-models/quote-pipeline.ts readPipelineBatches (Story 10.5)
- ✅ [Med] Quote-list lost-reason read is tenant-wide and unpaginated — past 1000 reason rows a displayed lost version can fall outside the returned subset, showing `—` in the Förlustorsak column. → Story 10.5 AC5. — resolved in src/features/quotes/read.ts readAllPages (Story 10.5)
- ✅ [Low] Accepted-value summation is unchecked `number` addition — a total exceeding `Number.MAX_SAFE_INTEGER` silently loses öre, against the canonical safe-integer öre contract (theoretical at ~90 trillion kr, but the contract is the contract). → Story 10.5 AC6. — resolved in src/server/read-models/quote-pipeline-aggregate.ts sumOre (Story 10.5)

## Deferred from: Codex review of epic-10, round 3 (2026-07-25)

- ✅ [Med] `quote_events` is client-insertable (authenticated INSERT grant + own-tenant policy) with a caller-chosen `occurred_at`, and Story 10.4's pipeline read-model now treats that log as its COUNT SOURCE — so an own-tenant admin can inflate/shift period counts and corrupt hit rate without any authoritative status change. The insertability itself is a PRE-EXISTING Story 6.4 deferral; what is new is the log being promoted to an analytics source. → Story 10.5 AC7. — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql
- ✅ [Med] A lifecycle transition and its audit row can diverge: `mark_quote_version_lost` commits status + event + reason in its OWN transaction, then the envelope writes `audit_events` separately. A transient audit failure after that commit leaves the user seeing a FAILED command, the retry refused (already `lost`), and the transition permanently unaudited. **Envelope-wide shape** — every RPC-backed command (incl. `acceptQuoteAndCreateJob`) shares it; likely needs its own ADR rather than a point fix. → Story 10.5 AC8. — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql atomic checked wrappers

## Deferred from: Codex review of epic-10 round 3 (2026-07-26)

- ✅ [Med] Pipeline metrics rest on `quote_events`, which carries an `authenticated` INSERT grant + own-tenant insert policy — an admin can insert arbitrary sent/accepted/lost rows with caller-chosen `occurred_at`, inflating or period-shifting counts and hit rate WITHOUT changing the authoritative version status or invoking a lifecycle command. Compounds the pre-existing `6-4` deferral (`quote_events` client-insertable; the append-only trigger only blocks UPDATE/DELETE). → Story 10.5 AC7. — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql
- ✅ [Med] A critical transition can commit WITHOUT its audit row: `mark_quote_version_lost` commits status+event+reason in its own RPC transaction, then the envelope writes the audit separately. A transient failure there leaves the transition committed, the audit missing, the user shown an error, and the retry rejected (already `lost`). **ENVELOPE-WIDE, not lost-specific** — every RPC-backed command shares the shape; likely warrants its own ADR (same transaction, or a durable transactional outbox). → Story 10.5 AC8. — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql atomic checked wrappers

## Deferred from: Codex review of epic-10 round 4 (2026-07-26)

- ✅ [Med] `readQuoteList`'s open-follow-up query is tenant-wide and unpaginated — the SAME silent `max_rows = 1000` truncation already ledgered for the pipeline reads. Past 1000 open follow-ups, quotes whose row falls outside the subset return `has_open_follow_up`/`overdue_follow_up` false, so the list FILTERS and overdue badges silently omit valid matches. This is the FOURTH instance of the class → fold into Story 10.5 **AC5** (query by the listed quote ids in bounded batches, or paginate). — resolved in src/features/quotes/read.ts bounded quote-id reads (Story 10.5)
- ✅ [Low] `quote_follow_ups` lacks an index for the quote-detail read path: the detail read filters ALL statuses by `quote_id` ordered by `created_at`, but only a `tenant_id` index and a status-specific `(quote_id) WHERE status='open'` index exist — Postgres cannot use the status-specific index for the all-status query, so opening one quote scans the tenant's follow-up history. Add `(quote_id, created_at)`. Pure performance, no correctness impact, invisible at pilot volume → Story 10.5 (extends the hardening scope). — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql

## Deferred from: Codex review of epic-10 round 5 (2026-07-26)

- ✅ [Med] The quote-detail follow-up read (`readQuoteDetail`) selects a quote's FULL follow-up history ascending, so once one quote has >= 1000 completed follow-ups the SILENT `max_rows = 1000` cap can omit the newer OPEN row — the detail page then renders neither the chip nor the completion sheet and may offer planning another follow-up that the unique index rejects. FIFTH instance of the truncation class → Story 10.5 **AC5** (fetch the open row separately, or paginate). — resolved in src/features/quotes/read.ts readAllPages (Story 10.5)
- ✅ [Low] `quote-lost-reason.e2e.spec.ts` is not retry-safe: `playwright.config.ts` sets `retries: 1` but global setup does not reseed between attempts, so a retry meets an already-terminal `lost` version, finds no mark-lost affordance, and fails deterministically before reaching the original assertion. Amplifies any flake into a hard failure. → Story 10.5 (seed/reset per attempt, or tolerate the already-committed state). — resolved in tests/e2e/global-setup.ts markLostQuoteAttempts and quote-lost-reason.e2e.spec.ts

## Deferred from: Codex review of epic-10 round 6 (2026-07-26)

- ✅ [Low] `quote-follow-up.e2e.spec.ts` plan-flow is not retry-safe: `retries: 1` with a globally seeded shared fixture means a retry meets a quote that ALREADY has an open follow-up, so `Planera` is replaced by `Klarmarkera` and the retry fails deterministically before reproducing the original flake. → Story 10.5 (seed/reset per attempt, or tolerate the already-planned state). — resolved in tests/e2e/global-setup.ts followUpQuoteAttempts and quote-follow-up.e2e.spec.ts
- ✅ [Low] Same file, completion-flow: a retry meets an already-completed row, so `Klarmarkera` is absent and the retry fails before reaching the flaky assertion. Same class as the lost-E2E retry item already ledgered in round 5 — THREE E2E specs now share it; fix them together. → Story 10.5. — resolved in tests/e2e/global-setup.ts completeFollowUpQuoteAttempts and quote-follow-up.e2e.spec.ts

## Deferred from: Codex review of epic-10 round 7 (2026-07-26)

- ✅ [Med] `acceptQuoteAndCreateJob` does not complete an open follow-up, so accepting a quote that has one leaves the row open forever with no UI path (the panel is sent-only; list/header/pipeline deliberately hide follow-ups on decided quotes). This is the SAME accepted residual already documented for the lost path's non-atomic auto-complete — the row is invisible and non-escalating, so it is a data-hygiene wart rather than a user-visible defect. Fix alongside the lost-path seam: either complete the follow-up in the acceptance flow, or (cleaner) give decided quotes a single reconciliation path that closes stranded rows. → Story 10.5 (extends AC4's area). — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql
- ✅ [Med] `readQuoteList` returns at most the newest 1000 quotes (PostgREST cap) and the new status/follow-up filters run CLIENT-SIDE over that truncated array, so a lost or follow-up-bearing quote outside the first page can never appear and the filtered view can wrongly show no matches. SIXTH instance of the silent-truncation class — and the one with the clearest user-visible symptom, because the FILTERS are the feature. Push the filters into the database and paginate the base query. → Story 10.5 **AC5** (raise this to the AC's headline case). — resolved in src/features/quotes/read.ts readAllPages (Story 10.5)

## Deferred from: Codex review of epic-10 round 8 (2026-07-26)

- ✅ [DUPLICATE — already Story 10.5 AC1] `quote_lost_reasons` composite parent: independent same-tenant FKs let `quote_id` name quote A while `quote_version_id` names a version of quote B, occupying B's unique reason slot and blocking the legitimate RPC. AC1 already requires exactly this ("rejects a reason whose quote_version_id does not belong to the supplied quote_id"). No new work — same fix. — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql
- ✅ [Med] `quote_follow_ups.due_date` has NO database backstop for the command's "due_date >= today in Europe/Stockholm" rule, so a direct insert can create an already-overdue follow-up that immediately inflates the overdue list and pipeline counts — bypassing the FOLLOW_UP_DUE_DATE_IN_PAST validation the command explicitly performs. Same "table API bypasses a command-layer invariant" theme as AC1-AC3 → Story 10.5 (fold into AC2/AC3's trigger work). — resolved in supabase/migrations/20260902120000_story_10_5_quote_table_hardening_and_pagination.sql

## Deferred from: code review of 10-6-tax-answer-reconciliation (2026-08-06)

- ✅ [x] [Review][Defer][High] Same-tenant direct quote RPC authority can create internally inconsistent commitments [supabase/migrations/20260805120000_tax_answer_reconciliation.sql:1956] — deferred, pre-existing SECURITY INVOKER/table-grant architecture requiring the already-identified owner-approved privilege/API redesign; Sources: Blind Hunter primary. — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql

## Deferred from: code review of 10-6-tax-answer-reconciliation (2026-08-07)

- ✅ [x] [Review][Defer][Low] PDF policy-window wording treats exclusive `validTo` as inclusive [src/server/quote-pdf/render.ts:275] — deferred, customer documents say the rule is valid “till” the exclusive boundary date, overstating the frozen window by one day; render the inclusive previous date or state that validity ends before `validTo`. Sources: Blind Hunter primary; Edge Case Hunter primary. — resolved in src/server/quote-pdf/render.ts exclusiveValidToLabel and 10.9 render test

## Deferred from: build-auto review of 10-6-tax-answer-reconciliation (2026-08-31)

- ✅ source_spec: `spec-10-6-tax-answer-reconciliation.md` — resolved in supabase/migrations/20260831124310_story_10_8_quote_review_authorization.sql
  summary: Historical finding: same-tenant authenticated administrators could invoke create_quote_version_from_calculation with an arbitrary SHA-256-shaped digest, bypassing the browser's reviewed-preview click as provenance. The resolution is the non-HMAC persisted review authority in Story 10.8; the separate HMAC boundary applies only to PDF-byte activation.
  evidence: Architecture follow-up: changing this trust boundary safely is broader than the finished reconciliation story and must not weaken the existing safeguards merely to close review.
  severity: high
- ✅ source_spec: `spec-10-6-tax-answer-reconciliation.md` — resolved in supabase/migrations/20260831124312_story_10_9_quote_pdf_attachment_validity.sql
  summary: Editing a draft quote's introduction, customer notes, validity date, or display mode can leave an already-generated PDF marked ready, while the send gate does not require a newly rendered PDF.
  evidence: Pre-existing quote lifecycle issue outside the tax-answer reconciliation change; address with explicit PDF invalidation/regeneration semantics.
  severity: high
- ✅ source_spec: `spec-10-6-tax-answer-reconciliation.md` — resolved in src/server/commands/quotes/new-version.ts and Story 10.9 successor attachment carry-forward
  summary: The create-new-version UI does not submit attachment identifiers even though the successor command supports them, so successor versions can omit attachments.
  evidence: Pre-existing attachment-retention/UI workflow issue outside this story's no-attachment initial reviewed-creation path.
  severity: medium
