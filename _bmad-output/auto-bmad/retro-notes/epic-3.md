## Story epic-3
- [Phase 2 — epic test design] Epic 3 is the first money (öre) + first snapshot touch, but NO money/VAT/snapshot primitives exist in the repo yet (Epic 4 owns them). Fence Epic 3 to integer-öre storage/validation + a SMALL Phase-A snapshot contract — do NOT build the Epic 4 money/VAT/ROT engine here.
- [Phase 2 — epic test design] STANDING CONTRACT (Epics 3-9): every new tenant-owned table MUST be enrolled in the H4 RLS inventory gate (cross-tenant + anon seams). Highest-leverage, easiest-to-forget Epic 3 obligation; non-negotiable exit criterion — esp. the anon seam.
- [Phase 2 — epic test design] Non-negotiables: NO personnummer field; NO supplier-scope fields on articles; integer-öre money; snapshot values frozen (no silent recompute); customer-facing tax/legal text never auto-approved.
- [Phase 2 — epic test design] Pending owner/legal sign-off (assumptions, not blockers in epic mode): is optional 'articles' in scope for 3-4 (gates R-007)? customer-type/requiredness (3-1/3-2); tax/legal quote-terms + VAT wording (3-3/3-5). Treat as assumptions-pending-sign-off; surface in report.
- [Phase 8 — epic trace gate] Epic 3 test design (2026-06-30) still lists R-009 as 'no personnummer field by default', but that was SUPERSEDED by the 2026-06-18 owner decision REQUIRING personnummer for private/ROT customers. The tests correctly encode the current (owner-approved) contract — reconcile the stale test-design risk wording at retro / in the next planning refresh.
- [Phase 8 — epic trace gate] Stale docstring in tests/integration/rls/rls-inventory-gate.int.test.ts header still names the Story-2.4 3-table set; the live assertion derives from TENANT_TABLES (now 9 tables) so it is correct — comment-only drift, one-line cleanup for a future test-hygiene pass.
- [Phase 8 — epic test-review] Score 92/100 (Grade A): Determinism 97, Isolation 95, Maintainability 86, Performance 85; 0 HIGH / 2 MED / 4 LOW, no weakened/skipped/vacuous tests. Recurring hygiene gap worth a convention: stale RED-phase header comments (describe.skip / notYetImplemented) persist verbatim on suites later un-skipped to green — add a 'clear the red-phase header when flipping ATDD scaffolds to green' step. Also: e2e re-auths through the UI per test instead of reusing storageState (perf).
- [Phase 8 — epic NFR] PASS (advisory), 6 PASS / 2 CONCERNS / 0 FAIL. The two CONCERNS (no `pnpm audit` dependency-scan CI gate; no coverage reporter) have now persisted UNADDRESSED across two consecutive epic NFR audits (2 and 3) — needs an explicit owner decision to either schedule them or formally accept-and-stop-reporting, rather than carrying a third time. R-013 (runtime perf at scale on the new CRM/settings/pricing query paths) is deferred N/A for the pilot; extend the perf baseline beyond the auth/RLS path post-pilot.
- [E_review — Tier-B] The Tier-A/Tier-B split earned its keep: the per-story 3-4 thin review DEFERRED the reactivate gap as a [Low] on a FALSE premise ('the command can already reactivate via UPDATE'). Diff truth: no `.update({is_active:true})` existed at any layer and reads filter is_active=true, so archive was a one-way, invisible door — a real HIGH AC gap (3-4 AC2 lists 'reactivates'). The epic integration (cross-story) auditor correctly escalated it and it was fixed (reactivate command + include-archived UI + e2e archive→reactivate round-trip). Lesson: a thin per-story auditor can under-rate a finding on an unverified belief about another layer; the integration pass is the backstop.
- [E_review — Tier-B] Tier-B also caught two real silent-failure/data-loss bugs a per-story auditor missed: logo_url wiped to NULL on every company-settings save (no logo_url input rendered → always undefined → forced ?? null), and pricing archive failures swallowed (archive useActionState result destructured to nothing → no error banner). Both fixed. Worth a convention: bind + render every server-action's error state; on an upsert, omit-absent rather than force-null optional fields.

## Story 3-1-tenant-owned-crm-data-model-and-commands
- [Phase 3 — create-story] PERSONNUMMER REVERSAL: epics.md AC1 + architecture §7/§10 + AR16/NFR16 say 'no personnummer by default', but a 2026-06-18 owner sign-off REVERSES this for 'private' customers (ROT requirement). Story 3-1 implements it access-controlled (not in default list UI) + surfaces the tension in the PR. The old 'stop if personnummer requested' Stop Condition is RESOLVED by the sign-off, not a blocker. HUMAN must confirm the 2026-06-18 sign-off is authoritative at PR review.
- [Phase 3 — create-story] migration-reset.int.test.ts:112-130 asserts the EXACT total public policy set (currently 3 SELECT-only policies). Story 3-1's CRM INSERT/UPDATE policies WILL fail-loud there — expected; EXTEND the exact enumeration (do NOT loosen to a superset).
- [Phase 5 — dev-story] assertNever exhaustiveness guard on the 6-helper inventory switch turned the crash's half-done enrollment (4 missing branches) into COMPILE errors — red, not silently wrong. Keep this pattern for every new tenant-owned table; it makes incomplete H4 enrollment fail-loud.
- [Phase 5 — dev-story] Supabase default privileges grant EVERY role (incl. anon) non-DML REFERENCES/TRIGGER/TRUNCATE on new public tables. The ATDD 'anon has zero grants' assertion was factually wrong vs the real grant model — scope anon-isolation assertions to the 4 DML privileges (SELECT/INSERT/UPDATE/DELETE). Future new-table stories (3-2..3-5): assert anon-DML-empty, NOT anon-grant-empty.

## Story 3-2-crm-tenant-admin-ux-and-lifecycle-context
- [Phase 3 — create-story] Browser E2E runner is now REAL and CI-gated (playwright.config.ts + e2e job + two-tenant fixture) — the older 'e2e gated / no Playwright runner' deferrals (2-1/2-2) are effectively resolved. Downstream UI stories should write .e2e.spec.ts specs (route-authorization coverage comes for free), not treat e2e as unavailable.
- [Phase 3 — create-story] Personnummer nuance: excluded from the list projection/list UI but INTENTIONALLY shown (restrained/masked) on the customer DETAIL for private customers — 'not in the UI' is list-scoped, not absolute.
- [Phase 5 — dev-story] Story 3-2 dev COMPLETED on disk (26/26 tasks, Status: review, full gate suite green: containment x3, typecheck, lint, unit 208, build, int 170/26, e2e 17) but its ab-deep agent crashed during final reporting/exit — the 3rd long-dev-run process exit this epic. Orchestrator verified all gates directly and reconstructed the summary. PROCESS RISK: long (15-20 min) dev-story runs are unstable in this environment; consider smaller dev chunks / more checkpoints for the remaining stories.

## Story 3-3-company-identity-quote-terms-and-vat-defaults
- [Phase 3 — create-story] Sign-off stop-condition encoded STRUCTURALLY (not just UI): quote_terms approval is a nullable approved_at (default NULL = not-approved); any edit invalidates a prior sign-off; the ONLY approve path is a deliberate human approveQuoteTerms command — no status='approved' default exists. Story 3-5 (snapshot contract) + the money/tax reviewer should rely on approved_at, not a derived flag.
- [Phase 3 — create-story] VAT/money values stored as basis points / integer öre (VAT rate default 2500 bp, configurable, NEVER a code literal). NO VAT/ROT calculation engine in epic 3 — settings storage only; Epic 4 owns the engine.
- [Phase 5 — dev-story] quote_terms is ONE-row-per-tenant (unique(tenant_id)); the settings-rls ATDD scaffold assumed multi-row-per-tenant seeding and was fixed to `on conflict (tenant_id) do update`. The one-row-per-tenant shape is the correct/intended design — later stories + reviewers should not assume versioned/multi-row terms.
- [Phase 5 — dev-story] LOAD-BEARING sign-off bug fixed: the quote-terms approve UI had an in-session state-precedence bug — a prior text-save left saveState.status==='success' and the !justSavedText guard suppressed the approved view after a deliberate approve. Fixed so a successful approve always wins. This is load-bearing for the sign-off stop-condition; Tier-A/Tier-B review should scrutinize the approve-vs-save state precedence.
- [Phase 6 — automate] The load-bearing approve-vs-save precedence (sign-off stop-condition) lived inline in a 'use client' component with only e2e coverage — a regression would escape the fast unit gate. Extracted to a pure resolveTermsApproved() and unit-pinned at the real call site (QuoteTermsEditor consumes it). Coverage-shape lesson: pull critical contract logic out of client components so the fast unit gate protects it.

## Story 3-4-work-roles-and-optional-manual-articles
- [Phase 3 — create-story] ARTICLES are IN scope (minimal manual rows): owner-decisions-applied-2026-06-18.md#Story 3.4 says 'owner: yes' — this OVERRIDES the epics.md 'optional'/Stop-Condition framing. Story 3-5's snapshot contract already covers articles; epic test design R-007 depends on them. Hard no-supplier-scope (schema + command + column-name guard test).
- [Phase 3 — create-story] Structural divergence from 3-3: work_roles + articles are MANY-per-tenant (collections), NO unique(tenant_id) — use the CRM (3-1) create/update/archive command shape, NOT the settings singleton-upsert. Money (cost/sell hourly rates, article unit prices) stored as integer öre (bigint); isOreAmount validator rejects float/negative/overflow/locale-comma. No calculation engine (Epic 4).
- [Phase 5 — dev-story] Story 3-4 dev crashed TWICE mid-run, but each crashed run had already landed a COMPLETE, correct implementation on disk (migration + commands + UI + test un-gating + H4 wiring) — the ONLY outstanding work was the story-file/sprint-status bookkeeping (checkboxes + Status). Crash-recovery insight: 'code done, paperwork undone'. PROCESS: in a crash-prone env, dev-story should write the cheap bookkeeping (checkbox/Status) EARLY so a crash leaves a self-evidently-complete story; the orchestrator verified gates directly to confirm before committing.

## Story 3-5-snapshot-source-contract-for-settings-and-pricing-inputs
- [Phase 3 — create-story] Snapshot-source contract is a PURE builder + tests over the EXISTING 3-3/3-4 tables — NO new table/migration/engine/UI (honors the no-new-table stop-condition; concrete quote persistence is Epic 6). R-008 immutability = copy-by-value + freeze + mutate-source-after-build proof. Captures: source id/kind, display name/content, integer öre, VAT assumptions (vat_rate_bp + display mode), source updated_at as the 'version' (no explicit version column exists), terms approved_at sign-off STATE (captures state, NEVER approves), tenant ownership, capturedAt. Cross-tenant source-id rejected at BOTH the RLS-client resolver (TENANT_ACCESS_DENIED) and RLS.
- [Phase 4 — ATDD] Story 3-5 ATDD agent crashed AFTER writing the (gated, complete) scaffolds; orchestrator verified them directly — immutability (build->mutate source->unchanged), both-layer cross-tenant rejection, and work-role/article golden fixtures all asserted; baseline green (unit 389, int 250/30). The atdd-checklist doc was NOT produced (minor, non-load-bearing) — the gated red scaffolds are the deliverable. Recurring pattern this epic: long agent runs crash near completion, disk work survives, orchestrator verifies + finalizes.
- [Phase 5 — dev-story] The `server-only` npm package is NOT installed in this repo — importing it (as an ATDD scaffold header suggested) would add an undeclared dependency and break the build. The established SERVER-ONLY convention here is a documented header + no next/headers/service-role import, enforced by verify:service-role-containment + verify:bundle-containment (same pattern as src/features/*/read.ts and envelope.ts). Future server-module stories should follow that, not `import 'server-only'`.
