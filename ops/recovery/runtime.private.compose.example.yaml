# Copy this file to `ops/recovery/.private.runtime.compose.yaml` in the dedicated
# recovery worktree. Reuse the exact same unique project name as the DB bootstrap.
# Pick unused high loopback ports.
name: elpro-isolated-recovery-replace-with-a-fresh-drill-id

services:
  db:
    ports:
      - "127.0.0.1:55432:5432"
  gateway:
    ports:
      - "127.0.0.1:58000:8000"